/* ==================================================================
 * 课时：阶段回顾：数据库扩展决策树（sc-db-review）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 回顾课：压缩本章，不引入新知识点。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "用一张决策树把本章压缩：瓶颈读还是写、各走哪条路、每步的一致性代价与迁移风险，供复习与自查。",
  blocks: [
    {
      type: "paragraph",
      text: "本章从「数据库为什么先成为瓶颈」走到「存储选型地图」，实际是一条递进的决策链：先判断瓶颈在库的哪个维度，再按「读 → 写 → 容量 → 归属」逐层选择。回顾课不给新知识，只把这条链压成可检索的地图——面试或排障时从症状出发，走完树就能落到方案。",
    },
    {
      type: "heading",
      text: "数据库扩展决策树",
    },
    {
      type: "list",
      items: [
        "第 1 层——先排除库内问题：索引、Buffer Pool、事务、连接池、慢查询都优化过了吗？（见 MySQL 课程）没做完就先做，做完再谈架构。",
        "第 2 层——瓶颈在读还是写：读先打满 → ① 热点重复读走缓存（缓存章：Redis + 多级缓存）；② 量大而杂的读走读写分离（从库扛不住再叠缓存兜底）。写/容量打满 → 进入第 3 层。",
        "第 3 层——写与容量到顶怎么办：事务与关联必须强一致、能接受分布式成本 → 分片（选分片键 → 迁移三步 → 扩容按 2 的幂或一致性哈希 → Proxy/SDK 中间件）；不想自建分片体系、又要自动分片 + 分布式事务 → NewSQL；能接受最终一致 → 队列化 + 异步落库（第 4 章），把写峰值摊平而不是扩容。",
        "第 4 层——数据归属：按一致性/访问模式/体量/查询类型四问，把热数据放缓存、主数据留 MySQL、搜索进 ES、大文件进对象存储（第 5 课决策表）。",
      ],
    },
    {
      type: "table",
      caption: "本章方案一览：解决的问题、代价与典型误用",
      headers: ["方案", "解决什么", "付什么代价", "典型误用"],
      rows: [
        ["读写分离", "读 QPS 打满单主库", "从库延迟 → 读己之写/单调读/延迟敏感错乱；多一套主从运维", "以为能解决写瓶颈；强一致读也丢给从库"],
        ["数据分片", "单库写/容量结构性上限", "跨片 JOIN/事务/分页、分布式 ID、扇出查询；不可逆的架构税", "为读慢而分片；20% 水位提前上分片"],
        ["一致性哈希/双倍扩容", "扩容 rehash 全量迁移", "路由复杂度/片数粒度限制", "拿一致性哈希当分片路由默认答案，忽略 2 的幂取模"],
        ["Proxy/SDK 中间件", "分片路由与合并的承载位置", "Proxy 多一跳、SDK 绑语言栈", "以为中间件能替业务选分片键"],
        ["NewSQL", "自动分片 + 分布式事务", "共识复制延迟、生态差异、运维复杂度", "延迟敏感的毫秒级热点也硬上"],
        ["缓存/ES/对象存储", "读热点、全文搜索、大文件", "各副本最终一致，需先主后从 + 异步同步 + 对账", "把缓存/ES 当事实主存储"],
      ],
    },
    {
      type: "heading",
      text: "决策树两侧的一致性立场",
    },
    {
      type: "paragraph",
      text: "整棵树的每一次分叉，背后都是同一个交易：**用一致性换容量与吞吐，或反过来**。读写分离与多副本换来读吞吐，代价是从库的旧视图——业务按「强制主库读 / 写后粘性 / 延迟容忍 / 版本判断」分档消化；分片换来写与容量，代价是事务边界被切成片段——设计原则是让跨片事务尽量不发生，发生了就按第 8 章的谱系（2PC / TCC / Saga / 消息最终一致）选型；缓存与 ES 视图换来性能与搜索能力，代价是异步链路的最终一致——靠对账兜底。反过来，当一致性要求是硬约束（钱、库存、不可回退的状态），方案列表会自动缩短到「单写者 + 事务」或 NewSQL——先明确一致性底线，再在它之上谈性能。",
    },
    {
      type: "heading",
      text: "迁移风险清单（动手前的检查表）",
    },
    {
      type: "list",
      items: [
        "数据层面：双写是否在同一事务/可靠消息内完成？历史追平任务是否幂等、可断点续跑？切换前有没有抽样对账？",
        "架构层面：分片键是否已覆盖全部高频 SQL 与事务？不带分片键的查询是否有扇出预案？分布式 ID 是否在迁移前就位？",
        "一致性层面：每条被拆散的数据路径，最终一致窗口多大、谁容忍、谁不能容忍（不能容忍的必须留主库/同片）？对账任务是否已上线？",
        "回滚层面：灰度开关能否一键切回旧库？双写保留多久才敢拆除？",
        "容量层面：扩容预案（翻倍扩 or 一致性哈希）是否预先演练过，而不是故障当晚第一次执行？",
      ],
    },
    {
      type: "heading",
      text: "一句话记忆",
    },
    {
      type: "keypoints",
      items: [
        "库是请求路径的终点，也是唯一不可随意加机器的有状态组件——瓶颈总先到这里。",
        "读瓶颈：缓存先于从库、从库先于分片；写/容量瓶颈：分片或 NewSQL，别无他路。",
        "单写者模型是分界：不打破它（加副本、加缓存）代价小；打破它（分片）就进入分布式一致性的世界。",
        "每加一种存储，就多一份副本与一条对账义务；存储选型是数据特征的函数，不是流行度的函数。",
      ],
    },
    {
      type: "quiz",
      question: "闪购大促期间，某商品库存扣减写达到单主库上限，且扣减与建单必须保持强一致。按本章决策树，最合理的路线是？",
      options: [
        "再加几台从库扛写流量",
        "直接按 product_id 分片库存，同时接受扣减与建单可能跨片的现实",
        "设计上让扣减与建单同片（按同一分片键聚数据），必要时升级 NewSQL 获取自动分片 + 分布式事务",
        "把库存数据搬到 Redis 当主存储，牺牲持久化可靠性",
      ],
      answer: 2,
      explanation: "决策树第 2–3 层：从库解决不了写；裸分片若让扣减与建单跨片，就要背上分布式事务；Redis 当主库存违背「不可丢失数据进真数据库」；把强一致的写事务设计为同片落点，或用 NewSQL 换自动分片 + 分布式事务，才是既扩写又守一致性的路线。",
    },
  ],
};
