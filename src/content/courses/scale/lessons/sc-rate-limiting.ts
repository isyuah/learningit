/* ==================================================================
 * 课时：限流：算法与放置位置（sc-rate-limiting）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "系统讲解限流：固定窗口、滑动窗口、令牌桶、漏桶四类算法与取舍，单机与分布式实现，放置位置、限流维度与响应策略。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课把「限制进入」列为治理的第一件事。设想闪购的下单接口 POST /orders：日常每秒不过几十次请求，大促开售却有脚本和黄牛用成千上万的并发来抢——其中绝大多数注定失败，但每一个都会真实消耗网关、sale 的 CPU 和数据库连接。如果不加约束，系统会把垃圾请求和真实用户请求一视同仁地处理，直到所有人一起超时。**限流要做的事，就是先把进入速率锁死在容量之内**：与其让过量请求把队列拖爆，不如在入口就明确拒绝一部分。",
    },
    {
      type: "definition",
      term: "限流 rate limiting",
      definition: "控制单位时间进入系统的请求速率——超过速率的请求被拒绝、排队或降级处理。它保护的不是某个算法，而是请求路径上最脆弱的资源（数据库、连接池、下游服务），是「限制进入」这一治理动作的落地手段。",
    },
    {
      type: "heading",
      text: "四类算法：窗口计数与速率塑造",
    },
    {
      type: "paragraph",
      text: "评价一个限流方案只需看三条线：能不能平滑突发、实现精度与成本、窗口语义是否清晰。四个经典算法分属两个流派：**窗口计数派**（固定窗口、滑动窗口）回答「一段时间内最多几个」；**速率塑造派**（令牌桶、漏桶）回答「流量的节奏怎么走」。没有完美的算法，只有适合场景的算法。",
    },
    {
      type: "paragraph",
      text: "**固定窗口**：把时间切成固定长度（如 1 秒）的窗口，每个窗口一个计数器，打满就拒绝。实现最简单——本地一个整数，或 Redis 一条 INCR+EXPIRE 即可。它的著名缺陷是**窗口边界突刺**：前一个窗口末尾刚打满额度，下一个窗口开头又能立即打满，两个窗口交界处的瞬时速率可以达到额度的两倍。所以它适合「粗略别超太多」的场景（如登录失败次数限制），不适合对峰值敏感的路径。",
    },
    {
      type: "paragraph",
      text: "**滑动窗口**：让「窗口」随当前时刻连续滑动，任意长度等于窗口的时段都不会超额度，从根上消除边界突刺。精确做法是记录每个请求的时间戳（如用 Redis 有序集合存请求时刻并清理窗口外的记录），但每个请求都要写一条数据，内存与计算成本随流量线性增长；工程上常用**近似滑动窗口**：把窗口切成若干小格分别计数，计算时按时间比例加权求和，内存固定、精度可控。滑动窗口的本质是**用精度换成本**。",
    },
    {
      type: "paragraph",
      text: "**令牌桶**：以固定速率 r 向容量为 b 的桶中放令牌，请求先取令牌再放行，无令牌则拒绝或等待。**b 就是允许的突发量**：空闲时桶被攒满，突发一来可以一次打出至多 b 个请求，但长期平均速率被 r 锁死。它同时照顾了「平时够用」与「允许瞬间冲一下」，因而成为大量 API 网关与限流库的首选算法家族；调参时先定平均速率 r，再单独决定愿意容忍的突发 b。",
    },
    {
      type: "paragraph",
      text: "**漏桶**：请求先进一个有界队列，处理端以恒定速率把请求取出——无论进来多猛，出去的节奏恒定。平滑能力最强，代价是**突发请求被强制排队、延迟被拉高**，桶满则新请求被丢弃。它适合下游对速率极其敏感的场景（如控制对老旧系统的回源速率）。注意它的队列必须设上界，否则排队延迟无界增长——这正是上一课的排队爆炸；队列与速率的博弈详见第 4 章「背压与消费能力」。",
    },
    {
      type: "table",
      caption: "算法选择：按场景对号入座",
      headers: ["场景特征", "推荐算法", "理由"],
      rows: [
        ["只要「粗略别超过 X」的硬顶（登录尝试、防撞库）", "固定窗口", "实现最简；边界突刺可接受"],
        ["任意时间片都要精确不超（按量计费接口）", "滑动窗口（精确或近似）", "无边界突刺，精度可调"],
        ["平时低流量、允许短时突发但平均不超（API 调用量）", "令牌桶", "burst 参数直接表达「允许冲多少」"],
        ["下游只能匀速吃流量（老旧系统回源、限速写入）", "漏桶", "输出恒定，但接受排队延迟"],
        ["大促秒杀接口（对瞬时峰值敏感、预算不许超）", "令牌桶 + 多层叠加", "突发可控，网关与服务双层兜底"],
      ],
    },
    {
      type: "heading",
      text: "单机还是分布式",
    },
    {
      type: "paragraph",
      text: "**单机限流**把计数器放在进程内：零网络开销、微秒级判断，最适合「保护本实例」——每个实例按自己的容量限。它的局限是只约束单机视角：8 个实例各自每秒限 1000，全局瞬时可达 8000；流量不均时部分实例先被限死，而另一些还闲着。若要求「全局总量不超」，就需要分布式限流。",
    },
    {
      type: "paragraph",
      text: "**分布式限流**把计数放到共享的 Redis 或独立配额中心：用 INCR+EXPIRE 实现固定窗口，用有序集合或 Lua 脚本实现滑动窗口与令牌桶，原子性与窗口边界由脚本保证（各实现的细节与版本差异以官方文档为准）。代价很实在：每个被限请求多一次约 1–5ms 的网络往返，Redis 成为关键依赖，而且限流 key 可能被大促流量集中命中、自己先成为热点（机制见第 3 章「热点与惊群」）。工程上的标准解是**分层**：实例先用本地限流做毫秒级粗略拦截，再按配额向中心精确核对总量——本地兜突发，中心兜总量。",
    },
    {
      type: "heading",
      text: "放在哪里：网关、服务还是存储前",
    },
    {
      type: "paragraph",
      text: "放置位置决定限流的「口径」。网关层（见第 6 章「网关与南北流量」）在入口统一执行，按 IP、用户、接口等维度防外部洪峰与刷量，一份规则管住所有后端，但看不到业务内部状态；服务层在自己的实例上按容量限，能结合业务配额（例如按用户维度限制下单尝试频率，具体数值由业务规则与压测决定），保护自身资源；存储前（数据库代理或连接层）只关心一件事：**别让任何上游把我打穿**，是最底线的保护。三个位置可以叠加，各自回答不同的问题：网关回答「外部打进来多少」，服务回答「我还能吃多少」，存储前回答「库还剩多少」。",
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购：POST /orders 的双层限流",
      body: "大促对下单接口做两层限流：服务层按 user_id 限制下单尝试频率，拦脚本刷单，超限回 429 并带上 Retry-After 让客户端稍后再试；网关层再对整条下单链路做全局速率限制，防止服务层失守时把 MySQL 打穿。被限流的调用方应当按 Retry-After 或指数退避加抖动再试（重试纪律详见「熔断与超时」课时），而不是立刻原样重打——否则限流就失去了意义。限流的另两种响应——把请求排队整形放行、或直接返回降级内容——分别在「漏桶」的讨论与「降级与兜底」课时中展开。",
    },
    {
      type: "keypoints",
      items: [
        "限流 = 把进入速率锁死在容量内，超限请求拒绝、排队或降级；算法分窗口计数与速率塑造两派。",
        "固定窗口有边界突刺；滑动窗口用精度换成本；令牌桶用 burst 参数表达允许的突发；漏桶输出恒定但排队加延迟。",
        "单机限流护本实例，分布式限流护全局总量；标准做法是本地粗滤 + 中心精确配额分层。",
        "网关限外部口径、服务限业务口径、存储前是最后底线；三层可叠加，限流维度常用 IP、用户、接口。",
        "被限流方要按 Retry-After 或指数退避加抖动重试，且以幂等为前提，否则限流会与重试形成振荡。",
      ],
    },
  ],
};
