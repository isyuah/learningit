/* ==================================================================
 * 课时：一次请求的旅程：网络栈、Socket 与内核开销（sc-io-stack）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 章节 2「请求路径与 IO：第一公里的性能」第 1 课（开启本章）。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "从网络栈视角拆解一次请求：连接握手花几个 RTT、数据在内核里被拷贝几次、HTTP 版本如何复用连接——并纠正「网络慢 = 带宽不够」的直觉。",
  blocks: [
    {
      type: "paragraph",
      text: "大促期间，一位用户点击「立即下单」，这条请求要经历什么？从手机 App 出发，经 Nginx/网关进入 sale 服务，再访问 Redis 与 MySQL，最后把结果一路带回客户端。第 1 章建立了「请求路径 + 资源瓶颈」的分析框架（见第 1 章『性能的本质：请求路径、资源与权衡』）；从这一课开始，我们把放大镜对准路径本身，看一次请求在**网络栈与操作系统**里究竟在哪里花钱。第一个要纠正的直觉是：绝大多数「网络慢」不是带宽不够，而是**往返次数太多**——RTT × 次数才是延迟的大头。",
    },
    {
      type: "definition",
      term: "网络往返（RTT，round-trip time）",
      definition:
        "一个数据包从发送方到达接收方、再回到发送方的时间。延迟的累积以 RTT 为基本单位：每一次「发请求、等应答」的交互至少消耗一个 RTT。RTT 受物理距离的光速上限约束，加机器无法缩短，只能靠减少往返次数、并行发起来绕过。",
    },
    {
      type: "heading",
      text: "一次连接要付出几次往返",
    },
    {
      type: "paragraph",
      text: "以闪购最常见的 HTTPS 请求为例，一条**新建连接**完成第一笔业务请求，物理上最少要付出 3–4 个 RTT 的纯等待（还不算服务器处理时间）：TCP 三次握手（SYN / SYN-ACK / ACK）约 1 个 RTT；TLS 握手按版本再花 1–2 个 RTT（TLS 1.2 常见约 2 次往返，TLS 1.3 优化到约 1 次，会话恢复后更少，具体以协议实现为准）；之后真正的「请求→响应」还要 1 个 RTT。而 keep-alive（连接复用）可以把后续请求压回 1 个 RTT 量级——省下的正是反复握手的那些往返。",
    },
    {
      type: "table",
      caption: "典型 RTT 量级（示例值随距离与网络状况变化，用于建立数量级感）",
      headers: ["场景", "典型 RTT", "说明"],
      rows: [
        ["同机房内网一跳（sale → MySQL/Redis）", "约 1–5 ms", "本课程基线：网络 + 对方处理"],
        ["同城跨机房", "约 5–20 ms", "容灾/多活部署时的常态"],
        ["国内跨省（用户走公网）", "约 20–80 ms", "手机用户典型路径"],
        ["跨洋（中国 ↔ 欧美）", "约 100–300 ms", "全球化部署常见，物理距离决定"],
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购详情页：静态资源为什么必须走 CDN",
      body: "打开一个商品详情页通常要拉几十个静态资源（图片、脚本、样式）。若没有连接复用，每个新建连接的资源都要先付 3–4 个 RTT 的握手；浏览器对同一域名通常只开约 6 条并行连接，几十个资源只能分批串行等待——在跨省约 40 ms 的 RTT 下，握手往返很快累积到秒级，页面慢得「肉眼可见」。反过来，App/浏览器复用连接（keep-alive、HTTP/2 多路复用）后，这批资源只需一两次往返。这也是大促页面必须把静态资源放到离用户近的 CDN 的原因：**省的是 RTT，不是带宽**（CDN 在缓存章『缓存的心智模型与命中率』中进一步展开）。",
    },
    {
      type: "heading",
      text: "从网卡到进程：用户态与内核态的拷贝",
    },
    {
      type: "paragraph",
      text: "数据到达服务器后，还要穿过操作系统：网卡把数据 DMA 到内核缓冲区，应用通过 read/recv 系统调用把它拷进用户态缓冲区，处理完再用 write/send 拷回内核，由内核交给网卡发出。每一次系统调用都伴随**用户态 ↔ 内核态的切换**与数据拷贝。对一条小请求而言，拷贝与调度的开销往往大于业务计算本身——这就是高吞吐服务总是尽量减少每条请求的系统调用次数（合并小包、批量读写）的原因。",
    },
    {
      type: "definition",
      term: "零拷贝（zero-copy）",
      definition:
        "让内核把数据直接从来源（磁盘文件页缓存或某个 socket）送到目的地（socket），绕过「先拷进用户态进程、再拷回内核」的中间拷贝与两轮上下文切换，典型机制如 sendfile、mmap。它适合「业务不需要碰数据内容」的转发路径，如 Nginx 发静态文件、消息系统落盘转发。本课只需要记住结论：**业务不碰的数据，就别让它经手**。",
    },
    {
      type: "heading",
      text: "HTTP/1.1、HTTP/2、HTTP/3：连接复用技术的演进",
    },
    {
      type: "list",
      items: [
        "HTTP/1.1：keep-alive 默认开启，同一条 TCP 连接可以连续发送多个请求，但同一时刻一条连接只处理一个请求，后面的必须排队（应用层队头阻塞）；浏览器只能对同一域名开多条连接来并发。",
        "HTTP/2：二进制分帧 + 单连接多路复用，多条流并发共享一条 TCP 连接，并带头部压缩；代价是 TCP 层一旦丢包重传，整条连接上的所有流一起等待（传输层队头阻塞）。",
        "HTTP/3：基于 QUIC（在 UDP 之上自带可靠传输与加密），流与流互相独立，一条流丢包不再阻塞其它流；主要用在客户端到边缘的传输，服务端内部组网仍以 TCP 为主。",
      ],
    },
    {
      type: "definition",
      term: "队头阻塞（head-of-line blocking）",
      definition:
        "队列最前面的对象受阻，导致排在后面的对象一起等待。HTTP 语境下有两层：HTTP/1.1 的「一个响应没返回，同连接后续请求全部排队」；HTTP/2 的多路复用共享同一条 TCP 连接，一旦丢包触发 TCP 重传与拥塞控制，整条连接上的所有流一起等待（传输层队头阻塞）。HTTP/3/QUIC 把每条流做成独立的传输单元，一条流丢包不再拖累其它流，把队头阻塞限制在单条流内部。理解它就能明白：多路复用并不等于零延迟——只要多条流共享传输通道（HTTP/2 尤其明显），通道上的故障仍会放大延迟。",
    },
    {
      type: "callout",
      variant: "note",
      title: "对外与对内是两套优化动作",
      body: "在闪购架构里，用户侧的 TLS、HTTP/2/3、连接复用通常由 Nginx/网关终结，sale 服务对内只面向网关。内网连接靠 keep-alive 与连接池复用（下一课『连接与线程池：池化为什么是性能基石』细讲）——对外优化（就近部署、HTTP/2、CDN）与对内优化（池化、减少跳数）是两套动作，排查时别混在一起。",
    },
    {
      type: "paragraph",
      text: "把「网络慢」读成公式会更清楚：**响应总耗时 ≈ 各次交互的 RTT 之和 + 传输时间（数据量 ÷ 带宽）+ 各节点处理时间**。当响应只有几十 KB 时，传输时间是毫秒级，带宽几乎不影响体感，决定体感的是交互次数 × RTT；只有下载大文件、大列表（几十 MB 以上）时带宽才成为主项。因此「跨地域访问慢」的正确解法是**把服务/缓存部署到离用户更近的地方**来缩短 RTT（或让 CDN 就近响应），而不是一味加带宽。",
    },
    {
      type: "quiz",
      question:
        "用户反馈 App「每次打开商品详情都明显卡顿」，抓包发现每个静态资源都新建了 TLS 连接。最可能的根因与最有效的修复方向是？",
      options: [
        "出口带宽不足，应升级带宽",
        "每个资源都付出多次握手往返，应开启连接复用并把静态资源放到 CDN",
        "服务器 CPU 已满，应扩容实例",
        "响应体太大，需要更强的压缩算法",
      ],
      answer: 1,
      explanation:
        "详情页几十个小资源各自承担 3–4 个 RTT 的握手，RTT × 次数主导延迟，单资源只有几十 KB，带宽不是瓶颈。正确方向是减少往返次数：连接复用 + 就近的 CDN。",
    },
    {
      type: "keypoints",
      items: [
        "「网络慢」绝大多数是 RTT × 次数，不是带宽：交互型响应里带宽几乎不参与。",
        "新建 HTTPS 连接首笔请求最少 3–4 个 RTT（TCP 1 + TLS 1–2 + 请求/响应 1）；keep-alive 复用到 1 个 RTT。",
        "数据路径上，用户态/内核态切换与拷贝往往大于业务计算；业务不碰的数据走零拷贝（sendfile 等）。",
        "HTTP/1.1 应用层队头阻塞 → HTTP/2 单连接多路复用（仍有 TCP 层队头阻塞）→ HTTP/3/QUIC 流独立。",
        "服务端内部优化（池化、减少跳数）与对外优化（就近部署、HTTP/2、CDN）是两套动作。",
      ],
    },
  ],
};
