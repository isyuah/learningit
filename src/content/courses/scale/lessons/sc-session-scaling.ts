/* ==================================================================
 * 课时：会话与登录态：让用户状态可扩展（sc-session-scaling）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "从粘性会话出发，理解会话外置与 Token 化两条路线：状态存在哪、如何刷新与吊销，以及「可重建」与「不可丢」的取舍。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课「无状态设计」把「状态藏在实例内」列为水平扩容的头号敌人，而所有状态里最典型、也最难处理的就是会话：用户在闪购登录后，后续请求都要带着「这个用户是谁、登录是否有效」的信息才能正确响应。单体时代它只是内存里的一张 map；一旦 sale 要水平扩容成多实例，这张 map 就变成了一个必须先解决的架构问题。本课沿着两条路线展开：**把会话状态外置存储**，或者**让凭证本身自包含、服务端不再保存会话**——并回答三个现实问题：过期、刷新与吊销。",
    },
    {
      type: "definition",
      term: "会话（Session）",
      definition:
        "服务端为识别用户而维持的上下文：登录成功后，服务端记录「user_id=42 已登录」并与客户端持有的凭证（如 Cookie）建立关联，后续请求凭凭证找到这份上下文。与之相对的是**粘性会话（Session Affinity / Sticky Session）**：负载均衡把同一用户的请求固定分发到同一台后端实例，使存在该实例内存里的会话总能被找到——这是会话未外置时用「路由约束」硬凑出来的方案。",
    },
    {
      type: "paragraph",
      text: "粘性会话是「先有状态、再想办法路由」的产物：负载均衡器记录用户来源（通常按 IP 或 Cookie 做哈希）并持续把请求送回同一台实例，代价立竿见影——某台实例宕机，钉在它上面的所有用户会话一起消失；扩容后新增实例起初没有会话被哈希到，要等用户重新登录或哈希规则调整才慢慢接入流量，旧实例依旧满载，负载并不均衡（若干请求数均摊后，新实例要等到会话自然分散才能均摊负载，而不是立刻受益）；负载均衡器自身还得保存并同步这份粘性映射，成为新的复杂度与故障点。它把「实例可替换」变成了「实例不可替换」，正好抵消了上一课讲的水平扩容的全部收益。因此粘性会话不是解决方案，而是问题状态的具象化：**只要会话留在实例里，路由就得迁就它**；反之，会话一旦外置到共享存储，任意实例都能凭凭证查回上下文，路由不再被绑定——这就是下面要讲的路线一。",
    },
    {
      type: "heading",
      text: "路线一：会话外置——把上下文搬进共享存储",
    },
    {
      type: "table",
      caption: "会话外置的存储选择与取舍",
      headers: ["存储", "读延迟量级", "一致性", "适合场景与注意点"],
      rows: [
        ["Redis", "亚毫秒–毫秒", "主从复制有短暂窗口", "默认首选；要设置 TTL 防止会话堆积；宕机会全员掉线，需持久化与高可用（第 3 章 Redis 定位）"],
        ["MySQL", "1–5ms（一跳）", "事务性、可回查", "会话读写量不大时够用，天然抗 Redis 单点故障；注意别让会话读写占用主库连接"],
      ],
    },
    {
      type: "heading",
      text: "路线二：Token 化——让凭证自包含、服务端无状态",
    },
    {
      type: "paragraph",
      text: "会话外置之后，服务端每次收到请求都要查一次存储。能不能让凭证自己把该说的都说了？JWT（JSON Web Token，JSON 格式的签名令牌）就是这么一种凭证：它把「用户是谁、何时过期」等声明编码进 Token 本身，并由服务端持有密钥做签名——Token 一旦被篡改，签名校验必然失败。收到请求后服务端只做一次本地校验（验签名 + 看过期时间），不查任何会话存储，就完成了身份识别。这就是「无状态认证」：**状态写在凭证里，而不是存在服务端**。对比之下，Cookie 里的 session_id 只是服务端才能解释的随机串；而 JWT 把身份直接写进了凭证本身，任何持有密钥的服务都能独立验真。JWT 是一份公开规范，本课只取与扩展性相关的三个要点：自包含、签名防篡改、过期时间；完整规范与算法细节以官方文档为准。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "无状态 Token 的代价：吊销困难",
      body: "服务端会话可以随时删掉一条会话让用户立刻失效；JWT 在过期前天然有效，服务端没有「一份中央名单」可查——这正是它无状态的原因。要主动踢人（改密、封号、登出全部设备），只能引入黑名单/版本号再查一次存储，等于把状态又请了回来，无状态收益打了折扣。所以现实系统的常见做法是「**短寿命 Access Token + 可吊销的 Refresh Token**」：Access Token 几分钟到十几分钟过期，泄漏窗口有限，服务端无需保存；Refresh Token 存服务端（或可撤销清单），负责换取新 Access Token，登出/封号时吊销它即可。",
    },
    {
      type: "heading",
      text: "登录态刷新与令牌轮换",
    },
    {
      type: "paragraph",
      text: "「刷新」之所以存在，是为了平衡两个方向：Token 太短，用户频繁重登，体验差；Token 太长，泄漏后被人冒用的窗口太大。典型时序是：登录成功签发短寿命 accessToken（如 10 分钟）与长寿命 refreshToken（如 7 天）；accessToken 快过期时，客户端带 refreshToken 调 /auth/refresh，服务端校验仍有效后**轮换**——吊销旧 refreshToken、签发新的一对。轮换让被重用的旧 refreshToken 立刻暴露泄露。另外要小心客户端在多个请求同时过期时并发刷新——应做单飞（同一时刻只允许一次刷新在途），否则刷新接口会被自己的客户端打成热点，这是第 7 章限流话题的一个预演。",
    },
    {
      type: "heading",
      text: "缓存的取舍：会话不可丢，缓存可丢",
    },
    {
      type: "table",
      caption: "会话 vs 本地缓存的「可重建性」分级",
      headers: ["数据", "丢了会怎样", "能否放本地/不持久", "正确做法"],
      rows: [
        ["会话 / 登录态", "用户被登出、看到错误身份", "不能——不可丢、不可错", "放 Redis/DB 等持久共享存储"],
        ["商品只读快照 / 热门详情", "回源数据库，可自动重建", "能——顶多多几次回源", "进程内缓存 + Redis，丢了重建（第 3 章）"],
        ["验证码 / 短时计数", "用户重发即可", "能但不宜本地——实例间会不一致", "Redis + 短 TTL，不必持久化"],
      ],
    },
    {
      type: "paragraph",
      text: "一个常见的错误是把「缓存可以放本地」推广成「什么都可以放本地」。会话与缓存有一个本质区别：缓存是加速层，丢了只是变慢，回源即可；会话是正确性数据，丢了用户就「不是用户了」。分级原则一句话：**可重建的数据才允许只存本地，正确性依赖的数据必须外置并持久。** 第 3 章「多级缓存与整体设计」专门讨论本地缓存怎么与 Redis 分层；本课要强调的是反方向：凡是会话这一类「不可丢」的数据，本地内存一律不碰。",
    },
    {
      type: "heading",
      text: "决策：服务端会话还是 JWT",
    },
    {
      type: "table",
      caption: "服务端会话 vs JWT 决策表",
      headers: ["维度", "服务端会话（外置存储）", "JWT（自包含）"],
      rows: [
        ["状态存哪", "存储里一份中央会话", "写在 Token 里，服务端不存"],
        ["每次请求开销", "一次存储读取（Redis 亚毫秒）", "本地验签，无网络开销"],
        ["主动吊销 / 踢人", "删会话即可，立刻生效", "困难，需黑名单/短 TTL + Refresh 机制"],
        ["登出全部设备", "删全部会话，直接", "吊销 Refresh Token，仍须等 Access 过期"],
        ["多服务共享身份（SSO 简况）", "要共享同一份会话存储，或引入统一认证协议（协议细节以官方文档为准）", "各服务本地验签即可共享，天然适合；但跨系统信任需额外签发/校验机制"],
      ],
    },
    {
      type: "quiz",
      question: "闪购把用户会话从实例内存外置到 Redis 之后，下面哪一条才是它「解决了粘性会话问题」的正确描述？",
      options: [
        "会话数据仍然存在服务端，所以并没有解决任何问题",
        "实例不再保存会话，任意实例都能凭凭证查 Redis 完成认证，实例可以自由替换与扩容",
        "Redis 需要粘性会话才能让同一用户请求命中同一台实例",
        "会话外置之后 JWT 就不再需要了",
      ],
      answer: 1,
      explanation:
        "外置的本质是把状态从「实例内」搬到「实例间共享」，实例变成无状态、彼此等价，负载均衡随意分发即可，粘性会话自然没有存在必要。选项 A 把「状态还在服务端」与「问题没解决」混为一谈——问题从来不在状态存在与否，而在它是否钉住某台实例；选项 C 正好说反了；选项 D 把两种方案当成了串行依赖，它们其实是独立路线。",
    },
  ],
};
