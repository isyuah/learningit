/* ==================================================================
 * 课时：架构总览：从请求到存储的完整链路（sc-capstone-architecture）
 * ----------------------------------------------------------------
 * 第 9 章「综合设计：闪购系统的扩展与高吞吐设计」第 2 课。
 * 课时元信息（title/minutes/kind）以 course.ts 大纲为权威，此处省略。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "把第 1 课推导出的规模落成分层架构图：读走多级缓存、写走队列、库做最终扣减，并把前八章的知识点逐一挂到图上。",
  blocks: [
    {
      type: "paragraph",
      text: "第 1 课把需求与数字钉死了：读峰值约 18 万 QPS（爆款单品独扛 13.3 万）、抢购写峰值约 3.3 万 QPS、库存 80 万件不可超卖、订单三年不到 100GB 无需分片。本课回答「长什么样」：把规模翻译成分层架构。评判架构只有一个标准——**每一层是不是都在替下一层挡掉它扛不住的流量**。图看全貌，表看分工，最后用一张知识地图表把前八章串起来。",
    },
    {
      type: "code",
      title: "闪购 sale 大促目标架构（文字架构图）",
      language: "text",
      code: "用户（App/浏览器）\n   │  HTTPS + CDN（静态资源：图片/页面壳，不占源站带宽）\n   ▼\n【网关层】Nginx/API 网关 ×N —— 鉴权、限流、协议卸载、动静分离\n   │  （读路径）                         │（写路径）\n   ▼                                  ▼\n【接入层】sale-web（Go 无状态）    【sale-web 同层】同批 Go 实例\n   静态页/会场信息直接回                POST /orders → 校验 → Redis 预扣 → 写 MQ\n   │                                  │\n   ▼                                  ▼\n【数据读】                         【队列层】MQ（削峰：仅预扣成功的订单入队）\n   ① 本地缓存（进程内，L1）            │ 消费者 sale-order-worker\n   ② Redis 集群（L2，热点副本）        ▼\n   ③ MySQL 从库（回源兜底）        【数据写】MySQL 主库：幂等最终扣减 + 建单\n   │                                   │ 对账任务 ⇄ Redis 预扣计数\n   ▼                                   ▼\n【外部下游】pay（支付回调）         notify（短信/推送，异步通道，可重发需去重）",
    },
    {
      type: "heading",
      text: "七层全景：每层的职责、扩容方式与故障表现",
    },
    {
      type: "table",
      caption: "分层职责总表（读第 1 列时，想象一条请求依次经过每一层）",
      headers: ["层", "职责", "扩容方式", "故障时表现（由下游防线兜住）"],
      rows: [
        ["客户端 + CDN", "展示、静态资源就近分发", "客户端侧无容量问题；CDN 按带宽扩容", "弱网/离线：请求到不了源站，不消耗后端资源"],
        ["网关层（Nginx/API 网关）", "TLS 卸载、路由、鉴权、**限流**、动静分离", "无状态，水平加实例；限流规则集中配置", "网关挂 = 全站不可用 → 双活 + 健康检查自动摘除（第 7 章）"],
        ["接入层 sale-web", "无状态 API：组装详情响应、下单校验（登录/限购/风控）、Redis Lua 预扣、写队列", "无状态水平扩（第 6 章核心）：发布会话与本地状态全部外置", "实例被摘除/重启不影响其他实例；突发流量先被网关限流挡掉"],
        ["队列层 MQ", "承接写洪峰，削峰填谷；解耦 notify", "broker 集群水平扩；消费能力由 worker 数量与 DB 预算决定", "队列积压 = 订单延迟落库但**不丢失** → 提高 worker 并观察 DB 水位（第 4 章背压）"],
        ["缓存层 Redis", "商品/会场缓存、热点副本、预扣计数、分布式限流计数", "主从 + 分片集群；热点 key 加副本（第 3 章）", "Redis 挂 = 读回源 MySQL、预扣切换降级通道；绝不让缓存故障变成数据丢失（第 7 章降级预案）"],
        ["存储层 MySQL", "主库：幂等最终扣减 + 建单（真账）；从库：兜底读", "读写分离；容量不够才考虑分片（本场景三年 < 100GB，不分）", "主库故障 = 自动切换从库/HA；短暂只读窗口由网关限流配合（第 5 章）"],
        ["外部下游 pay/notify", "支付回调（幂等处理）、异步通知", "不可控，只能适配", "pay 慢/挂 = 订单停在待支付 → 超时释放 + 对账兜底；notify 走异步通道，可重发但需去重，不允许重复骚扰（第 4/8 章）"],
      ],
    },
    {
      type: "definition",
      term: "关键路径与旁路（critical path vs side path）",
      definition: "关键路径是用户**等待结果**、直接决定体验与正确性的请求链——读：客户端 → 网关 → 服务 → 缓存/库 → 返回；写：客户端 → 网关 → 服务 → Redis 预扣 → 队列 → 落库。旁路是不在用户等待链上的处理：支付回调、通知、对账、超时释放——它们异步执行，延迟要求宽松，但同样需要幂等与可观测。设计时先分清一条链路在不在关键路径上，再决定给它多少同步成本与一致性保证。",
    },
    {
      type: "heading",
      text: "读路径与写路径：两条主链怎么走",
    },
    {
      type: "paragraph",
      text: "读路径的原则是**多级缓存逐级卸载，回源是最后手段**：本地缓存（L1，进程内，纳秒级命中）→ Redis（L2，跨实例共享）→ MySQL 从库。为什么 13.3 万 QPS 的爆款详情能扛住？因为 13.3 万次请求里，绝大多数在同一实例的 L1 命中直接返回（第 3 章「缓存的心智模型」：时间局部性——同一用户 1 分钟内反复刷新同一商品），只有漏过 L1 的才去 Redis，漏过 L2 的才回源 MySQL。逐级漏下来算：L1 命中约 70%，漏到 Redis 的约 4 万 QPS；L2 再命中约 95%，最终漏到 MySQL 的只剩约 2000 QPS——真正打到库的只是总流量的零头，正是第 1 课容量表里「总回源读 ≈ 0.2–1 万 QPS（爆款详情仅约 2000）」这个量级的来源。",
    },
    {
      type: "paragraph",
      text: "写路径的原则是**把最贵、最怕并发的资源留到最后一步，用队列把它和用户隔离**：请求先过网关限流（防止重试风暴与脚本灌单），到 sale 服务做登录与限购校验，随后在 Redis 上用一段原子 Lua 做「限购检查 + 库存预扣」（第 8 章「设计练习：秒杀下单的库存扣减」的最终形态：单 key 原子操作，单实例约 5–8 万次 Lua/s 的写能力内无锁竞争），预扣成功才把一条「扣减指令」消息投递到 MQ 并立即返回「抢购成功」；sale-order-worker 消费消息，在 MySQL 里做**幂等的最终扣减与建单**。第 1 课 3.3 万 QPS 的瞬时洪峰，到 MySQL 侧已被摊平成约 3000 单/s 的可控写入。",
    },
    {
      type: "code",
      title: "写路径状态流（预扣 → 落库 → 支付 → 终态）",
      language: "text",
      code: "用户点击「立即抢购」\n  1. 网关限流（每用户/全局令牌桶）→ 超限返回 429\n  2. sale-web 校验登录、限购资格（每用户已购 1 件？）\n  3. Redis Lua 原子预扣：库存 key（stock:act1:p1）减 1，且把 user_id 写入已购集合（bought:act1:p1，限购 1 件）\n       · 库存不足 / 已购过 → 直接返回「已抢光/限购 1 件」，不到达队列\n       · 预扣成功 → 生成订单号 order_id，投递 MQ：order.created 消息\n  4. 立即返回用户：抢购成功（此时订单尚未落库 → 最终一致窗口开始）\n\n[异步] sale-order-worker 消费 order.created：\n  5. MySQL 幂等落库：INSERT 订单（order_id 唯一键防重）\n      + UPDATE product SET stock = stock - 1（库存权威真账，防预扣记录丢失）\n  6. 投递「待支付提醒」→ notify\n\n[异步] 支付回调 pay → 幂等更新订单 paid → notify 发货通知\n[异步] 15 分钟定时扫单 → 未支付订单取消（PENDING→CANCELLED，状态机幂等）：\n       DB 回补库存（UPDATE ... SET stock = stock + 1，权威账本）+ Redis 回补预扣计数与已购标记，\n       回补失败则进入重试/对账（两侧净额对账兜底，绝不让库存悬空或重复释放）\n[异步] 对账任务：Redis 净预扣 vs MySQL 净扣减 → 不一致则告警并回补",
    },
    {
      type: "callout",
      variant: "warning",
      title: "为什么预扣放在 Redis、扣减放在 MySQL 是两道而非一道",
      body: "如果只做 Redis 预扣、从不落库，Redis 丢数据（主从切换、内存淘汰）就真的超卖——缓存不能当数据库（第 5 章「存储选型地图」的硬结论）。如果只做 MySQL 扣减、不做预扣，3.3 万 QPS 全打在单行 UPDATE 的行锁上，MySQL 在排队爆炸中先挂掉（第 7 章「流量治理的目的」：过载的机制）。所以本设计用 Redis 做**高频计数闸门**（快、原子、扛得住），用 MySQL 做**低频权威账本**（持久、可对账），两道之间用消息 + 对账缝合——这是秒杀系统的标准答案形态，也是第 8 章课程练习的工程落点。",
    },
    {
      type: "heading",
      text: "组件选型：一句话理由",
    },
    {
      type: "table",
      caption: "选型表（不是工具崇拜，是每个组件都只为解决一个具体约束）",
      headers: ["组件", "选它的一句话理由", "不选它的替代（及代价）"],
      rows: [
        ["Nginx / API 网关", "L7 限流、TLS 卸载、静态分发，成熟稳定，团队都会运维", "自研网关：要自己扛 TLS/限流/路由的工程量与故障面"],
        ["sale：Go + net/http", "协程模型天然适合 IO 密集的 API 层，单实例 2–5 万 RPS（第 2 章）", "Java 线程模型：需要更精细的线程池调优才达到同等吞吐"],
        ["Redis", "单 key 原子 Lua + 10 万级 QPS + 低延迟，秒杀预扣与热点读的标准件", "纯 MySQL 扣减：行锁排队爆炸，前文已证伪"],
        ["MQ（RabbitMQ/Kafka 族）", "把瞬时写峰摊平成可控消费速率，兼作 pay/notify 解耦（第 4 章；操作细节见 RabbitMQ 课程）", "同步调用下游：尖峰直击 DB 与 notify，级联失败"],
        ["MySQL 主从", "订单是强一致业务账本：事务、唯一索引、可对账，主从分担读（第 5 章）", "NewSQL/TiDB：本场景数据量 < 100GB、无跨库事务，引入的复杂度不值（以官方文档为准）"],
        ["CDN", "静态资源与带宽从源站剥离（第 1 课算过 6 Gbps 出口）", "全部走源站：带宽成本与源站压力双高"],
      ],
    },
    {
      type: "heading",
      text: "知识地图表：前八章如何落到这张图上",
    },
    {
      type: "table",
      caption: "组件/机制 → 对应课时（答题时按图索骥，讲清「图上这个点为什么这样设计」）",
      headers: ["图上的位置", "用到的核心机制", "对应课时"],
      rows: [
        ["整体判断：先测量/先算再设计", "容量推导、瓶颈定位", "第 1 章「性能的本质」「容量估算与规划」"],
        ["sale-web 并发模型、连接池", "goroutine、池化、keep-alive", "第 2 章「并发模型」「连接与线程池」"],
        ["详情读 L1/L2/从库三级", "本地缓存、热点与惊群、多级缓存设计", "第 3 章「热点与惊群」「多级缓存与整体设计」"],
        ["写峰进 MQ 摊平", "削峰填谷、背压、消费速率", "第 4 章「异步化的本质」「背压与消费能力」"],
        ["worker 幂等落库、回调幂等", "消息可靠性、幂等消费、最终一致", "第 4 章「消息可靠性」「最终一致性与补偿」"],
        ["MySQL 主从、不分片", "读写分离、分片判断、存储选型", "第 5 章「读写分离」「数据分片」「存储选型地图」"],
        ["sale 无状态水平扩", "无状态设计、会话外置", "第 6 章「无状态设计」「会话与登录态」"],
        ["网关限流/鉴权/路由", "网关职责、限流放置", "第 6 章「网关与南北流量」第 7 章「限流」"],
        ["过载保护、降级预案", "限流降级熔断、过载保护", "第 7 章「流量治理」全章"],
        ["Redis Lua 预扣 + MySQL 幂等扣减", "并发正确性、幂等、库存扣减方案链", "第 8 章「单机并发正确性」「幂等」「设计练习：秒杀下单的库存扣减」"],
        ["对账/回补/超时释放", "最终一致、补偿、对账、幂等取消", "第 4 章「最终一致性与补偿」+ 第 8 章「幂等」「跨库事务的现实」"],
      ],
    },
    {
      type: "quiz",
      question: "关于本课架构，下列哪个理解是**正确**的？",
      options: [
        "下单请求先写 MySQL 扣库存，扣成功再把订单同步通知 notify——保证用户看到的一切都强一致",
        "Redis 预扣是权威账本，MySQL 落库只是可选的备份，Redis 主从切换后以 Redis 数据为准即可",
        "读路径逐级卸载：本地缓存 → Redis → MySQL 从库；写路径队列化：预扣 → MQ → 幂等落库，两条链各有自己的缓存/缓冲策略",
        "为了最大可用性，网关限流应该在大促时关闭，让所有请求都进系统由数据库自行消化",
      ],
      answer: 2,
      explanation: "C 正确：这正是本课架构图的骨架——读链路用多级缓存把回源压到零头，写链路用 Redis 预扣 + MQ 削峰把 MySQL 从瞬时洪峰中保护出来。A 错误：强一致同步写 notify 会让下单请求等所有下游，尖峰直击 DB（第 2 章「同步与异步的边界」论证过的错误做法）。B 错误：Redis 是高频闸门不是账本，权威账本在 MySQL，Redis 丢数据由对账回补兜底。D 错误：限流正是容量保护的第一道闸（第 7 章），关掉它等于让过载自行摧毁系统。",
    },
    {
      type: "exercise",
      title: "给别人讲懂这张图",
      description: "不看本课内容，对着上面「文字架构图」的代码块，向一个只学过前八章的同学口头讲解 5 分钟：① 为什么读链路要有三级而不是直接查库；② 为什么写链路在 Redis 预扣与 MySQL 扣减之间隔了一条 MQ；③ 如果删掉网关层，哪些故障会直接打到 sale 服务。讲完对照本课「知识地图表」检查自己漏了哪些课时的知识点。",
      hint: "每条链都问自己三个问题：这一层在替谁挡流量？它挂了谁兜底？它的一致性承诺是什么？答不上来的点就是你要回看的课时。",
    },
    {
      type: "keypoints",
      items: [
        "架构 = 分层接力，每一层替下一层挡掉它扛不住的流量：CDN 挡静态带宽、网关挡非法与超量、L1/L2 缓存挡热点、MQ 挡写洪峰、MySQL 只留幂等落库。",
        "关键路径（读/写两条主链）决定用户体验与延迟预算；旁路（回调/通知/对账/释放）决定数据收敛，同样要幂等与可观测。",
        "读：本地缓存 → Redis → MySQL 从库，回源是最后手段；写：网关限流 → Redis Lua 预扣 → MQ → worker 幂等落库 → 对账回补。",
        "预扣在 Redis（高频闸门）、扣减在 MySQL（权威账本），两者用消息与对账缝合；缓存永远不是数据库。",
        "组件选型一句话理由即可答辩；知识地图表（第 1–8 章 → 图上的位置）是综合问答的索引。",
      ],
    },
  ],
};
