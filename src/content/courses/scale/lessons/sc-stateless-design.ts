/* ==================================================================
 * 课时：无状态设计：水平扩容的前提（sc-stateless-design）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "理解「实例不保存跨请求必需状态」为何是水平扩容的前提：状态藏在哪、外置到哪，以及健康检查与优雅上下线。",
  blocks: [
    {
      type: "paragraph",
      text: "第 1 章「容量估算与规划」的账本上写着两组数字：单机 Go 服务大约能扛 2 万到 5 万 RPS，单机 MySQL 的简单读大约在 3k–8k QPS；而闪购大促时，一个热点商品的读峰值会冲到 10 万 QPS 以上。结论不用算也知道——一台机器不够，要靠水平扩容加机器。可如果只是把代码复制成十份部署、流量随意分发，系统真的能随机器数量线性变强吗？答案是：只有当每个实例都不欠「上一笔请求的账」时，扩容才成立。这一课把「无状态」讲透：它为什么是水平扩容的前提、状态通常藏在哪些角落、外置之后要付出什么代价。",
    },
    {
      type: "heading",
      text: "什么是无状态：实例不欠上一笔账",
    },
    {
      type: "definition",
      term: "无状态（Stateless）",
      definition:
        "服务实例不保存跨请求必需的状态，任意实例都可以服务任意请求。它的反面是「有状态」：实例在自己的进程内存、磁盘或本地文件里保存了后续请求依赖的数据（会话、计数器、待办、上传文件），于是某些请求必须回到同一个实例才能得到正确结果。",
    },
    {
      type: "paragraph",
      text: "为什么水平扩容必须以无状态为前提？回顾第 1 章「垂直与水平扩容」：水平扩容的本质是「加机器 = 加容量」，而加出来的容量只有在流量能被自由地分发给任意一台实例时才真正生效。无状态实例彼此等价，负载均衡把请求打到哪一台都行，于是：**新实例加入就立刻分担流量，容量线性增长**；**任意实例宕机，其余实例无缝接管，不丢任何用户状态**；**滚动发布时新旧实例并存，流量在两者间无差别分发**。这三条分别对应扩容、可用性与发布的自由——它们全被「状态钉在某台实例上」这一点摧毁。",
    },
    {
      type: "heading",
      text: "状态都藏在哪：四种典型的「隐形式状态」",
    },
    {
      type: "table",
      caption: "实例内的隐形式状态与对应外置方案",
      headers: ["藏在实例里的状态", "典型例子", "留在实例内的隐患", "外置方案"],
      rows: [
        ["会话与登录态", "登录后把 user_id 存在进程内 map", "后续请求必须回到同一台；重启即全员掉线", "Redis / DB（下一课「会话与登录态」专讲）"],
        ["本地内存缓存", "把商品、库存热点数据缓存在进程内", "每台实例各存一份，命中率随实例数下降、一致性分裂", "分布式缓存 Redis；进程内只留可重建热点（见第 3 章「多级缓存与整体设计」）"],
        ["本地文件", "上传图片、导出的对账文件、落盘的日志", "请求被分发到没有该文件的实例即失败；实例重建即丢失", "对象存储存文件；日志走采集系统（observability 课程）"],
        ["进程内定时任务与单机锁", "「每天 0 点开活动」「整点对账」写在实例代码里", "十台实例把同一个任务重复执行十遍；本地锁只能锁住本进程", "独立调度或任务队列；跨实例互斥用分布式锁（见第 3 章「缓存一致性、分布式锁与计数器」）"],
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "无状态 ≠ 一无所有",
      body: "无状态指的是「不保存跨请求必需、不可重建的状态」。连接池、启动时加载的只读配置、编译好的路由表这类每实例一份、随时能重建的资源，完全可以留在进程内——它们不破坏「任意实例可服务任意请求」。判断标准只有一句话：把这台实例杀掉换一台全新的，用户请求的正确性会不会受影响？不受影响，就是无状态；会受影响，那个「会受影响的原因」就是你需要外置的状态。",
    },
    {
      type: "heading",
      text: "无状态与幂等：重试的两个前提",
    },
    {
      type: "paragraph",
      text: "分布式系统里没有「一定成功」的调用，超时、重试是家常便饭。无状态让「重试请求可以发给任意一台健康实例」成为可能——这是重试的**可分发前提**；但把「扣库存」重复执行一次，会不会把库存扣两次，取决于操作本身是否幂等（同一操作重复执行与执行一次效果相同）。一句话概括两者的分工：无状态解决「能不能安全地换个地方再试」，幂等解决「再试一次会不会出事」，它们是重试安全网的两半。幂等的定义、幂等键与去重表实现在第 8 章「幂等：分布式写的第一原则」展开，本课只先立一个观念：无状态化的服务天然适合重试，写接口从设计第一天就为幂等留好位置，这笔账后面会加倍还回来。",
    },
    {
      type: "heading",
      text: "无状态的代价：状态不会消失，只会搬家",
    },
    {
      type: "list",
      items: [
        "上下文重建：状态外置后，每次请求都要重新取上下文——多查一次 Redis/DB 拿用户、权限或购物车。本地函数调用是纳秒级，跨进程一跳约 1–5ms，需要靠缓存与批量把这些访问压下来（第 3 章）。",
        "本地缓存命中率下降：同一份热点数据在十台实例各存一份，单实例命中率变低、回源请求被放大（教学示意：若某热点 100 QPS 均匀分发到 10 台，每台只能观察到约 10 QPS 的访问，时间局部性被稀释，命中率随之下降）。无状态化不等于不能有本地缓存，而是本地缓存只能放「丢了能重建」的数据——这正是多级缓存与热点 key 治理要解决的问题（第 3 章「热点与惊群：单 key 放大问题」）。",
        "状态存储成为新的故障点：会话、计数都搬进 Redis/DB 后，这些存储的故障会直接传导为业务故障。外置不是消灭状态，而是把状态搬到你更容易扩展、更有能力保障的地方。",
        "运维心智变化：实例变成「牲畜而不是宠物」，随时可能被替换——日志必须集中、慢请求必须能跨实例追踪（observability 课程的三支柱），否则排障会变成大海捞针。",
      ],
    },
    {
      type: "heading",
      text: "健康检查与优雅上下线：让「替换实例」不伤人",
    },
    {
      type: "definition",
      term: "优雅上下线（Graceful Up/Down）",
      definition:
        "下线时先摘流量再停实例——负载均衡停止向该实例派发新请求，等它在途请求处理完（排空）再退出；上线时就绪后再接流量——新实例完成预热、通过健康检查后才开始接收分发。没有这两步，滚动发布时请求会打到正在停机的实例，新实例也会被冷启动流量直接压垮。",
    },
    {
      type: "code",
      title: "Go：就绪探针 + 优雅停机（片段示意）",
      language: "go",
      code: "// 片段示意，非完整程序。ready 是 atomic.Bool，启动预热完成后置 true\nvar ready atomic.Bool\n\n// 就绪探针：负载均衡/注册中心按它决定是否派发请求\nmux.HandleFunc(\"/healthz\", func(w http.ResponseWriter, r *http.Request) {\n    if !ready.Load() {\n        w.WriteHeader(http.StatusServiceUnavailable) // 503 → 摘流量\n        return\n    }\n    w.WriteHeader(http.StatusOK)\n})\n\n// 收到 SIGTERM/SIGINT 后：\nready.Store(false) // 1. 摘流量：探针开始返回 503，新请求不再进来\nctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)\ndefer cancel()\nsrv.Shutdown(ctx) // 2. 排空在途请求：等存量请求处理完，超时则强制退出",
    },
    {
      type: "paragraph",
      text: "回到闪购，把 sale 的每个实例逐项过一遍「无状态化改造」：登录会话从实例内存搬进 Redis（下一课展开）；热点商品与库存读数进 Redis，进程内只留可重建的只读快照（第 3 章）；对账报表等文件上传改走对象存储；「0 点开活动」「整点对账」这类定时任务从实例内迁到独立调度或任务队列（第 4 章「消息之外的异步」），避免十台实例重复执行；MySQL 连接池上限要按「实例数 × 每实例池上限」不超过数据库可承受连接数来重算（MySQL 课程「连接池与高并发连接管理」）。最后给每个实例暴露 /healthz 并实现优雅停机。完成这些之后，任意一台新加的 sale 实例与既有实例完全等价——到这一步，「水平扩容」才从口号变成工程事实。",
    },
    {
      type: "quiz",
      question: "下列哪种情况说明 sale 的实例仍然「有状态」、会破坏水平扩容？",
      options: [
        "每个实例持有到 MySQL 的连接池，池上限按实例核数设置",
        "把用户会话存在实例进程内存里，后续请求需要读同一台实例",
        "各实例启动时把商品只读配置加载进本地内存",
        "多个实例通过 Redis 读写共享的热点计数",
      ],
      answer: 1,
      explanation:
        "会话是跨请求必需、且不可重建的用户状态，钉在实例内存里后，同一用户的请求必须被送回同一台实例，「任意实例可服务任意请求」被破坏。连接池与只读配置每实例一份且可随时重建，不破坏无状态；通过 Redis 读写计数是状态外置的正解。",
    },
  ],
};
