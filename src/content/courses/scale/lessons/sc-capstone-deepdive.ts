/* ==================================================================
 * 课时：关键链路深挖：热点读、秒杀写与一致性（sc-capstone-deepdive）
 * ----------------------------------------------------------------
 * 第 9 章「综合设计：闪购系统的扩展与高吞吐设计」第 3 课。
 * 课时元信息（title/minutes/kind）以 course.ts 大纲为权威，此处省略。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "把第 2 课的两条主链拆到代码与状态机级别：热点读怎么挡、秒杀写怎么不超卖、三道一致性防线怎么兜底，以及四种故障的推演与恢复。",
  blocks: [
    {
      type: "paragraph",
      text: "第 2 课给了架构骨架，本课把它拧紧到可以答辩的细节。三件事必须讲透：**热点读**（13.3 万 QPS 打同一行数据，怎么挡）、**秒杀写**（80 万库存、3.3 万 QPS 涌入，怎么保证不超卖还不拖垮 MySQL）、**一致性**（预扣与落库、支付回调、超时释放这三类「对不上」怎么自动修复）。最后做四场故障推演——大促答辩最怕的不是方案不会，而是「挂了怎么办」答不上来。",
    },
    {
      type: "heading",
      text: "链路一：热点读——13 万 QPS 打同一行",
    },
    {
      type: "code",
      title: "热点详情读的完整路径与每级容量账",
      language: "text",
      code: "客户端（大促开抢前 1 分钟：反复刷新详情）\n   ↓ 约 13.3 万 QPS 集中在爆款单品（其他 SKU 合计约 4.5 万）\n网关层：限流按用户维度放行；静态壳/图片已被 CDN 吃掉\n   ↓\nsale-web 实例 A（无状态，共 12–16 台，每台约分到 1 万 QPS）\n   ① L1 本地缓存（进程内 map，商品快照 + 短 TTL 300ms）\n       · 本地命中率估 70–80%：同一实例反复服务同一批用户（网关哈希），\n         开抢前用户高频刷新 → 时间局部性强（第 3 章）\n       · 未命中的（约 30%）才继续向下，即打到 Redis 的约 13.3 万 × 30% ≈ 4 万 QPS\n   ② 未命中 → Redis L2（key: product:hot:1001，TTL 5s）\n       · L2 命中率约 95%（漏过 L1 的大多是换实例/冷启动，L2 里能命中）\n   ③ 仍未命中 → MySQL 从库回源 + 回填两级缓存\n       · 到 MySQL 的总回源 ≈ 13.3 万 × (1-0.7) × (1-0.95) ≈ 2000 QPS\n       · 从库单机 3–8k QPS → 完全兜得住（第 1 课容量表已对账）",
    },
    {
      type: "paragraph",
      text: "这套组合拳回答了「13.3 万 QPS 怎么扛」：**没有任何一层单独扛，是每一级只处理上一级的漏网之鱼**。其中最容易翻车的是第 ① 级之后的惊群——当热点 key 在 Redis 恰好过期、而 L1 又集体冷启动时，数千个请求会同时回源 MySQL，这就是第 3 章「穿透、击穿、雪崩」定义的**击穿**。标准防线在本设计中全用上了：热点 key 的 L2 TTL 加随机抖动、重建用互斥锁（同一 key 只允许一个请求回源，其余短暂等待或拿旧值）、L1 兜底返回缓存中的旧快照（详情允许秒级陈旧，见第 1 课功能需求表）。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "热点读的隐藏坑：从库延迟与本地缓存陈旧",
      body: "详情页展示「剩余库存」时，从库可能落后主库几百毫秒到秒级，用户看到的是已过期的数字——这在需求里被明确允许（浏览不承诺实时库存，见第 1 课），但你必须**在答辩里主动说出这个取舍**，否则面试官会认为你没想过。另一个坑是本地缓存放大陈旧：L1 的 TTL 越长、实例越多，各实例看到的库存越不一致，所以热点库存字段应走「短 TTL + 活动开始后主动失效」而不是长缓存。",
    },
    {
      type: "heading",
      text: "链路二：秒杀写——不超卖的状态机",
    },
    {
      type: "paragraph",
      text: "第 8 章「设计练习：秒杀下单的库存扣减」给出了候选方案链：悲观锁 → 乐观锁 → Redis 原子 → 队列化 + 预扣 → 分片扣减。本设计选择的是**队列化 + Redis 预扣**这一档：它把并发正确性从 MySQL 行锁转移到了 Redis 单 key 原子操作上。为什么不是更简单的方案？悲观锁/乐观锁在 3.3 万 QPS 下都会让单行库存成为排队瓶颈，且每次尝试都消耗 MySQL 连接与事务资源；而 Redis 预扣把「抢」这个高频动作压缩为一次 Lua（微秒到亚毫秒级、单实例 5–8 万 QPS 写能力），MySQL 只承接已确定成功的约 3000 单/s。",
    },
    {
      type: "code",
      title: "Redis 原子预扣（Lua）——超卖与限购的第一道闸",
      language: "go",
      code: "// 注意：以下为教学示意，使用 Go + redis 客户端，具体 API 以官方文档为准。\n// KEYS[1] = stock:act1:p1001   活动库存计数\n// KEYS[2] = bought:act1:p1001  已购用户集合（限购 1 件）\n// ARGV[1] = user_id\nvar lua = redis.NewScript(`\n  local stock = tonumber(redis.call('GET', KEYS[1]) or '-1')\n  if stock <= 0 then return -1 end          -- 已抢光\n  if redis.call('SISMEMBER', KEYS[2], ARGV[1]) == 1 then return -2 end  -- 已购过\n  redis.call('DECR', KEYS[1])\n  redis.call('SADD', KEYS[2], ARGV[1])\n  return 0                                   -- 预扣成功\n`)\n\n// sale-web 中调用（单次往返，天然原子，无需分布式锁）\nres, err := lua.Run(ctx, rdb, []string{\"stock:act1:p1001\", \"bought:act1:p1001\"}, userID).Int()\nswitch res {\ncase 0:  // 预扣成功 → 生成订单号，投递 MQ（见下）\ncase -1: // 返回「已抢光」给用户\ncase -2: // 返回「每人限购 1 件」\n}\n\n// 预扣成功但投递 MQ 失败 → 必须回补库存（补偿语义，见链路三）",
    },
    {
      type: "paragraph",
      text: "Lua 脚本在 Redis 内原子执行，杜绝了「两个请求同时读到库存 1、各自扣成 0 和 -1」的竞态；SISMEMBER 用集合天然实现每用户限购 1 件。**这是不超卖的第一道、也是最关键的一道闸**：只要预扣是原子的且不超卖，后面落库阶段无论怎么重试都不会产生超过 80 万的成交。",
    },
    {
      type: "code",
      title: "订单落库状态机（worker 侧）与补偿动作",
      language: "text",
      code: "order.created（MQ 消息，至少一次投递语义，见第 4 章）\n   │ worker 消费\n   ▼\n① 幂等检查：INSERT orders(id=order_id, user_id, product_id,\n        status='PENDING', created_at)  —— order_id 由预扣时签发，DB 唯一键防重\n   · 若唯一键冲突 → 消息已处理过，ACK 丢弃（消费幂等，第 4/8 章）\n   ▼ （①与②在同一个数据库事务里，要么都成要么都回滚）\n② 权威扣减：UPDATE product SET stock = stock - 1 WHERE id = :pid AND stock > 0\n   · 与预扣计数相互独立；即使 Redis 预扣记录丢失，DB 库存仍是真账（条件更新兜底）\n   · 影响行数为 0（两级库存漂移，如预扣计数被重置）→ 整个事务回滚，\n     回补 Redis 预扣并交对账（不给用户建一笔扣不到库存的单）\n   ▼\n③ 事务提交 → ACK + 投递 notify「待支付提醒」\n   · 落库失败（DB 抖动）→ 不 ACK，MQ 重试；重试超限进死信队列人工处理\n\n状态流转：\n  PENDING --支付回调成功--> PAID --发货--> DONE\n  PENDING --15 分钟未支付--> CANCELLED（DB 回补库存 + Redis 回补计数与已购标记，\n       状态机 CAS 保证只执行一次；释放的库存可再次抢购）\n  PAID   --支付回调乱序/重复--> 幂等：仅当 PENDING 才置 PAID，否则忽略",
    },
    {
      type: "heading",
      text: "链路三：一致性——三道防线把「对不上」变成「自动修好」",
    },
    {
      type: "table",
      caption: "三类不一致风险的机制与防线",
      headers: ["风险", "怎么发生", "防线", "恢复手段"],
      rows: [
        ["预扣成功但落库失败/丢失", "预扣后进程崩溃、MQ 投递失败、worker 消费失败重试耗尽", "落库是独立真账 + 消息至少一次 + 死信队列", "对账任务比对两侧净额：Redis 净预扣（预扣 − 释放回补）vs MySQL 净扣减（落库 − 取消回补），差异告警并重放/回补"],
        ["支付回调乱序/重复/迟到", "pay 网络重试、异步通道乱序", "回调处理幂等：以订单状态机为准，仅 PENDING→PAID 合法；回调带支付单号唯一索引", "重复回调被状态机挡住；迟到回调若订单已 CANCELLED 则转人工/自动退款流程"],
        ["超时释放重复执行", "定时扫单与用户支付竞态、扫单任务重试", "释放动作幂等：先 CAS 状态 PENDING→CANCELLED 成功者才回补 DB 库存与 Redis 预扣/已购标记", "唯一持有者模式：状态转移成功的那个任务才执行回补，天然只释放一次；失败进入重试与对账"],
      ],
    },
    {
      type: "paragraph",
      text: "设计一致性防线时记住一个总原则：**状态机 + 幂等键能挡住绝大多数乱序与重复，对账是最后一道不依赖时序的保险**。前两道防线要求「事件按序到达」，对账不要求——它周期性地问一个简单问题：「Redis 侧净预扣（预扣 − 超时释放回补）是 X 件，MySQL 侧净扣减（落库 − 取消回补）是 Y 件，X 与 Y 差多少？」有差就告警，再按差量重放消息或回补库存。这也是为什么第 4 章「最终一致性与补偿」把对账列为最终一致系统必备的组件：**最终一致不是放任不一致，而是不一致有上界、有发现、有修复**。本设计的对账时限目标是 10 分钟内发现并修复（第 1 课验收口径）。",
    },
    {
      type: "callout",
      variant: "tip",
      title: "答辩要点：把「为什么不会超卖」讲成三层",
      body: "一层 Redis Lua 原子预扣（并发闸门，杜绝竞态超卖）；二层 order_id 唯一键 + 状态机幂等（重复消息不产生第二单）；三层 DB 权威扣减 + 对账（预扣记录丢失也被真账兜住）。三句话按顺序讲，面试官最想听的「分片扣减/扣减在 Redis」追问就能自然展开——预扣计数放在 Redis 是高频闸门，权威扣减留在 MySQL 是账本，两者职责不同，不能合并。",
    },
    {
      type: "heading",
      text: "故障推演：挂了怎么办",
    },
    {
      type: "table",
      caption: "四场故障推演：表象 → 保护动作 → 恢复（大促前应逐条写成预案并演练）",
      headers: ["故障", "表象", "保护动作（先止血）", "恢复（后治本）"],
      rows: [
        ["Redis 整体故障/主从切换", "详情读全部回源 MySQL、预扣请求失败率飙升", "读：L1 本地缓存继续服务（陈旧可接受）；写：网关开启降级开关，预扣降级为「直接拒绝 + 提示稍后再试」，**绝不让写穿透到 MySQL 行锁**", "Redis 恢复后自动回源；对账任务重放差异；复盘切换耗时与哨兵/集群配置"],
        ["MQ 积压（消费慢于生产）", "订单延迟落库：用户已见「抢购成功」但订单迟迟查不到；队列深度告警", "监控队列深度与消费延迟（第 4 章背压）；临时加 worker 实例并同步观察 MySQL 写水位", "若 DB 是瓶颈则分批/限速消费而非无限加 worker；积压消化后对账确认无丢失"],
        ["MySQL 主库故障", "落库失败、下单回调大面积超时", "主从切换（半同步尽量少丢）；期间落库消息在 MQ 中排队不丢；网关对写接口限流降级", "新主接管后对账：以 Redis 预扣为线索重放未落库消息；复盘 binlog 同步延迟"],
        ["热点 key 倾斜到单分片", "Redis 某分片 CPU 打满而其他分片空闲（单 key 不可分片）", "热点 key 增加本地副本/Redis 副本读；key 后缀多副本随机读（第 3 章「热点与惊群」）", "活动结束后下线副本；复盘热点识别是否及时（预热脚本提前探测）"],
      ],
    },
    {
      type: "paragraph",
      text: "四场推演背后是同一套心法，也是第 7 章「流量治理」的浓缩：**先判断故障影响的是读还是写，读降级到缓存旧值、写降级到拒绝或排队；任何故障都不允许把压力传导到 MySQL 主库的行锁上**——因为那是全链最贵、最难恢复的资源。恢复的顺序永远是：止血（挡住新流量）→ 恢复（让组件回来）→ 对账（修复窗口期差异）→ 复盘（改预案与监控）。",
    },
    {
      type: "quiz",
      question: "大促中 Redis 集群突然不可用，正确的降级动作是下列哪一组？",
      options: [
        "详情读继续依赖 Redis 重试直到恢复；下单请求全部直接穿透到 MySQL 扣库存，保证用户能买到",
        "详情读走本地缓存旧值（允许秒级陈旧）；下单接口开启降级开关直接返回「活动火爆稍后再试」，恢复后未落库的预扣消息由队列/对账补齐（被拒请求由用户重试）",
        "把 Redis 的 key 全部迁移到 MySQL 内存表继续扛 13 万 QPS 热点读",
        "关闭网关限流让请求尽快耗尽排队，逼 Redis 快速恢复",
      ],
      answer: 1,
      explanation: "B 正确：读降级到本地缓存旧值、写降级为拒绝新请求，是 Redis 故障的标准止血动作——绝不让写穿透到 MySQL 行锁（第 7 章降级 + 第 8 章预扣语义）；已预扣未落库的消息在恢复后由队列续传与对账补齐，被拒的用户请求靠重试解决。A 错误：让下单直接打 MySQL 行锁会在 Redis 恢复前把主库打挂，故障扩散。C 错误：MySQL 扛不住 13 万 QPS 热点读，这正是 Redis 存在的意义。D 错误：关闭限流等于主动过载，只会让故障更久。",
    },
    {
      type: "exercise",
      title: "口述一场完整的大促故障演练",
      description: "假设你是值班负责人，凌晨 2 点大促进行中收到三连告警：① 队列深度 10 分钟涨了 50 倍；② 支付回调成功率降到 60%；③ 详情读 P99 从 120ms 涨到 2s。请按「止血 → 恢复 → 对账 → 复盘」四步口头推演：先判断三件事各自的根因假设与最可能的共同根因，再给出每一步的具体动作与回滚条件。提示：先想哪个组件故障会同时导致这三件事。",
      hint: "三件事同时发生，优先怀疑共享依赖——例如 MySQL 主库故障会同时拖垮落库 worker（队列积压）、支付回调的状态更新（成功率下降）与回源读（P99 飙升）。单点故障假设比三个独立故障更符合奥卡姆剃刀。",
    },
    {
      type: "keypoints",
      items: [
        "热点读 = 多级卸载：L1 本地缓存（时间局部性）→ L2 Redis（加副本）→ MySQL 从库回源兜底，每级命中率递减、流量递减；击穿由短 TTL + 抖动 + 互斥重建 + 旧值兜底防住。",
        "秒杀写 = Redis Lua 原子预扣（并发闸门，杜绝竞态与超卖）+ order_id 唯一键幂等落库 + MySQL 权威扣减（条件更新，0 行即整体回滚）；预扣与真账职责分离，用 MQ 削峰缝合；超时取消回补两侧（DB 库存与 Redis 计数）。",
        "一致性三道防线：状态机 + 幂等键挡乱序重复，对账不依赖时序、兜底一切差异，最终一致 = 有上界、有发现、有修复。",
        "故障推演四步：止血（读降级旧值、写降级拒绝/排队，绝不穿透到主库行锁）→ 恢复 → 对账 → 复盘；四类故障（缓存挂/队列积压/主库故障/热点倾斜）各有预案。",
        "答辩结构：三层「为什么不超卖」+ 四条「挂了怎么办」，是本课可整体复述的最小答辩包。",
      ],
    },
  ],
};
