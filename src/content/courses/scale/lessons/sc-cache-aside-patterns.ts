/* ==================================================================
 * 课时：Cache-Aside 与常见读写模式（sc-cache-aside-patterns）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "完整掌握 Cache-Aside 的读写流程与失效语义：为什么删缓存而非更新缓存、一致性窗口与延迟双删，以及 Write-Through / Write-Back / Read-Through 的适用场景。",
  blocks: [
    {
      type: "paragraph",
      text: "第 1 课确立了缓存的位置与命中率，本课把「缓存放在请求路径上以后，读和写各怎么走」讲成可落地的流程。MySQL 课程的面试课时已经用快速问答的方式覆盖过缓存一致性的标准答案；本课把同一主题做系统化展开：把 Cache-Aside 的每个环节、以及「为什么不直接更新缓存」的并发机理拆开讲透，再对比其它三种读写模式各自的适用场景。读多写少的闪购商品详情，将从这一课开始获得第一条具体的读路径实现。",
    },
    {
      type: "heading",
      text: "Cache-Aside（旁路缓存）：读写都在应用侧控制",
    },
    {
      type: "code",
      title: "Cache-Aside 的读写流程",
      language: "text",
      code: "读 product(id)：\n  1. GET cache key=product:{id}\n  2. 命中 → 返回缓存值\n  3. 未命中(miss) → 查数据库 SELECT ...\n  4. 写回缓存（设 TTL）→ 返回\n\n写 product(id)：\n  1. 更新数据库（事务内完成真正的写）\n  2. 删除缓存 key=product:{id}\n     （删除失败 → 记录并重试，见第 5 课）",
    },
    {
      type: "paragraph",
      text: "关键点：应用代码完全掌控缓存与数据库的两条路径，缓存只是旁路——**数据库始终是唯一的事实源**，缓存里任何时刻都可能没有某个 key，但只要有，它就该是「最近一次有效数据的副本」。读侧 miss 时的回源写回（也称回填）天然带 TTL，保证数据不会永久滞留；写侧不直接改缓存、而是删除缓存，把「刷新缓存」的责任交还给下一次读。这套模式在闪购里对应商品详情的读路径：GET /products/{id} 先查 Redis 的 product:{id}，命中直接返回；管理后台改价时先 UPDATE products 表，再删除该 key。",
    },
    {
      type: "heading",
      text: "为什么写侧删缓存，而不是更新缓存",
    },
    {
      type: "paragraph",
      text: "**两个并发写：更新缓存会产生覆盖次序错误。**设想两个管理员并发修改同一商品价格，写请求 W1（新价 99）和 W2（新价 129）都以「先更库、再更新缓存」执行。数据库按事务提交次序落定——假设最终价格是 129；但缓存的更新次序未必与数据库一致：W1 的缓存写可能晚于 W2 到达，缓存里留下 99，与数据库的 129 长期不一致。**缓存成了第二次独立的写**，两个写源（事务提交与缓存更新）之间没有任何顺序保证，任何并发调度都可能产生旧值覆盖新值。改用「删除缓存」后，缓存里根本没有值，也就不存在两个缓存写的次序问题——下一个读请求 miss 回源，读到的一定是数据库当前值。",
    },
    {
      type: "paragraph",
      text: "**读与写竞争：更新缓存会把旧数据回填。**即使只有一个写，更新缓存也可能被读请求破坏：读请求 R 在时刻 t1 查数据库拿到旧值 v_old（此刻写事务尚未提交），随后写事务提交并更新缓存为 v_new；但 R 的网络/代码路径上，它的「更新缓存为 v_old」发生在写之后——旧值覆盖新值，且之后没有人再改它，脏数据长期滞留（TTL 到期前一直错）。删除缓存同样会遭遇此类交错（详见本课「延迟双删」），但窗口要小得多，而且**miss 回源的读自带较新的数据库值，脏值滞留的概率被 TTL 与后续写再次兜底**。工程共识由此而来：写侧删缓存，把「什么值放进缓存」的决定权统一交给读侧的回源，单一写源（数据库提交）决定内容，避免第二个写源制造次序问题。",
    },
    {
      type: "heading",
      text: "「先更新库、再删缓存」的一致窗口",
    },
    {
      type: "paragraph",
      text: "为什么顺序必须是「先更库、后删缓存」？反过来（先删缓存、再更库）会有一个明显的空窗：删完缓存、尚未提交数据库更新的那段时间里，读请求 miss 回源——读到的仍是数据库里的旧值，并把它回填进缓存。回填发生在数据库提交之前，于是缓存又变回旧值，数据库提交后两者不一致，直到旧缓存过期——这个窗口比「先更库后删」大得多，而且几乎必然发生。所以正确顺序是先更库、后删缓存。但即便如此，**删除动作与读回源之间仍有竞争窗口**：读 R 在写事务提交前 miss 并回源，从数据库拿到的是旧值（此刻缓存为空，如刚过期或尚未回填）；随后写事务提交、删除执行完毕，R 的回填才姗姗来迟地写入缓存——缓存重新出现旧值，而删除已经发生过，没有人再纠正它，只能等 TTL 兜底。",
    },
    {
      type: "callout",
      variant: "note",
      title: "延迟双删：把窗口压低，但别神话它",
      body: "「先更库 → 删缓存 → 睡一小段（如几百 ms）→ 再删一次」的思路是：把可能晚到的那次回源「让过去」，第二次删除负责清掉它回填的旧值。它确实能进一步压缩脏窗口，但有两个现实局限：一是延迟窗口是拍脑袋估的，回源慢于延迟（GC 停顿、网络抖动）时第二次删除照样错过；二是删除本身可能失败，双删只是把失败概率又乘了一次，不是消除。真正的兜底仍是 TTL——所以延迟双删只在「脏窗口必须再短一点」且能接受实现复杂度的场景用，多数业务靠 TTL 兜底已经足够。",
    },
    {
      type: "table",
      caption: "Cache-Aside 写路径上各环节的职责与兜底",
      headers: ["环节", "作用", "残留窗口/失败时靠什么兜底"],
      rows: [
        ["先更新数据库", "让数据库先成为新事实，缓存随后失效才有依据", "（写事务自身的原子性由数据库保证）"],
        ["删除缓存", "清掉旧副本，让下一次读 miss 回源拿新值", "删除失败 → 重试/异步删除（第 5 课）"],
        ["TTL", "任何删除遗漏后的自愈", "最坏脏一个 TTL 的长度"],
        ["延迟双删（可选）", "压缩「读回填晚于删除落地」的竞争窗口", "删除失败仍会残留，不改变 TTL 兜底的必要性"],
      ],
    },
    {
      type: "heading",
      text: "其它三种读写模式",
    },
    {
      type: "paragraph",
      text: "Cache-Aside 之外还有三类由缓存系统/应用代管读写的模式。**Read-Through**：应用不直接管缓存，而是让缓存层自己 miss 后从数据库加载——与 Cache-Aside 的读路径几乎等价，只是把回源逻辑下沉到缓存库层，多用在库方自带加载器的组件里。**Write-Through**：写请求同步写缓存并立即同步写数据库，两层一起成功才算成功；读永远命中（或几乎总是命中），代价是每次写都双写、延迟增高，且写必须强一致地穿透到底。**Write-Back（Write-Behind）**：写请求只写缓存、立即返回，由缓存层异步把脏数据落库——写延迟最低，但缓存一旦在落库前崩溃，这段时间的写就丢了，因此只适用于能容忍丢失的流水类数据。",
    },
    {
      type: "table",
      caption: "四种读写模式的对比（★ 越多代表该项越好）",
      headers: ["模式", "读流程", "写流程", "读性能", "写性能", "数据丢失风险", "典型适用"],
      rows: [
        ["Cache-Aside", "缓存 → miss 回源 DB", "DB 提交后删缓存", "★（靠命中率）", "★★", "无（DB 是唯一事实源）", "读多写少、可容忍短暂旧值的业务数据"],
        ["Read-Through", "缓存层自己回源", "同 Cache-Aside", "★（靠命中率）", "★★", "无", "使用自带加载器的缓存组件"],
        ["Write-Through", "几乎必命中", "同步写缓存 + DB", "★★★", "★（每次写双写、延迟高）", "无", "写少、且读要极稳的强一致场景（很少用）"],
        ["Write-Back", "几乎必命中", "只写缓存，异步落库", "★★★", "★★★", "有：落库前崩溃即丢", "可容忍丢失的高频写（流水、计数快照）"],
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购里每种模式的位置",
      body: "商品详情与活动配置——Cache-Aside（数据库提交后删 key，TTL 兜底）。缓存库自带加载器的内部组件——Read-Through。高价值、不能丢的写（订单、库存扣减）——不写缓存，直接走数据库事务，宁可让读 miss。可容忍丢失的高频计数（浏览数、点击量的中间快照）——Write-Back 的思想在异步章节与第 5 课的计数方案里会以更工程化的形态出现。",
    },
    {
      type: "keypoints",
      items: [
        "Cache-Aside：读走「缓存 → miss 回源 → 回填带 TTL」；写走「更新数据库 → 删除缓存」。数据库是唯一事实源。",
        "写侧删缓存而非更新缓存：更新等于引入第二个写源，并发下缓存值与数据库值可能永久错位。",
        "顺序必须是先更库后删缓存；但删除与并发回源之间仍有一致窗口，TTL 是最终兜底，延迟双删只是压低窗口、不消除问题。",
        "Write-Through 换强一致但写慢，Write-Back 换写快但可能丢数据；读多写少的业务主流是 Cache-Aside。",
      ],
    },
  ],
};
