/* ==================================================================
 * 课时：穿透、击穿、雪崩：三种故障的机制与防线（sc-cache-penetration-avalanche）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "完整定义缓存三大失效故障——穿透、击穿、雪崩——各自的机制、受害方、表象与防线，并给出判别表与排查路径。",
  blocks: [
    {
      type: "paragraph",
      text: "第 2 课的 Cache-Aside 有一个隐含前提：缓存里「有」的 key 都能挡住读。但真实流量里，缓存这道闸会以三种方式漏掉请求、把它们放行到数据库——这就是缓存三大失效故障。MySQL 课程的面试课时用速记卡讲过它们的应对口诀；本课是本章对三种故障的**完整定义与机制展开**：每种故障的成因链条、谁在受害、表象长什么样、防线为什么有效，以及三者表象相似时如何判别。闪购大促是它们的完美舞台：大促期间数据库本就接近容量上限，任何一类故障漏过去的流量都可能把它压垮。",
    },
    {
      type: "definition",
      term: "缓存穿透",
      definition: "查询一个缓存和数据库里都不存在的数据：每次查询都 miss、每次都绕开缓存直达数据库。数据库收到的是「永远查不到结果」的无效读，却被真实消耗了查询资源。",
    },
    {
      type: "paragraph",
      text: "穿透的成因往往不是自然流量，而是恶意或异常流量：攻击者用随机负数 id、越界 id 批量请求 GET /products/-999，或爬虫遍历不存在的商品；商品被下架、活动被删除后，曾经的合法 key 也变成「存在过、现在没有」的穿透源。缓存只对存在的 key 有意义，而数据库对「查不到」也要付出一次索引查找的代价——单机 MySQL 3k–8k QPS 的天花板里，全是这种无效查询时，真实用户反而被挤掉。**穿透的本质是：缓存对「不存在」这件事失忆**——它既不缓存结果，也没法知道 key 不存在，于是每一次相同的不存在查询都完整地打到数据库。",
    },
    {
      type: "definition",
      term: "缓存击穿",
      definition: "某个访问量极高的热点 key 在缓存过期的瞬间失效：大量并发请求同时 miss、同时回源到数据库，把数据库瞬时打垮。击穿只发生在「单 key 极热 + 恰好过期」的交点上。",
    },
    {
      type: "paragraph",
      text: "击穿与穿透只差一个字，机制完全不同。穿透方是不存在的数据，击穿方是**存在、且极热**的数据：大促开始的那一刻，闪购某爆款商品的缓存 product:1001 正好带着第一批 TTL 到期，而此刻恰是它 10 万 QPS 峰值流量的起点——这 10 万个请求全部 miss，一起涌向数据库。注意第 1 课的公式在这里反转：正常时命中率 99% 意味着只有 1% 回源；热点 key 过期的那一瞬间，这个 key 的命中率是 0%，回源放大因子达到 100 倍甚至更高。受害方是数据库（瞬时洪峰），表象是「某 key 过期后立刻出现一波数据库慢查询与错误」。它和雪崩的区别是规模：击穿是一个 key，雪崩是一批 key（见下文）。",
    },
    {
      type: "definition",
      term: "缓存雪崩",
      definition: "大量 key 在同一时间附近集中失效，或缓存层整体不可用（宕机、网络分区），导致原本被缓存挡住的巨量读流量同时落到数据库，数据库被瞬间压垮。",
    },
    {
      type: "paragraph",
      text: "雪崩有两个触发路径。路径一是**大量 key 同时过期**：一批活动商品在同一个时间点写入缓存、TTL 相同，到期时刻也相同，于是几百个 key 同时 miss、同时回源；路径二是**缓存层整体故障**：Redis 实例宕机或网络分区，所有 key 同时不可达——这比 key 过期更危险，因为没有任何缓存兜住流量，全部读直接穿透到数据库。两条路径的受害方相同：数据库（也可能连带下游），表象也相同：缓存可用性指标跳水的同时，数据库负载与错误率同步飙升。与击穿的关键区别在规模与原因：击穿是一个极热 key 的定时炸弹，雪崩是一批 key 或整层缓存的集体缺席。",
    },
    {
      type: "paragraph",
      text: "**防线一：穿透——空值缓存与布隆过滤器。**对付穿透的第一道防线是让缓存「记得不存在」：对查不到数据的 key 也写一条空值标记（value=null 或空对象）并给一个**很短**的 TTL（如 30–60 秒），后续相同的无效查询直接命中空值、不再打库。空值缓存照顾的是「同一个不存在 key 反复来」的情况；对攻击者批量构造、每个 id 只来一次的穿透（每次都是不同的不存在 key），空值缓存几乎没有命中机会，此时需要**布隆过滤器（Bloom Filter）**：一种概率型数据结构，把全部合法 id 预置进去，查询前先问布隆「这个 id 存在吗」——回答「不存在」就一定不存在，直接拦截；回答「存在」则可能误判（把不存在的当存在），放行后仍靠数据库兜底。布隆过滤器用很小的内存（几 MB 可容纳千万级 id）换来「确定不存在的请求根本不进数据库」，代价是**误判率**：误判率随位数组大小与哈希函数数量下降，工程上通常配置到 1% 以下，且误判只会造成少量无效查询漏过去，方向安全（绝不会把存在的判成不存在）。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "穿透防线的次序",
      body: "先参数校验与风控（拦截明显非法的 id、频控恶意调用方），再空值缓存（挡重复的不存在查询），最后布隆过滤器（挡海量各不相同的不存在查询）。次序即成本：校验最便宜、布隆需要维护 id 集合（商品新增/删除要同步更新）、空值缓存会占用缓存内存——别把昂贵的防线用在便宜的流量上。",
    },
    {
      type: "paragraph",
      text: "**防线二：击穿——互斥重建、逻辑过期与永不过期。**击穿的对策核心只有一句：**热点 key 过期瞬间，只允许一个请求回源重建缓存，其余请求等它**。最直接的是**互斥重建（singleflight）**：key miss 后先抢一把重建锁（进程内即可，跨实例用 Redis 的 SET NX，见第 5 课），抢到锁的请求查库并回填缓存、释放锁；没抢到的请求短暂等待后重读缓存（此时通常已回填完成）。同一时刻数据库只收到一个回源，洪峰被收敛成单请求。**永不过期（逻辑过期）**是另一个思路：缓存里的值带一个写入时间戳，读取时发现逻辑上已过期，不直接丢弃，而是返回这份旧值（对用户体感几乎无损），同时异步地触发一次后台刷新——热点 key 从此不存在「缓存里没值」的时刻，回源被摊平到后台、且只发生一次。实际工程常两者结合：永不过期保证读永远有值，singleflight 保证任何一次重建都只有一个人去查库。",
    },
    {
      type: "paragraph",
      text: "**防线三：雪崩——TTL 抖动、多级缓存与缓存高可用。**针对「大量 key 同时过期」，最便宜的防线是**TTL 抖动**：写缓存时在基础 TTL 上加一个随机偏移（如 5 分钟 ± 0–3 分钟的随机值），让 key 的到期时刻自然错开，不再出现「整批一起过期」的共振。针对「缓存层整体故障」，防线是多级的：**本地进程内缓存（L1）作为第二道闸**——即使 Redis 全挂，每台实例的本地缓存仍能承接相当比例的读，为故障恢复争取时间（这就是第 4、6 课的多级缓存主题）；同时缓存层自身要**高可用**：Redis 主从 + 哨兵或集群，让单点故障自动切换（本课只讲定位，不教配置）。与高可用配套的是**持久化与重启风险**：Redis 故障后重启（或发布升级、主从切换）的瞬间，若内存数据未持久化或恢复缓慢，所有 key 会同时「不存在」，读流量瞬间全部回源——等于人为触发一次雪崩路径二。所以缓存层的故障预案必须把「Redis 挂了怎么切、重启后怎么预热回填」一并演练（持久化与集群的具体配置以官方文档为准）。数据库侧还需要最后一道硬防线：**限流与降级预案**——缓存挂掉时要不要让读请求直连数据库，答案是看流量：闪购详情读 10 万 QPS 全部直连必死无疑，此时应降级为「只返回本地缓存 + 默认兜底数据」，或对回源请求限流，宁可部分请求快速失败（503/降级文案），也不让数据库被击穿后全站不可用。降级与限流的机制在流量治理章节有完整展开，本课只确立这条预案原则：**缓存故障时保护数据库，优先于让每个请求都拿到新鲜数据**。",
    },
    {
      type: "paragraph",
      text: "**三者如何判别**——表象相似、机制不同，排查时用下面这张表先定位，再动手：",
    },
    {
      type: "table",
      caption: "三种故障判别表：表象相似、机制不同，先看 key 是否真实存在、再看规模与时机",
      headers: ["判别维度", "穿透", "击穿", "雪崩"],
      rows: [
        ["查的数据存在吗", "不存在（缓存、库里都没有）", "存在且极热", "存在（很多个）"],
        ["故障触发点", "每次查询（无 key 缓存可言）", "单 key 恰好过期的瞬间", "大量 key 同时过期 / 缓存层整体故障"],
        ["数据库受害形态", "持续的无效查询流", "单个时间点的瞬时洪峰", "持续或突发的巨量回源"],
        ["典型流量来源", "恶意/越界/已删除数据", "大促爆款的自然峰值", "批量写入同 TTL / Redis 宕机"],
        ["主防线", "参数校验、空值缓存、布隆过滤器", "互斥重建（singleflight）、永不过期+异步刷新", "TTL 抖动、多级缓存、缓存高可用、限流降级"],
      ],
    },
    {
      type: "paragraph",
      text: "排查时可以按「三问」定位：**数据存在吗**——不存在是穿透，存在看规模；**是一个 key 还是一批 key**——一个极热 key 是击穿，一批或整层是雪崩；**缓存层本身健康吗**——Redis 宕机/网络分区造成的集体 miss 是雪崩路径二，单 key 过期是击穿。三个问题对应的监控指标也不同：穿透看「回源 QPS 中查不到结果的比例」与非法参数占比；击穿看「热点 key 过期时刻前后的 DB 错误突刺」；雪崩看「缓存可用性指标与 DB 负载的同涨同跌」。大促前把三类预案都演练一遍——这正是流量治理章与第 6 课故障预案的交接点。",
    },
    {
      type: "quiz",
      question: "某商品缓存过期后，数据库在几十毫秒内涌入约 10 万个相同商品 id 的查询，随后恢复。数据库无持续异常，其它 key 均正常。这属于哪种故障？",
      options: [
        "缓存穿透——该商品在数据库中不存在",
        "缓存雪崩——所有缓存 key 同时失效",
        "缓存击穿——单个热点 key 过期瞬间并发回源",
        "布隆过滤器误判导致回源",
      ],
      answer: 2,
      explanation: "症状是「单个商品 id 的过期瞬间出现十万级瞬时回源、随后恢复」，这正是击穿：一个热点 key 恰好过期、并发 miss 一起打库。穿透要求数据不存在且通常持续；雪崩要求大量 key 或整层缓存失效，与「其它 key 均正常」矛盾；布隆误判只放行少量请求，不会造成十万级洪峰。",
    },
    {
      type: "keypoints",
      items: [
        "穿透：查不存在的数据，缓存对「不存在」失忆，每次查询都打库；防线 = 参数校验 + 空值缓存 + 布隆过滤器（存在误判率）。",
        "击穿：一个热点 key 过期瞬间并发回源；防线 = 互斥重建（singleflight）+ 永不过期/逻辑过期、异步刷新。",
        "雪崩：大量 key 同时过期或缓存层整体故障；防线 = TTL 随机抖动 + 多级缓存 + 缓存高可用 + 回源限流降级。",
        "判别三问：数据存在吗？一个 key 还是一批？缓存层本身健康吗？",
        "缓存故障时的第一原则：保护数据库，优先于让每个请求都拿到新鲜数据。",
      ],
    },
  ],
};
