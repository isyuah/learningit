/* ==================================================================
 * 课时：过载保护：压垮系统的最后一根稻草（sc-load-shedding）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "从排队理论讲透过载：过载时拒绝新请求而非排队，自适应过载的直觉与实现、503 拒绝语义、对重试风暴的总防线与四件套分工收口。",
  blocks: [
    {
      type: "paragraph",
      text: "限流把入口速率锁进容量、熔断把坏下游隔在门外、降级把资源让给核心——但如果前面三层都失效，或流量形态超出所有预设阈值，系统还是会过载。**过载保护是最后一层，也是整章的总收口**：当系统真的已经被压到极限，它决定「继续服务谁、拒绝谁」，用一次有尊严的快速失败，避免整台机器进入排队爆炸式的慢死。",
    },
    {
      type: "heading",
      text: "排队理论视角：过载时，新请求该被拒绝而不是排队",
    },
    {
      type: "paragraph",
      text: "直觉上，服务处理不过来时让请求「排队等一下」很自然——但第 1 章与本章开头的结论在这里合流：**到达速率一旦超过处理速率，队列长度就没有上界，排队延迟会无限发散**。排队中的每个请求都在消耗内存与调度资源，而服务端为排队者预留的工作，很可能再也等不到发起方（客户端早已超时放弃）。此时最优策略与直觉相反：**立刻拒绝新请求**，让已经接受的工作尽快做完，系统才可能从过载中恢复——让每个请求快速得到明确答复，远好于让所有请求一起无限等待。从用户与调用方的角度看，瞬时拿到「稍后再试」也比迟迟拿不到任何结果更可预期。",
    },
    {
      type: "heading",
      text: "自适应过载：不靠拍脑袋的阈值",
    },
    {
      type: "paragraph",
      text: "固定限流阈值（每实例每秒 N 个）需要人预先猜容量，而容量会随机器配置、数据分布、下游状态漂移，猜错就白做。自适应过载不数请求，而是**观察系统真实状态**：典型实现每实例维护一个容量预估——如滑动窗口内完成请求的速率（吞吐），当窗口内「未完成请求 + 排队中请求」超过吞吐乘一个系数时，判定系统已过载，此后新请求直接拒绝。另一个著名变体改变的是队列语义：正常时请求 FIFO 排队；过载触发后改为 **LIFO**——后到的请求插到队首优先处理。初看反直觉，直觉却是对的：过载时队列中等待最久的那些请求，其客户端多半已经超时放弃，继续为它们工作，响应很可能无人接收；LIFO 让新到（最可能还活着）的请求先完成，把资源从「注定等不到响应」的旧请求上省下来——既提高有效吞吐，又让等待方更快得到明确的失败反馈从而退避。互联网基础设施领域著名的 CoDel 队列管理也基于同一条直觉：与其让队列积压所有人，不如丢弃或标记积压最久的数据包，用明确的信号逼发送方减速。这类机制统称自适应过载控制，细节各有实现差异，共同点是**基于负载反馈决策，而不是拍脑袋定死一个并发数**。这里介绍的 LIFO 等具体做法是启发式的工程思路而非标准答案——是否需要、如何调参，要结合自身业务的超时分布用压测验证。",
    },
    {
      type: "code",
      title: "自适应过载的判定骨架（Go 示意）",
      language: "go",
      code: "// 过载判定（伪代码）：滑窗内系统吞吐 capacity 与未完成工作 inflight 的比较。\n// 若 inFlight 已显著超过单位时间能完成的工作，继续接收只会加深排队爆炸，故拒绝。\n// 数值仅为示意，工程实现需结合压测标定系数与统计窗口。\nfunc shouldReject(metrics *WindowMetrics) bool {\n\tcapacity := metrics.completedPerWindow()      // 滑窗内实际完成速率（件/窗口）\n\tinflight := metrics.arrivedButNotDone()        // 已到达未完成：处理中 + 排队\n\treturn inflight > capacity+int(float64(capacity)*overloadFactor)\n}",
    },
    {
      type: "heading",
      text: "拒绝的语义：503 与重试引导",
    },
    {
      type: "paragraph",
      text: "过载拒绝的响应语义必须明确：HTTP 层返回 **503 Service Unavailable**（配合 Retry-After 头给出建议等待），内部调用返回明确的「过载」错误而非笼统 500——因为 500 会诱导调用方重试，而过载时最怕的就是立刻重试。被拒绝的调用方应把 503 当作「请退避」信号：按指数退避加抖动延迟后重试，而不是原样重打（重试纪律见「熔断与超时」课时）。这让拒绝与重试形成负反馈：过载的一方在拒绝中挤出空间，调用方在退避中降低压力，双方共同收敛。",
    },
    {
      type: "heading",
      text: "对重试风暴的总防线：入口限流 + 拒绝策略一致",
    },
    {
      type: "paragraph",
      text: "本章开头说重试风暴能把一次故障放大成雪崩，过载保护正是那根最后的保险丝，但它单独挡不住风暴：若每个上游都在退避后继续猛烈重试，过载服务每拒绝一个又涌入十个。总防线需要三条同时成立：**入口限流**（网关按总量限制进入，见「限流」课）先把风暴挡在系统外；**各层重试次数封顶且带抖动**（见「熔断与超时」课）让风暴本身不产生；**所有层拒绝语义一致**——入口拒绝、实例拒绝、存储前拒绝都返回同样的 503 语义并带上重试引导，调用方不用分辨是哪一层拒的，统一退避。三者缺一，过载保护就只是在给重试风暴当沙包。",
    },
    {
      type: "subheading",
      text: "与熔断、限流的分工：最后收口",
    },
    {
      type: "table",
      caption: "流量治理四件套：各自回答的问题、作用面与失效时的兜底",
      headers: ["机制", "回答的问题", "作用位置", "典型触发", "响应"],
      rows: [
        ["限流", "来的太多怎么办", "入口（网关/服务前）", "到达速率超预设配额", "429 拒绝 / 排队 / 降级响应"],
        ["熔断", "下游坏了怎么办", "出口（调用方侧）", "下游失败率达阈值", "快速失败 + half-open 探测"],
        ["降级", "容量不够保什么", "业务路径（开关）", "核心资源吃紧", "读退缓存、写转异步、功能裁剪"],
        ["过载保护", "系统真的被压垮怎么办", "自身（每实例）", "in-flight 超实际容量", "503 拒绝新请求、保护已接受工作"],
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "示例：网关对 sale 的过载保护",
      body: "大促中某次运营事故让通知类回源请求暴涨，sale 处理线程被占满。sale 每实例的自适应过载判定发现 in-flight 远超实际吞吐，开始对非白名单请求返回 503 + Retry-After；同时网关侧按服务维度限流（见「限流」课）把总量压到 sale 集群容量内，评价等非交易接口的降级开关被打开（见「降级与兜底」课），pay 异常时的熔断把失败调用隔离在外（见「熔断与超时」课）。四个机制各管一段：网关限流挡住洪峰总量、sale 自身过载拒绝拦住突发形态、降级释放核心资源、熔断排除故障下游。最后的效果是：**少量请求被明确拒绝并退避，但系统没有整体崩溃，下单主路径在降级态下保持可用**——这就是本章全部手段的总目标：稳态下的可用性。",
    },
    {
      type: "keypoints",
      items: [
        "过载时最优策略是拒绝新请求而非排队：队列无界 = 延迟发散，保护已接受的工作才有恢复可能。",
        "自适应过载基于负载反馈：in-flight 超过滑窗实际吞吐即拒绝；LIFO 优先服务新请求，因为排队最久的旧请求客户端多半已超时，别把资源花在注定无人接收的响应上（启发式思路，需压测验证）。",
        "过载拒绝返回明确的 503 + Retry-After，调用方指数退避加抖动；拒绝与退避形成负反馈。",
        "对重试风暴的总防线 = 入口限流 + 各层重试封顶带抖动 + 全链路一致的 503 拒绝语义。",
        "限流管入口、熔断管出口、降级管取舍、过载保护兜底；四件套合起来保证过载稳态下核心路径可用。",
      ],
    },
  ],
};
