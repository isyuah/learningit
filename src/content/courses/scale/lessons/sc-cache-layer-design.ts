/* ==================================================================
 * 课时：多级缓存与整体设计（sc-cache-layer-design）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "把前五课拼成整体：L1 进程内 + L2 Redis + 数据库的分层读写路径与一致性策略、容量与淘汰、缓存治理（key 规范/命中率监控/故障预案），以及数据进哪级缓存的设计决策表。",
  blocks: [
    {
      type: "paragraph",
      text: "前五课各自解决了一块：心智模型（第 1 课）、单层读写模式（第 2 课）、失效故障（第 3 课）、热点单点（第 4 课）、Redis 的正确用法（第 5 课）。本课把它们拼成**一座完整的多级缓存系统**：进程内 L1、分布式 L2（Redis）、数据库三层如何协同读写、一致性策略如何分层、缓存容量如何治理、以及最实用的那张决策表——一份数据到底进哪级缓存、还是干脆不进。学完本课，你应该能对着闪购的任何一种读流量，说出它的完整缓存设计。",
    },
    {
      type: "heading",
      text: "三层结构的读写路径",
    },
    {
      type: "code",
      title: "多级缓存（L1 → L2 → DB）读路径",
      language: "text",
      code: "读 GET /products/{id}：\n  1. 进程内 L1（本实例内存，热 key 短 TTL 1~5s）\n      命中 → 返回（ns~µs）\n  2. Redis L2（共享，全量商品，TTL 分钟级/永不过期+异步刷新）\n      miss → singleflight 收敛后查库回填（第 4 课）\n      命中 → 回填 L1 → 返回（约 0.1~1ms）\n  3. 数据库（唯一事实源，回填 Redis；仅承接 miss 与后台刷新）\n\n写（改价/上下架）：\n  1. 数据库事务提交（事实源）\n  2. 删 Redis key（失败走重试/异步删除，见第 5 课）\n  3. 通知各实例失效本地 L1（见下方「本地失效广播」）",
    },
    {
      type: "paragraph",
      text: "每一层缓存都是「上一层的兜底 + 下一层的闸门」：L1 挡掉每台实例上的高频重复读（热点场景下把 Redis 流量降两个数量级，第 4 课已算过账）；L2 挡掉跨实例共享的读（把数据库流量降到个位数 QPS）；数据库只承接两层都 miss 的流量与后台刷新。设计的关键不是「哪一层快」，而是**让每一层的 miss 率可控**：L1 miss 后必须能快速从 L2 拿回（singleflight 收敛、不给 DB 加压），L2 miss 后才允许碰数据库。任何一层出现「miss 直接打穿到数据库」的路径，都是设计缺陷——那是第 3 课三类故障的入口。",
    },
    {
      type: "heading",
      text: "一致性策略的分层",
    },
    {
      type: "paragraph",
      text: "三层缓存的一致性代价逐层递减，策略也随之分层：**L2（Redis）**离数据库最近，删除可靠性最重要——先更库后删 key、删除失败重试或 binlog 订阅（第 5 课），TTL 做分钟级兜底。**L1（进程内）**的副本在每台实例上、离数据库最远，精确失效最难——因为它没有统一的删除通道：写请求只落在某几台实例上，其它几十台实例的本地副本根本不知道数据变了。工程上对 L1 用两个手段：**短 TTL 自愈**（1–5 秒，旧值最多存活几秒，这是 L1 的默认一致性策略）与**本地失效广播**（写侧通过 Redis Pub/Sub 或消息通知所有实例删除本地 key——概念上成立，但广播有延迟、可能丢消息，所以它只能缩短脏窗口，不能替代短 TTL 兜底）。记住分层的一致性铁律：**每层的兜底策略独立成立**——即使广播全丢、删除全失败，L1 靠短 TTL、L2 靠 TTL+重试，系统依然最终一致，只是窗口长度不同。",
    },
    {
      type: "table",
      caption: "各层一致性策略一览：越靠上 TTL 越短、越依赖自愈",
      headers: ["层级", "失效手段", "脏窗口兜底", "典型 TTL"],
      rows: [
        ["L1 进程内", "写侧广播本地失效（尽力而为）", "短 TTL 自愈（主要手段）", "1–5 秒"],
        ["L2 Redis", "写侧删 key + 失败重试/binlog 订阅", "TTL + 删除重试", "分钟级；热点可永不过期+异步刷新"],
        ["数据库", "（事实源，无失效问题）", "——", "——"],
      ],
    },
    {
      type: "paragraph",
      text: "**缓存容量与淘汰策略。**缓存不是无限的：内存上限内要容纳的 key 数远超容量时，必须决定**淘汰谁**。两种经典策略：**LRU（最近最少使用）**——优先淘汰最久没被访问的 key，匹配「旧热点让位新热点」的自然规律，是大多数场景的默认；**LFU（最不经常使用）**——优先淘汰访问频率最低的 key，适合「少数 key 长期高频、多数 key 偶尔访问」的分布（闪购爆款正是这种），能避免 LRU 下「刚被扫过一轮的冷 key 挤掉真热点」的缺陷，代价是实现更复杂、需要维护频率计数。Redis 通过 maxmemory 与淘汰策略配置控制（如 allkeys-lru / allkeys-lfu 等，具体选项以官方文档为准）；进程内缓存库通常也内建 LRU/LFU 可选。工程要点是**为每层缓存定容量预算**：L1 只放热 key（否则每实例几十 GB 商品全量进本地内存会挤占业务内存）；L2 按数据冷热分层 TTL；淘汰策略选错的表现是命中率异常下滑——这正是下面监控要盯的信号。",
    },
    {
      type: "heading",
      text: "缓存治理：key 规范、命中率监控与故障预案",
    },
    {
      type: "paragraph",
      text: "**Key 命名规范**是缓存的公共契约：Redis 的 key 一旦上线就很难批量改，必须从一开始就用统一的命名空间格式，如 `业务:对象:标识:维度`（`sale:product:1001:detail`、`sale:activity:618:info`）。规范带来的直接收益是可检索、可监控、可批量治理——按前缀统计流量、定位热点、清理历史版本都依赖它；没有规范的 key 是一堆无法归类的字符串，第 4 课的热点探测与任何自动化治理都无从谈起。**命中率监控**是缓存的仪表盘：按 key 前缀聚合统计读 QPS、命中率与回源 QPS（观测的指标设计见 observability 课程，PromQL 面板与告警规则在那边学，本课给「盯什么」：各层命中率、回源 QPS、平均延迟、缓存内存水位、淘汰计数）。**容量水位**监控内存使用与淘汰速率，逼近上限前扩容或调整预算——Redis 内存打满会触发淘汰风暴，表现为命中率骤降。",
    },
    {
      type: "subheading",
      text: "故障预案：缓存挂了怎么办",
    },
    {
      type: "paragraph",
      text: "缓存层的故障预案要提前写成可执行的决策，而不是故障现场临场想。核心决策是**「缓存不可用时，读流量直连数据库还是降级？」**——依据是流量与数据库余量：日常流量下（DB 有大量余量），可以允许短暂直连数据库，保证功能完整；大促峰值下（DB 本就接近上限），直连等于自杀，必须降级——L1 还在就只服务 L1 命中、miss 的请求返回默认兜底数据（活动配置的静态快照、商品基础信息的最近一次值），同时把缓存重建的流量交给后台限速重试，等 Redis 恢复后自动切回。降级开关、兜底数据、恢复流程都要在演练中验证过（降级机制的系统化内容在流量治理章节；本课确立它在缓存语境下的决策依据：**看流量与数据库余量，而不是看缓存能不能救**）。",
    },
    {
      type: "heading",
      text: "决策表：什么数据进哪级缓存",
    },
    {
      type: "table",
      caption: "缓存设计决策表（闪购读流量示例）",
      headers: ["数据", "特征", "是否缓存/进哪级", "TTL 与策略"],
      rows: [
        ["商品名称/图片/详情页配置", "读多写少、变化极慢、高复用", "L1 + L2（大促爆款）或仅 L2（普通商品）", "分钟级；爆款 L1 短 TTL + 预热"],
        ["库存余量（展示）", "读极多、高频写、可容忍数秒旧值", "仅 L2，秒级 TTL 或短时快照", "秒级；与真实扣减解耦（第 4 课）"],
        ["下单时的库存校验", "强一致、正确性关键", "不缓存，直接数据库原子扣减", "——（正确性章节）"],
        ["活动配置/规则", "低频更新、全量共享", "L1 + L2", "较长 TTL + 发布时主动失效"],
        ["用户私有数据（购物车）", "每人一份、复用低", "不进公共缓存；需要时 Redis 按用户 key 隔离存储", "随业务定，注意容量线性增长"],
        ["订单/支付状态", "强一致、不可旧", "不缓存（或仅缓存展示快照并短 TTL）", "——"],
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "从一张决策表到完整设计",
      body: "对着闪购读流量走一遍决策表：详情页商品信息进 L1+L2（爆款带预热与热点探测）；库存展示只进 L2 且秒级（下单校验不进缓存）；活动配置双缓存加发布失效；订单状态不缓存。再加上第 3 课的三类防线、第 4 课的 singleflight、第 5 课的删除可靠性——这就是一份可以答辩的多级缓存设计。面试与实战的差别只在于：面试停在「我选了 L1+L2」，实战要多回答一句——**每一层的 TTL、失效手段与故障预案分别是什么**。",
    },
    {
      type: "keypoints",
      items: [
        "多级缓存 = L1 进程内（挡实例内重复读，短 TTL 自愈）+ L2 Redis（挡跨实例共享读，删 key+TTL）+ DB（唯一事实源）；任何 miss 路径不得直接打穿到 DB。",
        "一致性策略分层：越靠上越依赖短 TTL 自愈；本地失效广播只能缩短脏窗口，不能替代兜底。",
        "淘汰策略：LRU 适合一般分布，LFU 适合少数 key 长期高频的分布；为每层缓存定容量预算与水位监控。",
        "治理四件事：key 命名规范（可检索可监控的前提）、命中率/回源 QPS 监控、容量水位、故障预案。",
        "故障预案依据流量与数据库余量决策：日常可短暂直连、大促峰值必须降级保数据库。",
        "数据进哪级缓存看特征：读多写少可旧值高复用 → 缓存；强一致正确性关键 → 数据库。",
      ],
    },
  ],
};
