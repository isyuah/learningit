/* ==================================================================
 * 课时：连接与线程池：池化为什么是性能基石（sc-conn-pooling）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 章节 2「请求路径与 IO：第一公里的性能」第 3 课。
 * 注：MySQL 服务器侧「为什么连接贵、max_connections、慢查询占用」
 *     已由 MySQL 课程「连接池与高并发连接管理」深讲，本课站在应用层
 *     视角讲池的定量配置、耗尽信号与正确归还，只引用其结论。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "把「池」当成复用 + 限并发 + 削峰的统一手段：连接池怎么定量、为什么不是越大越好、池耗尽是什么信号，以及线程池与 HTTP keep-alive 池的同一套逻辑。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课的事件循环/协程解决了「谁来等 IO」；这一课处理等 IO 时手里握着的东西——**连接与线程**。MySQL 课程已经讲透了「为什么一个数据库连接很贵、max_connections 是硬上限、慢查询如何占死连接」（见 database-mysql 课程『连接池与高并发连接管理』）。本课站到应用层视角回答另一组问题：连接池该开多大、取连接超时意味着什么、线程池与 HTTP keep-alive 池为什么是同一套思路。",
    },
    {
      type: "definition",
      term: "连接池（connection pool）",
      definition:
        "预先建立并复用的一组连接，避免每次请求重建连接的开销。三个作用：**复用**（省去反复建连/认证）、**限并发**（池上限钳住并发数，防止瞬间冲垮数据库）、**削峰**（借不到就排队等待，把峰值摊平到池的处理节奏上）。",
    },
    {
      type: "heading",
      text: "数据库连接池：不是越大越快",
    },
    {
      type: "paragraph",
      text: "连接池大小有一条广为流传的经验公式：`池大小 ≈ 核数 ×（1 + 等待时间 ÷ 计算时间）`。直觉是：一条连接干活的时间 = 计算 + 等待。当等待时间远大于计算时间（如 IO 密集），需要更多连接去「重叠等待」；当计算占主导（CPU 密集），池接近核数即可。MySQL 课程给出的是「核数 × 2 + 1」起步值，两者同源：都是「不要让并发查询数明显超过 CPU 能同时推进的量」。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "超过某一点后，连接越多越慢",
      body: "池不是越大越好：每多一条连接，数据库就多一个线程（MySQL 侧）与内存占用；当并发查询超过 CPU 核数，多出的连接只是在排队等 CPU，还会因为上下文切换与锁竞争把整体吞吐拉低。把池从 100 调到 1000 不会让你的库快 10 倍——多数时候只是把「取连接超时」换成「CPU 排队 + 内存告警」。经验公式只给起点，正确做法是压测 + 观察数据库 CPU 利用率与取连接等待时间再微调。",
    },
    {
      type: "heading",
      text: "池耗尽 = 容量信号，不是配置缺陷",
    },
    {
      type: "paragraph",
      text: "当请求频繁「取连接超时」（connection pool exhausted），第一反应不该是调大池子。要沿两个方向查：池内的连接**被什么占着**——若被慢查询、长事务占住不还，调大池只会让更多线程一起排队（这正是 MySQL 课程『连接池与高并发连接管理』里慢查询日志 / PROCESSLIST 的用武之地）；还是**到达速率真的超过了 DB 容量**——若并发真实需求超出了公式指引的容量，说明需要的是加核/读写分离（第 5 章），而不是无脑加连接。把「池耗尽」读作**容量信号**：要么单个请求占连接太久，要么同时需要连接的请求太多。",
    },
    {
      type: "code",
      title: "闪购下单：事务占住连接的时间 = 事务时长",
      language: "text",
      code: `// 伪代码：POST /orders 的简化事务时序
// 连接 t0 借出 → BEGIN → 扣库存 UPDATE（拿行锁）→ 建订单 INSERT
//        → COMMIT → t1 归还；占用时长 ≈ t1 - t0（整个事务！）

pool := NewDBPool(maxSize: 30)             // 按公式起步，不是拍脑袋调大

handler(orderReq):
    conn := pool.borrow(timeout: 2s)        // 借不到 = 取连接超时
    tx   := conn.begin()                    // 长事务里千万别夹外部调用
    try:
        tx.update("stock SET ... WHERE product_id=?")   // 锁行直到 COMMIT
        tx.insert("orders ...")                          // 建单
        tx.commit()                                      // 尽早提交、尽早还连接
    catch:
        tx.rollback()
    finally:
        pool.release(conn)                  // 任何路径都要归还！

// 若把「等支付回调」这种外部 IO 塞进事务，连接与行锁都会白占数秒，
// 池会被少数「慢事务」吃光——表现为大量取连接超时。`,
    },
    {
      type: "paragraph",
      text: "上面的例子说明池的一条铁律：**获取连接后，占用时长就是你的关键资源**。下单事务里任何一次慢查询、任何一段多余的等待，都换算成「这条连接被占多久」。把外部调用（支付、通知）从事务里挪走、缩短事务窗口，等于直接提高池的有效容量——这是应用层优化池的最有效手段。",
    },
    {
      type: "heading",
      text: "HTTP keep-alive：客户端侧的连接池",
    },
    {
      type: "paragraph",
      text: "第 1 课讲了 keep-alive 省握手 RTT；在服务端代码里，它表现为**HTTP 客户端连接池**：sale 调用 pay 等外部 HTTP 下游时要靠它复用连接——短信/推送类通知走异步队列、不属于同步 HTTP 调用（见第 4 章『异步化的本质』），Go 的 `net/http` Transport 会为每个目标主机维护一组空闲连接供复用，并受 MaxIdleConns / MaxIdleConnsPerHost 等参数约束（参数细节以官方文档为准）。同类还包含 Redis 连接池、gRPC 连接池（见 grpc-go 课程『Keepalive、流控与消息边界』）。共同规律：**池上限决定并发度，池的获取/归还语义决定正确性**。",
    },
    {
      type: "list",
      items: [
        "读响应体要读到底（或显式关闭 body），连接才能被放回空闲池复用；只读一半就返回，连接常被直接销毁。",
        "进程退出/排空（drain）时主动关闭池，让在途请求自然结束——见第 6 章『无状态设计』的优雅上下线部分。",
        "为每个目标独立限并发：某一下游变慢时，其专属池先耗尽并背压，不拖垮对其它下游的调用（舱壁思想，第 7 章『熔断与超时』展开）。",
        "HTTP/2 在一条连接上多路复用，能显著降低同一下游所需连接数（第 1 课）。",
      ],
    },
    {
      type: "paragraph",
      text: "**线程/协程池与连接池是同构的**：核心线程数（保底）、最大线程数（并发上限）、任务队列（等待缓冲）、拒绝策略（队列满后怎么办）。线程池加队列，天然形成一个小型削峰缓冲——瞬时洪峰先排进队列，处理速率被池上限约束，正是第 1 章 Little’s Law（L = λW）的直接体现：队列里的任务在等待，等待时间 = 排队长度 ÷ 处理速率。Go 里 goroutine 本身很轻，通常不需要手写池（net/http 已按请求调度），但受外部系统配额约束的任务（限制并发调用某 API、限制 SQL 同时执行数）仍用「信号量/有界 worker 池」限制，本质一样。",
    },
    {
      type: "callout",
      variant: "tip",
      title: "正确获取与归还：一把钥匙的纪律",
      body: "三条纪律适用于所有池：**① 获取要带超时**——借不到就快速失败或排队限时，而不是无限等；**② 归还必须无遗漏**——Go 里用 `defer` 保证任何错误路径都归还（或关闭 body），否则泄漏的连接最终把池占死；**③ 无效连接要剔除**——被服务端断开的死连接在复用时先校验/重建。连接泄漏的排查总是先看池的使用率是否随时间只涨不降。",
    },
    {
      type: "quiz",
      question:
        "大促开始后，sale 服务频繁报「数据库取连接超时」。SHOW PROCESSLIST 显示池中连接几乎全被同一个长事务（已运行 30 秒+）占用，且该事务在等一个外部 HTTP 回调。正确的处置是？",
      options: [
        "把连接池上限调大一倍，让更多请求能借到连接",
        "把数据库核数翻倍，提升并发查询能力",
        "缩短事务：把外部回调移出事务，并治理慢事务，让连接尽快归还",
        "关闭连接池，改为每个请求新建连接绕过限制",
      ],
      answer: 2,
      explanation:
        "池被长事务占住是「占用太久」问题而非「容量不足」：调大池、加核只会让更多请求排进同一个慢事务后面。把外部调用移出事务、缩短事务窗口，让连接及时归还，才是对池的有效扩容。",
    },
    {
      type: "keypoints",
      items: [
        "池的三重作用：复用省建连、上限限并发、排队削峰。",
        "DB 池经验公式 ≈ 核数 ×（1 + 等待/计算）；CPU 密集时接近核数，IO 密集可放大，超过某点反而变慢。",
        "池耗尽 = 容量信号：先查「被什么占着」（慢查询/长事务），再判断是否真的超容量，别盲目调大。",
        "事务占用连接的时间 = 事务时长：外部调用移出事务、缩短窗口即是对池扩容。",
        "HTTP keep-alive / Redis / gRPC 连接池与线程池同构：上限定并发、获取带超时、归还靠 defer、死连接要剔除。",
      ],
    },
  ],
};
