/* ==================================================================
 * 课时：读写分离：扩展读的经典第一招（sc-read-scaling）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "读写分离为什么能先解决读瓶颈、复制延迟给业务带来的三个具体后果，以及按一致性强弱分档的读策略。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课得出分岔：如果闪购的压力主要是读——商品详情、活动页、订单查询——那就不必动数据布局，先把主库的读流量卸掉。这就是本章第一招：**读写分离**。机制层面（binlog 复制怎么工作、relay log 怎么重放、GTID 怎么定位）是 MySQL 课程的领土，见「binlog 与主从复制」；本课讲它的架构位置与后果：部署怎么摆、路由怎么做、从库延迟在业务上会造成哪几类错乱、读到什么时候算到顶。",
    },
    {
      type: "definition",
      term: "读写分离",
      definition: "把数据库拆成主从结构：主库（primary/master）接受全部写请求并产生 binlog，一个或多个只读从库（replica/slave）重放 binlog、承担读流量。本质是用「多份只读副本」换读吞吐，主库仍然只有一个写入口——单写者模型没被打破，读被横向扩展了。",
    },
    {
      type: "heading",
      text: "部署形态与读写路由",
    },
    {
      type: "paragraph",
      text: "读写分离的部署是一主多从：主库负责写，从库用 `read_only` 挡住误写（防止主从不一致），从库之间再挂只读负载均衡把读摊到多台。真正要决策的是路由怎么做，两条路各有权衡：",
    },
    {
      type: "table",
      caption: "应用层路由 vs 中间件路由",
      headers: ["维度", "应用层路由", "中间件/代理路由"],
      rows: [
        ["做法", "代码/数据源层区分读写数据源（如按方法或注解选择主/从连接）", "SQL 先经过代理层，由代理解析后转发到主或从"],
        ["对应用侵入", "有：每条读写路径要显式表达意图", "无：应用只连代理，像连一个普通数据库"],
        ["性能", "少一跳网络", "多一跳代理，代理自身成为容量点与故障点"],
        ["灵活读策略", "高：可精细控制「这条读必须走主库」", "一般：通常按 SQL 类型粗分流"],
        ["升级与多语言", "每套代码/语言各自实现", "协议级统一，客户端语言无关"],
        ["代表形态", "数据源框架双数据源（第三方库以官方文档为准）", "ShardingSphere-Proxy 等（概念定位见本章第 4 课，不展开）"],
      ],
    },
    {
      type: "paragraph",
      text: "中小团队常从应用层路由起步：改动可控、能看到每条 SQL 走哪条路；规模上来后再引入代理：应用侧简单了，但代理变成新的共享组件，要按无状态方式水平部署并纳入监控。无论哪种路由，正确性都取决于同一个事实——**异步复制下，从库看到的是一个「延迟的旧视图」**。",
    },
    {
      type: "heading",
      text: "从库延迟的三个业务后果",
    },
    {
      type: "list",
      items: [
        "**读己之写（read-your-writes）被破坏**：用户下单后立刻刷新查订单，请求被路由到还没追平的从库，看到「订单不存在」。支付、改密、改资料后立刻读自己的数据，都是高发场景。",
        "**单调读被破坏**：同一用户第一次读从库 A 看到新状态，第二次读从库 B（或同一从库另一时刻）看到旧状态——时间倒流。它不发生在一次请求内，而是跨请求，用户感知是「数据在跳变」，比读己之写更难排查。",
        "**延迟敏感业务错乱**：依赖数据的业务规则在从库上被打破——用从库计数判断「库存还有没有」，旧值可能让超卖判定失效或让限购判断出错；运营后台基于延迟视图做统计，会与主库对不上账。",
      ],
    },
    {
      type: "heading",
      text: "半同步与 group commit：把窗口收窄",
    },
    {
      type: "paragraph",
      text: "MySQL 默认的异步复制里，主库提交后不等任何从库确认，延迟窗口不可控。**半同步复制（semi-sync）**让主库至少等一个从库确认「已收到 binlog」才向客户端返回提交成功——「主库已提交但从库还没拿到日志」的窗口被大幅收窄；注意它保证的是日志到达，不保证从库已重放，读仍可能略旧，且从库确认超时通常退化为异步（以官方文档为准）。另一个相关概念是 **group commit**：主库把一小段时间内并发的多个提交合并成一次落盘，提升 fsync 效率——半同步「等从库确认」的等待也因此按批摊薄，不必每个提交单独付一次确认往返。两者都是工程手段，不能替代业务侧的读策略。",
    },
    {
      type: "callout",
      variant: "note",
      title: "延迟监控：seconds_behind_master 的口径",
      body: "经典指标是 SHOW REPLICA STATUS 里的 Seconds_Behind_Master：从库执行线程「当前时刻」与「已重放日志记录的时间」之差，是重放落后程度的近似值，受时钟与瞬时采样影响，不等于精确主从差；更可靠的做法是结合 relay log 位点/GTID 推进做落点对比。版本差异与精确口径以 MySQL 官方文档为准。监控它的趋势并设告警阈值，别把它当精确秒表。",
    },
    {
      type: "heading",
      text: "一致性读策略：按时效分档",
    },
    {
      type: "list",
      items: [
        "**强制主库读**：必须看到最新数据的路径（下单后立刻查自己的订单、支付回调后的状态页）直接走主库；代价是该部分失去读分流，只给「必要」路径用。",
        "**会话/用户级粘性**：短时间窗口内（如写后数秒）该用户的读全走主库或刚确认追平的从库——实现上可按用户加标记，例如用 Redis 记「user_id 最近写过」，命中则主库读。",
        "**延迟容忍**：能接受最终一致的内容读（商品详情、活动页）放心走从库；这类数据通常已被缓存接走（第 3 章），从库只是回源兜底。",
        "**版本/时间戳判断**：数据带 `updated_at` 或版本号时，读到旧值可按业务规则丢弃或重读主库——适合校验类场景，详见第 8 章幂等与乐观锁的讨论。",
      ],
    },
    {
      type: "code",
      title: "路由决策示意（Go 伪代码）——按时效分档而不是全局分流",
      language: "go",
      code: `// 伪代码：同一函数内按一致性要求选数据源；第三方库细节以官方文档为准
func LoadOrder(ctx context.Context, userID, orderID int64, needLatest bool) (*Order, error) {
    if needLatest || recentlyWrote(ctx, userID) {
        // 强一致读：走主库
        return queryOrder(ctx, primaryDB, userID, orderID)
    }
    // 普通读：走从库连接池（自带多从库负载均衡）
    return queryOrder(ctx, replicaDB, userID, orderID)
}

// recentlyWrote：该用户最近是否写过（Redis 记 userID -> 写时间，TTL 数秒）
func recentlyWrote(ctx context.Context, userID int64) bool {
    ts, err := redis.Get(ctx, "w:"+strconv.FormatInt(userID, 10)).Int64()
    return err == nil && time.Since(time.Unix(ts, 0)) < 5*time.Second
}`,
    },
    {
      type: "keypoints",
      items: [
        "**扩展读的极限**：读写分离只分流读、不解决写；从库数量、复制带宽与故障半径随读流量同步膨胀。大促单品热点读 10 万+ QPS 时，几十台从库也未必够——这时让缓存接住重复读（热点商品已进 Redis，见缓存章「热点与惊群」），从库只做兜底；若连写也逼近单主库上限（秒杀集中扣减），问题已转向「写」，进入分片（本章第 3、4 课）。",
        "读策略三件套：半同步收窄窗口、监控延迟趋势、按时效分档决定每条读走主库还是从库。",
      ],
    },
    {
      type: "quiz",
      question: "读己之写与单调读破坏，本质区别是？",
      options: [
        "前者是「自己写后读不到」，后者是「先读到新又读到旧」的时间倒流，成因同为延迟的旧视图但表象与影响路径不同",
        "两者完全一样，都是主从延迟导致的旧读",
        "单调读破坏只发生在多从库架构，读己之写只发生在单从库架构",
        "读己之写是用户操作问题，单调读破坏是数据库 bug",
      ],
      answer: 0,
      explanation: "两者都源于异步复制的旧视图，但读己之写是「写后立刻读不到自己写的数据」（单次请求内），单调读破坏是跨请求的「先新后旧」跳变；缓解读己之写（写后走主库）并不天然解决单调读破坏，因此排查与对策要分开。",
    },
  ],
};
