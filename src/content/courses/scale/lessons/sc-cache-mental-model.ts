/* ==================================================================
 * 课时：缓存的心智模型与命中率（sc-cache-mental-model）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "从局部性出发理解缓存为什么快：缓存的位置谱系、命中率与收益曲线、缓存什么与 TTL，以及「用一致性换性能」这一根本交易。",
  blocks: [
    {
      type: "paragraph",
      text: "前两章把请求路径讲透了：一次读请求从客户端出发，穿过网关进入 sale 服务，最终打到 MySQL 才算完成。这条路每一跳都有成本——单机 MySQL 简单读的上限约 3k–8k QPS，一次回源查询通常要 1–10ms。问题在于，**如果每次请求都完整走完这条路，读得越多、路径越挤**。闪购是典型的读多写少系统：单个商品详情日常就有约 100 QPS 的读流量，而写（下单、改价）占比很低；更关键的是，成千上万用户在重复读同一份商品信息。缓存的全部出发点可以浓缩成一句话：**把重复的读，挡在离用户更近、更快的地方完成**。本章先建立「缓存位置 + 命中率」的心智模型，再依次展开读写模式（第 2 课）、三种失效故障（第 3 课）、热点放大（第 4 课）、Redis 的正确用法（第 5 课），最后拼出多级缓存的整体设计（第 6 课）。",
    },
    {
      type: "heading",
      text: "缓存为什么有效：局部性",
    },
    {
      type: "paragraph",
      text: "缓存有效的前提是**局部性（locality）**：程序对数据的访问不是均匀随机的，而是集中在少数数据上。**时间局部性**——刚被读过的数据，短期内很可能被再次读取：用户 A 刚看完商品 1001 的详情页，用户 B 紧接着也点进来，同一份数据在几秒内被反复读取。**空间局部性**——访问一个数据时，它旁边的数据也可能被访问：详情页往往把商品信息、价格、库存标签一次性都拉走。线上读流量还常呈「少数 key 承担多数流量」的分布：目录里大多数商品无人问津，少数爆款撑起绝大部分详情请求。局部性越强，缓存能挡下的重复读就越多——这就是闪购商品详情这种读多写少的数据天然适合缓存的根本原因。",
    },
    {
      type: "heading",
      text: "缓存的位置谱系",
    },
    {
      type: "table",
      caption: "缓存位置谱系：越靠上离用户越近、越快，但离数据源越远、越难精确失效",
      headers: ["位置", "典型实现", "挡下谁", "命中时的典型延迟"],
      rows: [
        ["浏览器缓存", "HTTP 缓存头（Cache-Control）", "同一个用户对同一资源的重复请求", "0（本机，不再发请求）"],
        ["CDN", "边缘节点缓存静态资源、图片", "地理上就近的一批用户", "几 ms~几十 ms（视地域距离）"],
        ["网关 / 反向代理", "Nginx 等代理的响应缓存", "进入该网关的所有请求", "亚毫秒~毫秒（本机或同机房）"],
        ["进程内缓存（L1）", "Go 实例内存（map、本地 LRU 库）", "打到这一台实例的请求", "纳秒~微秒级（本地内存读）"],
        ["分布式缓存（L2）", "Redis 共享 key-value", "所有服务实例（跨实例共享）", "约 0.1–1ms（同机房一跳）"],
        ["数据库缓冲池", "InnoDB Buffer Pool 等", "数据库自身的读（内存页命中）", "命中时通常在毫秒内，远快于磁盘 IO"],
      ],
    },
    {
      type: "paragraph",
      text: "读这张表有两条规律。第一，**越靠上的缓存越快**：进程内命中是纳秒~微秒级，Redis 一跳约 0.1–1ms，而数据库回源要毫秒级——差距是两个数量级，意味着同样一份读流量，靠前的缓存能轻松承接、靠后的存储却很快触顶。第二，**越靠上越难精确失效**：浏览器和 CDN 无法感知数据库的变更，只能靠 TTL 或显式失效来「自愈」；所以每加一层缓存，就多一份「可能过期」的副本要管理。缓存的正确姿势不是越多越好，而是每一层都回答清楚：它挡的是哪类流量，值不值这份一致性账。",
    },
    {
      type: "heading",
      text: "命中率：缓存有效的第一指标",
    },
    {
      type: "definition",
      term: "命中率（hit ratio）",
      definition: "在一段时间内，缓存直接命中的读请求数占全部读请求数的比例：命中率 = 命中次数 ÷（命中次数 + 未命中次数）。它衡量「缓存挡下了多少重复读」——命中率越高，回源到数据库的流量越低。",
    },
    {
      type: "paragraph",
      text: "命中率的收益可以用平均延迟算清楚：设缓存命中耗时 t_c、未命中时回源额外耗时 t_s，则 平均读延迟 ≈ 命中率 × t_c +（1 − 命中率）×（t_c + t_s）。代入闪购商品详情：Redis 命中约 0.5ms、DB 回源约 5ms，命中率 95% 时平均延迟约 0.75ms；命中率掉到 50% 时平均延迟约 3ms，是前者的四倍。对数据库负载的影响更直接：回源 QPS = 总读 QPS ×（1 − 命中率）。总读 1000 QPS、命中率 99% 时，数据库只收 10 QPS；命中率 90% 时收 100 QPS。对照单机 MySQL 约 3k–8k QPS 的天花板，**缓存相当于把数据库流量乘上（1 − 命中率）这个系数**——命中率每掉一个点，系数按比例翻涨。所以排查缓存问题时，命中率、回源 QPS、平均延迟三个数要一起看：命中率高但回源 QPS 也高，说明还有大量读根本没走缓存（治理口径见第 6 课）。",
    },
    {
      type: "quiz",
      question: "一个读接口加了 Redis 缓存后命中率 99%，但数据库回源 QPS 反而比加缓存前更高。最可能的原因是什么？",
      options: [
        "命中率的统计口径把写请求也算进去了",
        "大量请求读的是缓存未覆盖的数据或走了缓存外的路径，这部分请求全部直连数据库",
        "TTL 太长，脏数据反复触发回源修正",
        "Redis 性能太差，把请求都拖到超时重试",
      ],
      answer: 1,
      explanation: "命中率只统计「走了缓存读路径」的请求；若大量请求读的数据根本没进缓存（key 覆盖不全），或存在绕过缓存的路径，它们全部直接打库，整体回源 QPS 自然高。因此必须同时看总读 QPS 与回源 QPS，不能只看命中率。写请求不参与命中率口径，TTL 长会减少回源而非增加，Redis 慢也不会提高数据库回源 QPS，故其余选项不成立。",
    },
    {
      type: "heading",
      text: "缓存什么、缓存多久",
    },
    {
      type: "paragraph",
      text: "一个数据该不该进缓存，按三个特征判断：**读多写少**——同一份数据被反复读、很少被改（商品名称与价格、活动配置、榜单）。写一次就要失效一次，写密集的数据命中率天然低，缓存没有收益；**可容忍短暂旧值**——读到比数据库旧一点的数据，业务和用户无感，这是缓存的默认契约，先想清楚「旧 5 秒行不行」；**高复用**——大量用户读同一份数据才有共享价值，纯个性化数据（每个人各自独立的上下文）缓存价值很低。反过来，**强一致要求的数据不适合走缓存**：例如下单时的库存校验、支付状态判定——它们的正确性方案属于正确性章节与 MySQL 课程的事务与锁，本课只需要记住这条边界。",
    },
    {
      type: "paragraph",
      text: "TTL（过期时间）是缓存最重要的参数：**没有 TTL 的缓存是一份可能永远错误的副本**；有了 TTL，缓存与数据库之间就有了自愈机制——最坏情况下，数据最多旧一个 TTL 那么长。工程上不追求「永不脏」，而是按「业务能容忍多旧的数据」反推 TTL：商品详情的名称与图片可容忍分钟级，就给分钟级 TTL；库存余量这种半实时数字只能给秒级，甚至不缓存直读。TTL 越长命中率越高、脏窗口越长；TTL 越短越干净、回源越频繁。它是本章反复出现的杠杆——第 3 课的雪崩、第 4 课的热点，都绕不开对 TTL 的设计。",
    },
    {
      type: "heading",
      text: "缓存是拿一致性换性能的交易",
    },
    {
      type: "callout",
      variant: "warning",
      title: "先承认这笔交易，再谈缓存",
      body: "每一层缓存都是一份「可能过期」的副本：读到旧值不是 bug，而是缓存工作的方式。加缓存之前必须回答三个问题：这份数据旧一个 TTL 可接受吗？失效做不干净时最坏会怎样？换来的吞吐是否值得？商品详情值得，账户余额、下单库存就不值得。缓存失效的种种具体形态——缓存与库的一致窗口、副本缺失、整层故障——正是第 2、3 课要展开的机制；但判断标准从这一课就立住了：**凡是不能容忍旧值的数据，都不要进缓存**。",
    },
    {
      type: "keypoints",
      items: [
        "缓存解决的是读多写少下的重复读：把读挡在离用户更近、更快的地方完成。",
        "位置谱系从浏览器到数据库缓冲池：越靠上越快（进程内 ns~µs、Redis 约 0.1–1ms、DB 回源 1–10ms），也越难精确失效。",
        "命中率 = 命中次数 ÷ 总读次数；DB 收到的流量 = 总读 ×（1 − 命中率），平均延迟与回源 QPS 都随命中率剧烈变化。",
        "适合缓存的数据：读多写少、可容忍短暂旧值、高复用；TTL 长度 = 业务能容忍的最长脏窗口。",
        "缓存本质是拿一致性换性能的交易——不能容忍旧值的数据，不要缓存。",
      ],
    },
  ],
};
