/* ==================================================================
 * 课时：数据库为什么先成为瓶颈（sc-db-scale-bottleneck）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "在索引、Buffer Pool、事务等库内优化都已完成的假设下，讲清数据库先成瓶颈的几类结构性原因，以及判断瓶颈维度的观测方法。",
  blocks: [
    {
      type: "paragraph",
      text: "请求路径的最后一公里是数据库。前几章我们把闪购 sale 服务从一台扩到多台：Go 无状态 API 单机就能扛 2 万–5 万 RPS，网关与 Redis 挡在前面，读路径看起来一片坦途。但只要流量最终要「落库」——下单要建订单、秒杀要扣库存、支付回调要更新状态——所有实例就共享同一个终点：那个唯一的 MySQL。单机 MySQL 在「读多写少、简单查询」下大约只有 3k–8k QPS 的上限；更麻烦的是，这个上限不会随应用实例变多而增加。本章从「库内优化已经做完」出发，讨论数据库为什么先成为瓶颈、每一类瓶颈对应哪条扩展路线。本课先给原因清单与判断方法——以下所有讨论都默认你已经做完了 MySQL 课程里的库内优化，跳过那层直接上架构手段，等于背着错误配置去付架构复杂度。",
    },
    {
      type: "heading",
      text: "前提：先确认库内优化已经做完",
    },
    {
      type: "list",
      items: [
        "命中索引：EXPLAIN 无全表扫描、无意外 filesort（见 MySQL 课程「B+ 树索引：为什么是 B+ 树」「索引设计：最左前缀与如何为查询建索引」）；",
        "Buffer Pool 命中率高：热数据常驻内存，不反复穿透到磁盘（见「存储引擎对比与 InnoDB 架构」）；",
        "事务短小：无长事务、隔离级别与锁范围选择得当（见「事务与 ACID：为什么需要事务」「隔离级别：脏读、不可重复读与幻读」）；",
        "连接池参数合理：没有池耗尽、没有无谓的连接重建（见「连接池与高并发连接管理」）；",
        "慢查询日志没有稳定增长的长尾（见「慢查询定位与优化流程」）。",
      ],
    },
    {
      type: "heading",
      text: "瓶颈一：磁盘 IO 与内存的物理上限",
    },
    {
      type: "paragraph",
      text: "Go 服务把状态放在进程外，所以可以水平加实例；MySQL 却是一个有状态进程：每次提交都要保证 redo log 先落盘（WAL：日志先行、数据页由后台异步刷盘，见 MySQL 课程「redo / undo log 与 WAL、崩溃恢复」），热数据要常驻 Buffer Pool 才能快。这两件事都有物理上限——磁盘的随机 IO 每秒次数与带宽有限，机械盘与云盘差异巨大，即使全 SSD，单机的 IO 队列深度也会饱和；内存容量决定 Buffer Pool 装得下多大的工作集，一旦热点集合超过内存，随机读就只能排队等磁盘。加 CPU 核数可以加快计算，却无法线性加快磁盘。于是流量翻倍时，库的 CPU 可能还闲着，磁盘与内存已经先到顶——这是数据库与无状态服务在扩展性上最根本的差异。",
    },
    {
      type: "definition",
      term: "单写者模型",
      definition: "一个数据分区同一时刻只允许一个节点接受写请求的架构。对单主 MySQL 而言，所有写（INSERT/UPDATE/DELETE）都必须到达主库执行；读可以靠加从库堆机器，写却没有第二条路。单写者保证了事务与并发控制的简单，也把写扩展锁死在了单点上——这是理解本章所有方案（读写分离解决不了写、分片才能动数据布局）的出发点。",
    },
    {
      type: "heading",
      text: "瓶颈二：写路径的单点——主库与锁",
    },
    {
      type: "paragraph",
      text: "单写者模型带来第二个、也更隐蔽的瓶颈：并发写之间的锁竞争。MySQL 课程讲过 InnoDB 的行锁与间隙锁（见「锁机制：表锁、行锁、间隙锁与 next-key」）：同一行的写必须互斥，可重复读下防幻读还要间隙锁。闪购的秒杀写路径 POST /orders 是一串同库事务——先 `UPDATE stock SET stock = stock - 1 WHERE product_id = ?` 扣库存，再插入订单；大促时所有用户抢同一件商品，所有 UPDATE 竞争同一行库存，行锁天然把写串行化；订单表按时间密集插入时，主键附近的插入竞争还会进一步放大等待。锁竞争消耗的不是磁盘而是时间：QPS 没到硬件上限，P99 已经恶化，监控上表现为锁等待时间上升、死锁次数变多。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "长事务会同时放大所有写问题",
      body: "持锁越久，排在后面的写请求等得越长，锁等待与死锁概率同步上升；undo log 膨胀会拖慢 MVCC 快照读（见 MySQL 课程「MVCC：快照读、读视图与 undo log」）；长事务还让从库复制追平的压力变大（下一课的主题）。排查时先按 MySQL 课程「事务工程最佳实践」自查：事务里有没有外部网络调用、有没有不必要的大范围更新、有没有把用户交互放进事务。",
    },
    {
      type: "heading",
      text: "瓶颈三：数据规模——单表变大、查询变重",
    },
    {
      type: "paragraph",
      text: "还有一类与并发无关的瓶颈来自数据本身。表数据量上去之后：二级索引变大，同样内存能装下的索引页变少，回表随机 IO 增多（见 MySQL 课程「聚簇索引与二级索引：回表是怎么发生的」）；页分裂等写放大随表变大而变贵；即使索引合适，深分页与聚合查询要扫的页也更多（见「常见优化场景：分页、JOIN、count 与批量」）。MySQL 课程把这类问题交给备份运维、分区与归档讨论；在本章，它是引向「分片」的另一半理由——当表大到索引与数据难以在单机内存与磁盘上优雅服务时，问题的性质从「并发」变成了「容量」。",
    },
    {
      type: "heading",
      text: "分岔路口：读多写少，还是写多读少",
    },
    {
      type: "table",
      caption: "读多写少 vs 写多读少：扩展方向的分岔（数字见第 1 章基线）",
      headers: ["维度", "读多写少", "写多读少"],
      rows: [
        ["典型症状", "读 QPS 先打满，从库与缓存告警，写压力正常", "写 TPS 上不去、锁等待高、单行或单表写竞争"],
        ["首选扩展路线", "缓存 → 读写分离（本章第 2 课）", "分片 / NewSQL（第 3、4 课）"],
        ["关键前提", "多数读可接受最终一致", "数据可按键拆分，写可分区"],
        ["闪购对应", "商品详情日常约 100 QPS 读、大促单品热点读峰值 10 万+ QPS", "秒杀瞬间同一商品的集中扣减写"],
      ],
    },
    {
      type: "paragraph",
      text: "分岔的判断决定后续所有路线。闪购其实两者都有：日常与预热期是典型读多写少，所以第 3 章先把缓存铺上；秒杀瞬间，同一件商品的扣减是集中写，写路径是否成为瓶颈取决于库存扣减怎么设计（队列化、预扣，见第 8 章）。注意两条路线的成本差异巨大：读多写少可以不动数据布局，只加副本与缓存，风险小、回滚容易；写多读少必须动数据布局（分片），一旦动手就回不到从前。先分清自己是哪一边，再选路线。",
    },
    {
      type: "heading",
      text: "判断「库是不是瓶颈」：观测什么",
    },
    {
      type: "list",
      items: [
        "慢查询与长尾延迟：库内慢日志 + 应用侧 SQL 耗时分位，方法见 MySQL 课程「慢查询定位与优化流程」；",
        "连接与连接池：连接数是否打满、池等待是否上升，见「连接池与高并发连接管理」；",
        "锁等待与死锁：InnoDB 锁等待时间、死锁计数、事务平均持锁时长，见「死锁：成因、检测与规避」；",
        "磁盘 IO：利用率、延迟与队列深度——指标怎么采、告警怎么定，工具路径见 observability 课程（三支柱、Prometheus 指标与告警），这里不重复工具链细节；",
        "经验判据：扩容应用实例后以上指标毫无改善，而磁盘、锁等待或 CPU 任一先打满，瓶颈就在库——此时再进入本章的扩展路线，而不是继续堆 API 实例。",
      ],
    },
  ],
};
