/* ==================================================================
 * 课时：服务发现与调用：东西向流量的正确姿势（sc-service-discovery-rpc）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "实例动态上下线让静态 IP 列表失效——服务发现、注册中心、健康检查与摘除、调用可靠性，与 sale→pay 的发现落地。",
  blocks: [
    {
      type: "paragraph",
      text: "网关解决了南北流量，但系统里还有另一股流量：order 要调 inventory 扣库存、要调 pay 发起支付、sale 要接收 pay 的支付回调。上一课说东西流量面对的是「寻址与可靠性」问题——本课展开寻址：服务实例的地址是怎么被调用方找到的？单体时代这不成问题：一个进程一个地址，配置写死即可。服务化之后，实例的数量、位置时刻在变，写死的 IP 列表变成了错误答案。",
    },
    {
      type: "heading",
      text: "为什么需要服务发现：实例是动态的",
    },
    {
      type: "paragraph",
      text: "回顾无状态化与水平扩容带来的日常：大促前 inventory 从 5 台扩到 50 台，活动结束后缩回；某台实例健康检查失败被自动摘除、新实例滚动发布上线。这些操作每发生一次，调用方手里的地址列表就过时一次。如果调用方把下游地址硬编码或写死在配置文件里，每次扩缩容都要改所有调用方再重启——服务发现（Service Discovery）要解决的就是这个问题：让调用方「按服务名找地址」，而不是「记死地址」。",
    },
    {
      type: "definition",
      term: "服务发现（Service Discovery）与注册中心（Registry）",
      definition:
        "服务发现：服务实例地址的动态发现机制——调用方按逻辑服务名查询当前可用的实例地址列表，而不是持有静态 IP 配置。注册中心（Registry）：实现服务发现的协调组件——服务实例启动时向它**注册**自己的地址并持续**心跳**续约，下线时注销；调用方从它查询并**订阅**某服务的实例列表变化（增删、摘除）。健康检查决定一个实例是否留在可发现列表里。",
    },
    {
      type: "heading",
      text: "起步方案：DNS——简单但有局限",
    },
    {
      type: "code",
      title: "DNS 轮询做服务发现的形态与局限",
      language: "text",
      code: "# 服务名解析为一组 A 记录，客户端/LB 轮询使用——最简单形态\napi.example.com  →  10.0.0.11  10.0.0.12  10.0.0.13\n\n# 局限：\n# 1. 摘除慢——实例挂了要等 TTL 过期 + 运维改记录，分钟级才生效\n# 2. 看不到实例健康度——DNS 只回答“名字→IP”，不知道谁还活着\n# 3. 无端口/元数据——同一服务多端口、带版本标签时表达力不足\n# 4. 缓存层级多——客户端、操作系统、本地 DNS 层层缓存，变化更难及时",
    },
    {
      type: "paragraph",
      text: "DNS 轮询的价值在于零额外组件：负载均衡器后挂少量固定实例、实例变更极低频时，它完全够用。一旦「自动扩缩容 + 自动摘除」成为日常——第 1 章「垂直与水平扩容」预告过，水平扩容会把「换大机器」变成「随时加减实例」的常态能力——DNS 的分钟级生效与无健康感知就成为硬伤，这正是注册中心登场的地方。",
    },
    {
      type: "heading",
      text: "两种发现架构：客户端发现与服务端发现",
    },
    {
      type: "table",
      caption: "客户端发现 vs 服务端发现",
      headers: ["维度", "客户端发现（Client-Side Discovery）", "服务端发现（Server-Side Discovery）"],
      rows: [
        ["负载均衡在哪", "调用方进程内（客户端库直接查注册中心、自己做 LB）", "独立的 LB/代理（查询注册中心后转发）"],
        ["多一跳", "无，直连目标实例", "有——所有调用经中间代理"],
        ["需要写代码", "接入注册中心客户端库（或 SDK）", "调用方只连代理地址，少写代码"],
        ["语言/框架耦合", "每种语言都要有兼容库", "语言无关"],
        ["典型代表", "gRPC + 注册中心、自研 RPC SDK", "Nginx/LB 动态 upstream、服务网格 sidecar（定位见上一课）"],
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "健康检查与摘除：谁决定实例能不能被调用",
      body: "只有「健康」的实例才允许留在可发现列表里，判定方式分三类：主动检查（Probe）——注册中心或代理定期探测实例的健康端点（如 /healthz，见第 1 课），连续失败即摘除；被动检查——观察真实调用结果，错误率/超时率超阈值即停止分发（与第 7 章熔断同源）；心跳续约——实例周期性向注册中心续约，中断视为失联，适合实例多、探测成本高的场景，但要容忍网络抖动、避免误摘。摘除之后必须有恢复路径：恢复健康即回到列表，形成「注册 → 心跳 → 摘除 → 恢复」闭环——第 1 课的优雅上下线（先摘流量再停、就绪再接）正是喂给这套机制的正确信号。",
    },
    {
      type: "heading",
      text: "调用可靠性：超时、重试、熔断与 RPC 框架",
    },
    {
      type: "paragraph",
      text: "找到地址只是第一步，调用本身还会失败：网络抖动、实例过载、下游发布。可靠调用的三件套——超时、重试、熔断——在第 7 章「流量治理」有完整参数与机制教学，本课只交代它们在调用链中的位置与一条最重要的预警：**重试风暴**。当大量调用方同时发现下游变慢，如果各自无脑重试，重试流量会叠加在已经过载的下游上把它彻底压垮——这正是第 7 章「过载保护」要防的雪崩路径。所以重试必须带上限、退避与随机抖动，并且只对「幂等且失败可重放」的请求重试；熔断（连续失败达阈值后快速失败并定期探测）负责把故障隔离在调用方一侧。记住分工：服务发现解决「找得到」，超时/重试/熔断解决「调得稳」，前者只管地址，后者管成败。",
    },
    {
      type: "paragraph",
      text: "HTTP 加 JSON 足够做服务间调用，但服务化成熟后，团队通常转向 RPC 框架：用接口定义（IDL）一次生成多语言客户端与服务端、用二进制序列化降低带宽与 CPU、自带超时/重试/负载均衡等调用语义。gRPC 是其中的代表：基于 HTTP/2、默认 Protobuf 序列化。注意本课只给定位——gRPC 的接口定义、生成代码、四种调用模式属于 grpc-go 课程「gRPC 系统学习」，需要动手时去那里学。选择判断一句带过：同团队内部服务间、追求低延迟高吞吐、多语言协作，RPC 值得；对外、跨组织、要生态兼容，HTTP 仍是最稳的选择。",
    },
    {
      type: "heading",
      text: "闪购落地：sale → pay 的调用",
    },
    {
      type: "paragraph",
      text: "把机制落到闪购上：pay 是第三方支付，sale 对它的「调用」是跳转到收银台加接收异步回调，只面对 pay 固定的商户网关地址，不需要也不知道 pay 内部的实例列表；对 notify 的触达则走异步队列（第 4 章），也不经服务发现。真正走服务发现的内部调用是 order → inventory 这类服务间调用。推演其形态：inventory 实例启动时向注册中心注册「inventory 集群」并心跳续约；order 调用方持有注册中心地址，按服务名拉取 inventory 实例列表并订阅变更；库存服务每台实例的 /healthz 被注册中心主动探测，连续失败即摘除，恢复即重新上线。order 侧对扣库存调用设置超时与有界重试（扣减必须是幂等接口——第 8 章），下游持续变慢时触发熔断，把故障挡在 order 内部，不向用户请求链扩散。pay 这个例子恰好划出服务发现的边界：**服务发现只适用于你能控制实例生命周期的一方，外部第三方永远按「固定地址 + 重试 + 对账」处理**（第 4 章「最终一致性与补偿」的对账视角）。",
    },
    {
      type: "quiz",
      question: "闪购大促开始前把 inventory 从 5 台扩到 50 台。使用注册中心的服务发现，order 服务是如何拿到新实例地址的？",
      options: [
        "运维手工把 50 台 IP 写进 order 的配置文件并重启 order",
        "新实例向注册中心注册并心跳续约，order 订阅 inventory 的实例列表变更后自动感知",
        "order 通过 DNS 解析一个永不变化的虚拟 IP",
        "网关把 50 台实例的地址在每次请求里下发给 order",
      ],
      answer: 1,
      explanation:
        "注册中心模式下，新实例主动注册、调用方订阅变更——扩容对调用方完全透明，这正是服务发现存在的意义。选项 A 是它要消灭的手工时代做法；选项 C 的 DNS 只解决名字到地址、对实例增删的感知有分钟级延迟且无健康感知；选项 D 张冠李戴——网关处理南北流量，不负责下发内部服务地址。",
    },
  ],
};
