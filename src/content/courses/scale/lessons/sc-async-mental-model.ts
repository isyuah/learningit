/* ==================================================================
 * 课时：异步化的本质：为什么能削峰与解耦（sc-async-mental-model）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "从同步调用的结构性缺陷出发，理解队列如何削峰填谷、把耦合变成契约，以及异步化必须付的三笔代价。",
  blocks: [
    {
      type: "paragraph",
      text: "「闪购」的开售瞬间，POST /orders 每秒涌进上万请求。下单逻辑本身并不复杂——把订单写进 MySQL、扣减库存、再让 notify 服务发一条短信——可当这些步骤同步串在一个 HTTP 请求里时，系统的命运就被最慢的那个下游绑架了：短信网关抖一下，整个下单接口跟着慢；notify 一挂，订单模块的线程全部堵在等待上；洪峰则直接砸向数据库。这一课回答异步化最根本的问题：为什么把「调用」换成「投递」，能同时解决尖峰与耦合——以及这条路要付什么过路费。",
    },
    {
      type: "definition",
      term: "异步（异步调用）",
      definition: "调用方发起请求或发布消息后不等待处理完成，立即继续自己的工作；处理结果稍后通过消息、回调、轮询或查询获得。与之相对，同步调用会阻塞等待对方返回。本课与本章讨论的异步，特指「把工作交给消息队列，由消费者按自己的节奏稍后处理」这一类；消息之外的任务队列、事件流、回调/轮询等载体在第 6 课「消息之外的异步」统一对比。",
    },
    {
      type: "heading",
      text: "同步调用的三个结构性缺陷",
    },
    {
      type: "list",
      items: [
        "尖峰直击：大促开售瞬间的下单量是平时的百倍级，而数据库与下游的容量是固定的——单机 MySQL 简单读写约 3k–8k QPS，短信网关这类外部通道更慢。同步模型下，流量有多大，压力就实时传导多远：容量最小的环节先被打穿，整条链路超时。",
        "级联失败：下单接口的返回依赖 notify、库存等所有下游。任何一个下游变慢，等待它的线程就被占住，故障沿调用链扩散——一个模块的抖动，变成整站雪崩。",
        "耦合：业务代码里写死「调 notify 发短信」。短信服务升级、更换供应商、新增一个审计订阅者，都要改动下单代码并重新发布；系统拆得越多，这种硬编码依赖越编越乱。",
      ],
    },
    {
      type: "heading",
      text: "队列：把速率与耦合一起切开",
    },
    {
      type: "paragraph",
      text: "解法是在调用双方之间放一个队列。生产端（sale）把「订单已创建」这条消息写进队列就返回，请求不再等 notify；消费端（notify）按自己的处理能力消费，能消化多少就取多少——推送还是拉取只是中间件的消费机制（见第 6 课「消息之外的异步」的对比），对「速率由消费者掌控」这件事没有影响。于是两个关键性质同时成立：瞬时流量先进队列缓冲，压力不再实时传导；消费速率由消费者自己掌控，而不是被生产速率绑架——这层能力的边界与代价，正是本章后续「背压与消费能力」一课的主题。数量级地看：假设开售洪峰是每秒 1 万条（示意数字），而数据库与短信网关每秒只能处理数千乃至数百，同步直击必死；队列化之后，峰值变成队列里的积压，消费者匀速消化，「排队延迟」替代了「请求超时」。",
    },
    {
      type: "definition",
      term: "削峰填谷",
      definition: "用队列缓冲瞬时尖峰，让消费速率平稳。流量高峰先进队列堆积，消费者按自身能力匀速处理，下游看到的是被拉平的水流而不是尖峰；代价是高峰期间的消息会延迟一段时间才被处理完。",
    },
    {
      type: "code",
      title: "削峰填谷示意（文本）",
      language: "text",
      code: `开售瞬间                    队列（缓冲区）                 消费者匀速处理
请求 1 万条/秒 ──写入──▶  ┌─────────────────────┐  ──消费──▶  2000 条/秒
                         │ 峰值先在这里排队      │             数据库 / 短信网关
请求回落到日常 ──────▶  │ 之后逐渐被排空       │             始终按自己能力消费
                         └─────────────────────┘

结果：下游看到的不是 1 万条/秒的尖峰，而是稳定、可控的水流；
     尖峰被转换成了「可预期的排队延迟」。`,
    },
    {
      type: "heading",
      text: "解耦：从「调用对方」变成「依赖契约」",
    },
    {
      type: "paragraph",
      text: "队列化之后，sale 不再知道 notify 的存在，唯一的约定是消息格式：有哪些字段、什么含义、什么版本。notify 挂了，sale 照常下单；要新增审计、风控等订阅者，不用改 sale 一行代码；双方还能各自扩缩容、独立发布。所以「解耦」不是把依赖删掉，而是把对「对方进程」的依赖，换成对「消息契约」的依赖——契约比进程稳定得多。消息中间件本身怎么部署、怎么保证可靠，属于 RabbitMQ 课程的内容（见「消息队列：为什么需要它」等课时）；本课只讨论它带来的结构变化。",
    },
    {
      type: "heading",
      text: "异步的三笔代价",
    },
    {
      type: "list",
      items: [
        "延迟与一致性：处理从请求内挪到请求外，「已受理」和「已完成」之间出现一段不一致窗口，业务需要接受最终一致——这个概念在第 5 课「最终一致性与补偿」正式定义。",
        "可观测性：一次下单被切成生产端与消费端两段，调用链在队列处断开。必须把 trace_id 写进消息并随消息透传，消费端才能把新的一段拼回同一条 trace（机制见可观测性课程「采样与跨服务传播」）。",
        "消息生命周期：消息会重复、会积压、会乱序、可能丢失——可靠性、背压、语义、一致性，本章第 2–6 课全部在为这三笔代价善后。",
      ],
    },
    {
      type: "code",
      title: "让 trace_id 跨过异步边界（Go，示意）",
      language: "go",
      code: `// 生产端：sale 把 trace_id 随消息一起发出（字段以团队契约为准）
type OrderCreated struct {
    OrderID int64  // 订单 ID
    UserID  int64  // 用户 ID
    TraceID string // 跨异步边界的链路标识
}

// 消费端：notify 从消息里取回 trace_id，
// 把「发短信」这段续接在原有 trace 上（实现见可观测性课程）。`,
    },
    {
      type: "paragraph",
      text: "**什么时候值得异步**：请求路径上花时间的是 IO 与下游，而它们往往可以稍后完成——短信通知、归档、推荐、对账都是天然的异步对象。反过来，凡是「结果必须立刻返回、且强一致」的操作（如扣款、库存扣减的最终判定），留在同步路径上更简单。异步不是替代同步，而是给「可延后的工作」一条更合适的路；判断的总原则在第 2 章「同步与异步的边界」一课，本章则把「决定异步之后」的工程问题讲透。",
    },
    {
      type: "quiz",
      question: "闪购准备把「订单创建 → 短信通知」改造为队列异步。下列哪一类操作最不适合一起改造成异步？",
      options: [
        "给用户发送大促短信（notify 消费）",
        "订单创建后通知审计与风控等订阅方",
        "从用户余额中扣款并立即返回结果",
        "支付成功后更新订单状态并通知用户",
      ],
      answer: 2,
      explanation: "扣款属于「结果必须立即生效、强一致、不可重复」的操作，留在同步事务/强一致路径上更安全；短信通知、订阅广播、支付回调后的状态推进都可以延后处理，适合异步与最终一致。",
    },
    {
      type: "keypoints",
      items: [
        "同步调用有三个结构性缺陷：尖峰直击、级联失败、调用方与实现方强耦合。",
        "队列是缓冲区：生产与消费在时间与速率上解耦；削峰填谷把瞬时尖峰换成可预期的排队延迟。",
        "削峰填谷：用队列缓冲瞬时尖峰，让消费速率平稳。",
        "解耦 = 契约化：只依赖消息格式，不依赖对方在线、健康、同版本。",
        "异步的代价：延迟与最终一致、跨边界可观测性（trace_id 透传）、消息生命周期管理。",
        "消费速率由消费者掌控的能力边界、消费者跟不上时的应对，是第 3 课「背压与消费能力」的主题（第 2 课先解决消息不丢不重不乱）。",
      ],
    },
  ],
};
