/* ==================================================================
 * 课时：数据分片：为什么必须分、按什么分（sc-sharding-intro）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "读可以加副本、写却锁死在单写者模型上——分片通过改变数据布局同时扩展读写，代价是跨片查询与一致性的复杂化。本课讲什么时候必须分、按什么分、怎么分。",
  blocks: [
    {
      type: "paragraph",
      text: "读写分离解决了读，却留下一个没有答案的问题：写。第 1 课讲过单写者模型——所有写必须过主库，行锁、间隙锁把并发写串行化，单主 MySQL 的写上限与单库容量上限都绕不开。闪购的演进逼到这一步：订单表从千万级涨向亿级、日常写逼近单库上限，秒杀集中扣减靠队列化预扣仍不够时，缓存与副本都无能为力——它们都不改变「一份数据只有一个写位置」的事实。**数据分片**是改变数据布局的手段：把同一张表的数据拆到多个独立实例，让「一个库的瓶颈」变成「N 个库各自扛 1/N」。本课定义分片、给出分片键的选择原则与分法对比，并诚实回答：代价有多大、什么时候才真的值得。",
    },
    {
      type: "definition",
      term: "数据分片",
      definition: "按某个分片键（shard key，也称分区键）的取值，把同一张逻辑表的数据分布到多个物理实例（分片/分区）上。每个分片是一份完整独立的 MySQL（自己的 Buffer Pool、锁与日志），路由规则决定一条 SQL 落到哪个分片。分片同时扩展读与写——写也散到多台了——这是它与读写分离的本质差别；但跨分片的查询、事务与排序需要应用层自己解决。",
    },
    {
      type: "heading",
      text: "单库的上限从哪来",
    },
    {
      type: "paragraph",
      text: "分片前先量化「单库的墙」在哪：① 容量墙：数据量与索引超出单机磁盘/内存的合理承载，备份与恢复时间随数据量膨胀（见 MySQL 课程「备份、恢复与运维基础」）；② 写墙：单写者下写 TPS 受限于单机 CPU、redo/磁盘 fsync 能力，行锁竞争再叠加延迟（见第 1 课）；③ 运维墙：连接数上限（见 MySQL 课程「连接池与高并发连接管理」）——所有 API 实例共享同一批库连接，实例一多，先打满的可能是 MySQL 的连接数。经验上单机 MySQL 简单读写约 3k–8k QPS（本课程基线）：「撑不住」先要确认是查询没优化（那属于 MySQL 课程的地盘），还是上述结构性的墙——只有后者才需要分片。",
    },
    {
      type: "heading",
      text: "分片键：选择三原则",
    },
    {
      type: "list",
      items: [
        "**分布均匀**：分片键的取值基数要大、各取值流量相近。按 status（只有几个值）分片必致数据倾斜——某片注定成为热点。",
        "**访问局部性**：高频访问应尽量落在单个分片内完成：查询携带分片键（如「我的订单」按 user_id 定位），相关联的数据（用户 + 其订单）同片存放，让绝大多数 SQL 变成单片内的普通 SQL。",
        "**避免跨分片操作**：分片键一旦选定，JOIN、事务、聚合只要跨片就要付出应用层代价（见下文）——宁可设计时让跨片场景尽量少，不要事后补救。",
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购订单表的分片键之争",
      body: "分片键决定数据的「邻居」：同键的数据在同一分片，能享受单机事务、单机 JOIN 与排序；异键的天各一方。选键因此等于选「把哪些数据聚在一起」。按 user_id 分片：单用户的订单天然聚在一台机器，事务与查询都舒服，路由时把 user_id 带进 SQL 即可。按时间范围分片（如按月）：冷热分离好、归档方便、写分布均匀，但「某用户的全部订单」必然跨片聚合，且月尾数据量大时单片仍是热点。订单系统主流选 user_id；时间维度更适合做冷热归档而不是水平分片。判断标准就是上面三条：高频查询以谁为条件、谁能均匀分布、谁让事务不跨片。",
    },
    {
      type: "heading",
      text: "范围分片 vs 哈希分片",
    },
    {
      type: "table",
      caption: "范围分片与哈希分片对比",
      headers: ["维度", "范围分片（range）", "哈希分片（hash/mod）"],
      rows: [
        ["路由方式", "按分片键区间：如 user_id 1–1000 在片 1", "按分片键哈希取模：如 user_id % 16"],
        ["分布均匀性", "取决于数据与访问的分布，易倾斜", "统计上均匀，前提是键基数够大"],
        ["范围查询", "天然支持片内范围扫描（如某时间段）", "范围查询必然跨片——除非键本身带范围前缀"],
        ["扩容", "扩一片就多一段区间，迁移量小", "取模 N 变 N+1 触发大规模 rehash（第 4 课解决）"],
        ["热点", "区间内写集中则单片过热（如月尾）", "单一热点键仍会集中到一片（见第 4 课坑清单）"],
      ],
    },
    {
      type: "paragraph",
      text: "选型逻辑：哈希分片用均匀性换掉了范围友好性，适合「按用户/按对象等值定位」的访问模型；范围分片适合「时间序列、天然按段访问」的数据。不少系统采用**复合设计**：主分片键哈希打散保证均匀，数据内再带时间/序号列支持片内排序——鱼与熊掌不兼得，选择必须显式。",
    },
    {
      type: "heading",
      text: "分片后的 ID 生成：谁保证全局唯一",
    },
    {
      type: "paragraph",
      text: "分片后数据库自增主键失效——每个分片各自从 1 开始必然撞车；而订单这类实体常要「插入时就知道 ID」（下单后立刻返回单号）。两条主流路线：**号段（segment）**：一个发号服务维护每张表「当前段 + 步长」，应用一次取一段在内存自增，段用完再取；简单可靠、ID 趋势递增，但发号服务是新的依赖点。**雪花算法（Snowflake）**：64 位 = 时间戳 + 机器标识 + 序列号，各机器本地生成、不依赖任何服务，趋势递增且高吞吐；代价是依赖机器时钟，时钟回拨需要保护逻辑。UUID 全局唯一但无序且长，做聚簇主键会破坏 B+ 树插入局部性（见 MySQL 课程「聚簇索引与二级索引：回表是怎么发生的」）。要点：发号要「唯一、趋势有序、高可用、低延迟」四者权衡——工程取舍在第 4 课，本课只建立概念。",
    },
    {
      type: "heading",
      text: "跨分片的三笔账：查询、事务、JOIN",
    },
    {
      type: "list",
      items: [
        "**跨片查询**：不带分片键的查询要在每个分片跑一遍再合并——扇出（fan-out）。正确，但延迟 = 最慢分片，且随分片数放大资源消耗；深分页（第 N 页）在各分片取前 offset+N 再合排，代价随页深爆炸——所以分片后要对深分页重新设计（游标/限定深度，呼应请求路径章的分页讨论）。",
        "**跨片事务**：单库 ACID 边界只到分片内，跨两片的「扣库存 + 建订单」需要分布式事务——2PC 慢且脆弱、TCC/Saga 要业务补偿。工程现实是尽量避免：让事务的所有参与行同片（库存与订单按相同键聚一起），或改成最终一致（第 8 章「跨库事务的现实」是权威出处）。",
        "**跨片 JOIN**：SQL 层 JOIN 只存在于单片内。跨片关联要么应用层分片查 + 内存合并，要么预聚合/宽表冗余——无论哪种，复杂度都转移到了应用代码身上。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "分片是不可逆的架构税",
      body: "「分片是最后手段」不是口号，是可执行的判断：① 先确认查询与事务已按 MySQL 课程优化到位（索引、短事务、批量化）；② 再确认缓存与读写分离已上线且仍不够——分片不解决重复读热点，那些应先由缓存接走；③ 确认瓶颈确在「写总量/单键写/单库容量」之一，且垂直扩容与异步化（第 4 章削峰）都摊不平；④ 最后自问分片键能否稳定选出来——架构要跟着它走好几年，选错重来成本极高。一旦分片，JOIN 没了、自增主键没了、二级索引跨片靠扇出、事务边界重画，所有调用方与新功能都要带着分片键思考。判断锚点：分片解决的是单库结构性写/容量上限，不是解决「读慢」，更不是解决「查询写得差」。",
    },
    {
      type: "exercise",
      title: "为闪购的订单表设计分片键并指出代价",
      description: "假设订单量达到单库写与容量上限，需要水平分片。请评估 order 表分别用 user_id、product_id、order 号段做主分片键时，各牺牲了哪条高频路径（用户查自己的订单 / 运营按商品查销量 / 支付回调按订单号更新状态）；再说明：订单号若不携带分片信息，按订单号直接查询会怎样；最后说清你会让「秒杀库存」与订单同片还是单独分片、为什么。",
      hint: "把每条高频 SQL 的 where 条件列出来，看谁出现在所有查询里；再想扣库存与建单能否放进同一个单机事务。",
    },
  ],
};
