/* ==================================================================
 * 课时：单机并发正确性：锁、原子与事务边界（sc-concurrency-single）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "在讨论分布式之前先守住单机：共享可变状态、互斥锁与原子操作、数据库行锁与乐观锁，以及一个请求该不该开一个事务。",
  blocks: [
    {
      type: "paragraph",
      text: "水平扩容的前提，是先守住单机上的正确性。把 sale 服务从 1 个实例扩到 100 个实例之前，必须确保单实例内部的并发已经正确——否则错误不会因为「多开几台机器」而消失，只会被复制和放大。本章以闪购最核心的写路径 `POST /orders`（扣库存 + 建单）为主线：这一课先看清单机上的三个层次——共享可变状态、数据库行级并发、事务边界；下一课再处理把请求复制出去之后必然出现的「重复执行」。",
    },
    {
      type: "heading",
      text: "共享可变状态：一切并发错误的根源",
    },
    {
      type: "paragraph",
      text: "两个 goroutine 同时对同一个变量做「读-改-写」，最终结果取决于它们交错的方式，这就是**数据竞争（data race）**。问题不在「多线程」本身，而在于**共享 + 可变**：状态不被共享，或共享后不可变，就没有竞争。所以写并发代码的第一问不是「用哪种锁」，而是「这份状态归谁所有、能不能少共享、能不能下沉到唯一的权威存储」——把状态归属想清楚，需要的锁自然就少了。",
    },
    {
      type: "code",
      title: "反例：进程内计数器的读-改-写竞争（示意）",
      language: "go",
      code: "var inFlight int64\n\n// 两个 goroutine 可能同时读到同一个旧值、各自减一后写回：\n// 并发 100 个请求，最后 inFlight 可能不是 0，甚至变负数\nfunc onRequestStart() { inFlight = inFlight - 1 } // 读 → 减 → 写，非原子\n\n// 修复：把“读-改-写”收进临界区，或用原子操作（见下文）\nvar mu sync.Mutex\nfunc onRequestStartFixed() {\n    mu.Lock()\n    inFlight--\n    mu.Unlock()\n}",
    },
    {
      type: "callout",
      variant: "note",
      title: "用 race detector 抓数据竞争",
      body: "Go 提供了内置的数据竞争检测器：`go test -race ./...` 会在运行时检测并对每一处实际发生的竞争输出类似 `WARNING: DATA RACE` 的报告（涉及 goroutine、地址与读写栈）。注意它的盲区：**只能发现「实际执行到的」交错**，没跑到的时间片不会报。所以要用真实流量与压测反复跑 race 版本，同时靠 code review 盯住每一处新增的共享可变状态。",
    },
    {
      type: "heading",
      text: "单机并发三件套：互斥锁、读写锁与原子操作",
    },
    {
      type: "paragraph",
      text: "Go 的 `sync` 与 `sync/atomic` 覆盖了绝大多数单机需求：**互斥锁（sync.Mutex）**语义最强、开销也最小，适合「临界区短、写多」的场景，拿不到锁就阻塞等待；**读写锁（sync.RWMutex）**让读可以并行、写独占，适合读多写少的结构（比如读多写少的进程内配置表），但要小心写饥饿——持续不断的读可能让写者长时间拿不到锁；**原子操作（sync/atomic）**只解决「单个变量的读-改-写」，比如计数、开关，无锁、无阻塞，但它表达不了「跨多个字段的不变式」，那种约束还是要靠锁。除此之外，**尽量用 channel 传递所有权**而不是共享指针，是 Go 里最值得优先考虑的并发设计。",
    },
    {
      type: "code",
      title: "sync 与 atomic 的最小用法",
      language: "go",
      code: "import (\n    \"sync\"\n    \"sync/atomic\"\n)\n\nvar mu      sync.Mutex    // 互斥：写多、临界区短\nvar rwmu    sync.RWMutex  // 读写锁：读多写少\nvar online  int64         // 原子计数：单值读改写\n\nfunc markOnline() {\n    atomic.AddInt64(&online, 1) // 等价于“读+加+写”一步完成\n    defer atomic.AddInt64(&online, -1)\n    // …业务处理…\n}\n\nfunc dumpSnapshot() {\n    rwmu.RLock() // 多个读者可同时进入\n    defer rwmu.RUnlock()\n    // …读取共享结构…\n}",
    },
    {
      type: "heading",
      text: "数据库里的并发裁决：状态归属 → 锁粒度 → 原子性",
    },
    {
      type: "paragraph",
      text: "闪购的库存最终落在 MySQL 的一行里，进程内锁保护不了它——多实例、多连接都会碰到同一行。机制层面（当前读、行锁、MVCC、`SELECT ... FOR UPDATE`、版本号乐观锁）MySQL 课程已经讲透（见「事务与 ACID」「锁机制：表锁、行锁、间隙锁与 next-key」「面试——乐观锁 vs 悲观锁」）。这里换一个**并发正确性思维**的视角，用三个问题把方案推出来：状态归属、锁粒度、原子性。",
    },
    {
      type: "code",
      title: "数据库防超卖的两种写法",
      language: "sql",
      code: "-- ① 悲观行锁：先锁住裁决行，提交才释放（当前读）\nSTART TRANSACTION;\nSELECT stock FROM product WHERE id = ? FOR UPDATE; -- 本行写者在此串行\nUPDATE product SET stock = stock - 1 WHERE id = ?;\nCOMMIT;\n\n-- ② 条件原子更新：单条 UPDATE 在引擎内原子完成“判断+扣减”\nUPDATE product SET stock = stock - 1\nWHERE id = ? AND stock > 0;\n-- 影响行数 = 1 表示扣减成功；= 0 表示库存不足",
    },
    {
      type: "table",
      caption: "用三个问题组织并发正确性方案",
      headers: ["问题", "要回答什么", "闪购例子"],
      rows: [
        ["状态归属", "这份状态唯一的权威存储在哪里", "库存只有 product 行这一份真相；进程内/Redis 里的数字只是它的加速或预占"],
        ["锁粒度", "锁保护的范围是不是恰好覆盖被修改的状态", "单商品扣减 → 行锁足够；涉及多行/多表的操作 → 锁和事务范围要跟着放大"],
        ["原子性", "“判断”与“修改”是不是同一个原子动作", "`AND stock > 0` 让判断与扣减一次完成；拆成两步就留下超卖窗口"],
      ],
    },
    {
      type: "heading",
      text: "事务边界：一个请求该不该是一个事务",
    },
    {
      type: "paragraph",
      text: "什么时候开事务、事务里放几条语句，是单机正确性的第三道闸。经验法则：**只有必须一起成功或一起失败的一组写，才放进同一个事务**；纯读路径不要整段包事务。事务越短越好——长事务等于把行锁和连接持有时间放大到业务代码的执行时间：行锁竞争直接抬升（呼应第 5 章「数据库为什么先成为瓶颈」），连接被长时间占用则挤占池子（呼应 MySQL 课程「连接池与高并发连接管理」「事务工程最佳实践」）。两个高频反模式：把远程调用放进事务（等于把锁借给外部系统的延迟），以及把用户等待放进事务。闪购单库时代的正确形态是：一个短事务里 `UPDATE product 扣库存` + `INSERT order`，提交后再发通知——通知的可靠性交给第 4 章的消息机制，而不是用事务去保护网络。",
    },
    {
      type: "quiz",
      question: "关于单机并发与事务边界，下列说法正确的是？",
      options: [
        "只要开了数据库事务，进程内的共享变量就不用再加锁了",
        "事务应尽量短、只包含必须原子的一组写，且不应包含远程调用",
        "悲观锁永远优于乐观锁，因为它永远不会失败",
        "数据竞争只会影响性能，不会影响正确性",
      ],
      answer: 1,
      explanation: "事务只管辖数据库，管不了进程内共享变量；把事务拉长到包含远程调用，等于让行锁和连接去等外部系统的延迟，是典型反模式，所以 B 正确。A 混淆了两种状态域；C 忽略悲观锁的锁竞争与吞吐上限；D 说反了——数据竞争是产生错误结果的正确性缺陷。",
    },
    {
      type: "keypoints",
      items: [
        "并发错误来自「共享 + 可变」；先想清楚状态归属，再决定用不用锁。",
        "单机三件套：Mutex（写多）、RWMutex（读多写少）、atomic（单值计数）；优先用 channel 传递所有权。",
        "`go test -race` 只能抓「实际执行到的」交错，生产流量 + review 都不能省。",
        "数据库裁决按「状态归属 → 锁粒度 → 原子性」组织：归属唯一、锁粒度对齐、判断与修改同原子。",
        "事务只包必须原子的一组写，越短越好，事务内禁止远程调用——为下一课的「重复执行」留好干净的入口。",
      ],
    },
  ],
};
