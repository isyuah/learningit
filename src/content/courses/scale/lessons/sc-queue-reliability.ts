/* ==================================================================
 * 课时：消息可靠性：不丢、不重、不乱（sc-queue-reliability）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "定义 at-most-once / at-least-once / exactly-once 三种投递语义，拆解发送、存储、消费三个环节的可靠性，并给出重复消费的幂等防线与顺序性方案。",
  blocks: [
    {
      type: "paragraph",
      text: "第 1 课里，sale 把「订单已创建」写进队列就返回。但从这一刻起，问题变成了：这条消息会不会丢？会不会发两次短信？消费端看到的顺序会不会乱？异步化的全部收益都建立在「消息系统靠得住」之上——这一课把可靠性拆成三个可回答的问题：什么情况下会丢（发送/存储/消费三个环节）、丢了重了怎么办（语义与幂等）、顺序怎么保证（分区键）。RabbitMQ 的具体操作细节不在这里展开，指向 RabbitMQ 课程的对应课时。",
    },
    {
      type: "definition",
      term: "投递语义（delivery semantics）",
      definition: "消息系统在发送、传输、消费全程中，对「一条消息最终被消费端处理多少次」给出的承诺，是可靠性设计的第一步。标准分类：at-most-once（至多一次，可能丢失）、at-least-once（至少一次，可能重复）、exactly-once（恰好一次）。",
    },
    {
      type: "table",
      caption: "三种投递语义对比",
      headers: ["语义", "含义", "典型实现路径", "代价/风险"],
      rows: [
        ["at-most-once（至多一次）", "消息可能丢失，但绝不会重复", "不重试、不持久化或发送即确认", "丢消息：通知类场景要能容忍漏发"],
        ["at-least-once（至少一次）", "消息不丢，但可能重复处理", "发送确认 + 持久化 + 消费 ack + 失败重投", "重复消费是常态，必须幂等"],
        ["exactly-once（恰好一次）", "不丢、不重，效果上恰好一次", "at-least-once + 幂等消费/去重；或事务性机制", "不存在纯传输层保证，本质是去重"],
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "exactly-once 的现实",
      body: "严格意义上的「恰好一次」无法只靠传输层达成：网络可能重试、Broker 与消费者可能在确认的瞬间各自宕机，没有任何传输协议能同时保证「消息到了且只到一次」。工程上所谓的 exactly-once，都是 at-least-once 之上叠加幂等消费或事务性去重后的效果——「恰好一次」是业务层语义，不是传输层属性。",
    },
    {
      type: "heading",
      text: "三个环节的可靠性",
    },
    {
      type: "list",
      items: [
        "发送端：消息真的被 Broker 收下了吗？靠发送确认（publisher confirm）确认写入；而「本地业务提交」与「发消息」无法原子完成，是另一类丢消息源头——解决它的 outbox 模式在第 4 课「事件与命令」展开。",
        "存储：Broker 收到后，消息在内存还是磁盘？宕机会不会丢？持久化（durable 队列 + persistent 消息）与副本（Quorum 队列；RabbitMQ 4.0 起经典镜像队列已移除）决定这一层——操作细节见 RabbitMQ 课程「持久化、镜像与消息落盘」「Quorum 队列、镜像迁移与高可用」。",
        "消费端：消费者处理完才 ack，Broker 才删除；处理失败则消息重投。消费确认的语义细节见 RabbitMQ 课程「消费确认、prefetch 与重投递」。",
      ],
    },
    {
      type: "code",
      title: "消费端手动 ack：成功才确认（概念示意）",
      language: "text",
      code: `循环消费消息:
    处理业务（写库、调下游）         # 先处理
    成功 → 发送 ack（确认）          # Broker 才删除这条消息
    失败 → 不 ack / 拒绝并重投       # 消息回到队列，稍后重试
    （连接断开时未确认的消息自动重回队列）`,
    },
    {
      type: "paragraph",
      text: "三个环节都按「at-least-once」来配，系统就做到不丢——但代价是**重复**：网络超时后重发、ack 在途中丢失、消费端处理成功但 ack 前宕机、Broker 重投，都会让同一条消息被处理不止一次。既然重复是常态，防重复就不能寄望于消息系统，而要落在消费端：处理动作本身必须幂等。",
    },
    {
      type: "heading",
      text: "幂等消费：把重复变成无害",
    },
    {
      type: "definition",
      term: "幂等（idempotent）",
      definition: "同一操作重复执行，与执行一次效果相同。重复消费消息时，若消费动作幂等，则重复投递天然无害，无需额外去重。",
    },
    {
      type: "list",
      items: [
        "唯一键 + 去重表：notify 消费「订单已创建」前，先以消息里的 order_id 查/插去重表（带唯一索引），插入成功才真正发短信，冲突则直接丢弃——重复消息在这里被拦下。",
        "状态机幂等：把处理目标写成状态转移，「把订单从未支付置为已支付」是幂等的，重复执行结果不变；业务状态本身就是天然的判重依据。",
        "天然幂等操作：覆盖写（UPDATE 设置固定值）、幂等 API（带请求方幂等键）不需要额外去重；扣减、追加这类非幂等操作必须走前两种模式。",
      ],
    },
    {
      type: "code",
      title: "去重表拦截重复消费（SQL 伪代码）",
      language: "sql",
      code: `-- 去重表：消息处理的唯一凭证，order_id 建唯一索引
CREATE TABLE consume_dedup (
    msg_key   VARCHAR(64) PRIMARY KEY,  -- 如 'order.created:20260901-0001'
    created_at DATETIME
);

-- 消费流程：
-- 1. INSERT ... 成功  → 第一次见，执行真正业务
-- 2. INSERT 主键冲突  → 重复消息，直接跳过
-- （INSERT 与业务建议放同一本地事务，保证原子）`,
    },
    {
      type: "paragraph",
      text: "可靠性第三问是顺序。消息队列通常保证「同一队列内有序」，但分布式队列往往有多个分区（分区/队列分片），消息按**分区键**（partition key）路由——同一分区键的消息进同一分区、按序消费。要让某个业务实体的消息不乱，就给它们选同一个分区键：闪购里以 order_id 为键，同一订单的「已创建 → 已支付 → 已发货」事件就保持先后；以 user_id 为键，则同一用户的所有事件有序。代价很清楚：**全局有序**要求全系统只有一个分区，等于把并发度降回单点，吞吐随之受限——工程上几乎总是「按业务实体分区有序」，而不是全局有序。",
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购：订单事件可靠投递到短信",
      body: "链路：sale 发布「订单已创建」（含 order_id 与 trace_id）→ Broker 持久化存储 → notify 消费、发短信。sale 开发送确认并持久化消息防发送与存储两段丢失；notify 手动 ack、处理成功才确认防消费段丢失——三环齐备即「不丢」。notify 收到重复消息由去重表按 order_id 拦截，短信只发一次。投递语义：at-least-once + 幂等消费，效果等同恰好一次。",
    },
    {
      type: "exercise",
      title: "设计 notify 的可靠消费流程",
      description: "notify 从队列消费「订单已创建」，调用第三方短信接口发短信（接口偶发超时）。请给出：消费确认策略、去重与重试方案（含去重表结构或状态机设计），并说明各环节分别防住哪种丢失/重复。",
      hint: "先画三环节：发送端、存储、消费端各自可能丢在哪；再回答「处理成功但 ack 失败」这类重复路径如何被幂等吸收。",
    },
    {
      type: "keypoints",
      items: [
        "投递语义三定义：at-most-once 会丢不重、at-least-once 不丢会重、exactly-once 是 at-least-once 叠加幂等/去重后的业务效果，不存在纯传输层的严格一次。",
        "三个环节各司其职：发送确认（发送端）、持久化与副本（存储）、手动 ack 与重投（消费端）。",
        "重复消费是常态，幂等消费是防线：唯一键去重表、状态机幂等、天然幂等操作三类模式。",
        "顺序性靠分区键：同一实体同一分区即有序；全局有序=单分区，以吞吐为代价。",
        "闪购 notify 链路 = at-least-once 三环节 + 去重表，效果接近 exactly-once。",
      ],
    },
  ],
};
