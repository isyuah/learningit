/* ==================================================================
 * 课时：最终一致性与补偿（sc-async-consistency）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "从本地事务到跨服务数据的变化讲起，定义最终一致及其度量窗口，介绍补偿/Saga 与对账回查，并给出什么业务能接受最终一致的判断。",
  blocks: [
    {
      type: "paragraph",
      text: "前几课里，sale 把下单后的通知、跟进这类旁路工作交给了队列——于是出现了一个单机时代不存在的问题：同一笔订单的数据分散在订单库、通知记录、风控评分里，它们不再在同一时刻一起变化。用户在秒杀成功后立刻去查，可能要等一会儿才能在各个系统里看到一致的记录。这种「等一会儿才一致」到底能不能接受？窗口多大算病？这一课从本地事务讲起，定义最终一致与度量方式，并讨论异步化最让人不安的部分：出错了怎么回头收拾。",
    },
    {
      type: "heading",
      text: "从本地事务到跨服务：为什么拆开就管不住了",
    },
    {
      type: "paragraph",
      text: "单体时代，一次下单是「扣库存 + 建订单」一个本地事务：ACID 保证要么全成、要么全不成，出错自动回滚。在闪购的主线设定里，订单与库存本来就同属 sale 自己的库，POST /orders 的本地事务依然成立——真正把数据拆开的是两种演进：一是把某些环节切给外部系统（支付、短信通知、风控评分各有各的数据），二是服务化拆分（见第 6 章「服务拆分：微服务的动机与代价」）把原来一张库里的表分到不同服务名下。拆开后，没有哪个数据库事务能同时管住多张分布的表。任何「跨服务协调」都必须通过网络完成，而网络会延迟、会失败、会重复：这就是跨服务不能沿用本地事务的根本原因。本地事务的原子性来自单库的回滚日志与锁；跨服务没有共享的回滚日志，唯一的共享介质是网络消息——于是只有两条路：要么引入专门的一致性协议（2PC/TCC/Saga 等，系统展开在第 8 章「跨库事务的现实：2PC、TCC、Saga 与消息事务」一课），要么接受不同服务的数据先后达成一致。",
    },
    {
      type: "definition",
      term: "最终一致（eventual consistency）",
      definition: "无新更新的前提下，经过有限时间所有副本/相关数据收敛一致。核心在于：某一时刻各数据可能不同步，但只要系统持续运转、不再有新的变更，差异会在有限时间内消失。",
    },
    {
      type: "table",
      caption: "ACID vs BASE",
      headers: ["维度", "ACID（本地事务）", "BASE（跨服务/分布式常态）"],
      rows: [
        ["核心保证", "原子、一致、隔离、持久", "基本可用（Basically Available）"],
        ["一致性", "事务内强一致，提交即生效", "软状态 + 最终一致（Eventual consistency）"],
        ["出错处理", "事务回滚，自动还原", "补偿操作，人工/定时对账兜底"],
        ["适用边界", "单库内、短事务", "跨服务、跨库、可延后收敛的业务"],
      ],
    },
    {
      type: "list",
      items: [
        "不一致窗口：从「源数据变更生效」到「所有相关副本/服务看到该变更」的时间差。下单成功那一刻，订单状态已更新，而 notify 的发送记录、风控的统计还在路上——这段空档就是窗口。",
        "度量口径：平均不一致时长（多数请求的体感）与最大不一致时长（P99/P100，决定业务能否接受）。正常削峰时窗口取决于队列积压与消费速率，可用「最早未消费消息的等待时间」近似衡量。",
        "对业务的意义：窗口可量化之后才能定目标——短信通知允许几十秒的窗口没问题；库存余量显示晚几秒会引发超卖投诉；而「用户是否已支付」这类判定不允许含糊。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "最终一致不是「随意不一致」",
      body: "最终一致承诺的是「收敛」：差异必须消失，而不是放任各系统各说各话。要收敛，就必须有收敛机制——失败重试、补偿、定期对账，三者缺一，最终一致就退化成「永远不一致」。",
    },
    {
      type: "definition",
      term: "补偿（compensation）",
      definition: "当跨服务流程中某一步失败时，对已成功的前序步骤执行反向操作（如已扣库存就回补库存），使整体状态回到业务上可接受的样子。补偿是跨服务场景对「回滚」的替代——没有共享事务，只能一步一步地往回找补。",
    },
    {
      type: "paragraph",
      text: "把补偿串成完整方案的就是 Saga：一条正向流程（下单 → 扣库存 → 支付 → 发货），每一步都登记对应的补偿动作（取消订单、回补库存、退款），任一步失败就从失败点开始反向执行补偿。编排式 Saga 由一个协调者显式指挥每一步并触发补偿，各参与者是被动的执行者；协同式 Saga 没有协调者，每个服务处理完自己的步骤后发布事件、由下一个服务订阅接力，补偿同样靠事件链传递。需要强调：这里只建立「正向流程 + 补偿」与两种协调形态的心智模型——编排/协同在具体工程里的机制细节（状态机、超时、挂起）属于第 8 章「跨库事务的现实」的 Saga 部分，那里才展开。",
    },
    {
      type: "code",
      title: "Saga 的正向流程与补偿登记（伪代码）",
      language: "text",
      code: `正向流程:                    每步的补偿（登记备用）:
  1. 创建订单 ────────────────  取消订单（仅未支付可取消）
  2. 扣减库存 ────────────────  回补库存
  3. 发起支付 ────────────────  退款
  4. 通知发货 ────────────────  （不可补偿：发货后只能走售后）

任一步失败 → 从该步起反向执行已登记补偿；
补偿本身同样要幂等（重试安全），且可能失败 → 进入人工/对账兜底。`,
    },
    {
      type: "paragraph",
      text: "补偿建立在消息与重试之上，而消息会重复（第 2 课），于是两条铁律：**每一步（正向或补偿）都必须幂等**——支付回调重复到达不能重复加钱，回补库存重复执行不能把库存补爆，靠幂等键/去重表/状态机拦住；**重试是补偿的引擎**——瞬时失败靠指数退避重试消化，超过上限的进入死信等待人工或对账处理。不设计幂等的 Saga 不是可靠方案，是把故障从「会失败」升级成「失败了还反复造成伤害」。",
    },
    {
      type: "paragraph",
      text: "再好的重试与补偿也有漏网之鱼：进程崩溃在补偿前、消息彻底丢失、人为失误。于是需要对账——定期任务把跨系统的数据拉出来比对：订单状态与支付结果对不对得上、outbox 表里有没有超过 N 分钟仍未发出的行、库存流水与订单量是否平衡，发现差异就触发回查或人工介入。对账在后续课程里会反复出现：第 8 章「设计练习：秒杀下单的库存扣减」用对账收敛 Redis 预扣与数据库实扣的漂移，第 9 章综合设计把对账时限写进验收口径（见「需求与规模」的一致性条目）；本课先建立概念——对账是把最终一致从「期望」变成「可验证事实」的手段。",
    },
    {
      type: "list",
      items: [
        "能接受：短信/推送通知（晚几十秒无感）、积分与优惠券发放（可稍后到账、可对账补发）、搜索与报表索引（延迟分钟级没问题）、邮件与站内信。",
        "不能接受：扣款与余额变动（重复或丢失都是资金事故）、库存扣减的最终判定（秒杀超卖无法事后抹平）、权限与安全状态（封禁必须立即生效）。",
        "决策规则：看「不一致窗口内，用户或系统会不会做出不可逆的错误决定」。会→必须强一致或加防错约束（如支付前校验）；不会→可以最终一致 + 对账兜底。",
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购：下单 → 支付 → 发货的状态机",
      body: "订单状态（pending → paid → shipped → completed，与第 8 章状态机词汇一致）在状态机里逐步推进：建单后广播「订单已创建」；支付回调到达时，sale 幂等地把订单置为 paid 并广播「订单已支付」，通知/物流随后推进；任一步的消息丢失由对账任务发现并回查补推。用户看到的订单状态允许短暂滞后，但「扣款金额、库存数量、订单状态」三者必须在可接受的窗口内收敛——超过窗口仍不一致，就是需要告警的事故。",
    },
    {
      type: "quiz",
      question: "下列哪项业务最不适合采用最终一致 + 异步补偿的方案？",
      options: [
        "支付回调后给用户发送发货通知",
        "大促结束后给用户补发积分",
        "用户点击「立即购买」时同步扣减余额并返回结果",
        "把订单数据异步同步到搜索索引",
      ],
      answer: 2,
      explanation: "扣减余额属于不可逆、结果必须立即返回的资金类操作，任何重复或丢失都是事故，应留在强一致路径；通知、积分补发、搜索索引都允许延迟收敛，可用最终一致 + 对账。",
    },
    {
      type: "keypoints",
      items: [
        "跨服务没有共享回滚日志，网络会失败会重复——本地事务的 ACID 无法直接搬到跨服务场景。",
        "最终一致：无新更新时所有相关数据在有限时间内收敛；窗口用平均/最大不一致时长度量。",
        "补偿是跨服务的回滚；Saga 把正向流程与补偿串起来（编排式有协调者，协同式靠事件链），细节机制在第 8 章。",
        "幂等与重试是补偿的地基：每步幂等、重试有上限、上限之外进死信/人工。",
        "对账与回查是把最终一致变成可验证事实的最后防线。",
        "能接受最终一致：通知/积分/搜索索引；不能接受：扣款、库存判定、安全状态。",
      ],
    },
  ],
};
