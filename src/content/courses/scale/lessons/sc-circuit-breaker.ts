/* ==================================================================
 * 课时：熔断与超时：失败隔离（sc-circuit-breaker）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "讲透失败隔离：超时的必要性、重试纪律与抖动、熔断状态机 closed/open/half-open、舱壁隔离，以及 pay 故障时 sale 如何自保。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课预告了重试风暴：一次超时被每一层重试复制，能把小故障放大成雪崩。这一课把「隔离故障」落到实处——先看超时与重试为什么必须被管理，再讲熔断状态机与舱壁隔离，最后回到闪购：当外部支付服务 pay 故障时，sale 为什么还能活着。",
    },
    {
      type: "definition",
      term: "熔断 circuit breaker",
      definition: "连续失败达到阈值后快速失败、不再调用下游，并定期探测恢复的故障隔离机制。它模仿电路跳闸：平时闭合（closed）正常放行；失败积累到阈值后跳开（open）——调用方不再发起真实调用、立刻返回失败，给下游喘息恢复的时间；冷却后转入半开（half-open）试探，确认恢复再闭合。熔断解决的是「反复尝试一个已知坏掉的下游」带来的浪费与放大。",
    },
    {
      type: "heading",
      text: "超时：第一个也是最基本的动作",
    },
    {
      type: "paragraph",
      text: "没有超时的调用意味着什么？一个下游挂起，调用它的线程或协程就被无限期占住，连接池里的连接也一直被借不还——池被占满后，所有经过这条路径的新请求排队等连接，整个服务跟着瘫痪（池耗尽即容量信号，见第 2 章「连接与线程池」与 MySQL 课程中的连接池讨论）。所以**工程上的第一个动作是给每次调用设超时**：把「无限等待」变成「有界等待」，让失败至少是可预期、可统计的。超时取值有两条经验：其一，超时必须小于自身对外的延迟承诺——在总预算里先扣掉网关、序列化等其它环节的开销，剩下的才是调用能占用的时间；其二，若下游有可承诺的分位指标（如 P99 约 800ms），客户端超时通常设在其附近或略小——比下游 P99 大得多的超时等于没有超时，比 P99 小太多又会误杀本来就慢的正常请求（长尾）。具体数值要靠压测与线上分位数据校准，这里给的是思路，不是公式。",
    },
    {
      type: "paragraph",
      text: "超时之后，最常见的天真做法是立刻重试一次——如果故障是瞬时的，这没问题；如果下游正在过载，立刻重试等于再打一发。重试有三个纪律：**只重试幂等或可安全重试的调用**（重复执行无副作用，见第 8 章「幂等」）；**次数封顶**（通常 1–2 次），超过就放弃并向上失败；**两次之间必须退避**。指数退避让等待时间按 2 的幂次增长并封顶，抖动（jitter）再在退避值上叠加随机量。",
    },
    {
      type: "code",
      title: "指数退避 + 全抖动（示意）",
      language: "go",
      code: "// backoff 返回第 n 次重试前应等待的时长（全抖动：在 [0, exp) 内均匀随机）。\n// 指数退避负责「越失败等越久」，抖动负责打散各调用方的重试相位，避免共振。\n// 要求 cap > 0；生产实现还需考虑并发与随机源，这里只表达思路。\nfunc backoff(base time.Duration, n int, cap time.Duration) time.Duration {\n\texp := base << n // base * 2^n\n\tif exp > cap || exp <= 0 {\n\t\texp = cap // 封顶，同时兜住左移溢出的情况\n\t}\n\treturn time.Duration(rand.Int63n(int64(exp)))\n}",
    },
    {
      type: "subheading",
      text: "重试风暴与共振：为什么抖动不是锦上添花",
    },
    {
      type: "paragraph",
      text: "设想 100 个上游都调用同一个下游，超时都设为 5 秒：故障瞬间，这 100 个调用几乎同时超时、同时重试，5 秒后又是一轮整齐的重试——失败流量在时间上**共振**，每个周期都在同一瞬间轰击下游，下游永远等不到喘息，即使恢复了也可能被下一轮齐射再次打倒。抖动把重试时间随机化，破坏这种共振。再加上一条全局约束：**每一跳只允许少量重试**。网关重试一次、服务重试一次、客户端再重试一次，乘法效应就能把一次故障放大成数倍流量；当重试与限流、拒绝叠加时，还会形成「拒绝—重试—再拒绝」的振荡。工程上对重试风暴的总防线是：每层重试次数封顶 + 全局入口限流 + 统一的拒绝语义，详见「过载保护」课时。",
    },
    {
      type: "heading",
      text: "熔断：把「反复尝试」换成「快速失败」",
    },
    {
      type: "paragraph",
      text: "超时与重试约束了单次调用的成本，但没有解决「下游已经坏了，还在不断发起真实调用」的问题。熔断在调用方为每个下游（或下游分组）维护状态：closed 状态正常调用，并滚动统计窗口内的失败率与慢调用占比；窗口内失败率（或慢调用占比）达到阈值（如 10 秒内错误率超过一半）就跳闸到 open——期间的调用不再发往下游，直接快速失败；open 持续一段时间（冷却期，给下游恢复时间）后进入 half-open，放少量探测请求；探测成功则闭合，失败则再次跳开。下表完整描述状态机的行为：",
    },
    {
      type: "table",
      caption: "熔断状态机：三个状态的行为与转换",
      headers: ["状态", "对外行为", "进入条件", "离开条件"],
      rows: [
        ["closed（闭合）", "正常放行，同时滚动统计失败率/慢调用占比", "启动或上次恢复完成", "统计窗口内失败率或慢调用占比达到阈值 → open"],
        ["open（跳开）", "快速失败，不发起真实调用", "失败阈值被触发", "冷却时间到 → half-open"],
        ["half-open（半开）", "只放少量探测请求，其余仍快速失败", "冷却结束", "探测成功 → closed；探测失败 → open"],
      ],
    },
    {
      type: "paragraph",
      text: "熔断的四个参数——统计窗口（多久内的失败算数）、失败阈值（比例或次数）、open 持续时间（接近下游的预期恢复时间，通常数十秒级）、half-open 探测请求数（少而具有代表性，如 1–5 个）——要按下游的故障特征调：对偶发抖动频繁的下游，失败阈值设得太灵敏会误跳；对持续故障的下游，open 太短等于没跳。统计必须**按下游分组**（pay 与 notify 各自独立），位置最好放在调用方侧——RPC 框架或 HTTP 客户端中间件天然知道对每个下游的失败情况（见第 6 章「服务发现与调用」的客户端视角）；网关也可以对统一出口的外部依赖做集中熔断（见第 6 章「网关与南北流量」）。顺带厘清分工：限流管入口速率（来得太多），熔断管出口失败（下游坏了），降级管业务取舍（下一课），过载保护管服务自身（最后一课）。",
    },
    {
      type: "definition",
      term: "舱壁隔离 bulkhead",
      definition: "为不同的下游或关键路径划分独立的资源配额——独立的线程池或信号量——让一个慢或挂掉的下游最多耗尽自己的那份配额，波及不到其它路径。与熔断互补：熔断是「下游坏了就别调」，舱壁是「就算你坏，也只能坏在你自己那格船舱里」。代价是资源被预先划分，单池闲置时无法借给别的池，池满时请求仍需等待，因此必须与超时、队列上界配合使用。",
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购：pay 下游故障时 sale 的表现",
      body: "大促中 pay（第三方支付平台）持续 5xx。下单本身并不等待 pay——订单先落为「待支付」，用户随后去 pay 完成付款（订单状态机见第 8 章）；但用户付款后跳回 sale、需要当场确认支付结果的那条链路上，sale 会同步调用 pay 的确认/查单类接口（接口形态以支付平台官方文档为准）。没有熔断时：这些同步确认调用把线程与连接池占住，池一旦被占满，连不依赖 pay 的下单请求也开始排队等连接，下单 P99 从几十毫秒恶化到数秒，用户纷纷超时重试——整个 sale 被一个外部服务拖垮。加上熔断与舱壁后：pay 的错误率在统计窗口内触发阈值，sale 对 pay 的调用跳闸 open，确认请求快速失败、不再占住线程池，支付结果的确认改由支付回调与对账收敛（衔接第 4 章「最终一致性与补偿」与第 8 章「幂等」）；half-open 期间每隔数十秒放少量真实请求探测，pay 恢复后自动闭合。全程 sale 的下单路径延迟保持稳定——「pay 挂了」从 sale 的事故，降级为 sale 的一个监控告警。",
    },
    {
      type: "keypoints",
      items: [
        "无超时等于无限等待，最终拖垮线程池与连接池；超时常设在下游分位附近，且必须小于自身延迟预算。",
        "重试三纪律：只重试幂等或可安全重试的调用、次数封顶、指数退避加抖动；没有抖动，重试会在时间上共振。",
        "熔断状态机 closed/open/half-open；参数为统计窗口、失败阈值、冷却时长与探测请求数，要按下游分组统计。",
        "舱壁隔离用独立线程池或信号量让故障局限在单格船舱，必须与超时配合使用。",
        "熔断与舱壁实现在客户端库与网关；限流管入口、熔断管出口、降级管业务取舍、过载保护兜底。",
      ],
    },
  ],
};
