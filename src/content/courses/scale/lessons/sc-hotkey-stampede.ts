/* ==================================================================
 * 课时：热点与惊群：单 key 放大问题（sc-hotkey-stampede）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "本章对热点 key 的权威定义：单 key 的物理单点本质、本地缓存分摊、读放大与惊群机制，以及「本地缓存 + 短 TTL + 互斥重建 + 动态探测」的完整工程组合。",
  blocks: [
    {
      type: "paragraph",
      text: "第 3 课的击穿讲的是「热点 key 过期的那一瞬间」。本课把镜头拉远：**热点不是偶尔的过期事故，而是读流量长期、系统地集中在极少数 key 上的结构性形态**。闪购大促时，单个爆款商品的详情读能达到 10 万+ QPS——这个数字单靠 Redis 一次读一跳是撑得住的，但问题的真正难点在别处：一个 key 在缓存集群里只存在一份，而它要承载的流量却可以超过单分片、甚至单个 Redis 实例的承受上限。本课给出本章对**热点 key 的完整权威定义**，拆解「单 key 为什么是物理单点」「并发 miss 为什么放大成惊群」，再给出工程上成熟的组合应对。",
    },
    {
      type: "definition",
      term: "热点 key（hot key）",
      definition: "单位时间内访问量异常集中的单一数据。判定不是绝对的 QPS 数值，而是相对该数据所在存储层容量的比例：当单个 key 的访问量接近或超过它所在分片/实例能提供的上限时，它就是热点 key。闪购大促爆款商品的详情（单 key 10 万+ QPS）、实时排行榜、秒杀倒计时都是典型热点。",
    },
    {
      type: "paragraph",
      text: "热点 key 出现的场景在大促系统里非常固定：**单品流量集中**——闪购某款限量商品把 80% 的详情流量压在一个 product:id 上；**聚合结果**——全站排行榜、活动榜单把大量用户的行为收敛成一个 key；**资源型 key**——秒杀的倒计时、库存余量展示位。识别手段有两个方向：上线前从业务侧预测（运营已知哪些商品上主推位），运行中从数据侧发现——按 key 统计访问 QPS 分布（Redis 的 hotkey 探测、代理层日志聚合、或自研计数），找出流量尖峰对应的 key。识别是治理的前提：不知道哪个 key 热，就没有治理对象。",
    },
    {
      type: "heading",
      text: "为什么热点 key 无法靠「多分片」解决",
    },
    {
      type: "paragraph",
      text: "缓存集群的分片（Redis Cluster）解决的是「海量 key 分散在多台机器上」：把千万个 key 摊到几十个分片，每片只承担一部分。但热点 key 的问题恰好绕过了分片的逻辑——**一个 key 只能存在于一个分片上**，分片算法再均匀，也改变不了「product:1001 永远落在同一片」的事实。于是：扩容集群只把其它冷 key 摊得更开，热点所在的分片依旧独自承受 10 万 QPS；即便把它迁到最强的机器上，单实例（单分片）的网络带宽与 CPU 也终有上限。**集群解决的是横向的 key 数量，热点是纵向的单 key 强度**——这就是「热点无法靠加机器解决」的根本原因，也是热点与普通流量治理的分水岭。",
    },
    {
      type: "paragraph",
      text: "另一个直觉误区是把热点 key 复制成多份、分散到多个分片（每个分片放 product:1001_0、product:1001_1……读时随机取一份）。这份「多副本散列」确实摊开了单点，但代价巨大：写一致性爆炸——更新商品时要同步改所有副本，副本间瞬时不一致、需要额外的失效广播；内存浪费；复杂度极高。工程上只把它当作极端场景的备用手段，主流思路不是给热点 key 分身，而是**在它前面再加一层能分摊的缓存——本地缓存**。",
    },
    {
      type: "paragraph",
      text: "**读放大与惊群：miss 是放大器。**当热点 key 的流量全部命中时，问题只在于「单分片扛不扛得住 10 万 QPS 的读」。真正的灾难发生在**miss 的瞬间**：一个 key 在 Redis 里没有值（过期、缓存重建、淘汰），10 万个并发请求全部 miss，然后全部回源——注意这里的放大器是请求自身的并发性：10 万请求几乎同时到达，回源请求的数量不是「1」，而是「10 万」。这波并发回源（术语上称为**惊群 / stampede**）瞬间打向数据库或下游，正是第 3 课击穿故障的成灾时刻。热点 key 与惊群因此是一对孪生问题：**热点把流量集中到一个 key 上，miss 把这个 key 上的全部流量一次性地释放给回源目标**。谁回源谁挨打——数据库、或者下一个缓存层。",
    },
    {
      type: "paragraph",
      text: "**工程组合之一：本地缓存分摊单点（L1）。**对付「单分片扛不住」的正解是让热点流量在到达 Redis 之前就被分摊掉：**每台 sale 实例在进程内维护一份热点商品的本地缓存（L1）**。10 万 QPS 打在 50 台实例上，每台只收到 2000 QPS——本地命中直接返回（纳秒~微秒级），只有 miss 才去问 Redis。Redis 单分片承受的流量从 10 万 QPS 降到「每实例的本地 miss 之和」，通常只剩几千。这就是第 1 课位置谱系的实战应用：**把读流量从共享的 Redis 挪到各实例自己的内存里，热点从「全局单点」变成「每实例一份的本地副本」**。代价是每台实例各自持有一份可能过期的副本，一致性要靠短 TTL 与失效广播来控制（详见第 6 课）。",
    },
    {
      type: "paragraph",
      text: "**工程组合之二：短 TTL + 互斥重建收敛 miss。**本地缓存放大了另一个问题：如果本地 TTL 与 Redis TTL 相同，每台实例会在同一时刻一起 miss、一起回源 Redis——惊群从数据库转移到了 Redis 层。对策是两段式：**本地缓存用很短的 TTL（如 1–5 秒）**，让各实例的过期时刻自然错开；并且**每次本地 miss 回源 Redis 时用 singleflight（互斥重建）收敛**——同一实例上同一时刻只有第一个请求去读 Redis，其余请求等它回来（第 3 课已讲互斥重建的机制，本课强调它在多级缓存下的位置：保护 Redis 层不被 50 台实例的本地 miss 同时打到）。由此形成完整链条：**Redis 层用永不过期 + 异步刷新挡住击穿，本地层用短 TTL + singleflight 挡住实例间的同步 miss，数据库只被极少量的后台刷新访问**。",
    },
    {
      type: "paragraph",
      text: "**工程组合之三：热点探测与动态调整。**热点不是静态的：大促主推商品轮换、活动榜单每小时变一次。工程上需要**动态识别与调整**：运行时持续统计每个 key 的 QPS（采样计数即可，不必精确），超过阈值（如单 key > 1000 QPS）就把该 key 标记为热点并让它进入本地缓存治理——回源时主动预热本地副本、延长它的生命周期策略、必要时提高它的重建优先级；热度回落后再自动降级，避免「把每个 key 都当热点」造成本地内存被冷数据占满。预热（主动把即将上主推位的商品提前写进各级缓存）则从源头消除「大促开始时才第一次回源」的尖峰。",
    },
    {
      type: "code",
      title: "热点 key 分级的治理示意（伪代码）",
      language: "text",
      code: "读商品详情(productID):\n  key = \"product:\" + productID\n\n  // 1. 本地 L1：热 key 才进（判定来自动态探测）\n  if hot(key) and v = l1Get(key):   return v\n\n  // 2. L2 Redis：singleflight 收敛 miss\n  v = singleflight(key, func():\n       v = redisGet(key)\n       if v == nil:                // 冷启动/已被淘汰\n           v = dbLoad(productID)   // 只允许一个回源\n           redisSet(key, v, ttl=永不/长TTL)   // 永不过期+异步刷新见第 3 课\n       return v)\n\n  // 3. 本地回填：热点才写 L1，且 TTL 短\n  if hot(key):  l1Set(key, v, ttl=1..5s)\n  return v",
    },
    {
      type: "paragraph",
      text: "**热点写与读后写：把「热」留给读路径。**上面的治理都围绕热点**读**。热点**写**（如爆款商品的库存扣减、浏览计数）是另一个问题：库存余量每次下单都要扣减与读取，读与写在同一个 key 上竞争，任何一层缓存都只会放大一致性问题。闪购的现实解法是把库存扣减做成**数据库侧的原子写**（行锁/乐观锁/原子 UPDATE，正确性章节「单机并发正确性」「设计练习：秒杀下单的库存扣减」有完整展开），**展示层的读则与真实库存解耦**——详情页展示的是定期刷新的「可购买性快照」而非每次扣减的实时数字，用户点了下单才去走数据库事务真正扣减。计数类热点写（浏览数、点赞）则用「每实例本地累加 + 定期合并/异步落库」的合并思路：单 key 的写被摊到各实例本地，合并写把高频小写变成低频大写——这正是第 5 课原子计数与异步章节削峰的接口。**原则一句话：别让热点 key 同时承载高频读与高频强一致写——把强一致写留在数据库，缓存只负责可容忍旧值的展示读。**",
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购某爆款商品 10 万 QPS 热点读的应对全景",
      body: "大促开始前：运营圈定主推商品 → 预热写进 Redis（长 TTL/永不过期）+ 各实例本地预热。大促进行中：10 万 QPS 打到 50 台 sale 实例 → 本地 L1 命中绝大多数（短 TTL 1–5s，各实例错开过期）→ 只有少量 miss 经 singleflight 收敛后访问 Redis 单分片（该 key 本就常驻，几乎不 miss）→ 数据库只收后台异步刷新的低频查询。单分片、单 key 都不再是单点：Redis 挂掉时本地 L1 仍能撑住数秒，为降级与恢复争取时间（与第 3 课雪崩预案汇合）。",
    },
    {
      type: "quiz",
      question: "为什么把 Redis 集群从 10 个分片扩容到 100 个分片，解决不了单个热点 key 的打满问题？",
      options: [
        "扩容后热点 key 在多个分片上各存一份，写一致性成本过高，反而不如不扩",
        "一个 key 只能由分片算法映射到唯一一个分片，扩容不改变该 key 所在分片的单点承载",
        "Redis 集群扩容需要重启，重启期间热点 key 必然雪崩",
        "分片扩容只能提升写吞吐，读吞吐不受分片数影响",
      ],
      answer: 1,
      explanation: "分片把海量 key 摊到多台机器，但每个 key 仍然只落在唯一一个分片上：扩容只是让其它冷 key 更分散，热点 key 所在分片承受的流量不变。选项 A 描述的是人为多副本散列（一种代价高昂的可选手段而非扩容的默认行为），C 与集群运维无关，D 错误——分片扩容同样提升读吞吐，只是提升不了「单 key」的读吞吐。",
    },
    {
      type: "keypoints",
      items: [
        "热点 key：单位时间访问量异常集中、接近或超过所在存储层上限的单一数据——大促爆款单品、排行榜、倒计时。",
        "单 key 是物理单点：分片解决 key 数量、不解决单 key 强度，热点无法靠加机器摊开。",
        "惊群：key miss 瞬间，全部并发请求一次性回源——热点把流量集中、miss 把集中流量放大释放。",
        "工程组合：本地 L1 分摊（每实例一份）→ 短 TTL 错开实例间过期 → singleflight 收敛回源 → 热点探测动态进出治理 → 上架前预热。",
        "热点写（库存/计数）不放进强一致缓存路径：数据库原子写保正确，缓存只服务可容忍旧值的展示读；高频计数走合并写。",
      ],
    },
  ],
};
