/* ==================================================================
 * 课时：阶段回顾：正确性检查表（sc-correctness-review）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 回顾课：压缩全章，不引入新知识点。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "把第 8 章压缩成一张检查表与一条决策链：单机并发 → 幂等 → 跨库事务 → 一致性级别，并用闪购下单链路把防线串成一遍可复述的整体。",
  blocks: [
    {
      type: "paragraph",
      text: "这一章的主题是：**扩容之后，正确性靠什么守**。单机时靠锁与事务，扩容后多出了三类新问题——并发从进程内蔓延到跨实例、网络让「一次调用成没成功」变得不可知、数据被拆到多库多服务后原子性失效。本课不做新知识，只把四课的防线压成一张全景图、一条决策链和一份可复述的检查表，再串一遍闪购的下单链路做整体收束。",
    },
    {
      type: "heading",
      text: "第 8 章全景：从单机到分布式的四道防线",
    },
    {
      type: "table",
      caption: "第 8 章内容地图",
      headers: ["课时", "解决什么问题", "核心防线", "关键工具"],
      rows: [
        ["单机并发正确性", "进程内共享可变状态的竞争", "状态归属 → 锁粒度 → 原子性；事务尽量短、不包远程调用", "Mutex/RWMutex/atomic、`-race`、行锁、`AND stock > 0`"],
        ["幂等：分布式写的第一原则", "重试/重投/重复回调导致重复执行", "幂等键 + 唯一索引去重 + 状态机", "Idempotency-Key、去重表、`pending→paid→shipped`"],
        ["跨库事务的现实", "写必须跨多个库/服务原子生效", "能不分就不分；outbox + 幂等；Saga 补偿；TCC 预留", "单库/NewSQL、本地消息表、编排式 Saga"],
        ["一致性级别", "读的人能看到什么、等多久", "按数据选级别：资损/裁决强一致，展示/统计最终一致", "线性一致、读己之写、单调读、最终一致、quorum"],
      ],
    },
    {
      type: "heading",
      text: "正确性决策链：从问题到防线",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "这份状态归谁？能不能少共享——能归并到单一权威存储/单库，就不要让它跨资源（「什么时候干脆不分」）。",
        "单实例内部怎么保证？进程内共享可变状态用锁/原子；数据库写用行锁或条件原子更新；事务只包必须原子的一组写。",
        "这个入口会不会被重复执行？凡是扣减/追加/建单/回调，先补幂等键 + 唯一索引/去重表，重试必须复用同一键。",
        "有先后约束的步骤怎么防乱序重复？上状态机，只允许合法迁移。",
        "写跨了多个库/服务吗？不跨 → 单库事务收工；跨了且能接受最终一致 → outbox + 幂等消费；需要步骤化回滚 → Saga（补偿幂等）；有预留接口 → TCC；都不能接受 → 归并或 NewSQL。",
        "读的人要什么级别？资损/裁决要线性一致；体感用读己之写/单调读；展示/统计/通知走最终一致。",
      ],
    },
    {
      type: "heading",
      text: "与第 4 章的呼应：消息幂等是同一件事",
    },
    {
      type: "paragraph",
      text: "第 4 章讲消息可靠性时给过三个语义：at-most-once、at-least-once、exactly-once——结论是**exactly-once 在工程里不存在，只能靠「at-least-once + 幂等消费」逼近**（见「消息可靠性：不丢、不重、不乱」）。本章的幂等课就是给「幂等消费」补齐了通用工具：幂等键、去重表、状态机，对象从「消息」推广到「一切可能被重复执行的写」。反过来，本章的分布式事务课里，outbox 就是把「消息可靠投递」和「本地事务」缝在一起的机制——第 4 章讲它为什么需要（事件课），本章讲它在跨库方案谱系里的定位（最常用方案）。两条线到这里闭环：**消息的重复与回调的重复、单库的写与跨库的写，用的是同一套正确性语言**。",
    },
    {
      type: "heading",
      text: "把它们串起来：闪购下单链路的正确性总览",
    },
    {
      type: "paragraph",
      text: "用一章的防线重看一次 `POST /orders`：用户在网关限流后进入 → Redis Lua 原子预扣挡住超卖与峰值（第 8 章练习课的分层方案）→ 带 `client_req_id` 投递扣减队列 → 消费者在数据库本地事务里 `UPDATE ... AND stock > 0` + 建单（单机正确性：条件原子 + 短事务）→ 订单状态机 `pending→paid→shipped` 承载后续支付/发货（幂等与状态机）→ pay 回调重复到达时靠回调幂等表 + 状态机挡住（幂等课的完整示例）→ 跨服务动作（通知、发货）走 outbox + 消息，下游幂等消费（跨库事务课与第 4 章）→ 余额、库存账实靠定期对账收敛。每一道防线都有明确的归属层与兜底层——这就是扩容之后正确性仍然可维护的形态。",
    },
    {
      type: "callout",
      variant: "note",
      title: "自查三问",
      body: "对照你正在设计/维护的系统问三句：① 这个写入口被重复执行会怎样——有没有幂等键和唯一索引？② 哪些数据其实是「资损裁决」，它们有没有离开强一致的保护？③ 有没有一个动作跨了两个资源还指望「同时成功」——它能不能归并，不能的话用了 outbox/Saga 还是 2PC，为什么？三问全答得上，本章的防线就真正内化了。",
    },
    {
      type: "quiz",
      question: "一个支付回调接口被上游以「至少一次」语义投递，处理时应当怎么做才正确？",
      options: [
        "对上游返回失败，让它别再重试，从根上消灭重复",
        "用回调幂等表/唯一键挡重复 + 状态机只允许合法迁移，处理成功就对上游返回成功，重复由幂等承载",
        "只靠 Redis 里的一个过期标记去重，因为进程重启无所谓",
        "每次收到回调都重新执行扣款动作，保证状态最新",
      ],
      answer: 1,
      explanation: "「至少一次」意味着重复必然存在，去重要由接收方自己承载：幂等键/唯一索引挡同一事件的重复，状态机挡不同动作的乱序（B）。A 依赖上游行为且会触发无限重试；C 用内存/短生命周期存储做裁决，重启与丢失窗口都挡不住（正确做法是业务库唯一索引、与业务写同事务）；D 直接把非幂等动作重复执行了。",
    },
    {
      type: "keypoints",
      items: [
        "全景：单机并发（锁/原子/事务边界）→ 幂等（键/去重表/状态机）→ 跨库事务（不分 > outbox > Saga > TCC > 2PC）→ 一致性级别（资损强一致、展示最终一致）。",
        "决策链自问：状态归谁 → 单机怎么锁 → 会不会被重复 → 要不要状态机 → 跨不跨资源 → 读者要什么级别。",
        "与第 4 章闭环：exactly-once = at-least-once + 幂等消费；outbox 是消息可靠性与本地事务的缝合点。",
        "闪购总览：网关限流 → Redis 预扣 → 队列 + 数据库条件更新/短事务 → 状态机承接回调 → outbox 通知 → 对账兜底，每道防线都有归属层。",
        "自查三问：重复执行会怎样？资损数据是否在强一致保护内？跨资源动作归并了吗、用了哪个方案？",
      ],
    },
  ],
};
