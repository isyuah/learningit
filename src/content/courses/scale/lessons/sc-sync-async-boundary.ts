/* ==================================================================
 * 课时：同步与异步的边界：何时必须异步（sc-sync-async-boundary）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 章节 2「请求路径与 IO：第一公里的性能」第 4 课。
 * 第 4 章「异步化与消息」将系统深讲队列/削峰/背压；本课只做
 * 「何时同步、何时异步」的边界决策，细节引用第 4 章与 rabbitmq 课程。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "同步调用为什么慢、异步手段有哪些层级、哪些业务必须同步——用决策规则划清边界，也看清「为异步而异步」的代价。",
  blocks: [
    {
      type: "paragraph",
      text: "并发模型与池化解决的是「同一时刻多少请求在推进」；当单个请求内部要串起多次外部调用时，另一个问题浮现：**这些调用必须同步等结果吗？**「异步」常被当成性能万能药，但选错位置反而引入一致性与可观测性灾难。这一课划清边界：同步调用的延迟是怎么累加的，异步手段有多少层，哪些场景**必须**同步，以及异步的真正代价。第 4 章『异步化与消息』会深讲队列削峰、可靠性、背压（可先读该章导语建立全局），本课聚焦决策。",
    },
    {
      type: "heading",
      text: "同步调用的延迟累加",
    },
    {
      type: "code",
      title: "一个串得越来越慢的下单请求（伪代码时序）",
      language: "text",
      code: `// POST /orders 的同步实现：下游逐个调用，延迟直接相加
handler(orderReq):
    order  = db.createOrder(orderReq)        // 20ms  本地库
    pay    = payService.charge(order)        // 150ms 支付：要等第三方应答
    notify = notifyService.sendSms(order)    // 120ms 短信网关：要等回执
    return ok                                // 合计 ≈ 20 + 150 + 120 + 网络跳数 ≈ 300ms+

// 用户的体感 = 三者之和；任何一个下游抖动到 1s，整单就跟着变 1s+`,
    },
    {
      type: "paragraph",
      text: "同步是「下游延迟直接相加」：请求总时长 ≈ Σ 每一跳的延迟。这还没算级联——支付网关超时重试，短信网关排队，每个下游的不确定性都在放大总时长。经验数字：闪购的下单链路若保持全同步，日常尚可，大促时任意下游抖动都会直接吃掉用户体验。",
    },
    {
      type: "heading",
      text: "异步手段谱系：不止消息队列一种",
    },
    {
      type: "table",
      caption: "异步手段谱系（按延迟、解耦程度、复杂度递增）",
      headers: ["手段", "机制", "适用"],
      rows: [
        ["线程池/协程内异步", "把任务丢给另一线程/goroutine 执行", "本进程内的并行化，如批量预热、异步写日志"],
        ["回调 / Future / async-await", "发起后不阻塞，就绪再续跑", "单次 IO 的并行等待（并发调多个下游）"],
        ["协程（Go）", "goroutine + 同步风格代码", "并行等待多个 IO、简单编排"],
        ["消息队列（RabbitMQ 等）", "写入队列即返回，消费者异步处理", "跨服务解耦、削峰、可靠投递——细节见 rabbitmq 课程与第 4 章"],
      ],
    },
    {
      type: "paragraph",
      text: "注意两种「异步」的区别：**并发等待多个下游**（parallel fan-out：同时发支付与查库存，取最慢者）并没有真正异步化——请求仍等结果，只是等待被重叠；**真正的异步**是把某段工作推迟到请求返回之后（削峰、通知类），第 4 章『异步化与消息』专门处理后者。不要把两者混为一谈。",
    },
    {
      type: "heading",
      text: "何时必须同步",
    },
    {
      type: "list",
      items: [
        "用户需要结果才能继续：登录校验、支付结果页、下单是否成功的即时反馈——这些必须在响应里给出确定性答案。",
        "强一致写：资金扣减这类需要原子生效的写必须当场完成；库存扣减这类**裁决决定成败**的写（sale 的 POST /orders）必须当场给出不超卖的确定性答案——大促峰值下用 Redis 原子预扣先裁决、由落库消费者做最终扣减（即「先答应、后异步落库」，权威账本与幂等由第 8 章『幂等』与『设计练习：秒杀下单的库存扣减』保证），而不是无约束地把扣减丢给一个「慢慢来」、可能超卖的异步任务。",
        "交互式校验与参数依赖：后续步骤的入参依赖前一步返回值（取 token 再调 API），串行同步是唯一自然写法。",
        "结果立即驱动业务决策：库存不足要当场改提示语、改价格，等异步回来用户早已离开页面。",
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购下单的同步/异步划分",
      body: "POST /orders 里：**库存裁决必须当场给出确定性结果**（买没买到、绝不超卖、用户要即时答复），事务内严禁夹外部调用（第 3 课的连接占用教训）；大促峰值下先在 Redis 做原子预扣完成裁决、把「订单落库 + 最终扣减」放到队列消费者串行执行（见第 8 章『设计练习：秒杀下单的库存扣减』），但「回答用户成功/失败」的裁决仍在同步路径上；**支付回调处理**同步于回调请求本身、但相对下单是异步发生的（回调接口本身要幂等，见第 8 章）；**短信/推送通知**移出主链路走异步队列——用户不需要等短信发出，最终一致即可。这就是典型的「核心路径同步、外围路径异步」。",
    },
    {
      type: "heading",
      text: "异步的代价：不是免费的",
    },
    {
      type: "list",
      items: [
        "上下文丢失：请求处理完，线程/goroutine 与局部状态都散了。唯一能跨异步边界的是**显式传递的上下文**——trace_id（链路 ID）必须在消息体/任务里带上，否则日志无法串联，排查成本陡增（可观测性细节见 observability 课程）。",
        "错误处理后置：同步的 try/catch 变成「消费失败 → 重试 → 死信 → 人工」，错误不再当场可见（rabbitmq 课程『死信队列：拒绝、过期与路由失败』与第 4 章『背压与消费能力』）。",
        "顺序与重复：异步天然引入并发，消息可能乱序、重复消费；恢复有序要分区键，去重要靠幂等（第 4 章『消息可靠性』）。",
        "端到端延迟增加：异步只是不让用户等，处理本身往往更晚完成；「快」的是响应，不是任务。",
      ],
    },
    {
      type: "heading",
      text: "决策规则与误区",
    },
    {
      type: "list",
      items: [
        "先问：这个调用**阻塞的是用户的等待**，还是只是阻塞了本请求的资源？只影响资源的（如日志、通知、报表）才值得异步化。",
        "再问：结果**什么时候需要**？下次交互前必须知道 → 同步或半同步；永远不需要回传 → 异步。",
        "三问：异步后的一致性窗口能否被业务接受？不能 → 留在同步事务里。",
        "最大的误区是「为异步而异步」：把本可 5 ms 完成的本地小操作也丢进队列，换来的只是多一跳延迟、多一类故障和多一份可观测性负担。削峰（第 4 章『背压与消费能力』）靠的是队列缓冲瞬时洪峰，而不是把每个请求都拆成异步。",
      ],
    },
    {
      type: "quiz",
      question:
        "sale 的 POST /orders 主链路上，哪一个环节必须当场同步裁决、不能延后到请求返回之后？",
      options: [
        "下单成功后给用户发短信通知",
        "扣减库存的裁决与下单成功/失败结果（强一致判定，用户需要即时答复；其后续落库可队列化，见第 8 章）",
        "大促后生成运营报表",
        "给推荐系统推送用户行为事件",
      ],
      answer: 1,
      explanation:
        "扣库存 + 建单是强一致写且用户需要当场确定结果：真实库存的裁决与建单不能「慢慢异步」。大促峰值下可先用 Redis 原子预扣在响应内快速裁决、由落库消费者做最终扣减（第 8 章展开），但这一步仍属于用户等待的同步路径。通知、报表、行为事件都是外围路径，结果不需要回传给用户，可以异步化并接受最终一致。",
    },
    {
      type: "keypoints",
      items: [
        "同步调用延迟直接相加，且下游抖动会级联放大总时长。",
        "异步谱系从进程内（协程/回调）到消息队列，解耦与复杂度递增；「并行等待」不是「异步化」。",
        "必须同步：用户要结果、强一致写、交互式校验、结果驱动即时决策。",
        "异步的代价：trace_id 要显式跨边界传递、错误后置成重试/死信、乱序与重复需幂等兜底、端到端完成更晚。",
        "决策四问 + 警惕「为异步而异步」：小操作丢队列只增加复杂度。",
      ],
    },
  ],
};
