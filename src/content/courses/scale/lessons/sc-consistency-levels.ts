/* ==================================================================
 * 课时：一致性级别：从强一致到最终一致（sc-consistency-levels）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "强一致不是免费的：线性一致、读己之写、最终一致，每一级都在用可用性与延迟换一致性。本课给出级别谱系、CAP 直觉与业务决策表。",
  blocks: [
    {
      type: "paragraph",
      text: "前几课讲了怎么让「一次写」正确：单机用锁与事务、跨库用 Saga/outbox、重复用幂等。这一课退后一步看全局：**读的人能看到什么**。写完一个值之后，另一次请求马上读，读到的一定是新值吗？读到旧值要等多久？这个问题的答案不是一个开关，而是一整条谱系——从线性一致到最终一致，每一级都在用延迟与可用性换一致性。本课把级别谱系讲清楚（最终一致的完整定义在第 4 章「最终一致性与补偿」已给出，这里只引用并做定位），再回答一个工程问题：**什么数据要什么级别**。",
    },
    {
      type: "heading",
      text: "一致性级别谱系：从强到弱",
    },
    {
      type: "table",
      caption: "一致性级别谱系（从强到弱）",
      headers: ["级别", "直觉", "典型实现", "典型场景"],
      rows: [
        ["线性一致（linearizability）", "所有操作像在同一个单点上、按真实时间顺序发生——任何读都看到「它之前已完成」的最新写", "单机单库、单主同步复制（读主）、Raft/Paxos 多数派、NewSQL", "余额扣减、库存裁决、唯一性裁定"],
        ["顺序一致", "所有操作有一个全局一致顺序，但不必贴合真实时间——并发操作可在你写之前被其他进程看到", "某些强一致消息系统语义、教学概念", "需要全局排序但不强绑真实时间的场景"],
        ["读己之写", "自己写的一定能读到，别人的写不保证", "会话粘性路由、按用户路由到主库", "「我提交的订单立刻可见」的用户体验"],
        ["单调读", "一旦读到某个较新的值，后续读不会回退到更旧的值", "按副本版本号路由、会话内读固定副本", "防止刷新页面看到历史数据倒退"],
        ["最终一致（eventual consistency）", "无新更新的前提下，经过有限时间所有副本收敛一致（定义见第 4 章）", "异步复制、缓存、消息投递、多活", "浏览量、通知、榜单、统计"],
      ],
    },
    {
      type: "paragraph",
      text: "注意谱系的三个读法。第一，它们是**同一份数据上的不同承诺**，不是互相排斥的「模式」——同一系统里余额要线性一致、商品浏览数最终一致，并不矛盾。第二，级别越强，通常付出的延迟与可用性代价越大：线性一致要求「写必须在多数派确认后才返回」（见下文法定人数），一次写要等最慢的确认者。第三，读己之写与单调读是「弱于线性一致、但用户体验敏感」的实用级别，正是第 5 章读写分离架构里主从延迟要专门处理的那两种读（见「读写分离：扩展读的经典第一招」）。",
    },
    {
      type: "heading",
      text: "为什么不能既要又要：CAP 的直觉",
    },
    {
      type: "callout",
      variant: "warning",
      title: "CAP 的 C 特指线性一致",
      body: "CAP 定理说：网络分区（Partition）一旦发生，系统必须在一致性（Consistency）与可用性（Availability）之间二选一。这里常被误用的点有两个：**C 是线性一致**，不是「最终一致」这种弱级别；**A 是「每个请求都得到响应」**，不是「系统还活着」。所以 CAP 不是「三选二」，而是在分区这种极端时刻的一个二选一：分区时继续服务（牺牲强一致、等分区恢复后收敛），还是宁可不服务也不给出可能不一致的答案。注意它并不禁止「分区不存在时既强一致又可用」——平时没有分区，可以全都要。",
    },
    {
      type: "paragraph",
      text: "CAP 描述的是分区的极端时刻；日常工程里更常遇到的是它的温和版本：**强一致要花钱**。同步复制下，一次写要等副本确认才返回，主库与副本之间每个来回都是延迟（单条连接往返约 1–5ms，跨机房会到几十毫秒以上），而且副本越远、数量越多，写越慢；副本跟不上时主库只能阻塞等待或降级。所以工程上引入**半同步**这类折中（见第 5 章「读写分离」课：主库等一个从库确认即返回，兼顾不丢与延迟），或干脆允许异步复制、接受短暂读到旧值。决策的本质始终是同一笔交易：**这份数据晚几毫秒/几秒才一致，业务能承受多大的代价？**",
    },
    {
      type: "heading",
      text: "「大多数」与法定人数：为什么多数派能容忍故障",
    },
    {
      type: "paragraph",
      text: "无论是 Raft、Paxos，还是支持线性一致的分布式数据库，背后都站着同一个思想：**法定人数（quorum）**——把数据复制到 N 个副本上，读写都要求联系超过半数（≥ N/2 + 1）的副本。两个多数派必然相交：任何一次「已被确认的写」都覆盖了多数副本，因此后续读无论联系哪一批多数派，至少会碰到一个包含该写的副本——只要少于半数的副本故障，系统就能继续读写。这就是「大多数为什么能容忍少数故障」的直觉：**交叠保证新鲜，多数保证存活**（概念级，Raft/Paxos 的完整协议在一致性算法文献中，本课只取这个定位）。它对工程的含义：副本数取奇数（3、5），多数派写意味着一次写确实要等过半确认，延迟与容错在此直接交易。",
    },
    {
      type: "heading",
      text: "业务决策表：什么数据要什么级别",
    },
    {
      type: "table",
      caption: "闪购里不同数据的一致性级别决策",
      headers: ["数据", "需要的级别", "为什么", "落地手段"],
      rows: [
        ["库存、余额", "线性一致（强一致）", "超卖=资损；裁决必须唯一且立即生效", "单库事务 / 单主读；热点库存另走 Redis 预扣 + 对账（见第 8 章练习课）"],
        ["订单状态", "线性一致 + 状态机", "支付/发货有先后约束、有资损语义", "状态机迁移 + 幂等键（见「幂等」课）"],
        ["用户资料/昵称", "读己之写即可", "自己改完马上看得到，别人的不必强一致", "按用户路由主库读，或接受短暂延迟"],
        ["商品浏览数/点赞", "最终一致", "丢了几个无所谓，写入量却很大", "计数合并写 + 异步落库（见第 3 章热点写）"],
        ["搜索结果/榜单/推荐", "最终一致（可容忍分钟级）", "快照类数据，短暂旧版可接受", "索引/缓存异步更新"],
        ["通知/推送", "最终一致 + 幂等", "延迟到达可接受，但不能重复骚扰用户", "消息队列 + 幂等键（见第 4 章）"],
      ],
    },
    {
      type: "paragraph",
      text: "决策规则一句话：**凡是「裁决、资损、唯一性」都向强一致走，凡是「展示、统计、通知」都可以向最终一致走**；中间层用读己之写/单调读保护用户体验。判定「这是不是要强一致」可以问两个问题：这笔数据错了/旧了，会造成资损还是只是不好看？它后面是不是还跟着一笔必须顺序执行的写？库存扣多了是资损、浏览数少记几个只是统计偏差——级别立刻分出来了。",
    },
    {
      type: "heading",
      text: "闪购决策实例：从请求路径看一致性取舍",
    },
    {
      type: "paragraph",
      text: "把决策表套到闪购的一条完整下单链路上：用户点「抢购」→ sale 先在 Redis 做原子预扣（防超卖的第一道闸，Redis 的强一致只覆盖「单 key 计数」这一窄范围）→ 落库时在 MySQL 单行上完成最终扣减与建单（线性一致，因为这是资损裁决点）→ 随后异步发「下单成功」通知（最终一致，可稍晚）。同一笔业务里，强一致、最终一致与幂等共存：每条读写的级别是**按数据与动作单独选定**的，而不是整条链路取一个统一档位。这一点正是第 9 章综合设计里秒杀写路径能讲清楚的关键。",
    },
    {
      type: "quiz",
      question: "关于 CAP 定理，下列表述正确的是？",
      options: [
        "分布式系统平时就必须在一致性、可用性、分区容忍性中三选二",
        "CAP 里的 C 指最终一致，所以最终一致系统也算满足 C",
        "CAP 描述的是网络分区发生时必须在「线性一致」与「继续服务」之间二选一；无分区时两者可以兼得",
        "只要副本数够多（如 5 个），分区时也能同时保证线性一致与可用性",
      ],
      answer: 2,
      explanation: "CAP 只在分区时刻要求二选一，且 C 特指线性一致（A 把范围扩大成常态、B 把 C 降格为最终一致，均误用）；D 把多数派容错说成了可绕过分区——副本再多，写也需要多数派在线，全分区时仍无法两全。",
    },
    {
      type: "keypoints",
      items: [
        "一致性是谱系：线性一致（像单点按时序发生）→ 顺序一致 → 读己之写 → 单调读 → 最终一致（定义见第 4 章）。",
        "强一致按数据逐项购买，不是整条链路统一档位：资损/裁决要线性一致，展示/统计/通知走最终一致。",
        "CAP 的 C 特指线性一致，且只在分区时二选一；日常更真实的是「强一致 = 延迟与可用性的交易」，半同步是典型折中。",
        "法定人数：多数派写 + 多数派读必然相交，少于半数的副本故障不影响读写——交叠保新鲜、多数保存活（概念级）。",
        "库存/余额/订单状态 → 强一致；浏览数/榜单/通知 → 最终一致 + 幂等；用户体感用读己之写/单调读兜底。",
      ],
    },
  ],
};
