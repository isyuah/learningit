/* ==================================================================
 * 课时：幂等：分布式写的第一原则（sc-idempotency）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "网络不确定、超时只能重试、消息至少一次投递——分布式写的唯一出路是让每个操作对重复执行免疫：幂等键、去重表与状态机。",
  blocks: [
    {
      type: "paragraph",
      text: "单机上的正确性靠锁与事务守住（见上一课「单机并发正确性：锁、原子与事务边界」）。一旦把请求发出机器、跨过网络，就出现了一个单机事务永远解决不了的新事实：**你无法知道一次调用到底成没成功**。TCP 超时可能是请求丢了，也可能是响应丢了——在这两种情况下重发是唯一的选择，而重发意味着**同一操作可能被执行两次**。幂等（idempotent，同一操作重复执行与执行一次效果相同），就是分布式写的第一原则。",
    },
    {
      type: "definition",
      term: "幂等（idempotent）",
      definition: "同一操作重复执行多次，与执行一次的效果相同。注意判定的是「效果」，不是「执行次数」：用户点两下「支付」按钮，系统只应扣一次款、只建一个订单，但操作本身可能被内部执行了两遍。",
    },
    {
      type: "heading",
      text: "为什么分布式写必须幂等",
    },
    {
      type: "paragraph",
      text: "重试是分布式系统的默认策略，而重试的来源不止客户端：**超时重试**（调用方等不到响应就重发，见第 7 章「熔断与超时」的重试策略）、**消息重投**（至少一次投递语义下消费者会收到重复消息，见第 4 章「消息可靠性：不丢、不重、不乱」）、**回调重复**（外部系统如 pay 网关为保证送达，可能把同一个支付结果回调多次）。这三类来源在单机系统中几乎不存在，却是分布式写路径上的常态——所以凡是「扣减、追加、转账、下单」这类非幂等操作，都必须自带对重复的免疫力。",
    },
    {
      type: "heading",
      text: "先分清：天然幂等 vs 非幂等",
    },
    {
      type: "table",
      caption: "操作幂等性分类",
      headers: ["类别", "例子", "说明"],
      rows: [
        ["天然幂等（重复执行无副作用）", "`SET status = 'PAID'`、`DELETE WHERE id = ?`、`UPDATE SET x = 5`（赋值）、`PUT /users/{id}`（整体覆盖）", "不管执行几次，终态相同"],
        ["非幂等（重复执行会叠加效果）", "扣减（`stock = stock - 1`）、追加（INSERT / 列表 push）、余额增加、发短信", "执行两次就扣两次、发两条"],
        ["看似幂等实则不幂等", "`UPDATE SET x = x + 1`、生成订单号的 INSERT、依赖当前时间/随机数的更新", "每次执行结果都不同"],
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "不要在重复执行里叠加效果",
      body: "闪购的扣库存 `stock = stock - 1` 就是教科书级的非幂等操作：回调重发一次、消息重投一次，库存就被多扣一次。凡是这类操作，都禁止在没有任何防护的前提下裸露在可能重试的入口后面。防护手段就是下面三招：幂等键、去重表、状态机——三者经常组合使用。",
    },
    {
      type: "heading",
      text: "幂等键：给「同一件事」一个名字",
    },
    {
      type: "paragraph",
      text: "防护的第一步，是让接收方**能认出这是同一件事**。两种生成方式：请求方生成（客户端在创建资源前生成一个全局唯一的业务键，例如下单请求里的 `client_req_id`，重试时原样带上），或服务端签发（服务端先返回一个待确认的凭证，例如支付流水号 `pay_no`，后续查询与回调都用它指代这笔业务；REST 的 `Idempotency-Key` 头本质上就是请求方先生成、由服务端登记复用的第一种形态）。前者适合「创建类」入口，后者适合「先登记、后确认」的两段式流程。关键在于：**重试与原始请求必须使用同一个键**——键一旦每次重试都重新生成，等于没有防护。",
    },
    {
      type: "code",
      title: "用幂等键 + 唯一索引去重（示意）",
      language: "sql",
      code: "-- 幂等键要能唯一识别“同一业务事件”。\n-- 方案：幂等表 + 唯一索引，落库与业务写在同一事务里\nCREATE TABLE idem_order (\n    biz_type   VARCHAR(32)  NOT NULL,          -- 如 'create_order'\n    biz_key    VARCHAR(128) NOT NULL,          -- 客户端生成的 client_req_id / 业务键\n    order_id   BIGINT       NOT NULL,\n    created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    PRIMARY KEY (biz_type, biz_key)            -- 唯一索引 = 去重裁决点\n) ENGINE = InnoDB;\n\n-- 伪代码：sale 收到 POST /orders（携带 client_req_id）\n-- tx { INSERT INTO idem_order(biz_type, biz_key, order_id) …\n--        + UPDATE product 扣库存 + INSERT order }\n-- 唯一键冲突（Duplicate entry）→ 该请求此前已处理：\n--   查回当时的 order_id 原样返回，而不是报错",
    },
    {
      type: "callout",
      variant: "tip",
      title: "为什么落在数据库而不是内存/Redis",
      body: "去重裁决必须与业务数据同生共死：幂等记录要能与扣库存、建单**同事务提交**，这样「建单成功但去重记录丢了」不可能发生（否则客户端重试就会建出第二单）。进程内缓存去重会随实例重启消失；Redis 去重要额外处理「Redis 已提交、业务事务失败回滚」与「Redis 宕机」两个窗口。最稳的锚点永远是业务库里的唯一索引。",
    },
    {
      type: "heading",
      text: "状态机幂等：不是每次都从头裁决",
    },
    {
      type: "paragraph",
      text: "幂等键能防「同一请求执行两次」，但一个订单的生命周期里有多个动作（创建→支付→发货），每个动作的重复消息会以不同键、不同时刻到达。这时让「每笔业务」变成一台**只能沿合法边迁移的状态机**，是比逐个去重更省力的第二道防线：`pending → paid → shipped`，只有合法迁移可执行——`paid → paid` 直接返回成功（重复支付通知），`shipped → paid` 拒绝或忽略（乱序的旧事件）。重复的成对撤销/补偿也可以靠它识别。",
    },
    {
      type: "heading",
      text: "重试与幂等的配合：超时重试的安全网",
    },
    {
      type: "list",
      items: [
        "区分错误类型：网络超时/5xx 可安全重试；4xx 业务拒绝（库存不足、参数非法）重试也白费。",
        "重试要带幂等键：`POST /orders` 的请求头里透传同一个 `Idempotency-Key`/`client_req_id`，服务端据此去重。",
        "重试要有上限与退避：指数退避 + 抖动（见第 7 章「熔断与超时」），防止重试风暴把服务再打垮。",
        "重试的终点要能查询：客户端超时后先「查询这笔业务的状态」，确认后再决定是否重发——把「查询作为重试的前提」加进自己的超时重试逻辑（第 7 章「熔断与超时」讲的是次数封顶与退避，查询兜底正是对它的补全）。",
      ],
    },
    {
      type: "heading",
      text: "闪购完整示例：支付回调的重复通知处理",
    },
    {
      type: "paragraph",
      text: "pay 网关的「支付成功」回调不保证只来一次：可能重发多次，也可能因为链路故障延迟到达。sale 收到回调时，用「回调幂等表 + 状态机」双保险：先按 `pay_no` 查/插回调幂等记录挡住重复，再在订单状态机上执行 `pending → paid` 这一条迁移——只有这条边是合法的。两条防线一个挡住「同一次回调的重复」，一个挡住「不同动作的乱序/重复」。",
    },
    {
      type: "code",
      title: "支付回调处理：幂等表 + 状态机（伪代码）",
      language: "go",
      code: "func handlePayCallback(w http.ResponseWriter, r *http.Request) {\n    // ① 验签（防伪造）后取出 pay_no 与订单号，开始处理\n    // ② 回调幂等表：状态记录法，防同一次回调重放\n    //    tx {\n    //      SELECT 状态 FROM pay_callback WHERE pay_no=? FOR UPDATE\n    //      若状态='DONE' → 幂等命中，直接返回成功(避免重复发货等副作用)\n    //      否则 UPDATE pay_callback SET 状态='DONE' WHERE pay_no=?\n    //    }\n    // ③ 同一事务里做业务迁移：\n    //    UPDATE order SET status='paid' WHERE id=? AND status='pending'\n    //    -- 影响行数=1：真正执行了迁移（可触发发货）\n    //    -- 影响行数=0：订单不在 pending（已在 paid/shipped）→ 重复或乱序，也返回成功\n    // ④ 提交后向 pay 返回 200；业务后置动作（通知/发货）走异步消息\n}\n\n// 关键：对支付网关永远回“成功”——否则网关会一直重试；\n// 真正去重靠幂等表与状态机，而不是靠“报错让网关别发了”。",
    },
    {
      type: "quiz",
      question: "幂等判定的是「不重复执行」，还是「重复执行后效果不变」？下面做法哪个违背幂等原则？",
      options: [
        "重试时沿用首次请求的 client_req_id",
        "支付回调处理器里用唯一索引先挡住重复的 pay_no",
        "每次重试都新生成一个 client_req_id 再提交",
        "订单状态只有合法迁移（pending→paid）可以执行",
      ],
      answer: 2,
      explanation: "幂等靠「同一个键代表同一件事」实现去重；重试若每次新生成键，服务端会认为每次都是新请求，防护完全失效，故选 C。A/B/D 分别是幂等键复用、去重表、状态机三个正确手段。",
    },
    {
      type: "keypoints",
      items: [
        "幂等 = 同一操作重复执行与执行一次效果相同；网络不确定使重试成为默认策略，分布式写必须幂等。",
        "SET 赋值、DELETE、按 id 覆盖 PUT 天然幂等；扣减、追加、`x = x + 1`、发短信非幂等。",
        "幂等键由请求方生成（Idempotency-Key / client_req_id）或服务端签发（流水号），重试必须复用同一键。",
        "去重靠业务库唯一索引/去重表，与业务写同事务提交——不依赖进程内存或 Redis 的可靠性。",
        "状态机让每笔业务只沿合法边迁移（pending→paid→shipped），与幂等键互补，抵御不同动作的重复与乱序。",
        "支付回调的完整防线 = 验签 + 回调幂等表 + 状态机迁移，对上游永远返回成功、用幂等承载去重。",
      ],
    },
  ],
};
