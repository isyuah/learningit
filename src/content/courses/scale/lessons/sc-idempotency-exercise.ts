/* ==================================================================
 * 课时：设计练习：秒杀下单的库存扣减（sc-idempotency-exercise）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 本章嵌入式设计练习课：先由学习者自己推演，再逐段给参考答案。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "把本章的并发、幂等、分布式事务与一致性选择，压进一道经典设计题：秒杀下单的库存扣减。先自己做，再看方案链推演与可答辩的答案。",
  blocks: [
    {
      type: "paragraph",
      text: "前面四课把防线备齐了：单机锁与原子、幂等键与状态机、跨库事务方案谱系、一致性级别。现在把它们用到一道经典设计题上——**秒杀下单的库存扣减**。这道题在系统设计面试里几乎必考，本质是「不能超卖 + 高并发写 + 一致性与性能的交易」。建议的学习方式：先停在这里，按下面第一个 exercise 自己想 15 分钟，写出你的方案与顾虑，再往下看参考答案对照。",
    },
    {
      type: "exercise",
      title: "热身：先定义问题",
      description:
        "闪购大促：单商品限量 10,000 件，单品写峰值约 1 万–10 万 QPS（对比：单机 MySQL 约 3k–8k QPS、单机 Go 约 2 万–5 万 RPS）。需求约束：① 绝对不允许超卖（卖的不能比库存多）；② 库存不多卖不少卖，请求失败/超卖对用户体验影响大；③ 下单成功要建立订单、用户能看到；④ 系统要扛住瞬时峰值、不能被打垮。\n请先写一页纸方案：你会在哪一层做什么？「扣减」这个动作放在哪个存储、用什么手段保证不超卖？失败/超时/重复请求各怎么处理？",
      hint: "先不要追求一个完美方案。把问题拆开：裁决库存的权威数据在哪、并发怎么裁决、峰值怎么消化、重复怎么去重。每一层（浏览器/网关/服务/缓存/数据库）各自能贡献什么？",
    },
    {
      type: "paragraph",
      text: "下面进入方案链推演。我们把候选方案按「从简单到现实」排成一条链，每步先看它为什么不够，再看它贡献了什么——最终的答案不是链上某一步，而是它们的组合。",
    },
    {
      type: "heading",
      text: "候选 ①：数据库悲观锁",
    },
    {
      type: "paragraph",
      text: "最朴素的做法：下单即开事务，`SELECT stock FROM product WHERE id = ? FOR UPDATE` 锁住商品行，检查库存后扣减、建单、提交。正确性没有问题（当前读 + 行锁，见 MySQL 课程「锁机制」「面试——乐观锁 vs 悲观锁」），但它的天花板也一目了然：**所有抢购同一商品的请求都在这一行上串行排队**。单行锁的吞吐上限就是数据库单事务的串行速度，单机 MySQL 在 3k–8k QPS 量级——离 10 万峰值差两个数量级；而且事务持有锁期间要等建单、等提交，锁竞争会进一步拉高延迟与超时。结论：正确，但**瓶颈在锁竞争**，扛不住峰值。",
    },
    {
      type: "heading",
      text: "候选 ②：数据库乐观锁 / 条件原子更新",
    },
    {
      type: "code",
      title: "条件原子更新（乐观思路，见 MySQL 课程）",
      language: "sql",
      code: "UPDATE product SET stock = stock - 1\nWHERE id = ? AND stock > 0;\n-- 影响行数 = 1：扣减成功；= 0：库存不足，本次失败",
    },
    {
      type: "paragraph",
      text: "用单条 `UPDATE ... AND stock > 0` 把「判断 + 扣减」做成数据库内的原子动作，避免显式加锁，读不再阻塞。但它把竞争变成了**失败**：1 万并发抢 100 件商品，同一时刻只有极少数 UPDATE 成功，其余全部返回「影响行数 = 0」。如果客户端对失败一律立即重试，会形成**重试风暴**——失败请求不断重打数据库，行锁与 CPU 都被无效竞争占满，吞吐反而恶化。结论：代码最简单、没有显式锁，但**高冲突下靠重试兜底不成立**——这正是第 7 章讲过的「重试放大故障」。它适合冲突率低的日常扣减，不适合秒杀瞬间。",
    },
    {
      type: "heading",
      text: "候选 ③：Redis 原子扣减预扣",
    },
    {
      type: "code",
      title: "Redis Lua 原子扣减（防超卖的第一道闸）",
      language: "go",
      code: "// 用 Lua 脚本把“判断库存足够 + 扣减”做成原子操作（Redis 单线程执行脚本），\n// 避免 DECR 后才发现负数。伪代码示意：\n//   local stock = redis.call('GET', KEYS[1])\n//   if tonumber(stock) <= 0 then return -1 end     -- 已抢完\n//   if tonumber(stock) < tonumber(ARGV[1]) then return -2 end -- 库存不足\n//   return redis.call('DECRBY', KEYS[1], ARGV[1])\n// 单 Redis 实例单线程：对单个 key 的操作天然串行，不会并发扣超",
    },
    {
      type: "paragraph",
      text: "把扣减从 MySQL 行挪到 Redis 的单个 key 上：Redis 单线程执行 Lua，天然串行，10 万 QPS 热点写也能扛（单 key 原子操作是 Redis 少数真正擅长的写模式，第 3 章「缓存一致性、分布式锁与计数器」讲过它的边界）。但这里必须清醒：**Redis 不是可靠存储**——纯内存可能因宕机/重启丢数据，就算开了持久化，主从切换或宕机恢复也会出现短暂不可写，且 Redis 里的数字与 MySQL 的真相可能漂移。所以 Redis 扣减只能当「预扣」：它挡掉绝大多数并发，把真实裁决留给数据库，中间用**对账**兜底。结论：**解决吞吐，不解决可靠性与最终一致**——必须和数据库配合，而不是替代它。",
    },
    {
      type: "heading",
      text: "候选 ④：队列化串行 + 预扣",
    },
    {
      type: "paragraph",
      text: "另一个思路是把「同一商品的扣减」串行化：请求先进队列，由一个消费者单线程依次处理（每件商品一个队列/一个 worker）。单商品吞吐上限 = 单个消费者的处理速度——如果一次扣减+建单要 5–20ms，单队列就是每秒 50–200 单，秒杀 10 万峰值请求意味着队列瞬间堆积，消费者消化不完，「削峰」把压力转移成了**排队延迟与用户体验问题**（用户不知道自己是抢到了还是还在等）。结论：队列化适合「削峰填谷、保护数据库」，但**单商品队列的吞吐是硬上限**，且引入了异步一致性——它解决不了「裁决本身必须快」，只解决「落库别被打垮」。",
    },
    {
      type: "heading",
      text: "候选 ⑤：分片 / 预扣库存池",
    },
    {
      type: "paragraph",
      text: "把 10,000 件库存拆成多个库存分片（stock pool）：每个分片是一个独立的扣减单元（各自的 Redis key + 各自的库存记录），请求先哈希/轮询到某个分片，分片内扣减。这样把「一个热点行」摊成 N 个热点，单点竞争除以 N，吞吐近似线性放大。代价是引入了**分片间不均衡**（某个分片先卖完，别的分片还有货——用户可能因哈希运气不好抢不到）与**分片间协调**（库存汇总、回补、对账都要跨分片）。它是第 5 章分片思想在热点写上的应用，适合超大单品的极端峰值；对大多数秒杀，**用 Redis 预扣 + 数据库兜底通常已经够用**，分片是库存规模再上一个数量级时才考虑的进阶手段。",
    },
    {
      type: "table",
      caption: "库存扣减候选方案权衡表",
      headers: ["方案", "不超卖", "峰值吞吐", "一致性窗口", "主要风险"],
      rows: [
        ["① 悲观锁（FOR UPDATE）", "强", "低（行锁串行，3k–8k 量级）", "无", "锁竞争瓶颈，扛不住峰值"],
        ["② 乐观锁/条件更新", "强", "中（高冲突下靠重试）", "无", "重试风暴放大无效竞争"],
        ["③ Redis 原子预扣", "强（单 key 内）", "高（单线程 Lua 串行）", "Redis 与库之间漂移", "Redis 非可靠存储，需对账"],
        ["④ 队列化串行", "强", "低（单队列 = 单消费者速度）", "下单结果异步可见", "队列堆积、延迟体验"],
        ["⑤ 分片/预扣库存池", "强（分片内）", "最高（热点摊薄到 N 片）", "分片间不均衡", "分片协调与回补复杂度"],
      ],
    },
    {
      type: "exercise",
      title: "推演：你会怎么组合？",
      description:
        "现在把 ①–⑤ 看成组件而不是对手：请按「用户点下按钮 → 最终数据库账实一致」的完整旅程，画出你心目中的分层防线——每一层做什么、失败时由哪一层兜底、重复请求在哪一层被挡住。你不需要写代码，写清职责与顺序即可。",
      hint: "参考第 7 章流量治理的顺序感：先挡无效流量，再处理裁决，最后对账兜底。",
    },
    {
      type: "heading",
      text: "参考答案：一套可答辩的分层答案",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "前端/客户端层：按钮置灰防止连点；活动规则限制（每用户限购 N 件）在前端先拦一道，但这只是体验层，不能作为防线。",
        "网关/接入层：对秒杀接口做限流与排队——单用户限频（防脚本刷），总入口限流（防打垮），大量「陪跑」请求在网关就被 429/排队挡住，根本不进业务（见第 7 章「限流」）。",
        "业务预检层：读 Redis 里的活动与「已抢完」标记，秒杀开始前/已售罄的商品直接快速返回，不进扣减逻辑（防穿透与击穿的缓存思路，见第 3 章）。",
        "Redis 原子预扣：真正承担峰值裁决——Lua 单 key 原子扣减，成功才继续；扣减成功的请求拿到一个资格（记录 client_req_id / 资格号）进入下一步。",
        "异步落库（队列化 + 数据库最终扣减）：预扣成功的请求进队列，由消费者串行执行「数据库条件更新 + 建单」的本地事务（`UPDATE ... AND stock > 0`，影响行数 = 0 就回滚并补回 Redis 预扣）。数据库不再面对 10 万峰值，只消化真实成交的流量。",
        "幂等与去重：整个流程携带 client_req_id；建单用唯一索引/去重表防重（见「幂等」课），队列消费与回调天然重复也不怕。",
        "对账兜底：定时任务核对 Redis 预扣总数、数据库实扣数与订单数，发现漂移（Redis 重启、消息丢失、半途失败）就回补/修正（见第 4 章「最终一致性与补偿」的可靠回查与对账）。",
      ],
    },
    {
      type: "code",
      title: "分层防线的核心骨架（伪代码）",
      language: "text",
      code: "POST /orders\n  ① 网关：限流（用户级 + 入口级）\n  ② 预检：activity 未开始 / 已售罄 → 直接返回\n  ③ Redis Lua 原子预扣：stock-key 减 1；不足 → 返回「已抢完」\n  ④ 预扣成功 → 带 client_req_id 投递到扣减队列（防重：idempotency-key）\n\n消费者（单商品队列，串行落库）\n  ⑤ 幂等表判重：同一 client_req_id 已处理 → 直接返回\n  ⑥ 本地事务：UPDATE product SET stock=stock-1 WHERE id=? AND stock>0\n             + INSERT order（状态 pending）\n     影响行数=0 → 事务回滚 + 通知补回 Redis 预扣\n  ⑦ 返回成功后：异步通知/发货（最终一致）\n\n对账任务（每小时）\n  ⑧ Redis 预扣总数 == MySQL 实扣总数 + 待处理队列量？\n     不等 → 定位漂移并回补（补库存 / 补单 / 修正预扣）",
    },
    {
      type: "callout",
      variant: "tip",
      title: "面试答辩话术骨架",
      body: "把方案讲成一句话：**「峰值裁决在 Redis（原子预扣），最终账实以数据库为准（条件更新 + 幂等建单），中间用队列削峰、用对账收敛不一致」**。然后按需求约束逐条回应：不超卖靠 Redis Lua 原子性 + 数据库 `AND stock > 0` 双保险；吞吐靠预扣挡掉 99% 流量、数据库只处理真实成交；用户体验靠网关快速失败 + 异步结果可查询；可靠性靠幂等键与对账。能按约束而不是按方案倒着背，就是答辩的加分点。",
    },
    {
      type: "heading",
      text: "面试官追问预判",
    },
    {
      type: "list",
      items: [
        "「为什么数据库最终扣减还会失败？」——因为 MySQL 的 `UPDATE ... AND stock > 0` 在极端情况下也可能影响行数为 0：Redis 预扣数与数据库库存不一致（如补货、回滚、对账延迟）时，数据库判定库存不足。这不是 bug，是**两级库存本身可能漂移**——所以失败处理必须是「回滚并补回 Redis 预扣」，而不是简单重试。",
        "「对账窗口内出问题怎么办？」——对账是周期性兜底，窗口内 Redis 与库可能不一致（比如 Redis 扣了、库扣减失败且补回消息丢了）。所以关键动作本身要可靠：补回 Redis 用带重试的可靠消息，关键路径失败要告警；对账窗口由「可靠消息 + 定时核对」共同压缩，而不是只靠最后一个小时任务。",
        "「Redis 预扣了但用户最终没下单成功，库存不就悬空了？」——正是：预扣成功不等于成交。悬空的库存靠**超时释放**（用户迟迟不支付/下单未确认，预扣资格过期并回补 Redis）与对账回收，这也是把「预扣」叫预扣而不是扣减的原因。",
        "「为什么不用 2PC 让 Redis 和 MySQL 原子？」——2PC 的正确性代价（阻塞、协调者故障、吞吐）远大于收益：这里两个资源本来就不需要同一个原子瞬间——Redis 预扣只是限流与准入，MySQL 才是账实权威，两者之间用「可靠消息 + 幂等 + 对账」收敛即可（呼应「跨库事务的现实」课的选择地图）。",
      ],
    },
    {
      type: "quiz",
      question: "这套分层方案里，Redis 预扣扣多了/数据库扣减失败导致库存悬空时，最合理的处理是？",
      options: [
        "让用户无限重试直到数据库扣减成功",
        "回滚该笔并可靠地补回 Redis 预扣，悬空库存由超时释放与对账回收",
        "立刻用 2PC 让 Redis 和 MySQL 达到同一个原子瞬间",
        "把 Redis 的库存数直接当作最终账实，不再核对数据库",
      ],
      answer: 1,
      explanation: "Redis 是准入与限流层、MySQL 是账实权威，两者允许短暂漂移、靠可靠消息回补 + 对账收敛（B）。A 是重试风暴；C 用错了工具——这两个资源不需要同一个原子瞬间（见跨库事务课选择地图）；D 放弃了账实权威，Redis 非可靠存储不能当最终数据。",
    },
    {
      type: "keypoints",
      items: [
        "五步方案链：悲观锁（正确但锁竞争瓶颈）→ 乐观锁（高冲突重试风暴）→ Redis 原子预扣（解决吞吐、不解决可靠）→ 队列化（削峰但单队列有吞吐上限）→ 分片库存池（热点摊薄、复杂度最高）。",
        "可答辩答案 = 分层：前端限制 → 网关限流 → Redis Lua 预扣 → 队列化数据库最终扣减 + 幂等建单 → 对账兜底。",
        "防超卖双保险：Redis 单 key 原子 + 数据库 `AND stock > 0` 条件更新；重复靠 client_req_id + 唯一索引。",
        "预扣与账实之间允许漂移，靠可靠回补 + 超时释放 + 定期对账收敛；不用 2PC——两个资源不需要同一个原子瞬间。",
        "答辩话术：峰值裁决在 Redis、账实以数据库为准、队列削峰、对账收敛——按需求约束逐条回应。",
      ],
    },
  ],
};
