/* ==================================================================
 * 课时：跨库事务的现实：2PC、TCC、Saga 与消息事务（sc-distributed-transaction）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "单库事务在多库、多服务面前失效。2PC、TCC、Saga、消息最终一致各解决一部分问题，也各付一份代价——用一张选择地图决定该用谁。",
  blocks: [
    {
      type: "paragraph",
      text: "上一课用「幂等」守住了重复执行，这一课面对更难的问题：**一次操作要原子地跨多个资源**。闪购的库拆了以后（第 5 章分库分表）、服务拆了以后（第 6 章服务拆分），「扣库存」和「建订单」可能不再落在同一个库的同一个事务里——用户看到的现象是：要么都不发生，要么都发生，中间不能出现「钱扣了单没建成」。单机事务的 ACID 承诺为什么在跨库时失效？替代方案各长什么样？这是本课全部内容。",
    },
    {
      type: "heading",
      text: "为什么跨库事务这么难",
    },
    {
      type: "definition",
      term: "分布式事务",
      definition: "跨多个资源（多个库、多个服务）的原子操作——要么全部成功，要么全部失败且不留中间状态。难点在于：参与方之间只有网络连接，而网络可能分区、延迟、丢消息；任何一方都无法像单机事务管理器那样「掌握全局、原子提交」。",
    },
    {
      type: "paragraph",
      text: "单机事务能原子提交，靠的是共享一份日志（redo/undo log）和一个能统一裁决的引擎。换成多个独立库/服务后，三个问题立刻出现：**网络分区**——协调者与某个参与者之间断了，没人知道它那半边是提交了还是没提交；**协调者故障**——协调者自己在等待提交时宕机，所有参与者停在中间状态；**性能**——参与者必须在两阶段之间持有锁与事务资源，跨网络等待使这个窗口从毫秒级膨胀到不确定。这三个问题不是某个协议的 bug，而是「原子性需要全局裁决，而全局裁决依赖网络」这个结构矛盾。",
    },
    {
      type: "heading",
      text: "方案谱系一：2PC 与 XA——教科书方案，为什么很少用",
    },
    {
      type: "paragraph",
      text: "两阶段提交（2PC）的思路很直接：先让所有参与者把资源准备好并投票（prepare / 一阶段），全部同意后协调者才统一下令提交（commit / 二阶段）。它的正确性没问题，但代价决定了它在互联网业务里的位置：**阻塞**——prepare 之后参与者要一直持有行锁与事务资源等协调者的最终指令，协调者或网络一出问题，参与者可能无限期卡在「已 prepare 未提交」的中间态（协调者故障时无法单方决定回滚还是提交）；**性能**——两次跨网络往返都发生在持有锁期间，冲突率与延迟显著上升；XA 协议把它标准化，但工程上还要面对资源占用、恢复日志、实现一致性等一堆细节。结论：2PC/XA 常见于银行间清算、传统单体中间件等强监管低吞吐场景；对秒杀这种高并发互联网路径，它不是主力。",
    },
    {
      type: "heading",
      text: "方案谱系二：TCC——业务化的两阶段",
    },
    {
      type: "paragraph",
      text: "TCC 把「一个跨资源操作」拆成三个业务接口：**Try**（预留资源，如冻结库存、锁定余额）、**Confirm**（确认执行，此时只能成功）、**Cancel**（取消，释放预留）。它把锁从数据库行锁换成业务语义的「预留」，但正确性要靠业务代码维护，三个坑最常见：**空回滚**——Try 因网络原因没到对方，Cancel 却先到，需要识别并安全跳过；**悬挂**——Cancel 已经执行，迟到的 Try 又生效，把预留留在半空；以及 Confirm/Cancel 的重复执行——所以 Cancel 与 Confirm 都**必须幂等**（上一课的全部手段在此复用）。",
    },
    {
      type: "heading",
      text: "方案谱系三：Saga——正向执行 + 补偿",
    },
    {
      type: "definition",
      term: "Saga",
      definition: "把一个长事务拆成一串本地事务，每步只做自己库内的提交，并预登记一个补偿（compensation）步骤；某一步失败时，逆序执行已成功步骤的补偿来撤销效果。Saga 不追求「同时成功」，只保证「要么全部完成，要么全部撤销到初始状态」。",
    },
    {
      type: "paragraph",
      text: "Saga 有两种组织方式。**编排式（Orchestration）**：一个中央协调者（如 OrderSaga）按顺序调用各参与方并跟踪进度，挂了可以靠持久化的 saga 状态恢复重跑——流程清晰、易跟踪，代价是协调者成为耦合点。**协同式（Choreography）**：没有协调者，各服务通过领域事件驱动下一步（第 4 章事件课的事件驱动），解耦最好，但流程散落在各服务的消息处理里，难观测、难维护。两种方式的共同纪律：**补偿必须幂等**——补偿可能执行多次（重试、消息重投），且正向步骤与补偿步骤可能乱序到达，每步都要能对重复免疫（呼应上一课状态机思路）。",
    },
    {
      type: "table",
      caption: "Saga 编排式 vs 协同式",
      headers: ["维度", "编排式（Orchestration）", "协同式（Choreography）"],
      rows: [
        ["流程控制", "中央协调者显式定义步骤顺序", "无协调者，事件驱动隐式流转"],
        ["可观测性/调试", "好——集中查看 saga 状态", "差——要翻各服务日志拼全貌"],
        ["耦合", "参与方只需对接协调者，但协调者本身是耦合点", "服务间解耦最彻底"],
        ["新增步骤", "改协调者逻辑", "新增事件订阅即可，但流程更难理解"],
      ],
    },
    {
      type: "heading",
      text: "方案谱系四：消息最终一致——最常用的定位",
    },
    {
      type: "paragraph",
      text: "前面三种方案都在解决「怎么原子地完成一次跨资源操作」。对绝大多数互联网场景，还有一个更便宜的视角：**接受短暂的最终一致窗口**（第 4 章「最终一致性与补偿」的完整定义与度量），把「跨资源的原子提交」改写为「本地提交 + 可靠地发出后续动作」。实现它的经典机制是**事务性 Outbox（本地消息表）**：业务事务与「待发消息」写进同一个本地库事务，消息通过定时扫描/CDC 投递到队列（详见第 4 章「事件与命令」对 outbox 模式的动机讲解），消费者收到后再各自处理，重复靠幂等消化。它是本课程语境下**最常用**的方案：只付一次本地事务的代价，就把「多资源原子性」降级为「可靠消息 + 幂等消费」，这也是第 4 章异步体系与本章正确性防线的接合点。",
    },
    {
      type: "code",
      title: "消息事务（outbox）承载扣库存+建单的示意图（伪代码）",
      language: "text",
      code: "POST /orders（用户下单）\n  └─ 本地事务（订单库）\n       ├─ 扣库存（库存若同库，一起完成；若分库，见下）\n       ├─ 建 order（状态 pending）\n       └─ INSERT order_outbox(order_id, event='order_created', status='NEW')\n  └─ 提交后：outbox 扫描器把 event 投递到消息队列\n       └─ 下游（支付/通知/统计服务）消费 event，重复靠幂等键去重\n\n跨两个库的扣库存+建单：通常先把“扣减”与“建单”的裁决归并到\n一个库（见「选择地图」与第 5 章 NewSQL/单写者），或用 Saga/TCC 编排；\n消息最终一致更常用于“主库提交 → 通知下游”这种单向动作。",
    },
    {
      type: "heading",
      text: "选择地图：什么一致性等级配什么方案",
    },
    {
      type: "table",
      caption: "跨资源一致性方案选择地图",
      headers: ["决策输入", "倾向选择", "理由"],
      rows: [
        ["要求所有参与者看到同一个原子瞬间", "单库事务；或 NewSQL 分布式事务（见下）", "2PC 之外没有同时提交；接受不了最终一致就只能收敛到单一权威"],
        ["跨 2–3 个库/服务、可接受秒级窗口", "事务性 Outbox + 幂等消费", "最便宜、故障面小，互联网主路径的主流选择"],
        ["需要逐步尝试、每步有业务语义可回滚", "Saga（编排式优先，补偿必须幂等）", "订单流程天然是步骤化的"],
        ["对方提供预留/确认接口（支付、库存冻结）", "TCC", "用业务预留替代行锁，但防空回滚/悬挂成本高"],
        ["真正必须多资源同时生效、又不接受最终一致", "集中式 NewSQL（TiDB/CockroachDB 分布式事务）或不分", "用「一个逻辑库」的强一致替代手写跨库方案（见第 5 章）"],
      ],
    },
    {
      type: "heading",
      text: "什么时候干脆不分",
    },
    {
      type: "paragraph",
      text: "选择地图的第一行其实是最重要的一句：**跨库事务的问题，最彻底的解法是不让数据跨库**。「扣库存 + 建单」在数据规模允许时永远应该待在同一个库里用同一个事务完成——单库事务的成本和正确性都远胜任何分布式替代。真到单库撑不住、必须拆分时，优先想清楚哪些写可以**归并**到同一个权威库（把裁决集中到一处，其它只读/只收通知），或引入第 5 章介绍的 NewSQL（TiDB/CockroachDB 以「自动分片 + 分布式事务」把多分片重新包装成一个逻辑库）——用「假装它没分」代替手写 2PC/TCC/Saga 的运维成本。只有归并不了、又确实跨资源的写，才轮到本课方案谱系上场。",
    },
    {
      type: "callout",
      variant: "note",
      title: "面试答题的顺序感",
      body: "被问到「分布式事务怎么做」时，正确的展开顺序是：先答「能不分就不分 / 归并到单库或 NewSQL」→ 再答「接受最终一致就上 outbox + 幂等」→ 需要步骤化回滚上 Saga（补偿幂等）→ 有预留语义上 TCC（注意空回滚/悬挂）→ 最后才提 2PC/XA 并说明它为什么不适合高并发互联网主路径。从代价最小的方案说起，是这道题的加分项。",
    },
    {
      type: "quiz",
      question: "为什么 2PC 在秒杀这类高并发路径上不是主力方案？",
      options: [
        "因为两阶段提交的正确性无法证明",
        "因为参与者在两阶段之间要持有锁和事务资源，等待期间阻塞且吞吐受限；协调者或网络故障还可能让参与者卡在中间态",
        "因为参与者无法持久化自己的状态",
        "因为网络分区时 2PC 可以选择继续运行不阻塞",
      ],
      answer: 1,
      explanation: "2PC 结构上要求参与者 prepare 后持锁等待协调者的统一指令，跨网络等待让这个窗口不确定（阻塞与吞吐代价），协调者故障时参与者无法单方决定提交或回滚——这正是它不适合高频路径的原因。A 错（正确性可证）；C 错（参与者可持久化，问题在等待与裁决）；D 说反了，网络分区恰恰让 2PC 阻塞。",
    },
    {
      type: "keypoints",
      items: [
        "跨库事务难在结构矛盾：原子性需要全局裁决，而全局裁决依赖会分区、会故障的网络。",
        "2PC/XA 正确但阻塞、慢，协调者故障会卡住参与者——留给低吞吐强监管场景。",
        "TCC 用业务接口 Try/Confirm/Cancel 替换行锁，代价是空回滚、悬挂与 Confirm/Cancel 必须幂等。",
        "Saga 正向执行 + 逆序补偿，编排式易跟踪、协同式更解耦；补偿必须幂等。",
        "事务性 Outbox（本地消息表）+ 幂等消费是最常用方案：把多资源原子性降级为可靠消息 + 最终一致。",
        "第一选择永远是「不让数据跨库」：归并到单库，或交给 NewSQL；跨库方案只服务归并不了的写。",
      ],
    },
  ],
};
