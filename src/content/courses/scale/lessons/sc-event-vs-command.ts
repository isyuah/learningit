/* ==================================================================
 * 课时：事件与命令：消息的两种语义（sc-event-vs-command）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "区分命令消息与事件消息两种语义，介绍事件驱动与领域事件，并讲透事务性 Outbox 的动机与机制。",
  blocks: [
    {
      type: "paragraph",
      text: "消息不只是「换了个传输方式的函数调用」。同样是发一条消息，它携带的意图决定了整个系统的行为方式：是要对方「去做某件事并给我回执」，还是宣告「某件事已经发生、感兴趣的可以围观」？把这两种语义混在一起，会出现很诡异的系统：订单服务明明只是广播了一个事实，却要承担结果反馈；或者把「请扣库存」当事件广播，于是所有订阅者都以为自己该去扣一次。这一课把消息的两种语义分开讲清楚，并解决一个伴随事件化而来的经典难题：业务与消息如何原子地一起落地（outbox）。",
    },
    {
      type: "definition",
      term: "命令消息（command message）",
      definition: "携带「请执行某操作」意图的消息，语义上等价于一次异步的 RPC 调用：有明确的接收方、期望对方执行并最终反馈结果，失败时需要重试或补偿。关注点是「事情被做成了没有」。",
    },
    {
      type: "list",
      items: [
        "典型的命令消息：请扣库存、请退款、请发货。",
        "关联性：命令通常需要响应——通过回执消息、回调或查询把结果送回去，请求与响应要靠 request_id 关联。",
        "失败语义：命令没做成就是失败，必须重试或补偿，通常走 at-least-once + 幂等（见第 2 课）。",
        "一个命令通常只有一个执行者：多个执行者会重复做同一件事。",
      ],
    },
    {
      type: "definition",
      term: "事件消息（event message）",
      definition: "宣告「某件事已经发生」的消息，是对过去事实的描述，不携带指令。典型形态为「领域事件 + 时间戳 + 相关实体 ID」。关注点是「发生了什么、谁能从中知道什么」；发布者不关心谁会消费、消费后做什么。",
    },
    {
      type: "list",
      items: [
        "典型的事件：订单已创建、库存已扣减、支付已完成——都是已经成立的过去式。",
        "广播与订阅：事件走发布/订阅，一个事件可被多个订阅者各取所需（短信、审计、风控、数据同步各收各的）。",
        "没有回执：事件发布者不等待、也不该等处理结果；事件处理失败是订阅者自己的问题（重试、死信）。",
        "事件是事实，事实不会因为没人听而改变——这是事件能解耦的根本：发布者完全不知道订阅者是否存在。",
      ],
    },
    {
      type: "table",
      caption: "命令消息 vs 事件消息",
      headers: ["维度", "命令消息", "事件消息"],
      rows: [
        ["意图", "请对方执行操作（祈使）", "宣告已发生的事实（陈述）"],
        ["接收方", "明确的执行者，通常一个", "任意多个感兴趣的订阅者"],
        ["关心什么", "事情做成没有（结果）", "发生了什么（事实本身）"],
        ["需要回执吗", "通常需要（回执/回调/查询）", "不需要"],
        ["失败语义", "重试或补偿", "订阅者各自兜底"],
        ["闪购例子", "请扣库存（若库存独立成服务，见边界说明）", "订单已创建（sale 广播给 notify/审计等）"],
      ],
    },
    {
      type: "heading",
      text: "什么时候命令会变成事件",
    },
    {
      type: "paragraph",
      text: "同一条消息的语义会随系统演化而漂移。典型场景是语义从「请执行」变成「已经发生」：下单初期，sale 把「发短信」作为命令发给 notify，一对一、等结果；等下游多起来——审计要记日志、风控要算分、推荐要刷新——「请发短信」就不够用了，真正有价值的是「订单已创建」这个事实，谁要谁订阅。判断信号很简单：当「告诉一个执行者去做」变成「让许多对这件事感兴趣的系统知道」，命令就该改造成事件。反过来，如果业务真的需要强约束「某件事必须由谁完成并确认」，强行事件化只会把回执和失败处理做成一团乱麻——命令仍是正确选择。",
    },
    {
      type: "heading",
      text: "Outbox：业务与消息的原子落地",
    },
    {
      type: "definition",
      term: "事务性 Outbox（Transactional Outbox）",
      definition: "解决「本地事务提交」与「发消息」无法原子完成问题的通用模式：在业务所在的数据库里建一张 outbox 表，业务变更与写入 outbox 行放在同一个本地事务中提交；随后一个独立的发布器进程扫描 outbox 表，把未发送的行发布到消息队列，成功后标记已发送。保证要么业务和消息一起成功，要么一起不发生。",
    },
    {
      type: "paragraph",
      text: "动机来自一个死结：sale 想在「创建订单」的数据库事务里顺便把「订单已创建」发出去——但消息队列不在这个事务里。先发消息再提交事务：消息到了消费者手里，事务却回滚了，消费者处理了一件不存在的事；先提交事务再发消息：事务刚提交、进程就崩溃，消息永远没发出去，订单建了通知却没发。outbox 把「发消息」这个不可原子操作，降维成「同一事务里插一行」这个天然原子操作：发消息不再是第一步，而是事务提交后由发布器异步执行的第二步。",
    },
    {
      type: "code",
      title: "Outbox：事务内写业务 + outbox 行，事务外异步发布（SQL 伪代码）",
      language: "sql",
      code: `-- 1. 业务事务内：写订单 + 写 outbox，同一个本地事务
BEGIN;
INSERT INTO orders (id, user_id, product_id, status) VALUES (...);
INSERT INTO outbox (id, topic, payload, published_at)
VALUES (uuid(), 'order.created', '{"order_id": ...}', NULL);
COMMIT;

-- 2. 事务外：独立发布器扫描未发送行
--    查 published_at IS NULL 的行 → 发往消息队列
--    → 收到发送确认后 UPDATE outbox SET published_at = now() WHERE id = ?`,
    },
    {
      type: "list",
      items: [
        "发布器可以做成定时批量扫描（简单可靠），也可以接数据库 binlog/变更捕获流（延迟更低）；两种都要保证「发布成功才标记」，并配合至少一次投递。",
        "outbox 行要有唯一的业务标识（如 order_id 事件 ID），消费端照常幂等去重——outbox 解决「发没发」，幂等解决「重不重」，两者配合才是完整链路。",
        "发布器失败会导致消息延迟但不会丢失（行还在表里），积压可通过 outbox 表行数监控。",
        "通用模式讲到这里为止；它的实现走读在 flostra-execution 课程的「事务 Outbox」与「Dispatcher」两课里，那里可以看到真实代码如何落地这张表与发布器（领取、lease、重试）。",
      ],
    },
    {
      type: "callout",
      variant: "example",
      title: "闪购：订单已创建（事件）vs 请扣库存（命令）",
      body: "先交代边界：闪购主线里库存与订单同在 sale 自己的库中，由 POST /orders 一个本地事务完成扣库存与建单，此时并不存在「发消息给库存模块」的链路。把「请扣库存」讲成命令，是假设库存被拆成独立服务（第 8 章「设计练习：秒杀下单的库存扣减」的队列化扣减方案）来演示命令语义——若真如此，它就是发给库存模块的命令：明确要求执行、必须成功、通过回执确认，失败走重试与幂等。同一笔下单的另一侧是事件：sale 完成本地事务（建单 + outbox 行）后，发布器把「订单已创建」事件广播出去——notify 发短信、审计记日志、风控算分，各取所需、互不等待。命令与事件贴着同一笔业务，语义却完全不同——分清它们，是设计消息拓扑的第一步。",
    },
    {
      type: "quiz",
      question: "闪购团队上线「订单已创建」事件，notify、审计、风控分别订阅。下列哪种说法正确？",
      options: [
        "notify 处理失败时，sale 必须感知并重试整个下单流程",
        "订单已创建是命令消息，订阅者处理完要给 sale 回执",
        "sale 只负责发布事实，各订阅者自行保证自己的处理正确性",
        "每个订阅者都应该把订单已创建当作对自己的指令去扣一次库存",
      ],
      answer: 2,
      explanation: "事件消息宣告事实、无回执，发布者不关心订阅者；各订阅者按需消费、各自兜底（重试/死信），这正是事件解耦的体现。选项 A/B 把事件当成了命令（要结果、要回执），选项 D 把「请扣库存」的命令语义错套到「订单已创建」事件上——命令与事件的差别正在这里。",
    },
    {
      type: "keypoints",
      items: [
        "命令消息=异步 RPC：有执行者、要结果、失败要重试或补偿；通常一个接收方。",
        "事件消息=事实广播：发布/订阅、多订阅者各取所需、无回执、失败订阅者自己兜底。",
        "命令何时变事件：从「指定执行者去做」演化为「多方对已发生事实感兴趣」时；需要强结果约束的业务别强行事件化。",
        "Outbox：业务变更与 outbox 行同一本地事务提交，发布器异步把行发给 MQ——解决本地事务与发消息的原子性；「发没发」靠 outbox、「重不重」靠消费端幂等，两者配合。",
        "注意边界：主线场景里扣库存与建单同在 POST /orders 一个本地事务，命令示例是把库存抽象成独立服务来演示语义；库存真的独立（第 8 章「设计练习：秒杀下单的库存扣减」）时该命令链路才实体化。",
        "实现走读：flostra-execution 课程「事务 Outbox」「Dispatcher」两课（真实代码、领取/lease/重试）；领域事件驱动本身不深入本课。",
      ],
    },
  ],
};
