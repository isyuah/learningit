/* ==================================================================
 * 课时：缓存一致性、分布式锁与计数器（sc-redis-usage）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "Redis 在性能路径上的正确用法边界：先更库再删缓存为何仍有窗口及对策、分布式锁的真相（set NX EX、续租与 Redlock 争议）、原子计数与 Lua 的适用边界。",
  blocks: [
    {
      type: "paragraph",
      text: "前几课多次提到「用 Redis 的原子操作」「锁的重试」——本课把它们系统化：当 Redis 不再只是被动的 key-value 缓存，而被用来做**删除缓存、互斥锁、计数器**这些需要原子性与正确性保证的事时，它到底靠什么机制成立、边界在哪里。MySQL 课程的面试课时给出了缓存一致性题的标准答法；本课把 Redis 放进性能路径，回答三个层层递进的问题：先更库再删缓存为什么「仍然」有一致性窗口、窗口怎么进一步堵；分布式锁在什么前提下才可靠、为什么不要迷信 Redlock；原子计数与 Lua 脚本的原子性边界在哪。",
    },
    {
      type: "heading",
      text: "先更库再删缓存：为什么仍有窗口",
    },
    {
      type: "paragraph",
      text: "第 2 课确立了「更新数据库 → 删除缓存」的顺序，并指出删除与并发读回源之间仍有竞争窗口。本课把窗口的成因拆成两类，它们对应不同的对策。**窗口一：删除失败了**。删缓存这条 Redis 命令可能因网络抖动、Redis 超时、进程崩溃而没有执行——如果只删一次，缓存里的旧值会一直待到 TTL 到期（若 TTL 很长甚至接近永远脏）。**窗口二：读发生在写之前、回填发生在删除之后**。读请求在写事务提交前 miss 并回源拿到旧值，随后它的回填写入晚于删除命令执行——缓存里重新出现旧值，而删除已经发生过一次，没有人再清它。两条成因的公共点：**删除动作与读回源动作之间没有顺序保证，且删除可能缺席**。TTL 是这两种窗口的最终兜底（最坏脏一个 TTL），但若业务连「脏几十秒」都难接受，需要更主动的对策。",
    },
    {
      type: "paragraph",
      text: "**对策：删除重试与 binlog 订阅（概念级）。**针对窗口一（删除失败），工程做法是**让删除可靠**：删除失败后进入重试队列（本地重试几轮，仍失败则投递到异步消息队列，由独立消费者重试删除，直到成功），或退而求其次把 TTL 调短。针对窗口二（删除与回填竞争），第 2 课讲过延迟双删——睡眠后补删一次，压低窗口但无法根除。更强力的方案是**订阅数据库 binlog 做异步删除**：数据库写入（商品更新）提交后，binlog 解析组件（如 Canal）读到变更事件，由它统一执行缓存删除。这样做的好处是删除动作从应用代码里剥离出来：应用只管提交数据库事务，删缓存由 binlog 消费者负责，天然带重试与顺序（binlog 本身有序），且不占用请求路径的延迟。代价是要维护一套 binlog 消费链路，事件到达与缓存删除之间有一个异步延迟。本课只讲概念与取舍，具体组件操作以官方文档为准；这套「本地事务 + 可靠事件」的思想正是异步章节 outbox 模式的同族方案。",
    },
    {
      type: "callout",
      variant: "note",
      title: "一致性等级与方案选择",
      body: "对大多数读多写少业务，删缓存失败是低概率事件，短 TTL 兜底足够。只有当脏数据会造成真实损失（价格展示错误、配置即时生效需求）才值得上重试删除或 binlog 订阅。先量化「脏一个 TTL 会损失什么」，再决定投入——这是一致性工程的第一条成本纪律。",
    },
    {
      type: "paragraph",
      text: "**分布式锁：set NX EX 与续租。**缓存一致性之外，Redis 常被拉来当分布式锁：多个实例要互斥地执行某段逻辑（如第 3 课击穿场景里「只允许一个请求回源重建缓存」）。最小实现是一条命令完成「不存在才设置 + 带过期时间」，防止「检查与设置分离」产生的竞态与死锁——比如 Go 里用 SET key value NX EX 10（一次原子调用，key 不存在才写入，10 秒后自动过期）。NX 保证同一时刻只有一个实例能抢到；EX 是**兜底的租约**——持锁者崩溃时锁会自动释放，不会死锁。",
    },
    {
      type: "paragraph",
      text: "但 EX 带来了新问题：**持锁时间超过租约**。持有者只是做了一次较慢的数据库调用（超过 10 秒），锁就自动过期了——另一个实例抢到锁，两个实例同时进入临界区，互斥被打破。对策是**续租（看门狗）**：持锁期间由一个后台任务定期延长过期时间（如每 3 秒续到 10 秒），任务随持锁者进程退出而停止，锁随之自然释放。续租机制让「锁的持有期 = 临界区的真实执行期」，是工程上可用的分布式锁的标配。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "Redlock 的争议：不要迷信分布式锁的「绝对可靠」",
      body: "单实例锁有一个理论死角：Redis 主节点故障时，锁记录可能还没复制到从节点就丢了，新的主节点上锁不存在，两个客户端可能同时「持有」锁。Redlock（多实例轮流加锁、多数派成功才算持有）试图解决这一点，但分布式系统领域对它有著名争论：在网络分区、进程停顿（GC）等场景下，Redlock 无法提供真正的互斥保证——一个持有锁的进程 GC 停顿超过租约，锁过期释放，进程醒来后仍以为自己在临界区里。工程结论是：**分布式锁只适合「低频、短临界区、互斥失败后果可控」的场景**（缓存重建、定时任务互斥）；它不能用来保护「高频且正确性绝对关键」的操作（如扣库存）。后者的正确性必须靠数据库行锁、原子 UPDATE、乐观锁这类单一事实源上的原子操作保证——这正是正确性章节「单机并发正确性」与「设计练习：秒杀下单的库存扣减」的领地。把分布式锁当数据库事务用，是把性能工具错当成正确性工具。",
    },
    {
      type: "paragraph",
      text: "**原子计数与 Lua：Redis 的原子性边界。**Redis 的 INCR/DECR（以及 EXPIRE、SETNX 等）是**单命令原子**：Redis 是单线程执行命令的模型（针对单实例），一条命令从读到写不会被并发命令打断。因此 INCR 天然适合「高频计数」——闪购详情页的浏览次数、验证码发送频控（INCR + EXPIRE 实现窗口计数），一次调用完成「读当前值、加一、写回」，没有竞态，单实例吞吐通常可达数万到十万级 QPS（量级示意，随实例规格与网络状况浮动）。注意边界：INCR 的原子性只覆盖**一条命令**。需要「读几个 key、做判断、再写」这类多步操作时，单条命令不够，要用 **Lua 脚本**——Redis 以原子方式执行整个脚本，脚本运行期间其它命令不会被插入。典型例子是库存预扣的雏形：「若当前库存 > 0 则 DECR 并返回成功，否则返回失败」，在 Lua 里一次原子完成。",
    },
    {
      type: "code",
      title: "库存预扣雏形（Lua 原子判断+扣减，概念示意）",
      language: "text",
      code: "-- KEYS[1]=stock key, ARGV[1]=扣减量\n-- 返回 1 表示扣减成功，0 表示库存不足\nlocal cur = tonumber(redis.call(\"GET\", KEYS[1]) or \"0\")\nif cur >= tonumber(ARGV[1]) then\n    redis.call(\"DECRBY\", KEYS[1], ARGV[1])\n    return 1\nend\nreturn 0",
    },
    {
      type: "callout",
      variant: "warning",
      title: "把库存扣减放进 Redis = 把账本放进缓存",
      body: "Lua 预扣只能在 Redis 内存里保证原子，它解决不了：Redis 宕机丢数据（持久化策略之外的内存丢失）、Redis 与数据库库存两个账本之间的一致性、以及扣了 Redis 库存后数据库订单失败时的回补。闪购的正确性章节结论是：**真实库存账本留在数据库，Redis 预扣只作为流量闸门与体验层**（先挡掉大部分抢不到的人），最终扣减以数据库事务为准；两者之间用对账与补偿收敛。原子计数适合的用途是浏览数、频控、合并写这类「丢了可容忍、无需跨库对齐」的计数——不要把 Redis 的原子性误当成持久化的正确性。",
    },
    {
      type: "paragraph",
      text: "**Redis 的定位边界**——一句话概括 Redis 在闪购里能当什么、不能当什么（见下表），这是本课要带走的核心判断力：",
    },
    {
      type: "table",
      caption: "Redis 在闪购里的角色定位",
      headers: ["角色", "用法示例", "正确性底线", "边界在哪"],
      rows: [
        ["缓存（主要）", "商品详情、活动配置的 Cache-Aside", "DB 是事实源，缓存可旧、可丢、可重建", "不能容忍旧值的数据不入缓存"],
        ["热点计数器/频控", "浏览数、发送频控、限流计数", "丢失可容忍、可合并", "正确性关键的账本不入 Redis"],
        ["分布式锁", "缓存重建互斥、定时任务互斥", "低频、短临界区、失败后果可控", "不用于高频强正确性互斥（靠 DB 原子操作）"],
        ["持久化存储（✗）", "——", "——", "Redis 不是数据库：无事务回滚、持久化有丢失窗口、无 SQL 分析能力——存储选型见 database 章节"],
      ],
    },
    {
      type: "paragraph",
      text: "至于 Redis 自身的高可用形态——主从 + 哨兵负责故障自动切换，集群负责海量 key 的水平分散——本课只给定位：**它们是让「缓存/计数/锁」这层服务更稳的基础设施，不是正确性工具**。主从切换会丢最近未复制的写（锁与预扣随之失效，回到上面的警告），集群分片则与第 4 课的单 key 单点问题直接相关。配置细节属于运维领域，以官方文档为准；本课要带走的是判断力：把 Redis 用在性能路径上，但永远只在「错误后果可控」的地方依赖它的原子性。",
    },
    {
      type: "quiz",
      question: "以下哪种场景适合使用 Redis 分布式锁？",
      options: [
        "两个实例互斥执行「扣减同一商品库存并落库」的高频下单操作",
        "防止多个实例同时对同一个已过期的缓存 key 回源重建（低频、临界区几毫秒）",
        "保证跨库转账的原子性",
        "替代数据库事务实现订单创建的强一致",
      ],
      answer: 1,
      explanation: "分布式锁适合低频、短临界区、互斥失败后果可控的场景——缓存重建正符合：临界区只是查库写缓存，最坏后果是重复回源一次。高频扣库存需要数据库行锁/原子 UPDATE 保证正确（选项 A），跨库原子性与订单强一致属于数据库事务/分布式事务范畴（C、D），都不是 Redis 锁能可靠提供的。",
    },
    {
      type: "keypoints",
      items: [
        "先更库再删缓存仍有窗口：删除可能失败（对策：重试/异步可靠删除）、读回填可能晚于删除（对策：延迟双删压低，binlog 订阅统一删除）；TTL 永远是最终兜底。",
        "分布式锁的可靠形态 = SET NX EX（原子获取+租约）+ 续租/看门狗；锁只用于低频短临界区互斥。",
        "Redlock 无法在网络分区与进程停顿下保证绝对互斥——不迷信；高频强正确性互斥交给数据库原子操作。",
        "Redis 原子性边界：单命令原子（INCR/DECR）与 Lua 脚本原子，覆盖不了跨 Redis/数据库的一致性。",
        "Redis 是缓存/计数器/锁，不是数据库；正确性账本留在数据库，Redis 只处理「错误后果可控」的部分。",
      ],
    },
  ],
};
