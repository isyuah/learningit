/* ==================================================================
 * 课时：连接池与高并发连接管理（mysql-connection-pool）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  "slug": "mysql-connection-pool",
  "courseSlug": "database-mysql",
  "title": "连接池与高并发连接管理",
  "summary": "理解为什么每个数据库连接都很贵、连接池如何复用连接，以及如何用「不贪大」的池把并发控制住。",
  "minutes": 18,
  "kind": "reading",
  "blocks": [
    {
      "type": "paragraph",
      "text": "一个很容易被忽略的瓶颈藏在「连接」上。很多人以为「数据库慢 = SQL 慢」，但在高并发系统里，打开连接本身的开销、以及连接数量超过服务器能力造成的排队，可能才是真正的减速来源。这一课讲连接池：为什么连接是昂贵的资源、连接池为什么存在、池该设多大、以及监控什么信号能提示你「池子不够用了」。它属于工程实践——MySQL 本身的 SQL 再快，连接管理不当也会把你拖垮。"
    },
    {
      "type": "heading",
      "text": "为什么一个数据库连接很贵？"
    },
    {
      "type": "paragraph",
      "text": "在 MySQL 里，每一个客户端连接都对应服务器端的一份真实资源：MySQL 会为连接分配一个线程（可以用线程池优化，但连接=开销的本质不变）、为该会话分配内存与缓存，并完成一次完整的建连握手（认证、字符集协商、可能的 TLS 协商）。也就是说，「建立连接」不是一条轻量命令，而是一次成本不低的往返。"
    },
    {
      "type": "list",
      "items": [
        "每次建连都要做认证与握手，有真实的网络与 CPU 开销",
        "服务器为每个连接维护一个线程，线程数和内存占用随连接数增长",
        "MySQL 对同时存在的连接数有硬上限：`max_connections`",
        "连接长期闲置不关，也会空占资源、挤占 `max_connections`"
      ]
    },
    {
      "type": "callout",
      "variant": "note",
      "title": "max_connections 具体值随版本与配置变化",
      "body": "`max_connections` 的默认值与上限在不同 MySQL 版本、发行版和配置里并不相同，而且经常被运维调高。这里不给你一个「默认应该是多少」的假数字——你要记住的是机制：它是服务器能同时容纳的连接数的硬上限，一旦达到，新的连接请求会被拒绝或排队（报错通常类似 \"Too many connections\"）。查看当前值可以用 `SHOW VARIABLES LIKE 'max_connections';` 和 `SHOW STATUS LIKE 'Threads_connected';`。"
    },
    {
      "type": "paragraph",
      "text": "想象一个没有连接池的应用：每个请求进来都新建连接、用完就关闭。高并发下意味着频繁地握手、认证、分配线程再释放，而且并发的连接数会瞬间冲到很高，可能直接撞上服务器的 `max_connections`。这就是「没有连接池」的下场。"
    },
    {
      "type": "heading",
      "text": "连接池：复用闲置连接并设上限"
    },
    {
      "type": "paragraph",
      "text": "连接池（connection pool）的核心是「复用」+「限流」两层用意：一边在应用启动时（或首次需要时）建立一小批连接放进池子，需要时从池里借一条、用完归还，避免反复新建；另一边给池子的总连接数设一个上限，让并发连接数被池「钳」住，不会无限制地扑向数据库。"
    },
    {
      "type": "code",
      "title": "连接池的使用模型（伪代码）",
      "language": "text",
      "code": "// 应用侧视角（以常见连接池为例）\n// 1. 初始化时建一小批连接（比如 10 条）放进池\npool = new ConnectionPool(host, maxSize=10, minIdle=2, timeout=5s);\n\n// 2. 每次要操作数据库：借一条，用完归还，而不是新建+关闭\nfor each request:\n    conn = pool.borrow();      // 有闲置直接拿去，没有且未达上限则新建\n    try:\n        res = conn.execute(sql);\n    finally:\n        pool.release(conn);    // 归还，供下个请求复用\n\n// 3. 连接很宝贵：不要每个请求都 new 一条连接再 close"
    },
    {
      "type": "paragraph",
      "text": "这个免费伪代码展示的是「模型」而非某一种具体语言/框架的 API——无论 Java 的 HikariCP、Go 的 database/sql 连接池，还是其它语言/ORM 的池，背后都是同一件事：借 → 用 → 还，池负责维护一批可复用的连接并限制总数。重点理解机制，而不是背诵某个库的调用。"
    },
    {
      "type": "heading",
      "text": "池子不是越大越好：CPU 与「超额订阅」"
    },
    {
      "type": "paragraph",
      "text": "初学者常犯的错误是「把池设得很大」——以为连接越多并发行越好。实际并非如此：数据库是 CPU 密集的服务，真正能并行推进的查询数量受 CPU 核数限制。池过大时多数连接只是排队等待 CPU，反而因为「每个连接都占一个线程/内存」和上下文切换而把吞吐拖垮。这就是「超额订阅（oversubscription）」：承诺的并发远超硬件能真正并行处理的量，等于主动制造排队。"
    },
    {
      "type": "paragraph",
      "text": "因此池的大小有个广为流传的经验法则：`N_core * 2 + 1`（N_core 是数据库可用 CPU 核数的估计）。这个公式背后的直觉是「别让并发查询数明显超过 CPU 能同时干的活」，并且留一点余量给阻塞。它只是个起点，不是精确解——如果是 I/O 密集、锁等待多的负载，合适的池可能更接近「每个请求同时占用的连接数 × 需要的并发」，而不是核数的小常数倍。"
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "从 CPU-bound 的经验法则出发，然后实测",
      "body": "`N_core * 2 + 1` 是一个靠谱的「起步值」，因为它反映了「数据库查询本质是 CPU 密集、不该超额订阅」这个事实。但真实的最佳值依赖你的工作负载（读多写多、有没有长事务/长查询、锁竞争重不重）。正确做法：用经验法则起步，加压测，观测数据库侧的 CPU 利用率和连接等待时间，再上下微调。"
    },
    {
      "type": "heading",
      "text": "超时、退避与监控信号"
    },
    {
      "type": "paragraph",
      "text": "连接池还需要几个「保命」机制。**获取超时**：如果一个请求向池里借连接，等了太久还没借到，说明池已被借空——这时应抛出「取连接超时」而不是无限等待。**连接校验/重建**：长期闲置的连接可能被服务器断开（例如超过 `wait_timeout`），所以借连接前常要做有效性检查，失效就重建。**退避**：当连接获取失败或数据库忙时，通过重试间隔递增（退避）避免客户端同时狠撞数据库。"
    },
    {
      "type": "code",
      "title": "监控信号：什么时候说明池子顶不住了",
      "language": "sql",
      "code": "-- 数据库侧：看当前连接数离上限多近（Max_used_connections 是历史峰值）\nSHOW STATUS LIKE 'Threads_connected';\nSHOW STATUS LIKE 'Max_used_connections';\nSHOW VARIABLES LIKE 'max_connections';\n\n-- 应用侧观察两类现象：\n-- 1) 频繁出现「获取连接超时 / Connection pool exhausted」\n-- 2) 大量连接处于这种状态：SELECT ... 正常但迟迟不返回" 
    },
    {
      "type": "paragraph",
      "text": "这里有个非常实用的诊断逻辑：**「等连接等太久」通常不是池子太小，而是池里的连接被慢查询/长事务占住了**。因为每条连接在同一时刻只能执行一条 SQL，如果一批连接都被一条跑得极慢的查询或一个超长事务占住不放，其它请求自然借不到连接。「池耗尽」往往是症状，慢查询/长事务才是病根。上一课的定位方法（慢查询日志 / SHOW PROCESSLIST）在这里正好用上：去看看哪些连接进程占着不还。"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "连接耗尽优先查「被什么占着」，而不是盲目调大",
      "body": "当你看到大量「取连接超时」，第一反应不该是简单地把池调大——先确认这些连接是不是被几条慢 SQL 或长事务卡住了。如果是，正确动作是修那几条慢查询、缩短长事务（回到上一课的定位流程），而不是无限加连接。盲目加大池只是把「连接不够」换成「CPU 排队+内存占用更高」，问题往往更糟。"
    },
    {
      "type": "heading",
      "text": "把连接池放进你已有的心智模型"
    },
    {
      "type": "paragraph",
      "text": "把连接池和前面的知识连起来：`max_connections` 是服务器的连接硬上限，连接池是应用侧「复用 + 设上限」的一层，两者共同把并发连接数控制住。真正的瓶颈往往在「一条连接同一时间只能执行一条语句」这个事实——所以长事务和慢查询会同时消耗「连接」与「锁」两种稀缺资源。理解了连接、事务与慢查询这三者的相互作用，你才真正知道高并发下该怎么排障。"
    },
    {
      "type": "list",
      "items": [
        "连接昂贵：建连要握手认证、服务器为每连接分配线程与内存、受 max_connections 限制",
        "连接池 = 复用闲置连接 + 设总连接上限，避免每个请求新建连接",
        "池不是越大越好：数据库是 CPU 密集，核数 * 2 + 1 是常见起步经验，避免超额订阅",
        "配好获取超时、连接有效期/重建、失败退避",
        "「取连接超时」多半是慢查询/长事务占着连接不还，先查病根而非盲目调大池"
      ]
    },
    {
      "type": "quiz",
      "question": "高并发下频繁出现「连接池耗尽 / 获取连接超时」，最常见且最该先做的排查是？",
      "options": [
        "直接把连接池调得非常大，让更多请求并发执行",
        "先查是否存在慢查询或在超长事务里占着连接不放的会话，再决定是否调池",
        "关闭连接池，改让每个请求新建一个连接",
        "把 max_connections 调到无限大"
      ],
      "answer": 1,
      "explanation": "连接耗尽通常是慢查询/长事务占住连接的症状而非病根。应先定位占用连接的会话（慢查询日志/SHOW PROCESSLIST），修复慢 SQL 与长事务；盲目调大池只会带来 CPU 排队与更高内存占用。"
    },
    {
      "type": "exercise",
      "title": "为一个疑似连接耗尽的问题开排查方案",
      "description": "你的订单应用高峰期抛「连接池耗尽」。请列出你要看的数据库侧指标与应用侧现象（至少各两个），并说明你判断「该修复慢查询」还是「该调大池」的依据是什么。",
      "hint": "数据库侧看 Threads_connected 与 max_connections 的差距、看 SHOW PROCESSLIST 里长时间运行的会话；应用侧看取连接超时频率与是否有长事务。核心分叉点：连接是被慢查询「长时间占用」还是被高频请求「短期借空」。"
    },
    {
      "type": "keypoints",
      "items": [
        "每个数据库连接都很贵：握手认证 + 每连接一个线程/内存 + max_connections 硬上限",
        "连接池复用闲置连接并限定总数，借→用→还取代每请求新建",
        "池勿贪大：数据库是 CPU 密集，用核数 * 2 + 1 起步，警惕超额订阅",
        "池要配获取超时、连接效期/重建、失败退避",
        "连接被占空常是慢查询/长事务的符号，先修源头再考虑调池"
      ]
    }
  ]
};
