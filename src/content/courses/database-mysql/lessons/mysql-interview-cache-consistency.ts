/* ==================================================================
 * 课时：面试——数据库与缓存一致性（mysql-interview-cache-consistency）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 本节把「数据库 + 缓存（Redis）」这一后端面试极高频话题整理成
 * 答题框架：缓存旁路、双写一致性、缓存穿透/击穿/雪崩及应对。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-interview-cache-consistency",
  courseSlug: "database-mysql",
  title: "面试：数据库与缓存一致性",
  summary: "缓存旁路（Cache-Aside）、双写一致性与四类缓存问题（穿透/击穿/雪崩/双写不一）的成因与对策。",
  minutes: 22,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "「先查缓存、miss 再查数据库」是现代后端最常见的架构，几乎所有后端面试都会追到缓存一致性问题。这一节回答四个高频问题：为什么要加缓存、怎么保证数据库与缓存一致、以及缓存穿透/击穿/雪崩分别是什么、怎么应对。它虽然不是 MySQL 自身的机制，但和数据库绑定极深，是「数据库相关」面试的必修。",
    },
    {
      type: "heading",
      text: "高频题 1：为什么需要缓存，怎么加？",
    },
    {
      type: "paragraph",
      text: "答题框架：缓存的目的是把「高频读、数据变化不大」的热点数据放在内存里（如 Redis），避免每次都打到数据库，从而降低数据库负载、提升读吞吐与响应。最常见模式是**缓存旁路（Cache-Aside / 旁路缓存）**：读时先查缓存，命中直接返回；未命中则查数据库、写回缓存、返回。写的时候一般是「先更新数据库，再删除（或更新）缓存」。这块只要答出「旁路缓存 + 读 miss 回源 + 写后失效」这条主线就算过关。",
    },
    {
      type: "code",
      title: "Cache-Aside 读流程",
      language: "text",
      code: "读 product(id=1)：\n  1. 查缓存 key=product:1\n  2. 命中 → 直接返回\n  3. 未命中 → 查数据库 SELECT ... WHERE id=1\n  4. 写回缓存 key=product:1 = 结果\n  5. 返回\n\n写 product(id=1)：\n  1. 更新数据库\n  2. 删除缓存 key=product:1  （让下次读重新回源）",
    },
    {
      type: "heading",
      text: "高频题 2：数据库与缓存如何保持一致（双写一致性）？",
    },
    {
      type: "paragraph",
      text: "答题框架：先承认一个事实——只要用了缓存，缓存与数据库之间就存在一个「短暂不一致」的窗口，目标是把它压到极小、且不能让旧数据长期残留。最常用且被广泛接受的做法是**Cache-Aside + 删除缓存（而不是更新缓存）**：写操作先改数据库，再把缓存 key 删掉，让读时重新回源得到最新值。为什么删而不是直接更新？因为更新缓存要处理并发（两个写请求、读旧写新等），而「删除后由读方重新填充」更简单、出错面小。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "追问点：为什么是「先更新数据库，再删缓存」？",
      body: "顺序很重要。如果先删缓存、再更新数据库，中间读请求会 miss 并把旧数据库值写回缓存，导致旧数据回填。而「先更新数据库、再删缓存」通常配合短 TTL 兜底：即使删除失败或中间有并发，缓存一般也不至于长期脏。更严格的做法是删除失败时走重试（如消息队列做异步重试删），或结合版本号，保证最终一致。面试答出「先库后删 + 删除失败重试 + TTL 兜底」基本就完整了。",
    },
    {
      type: "heading",
      text: "高频题 3：缓存穿透是什么？怎么应对？",
    },
    {
      type: "paragraph",
      text: "答题框架：缓存穿透 = 查询一个「缓存和数据库里都不存在」的 key（例如恶意构造的 id=-1 或随机 id），每次都 miss、每次都打到数据库，数据库被无效查询打穿。应对：① 缓存空值：对不存在的 key 也缓存一个空结果 + 短 TTL，避免每次都打库；② 布隆过滤器（Bloom Filter）：在缓存前用布隆判断 key 是否存在，不存在直接拦截，减少对数据库的无效请求；③ 加强参数校验与风控，拦截明显非法/超范围的 id。",
    },
    {
      type: "heading",
      text: "高频题 4：缓存击穿是什么？怎么应对？",
    },
    {
      type: "paragraph",
      text: "答题框架：缓存击穿 = 某个「热点 key」在缓存**过期的那一瞬间**，大量并发请求同时 miss，一起打到数据库，把数据库瞬时打垮。它和穿透的区别：穿透打的是「不存在的 key」，击穿打的是「一个极热、恰好过期的 key」。应对：① 热点数据不过期（逻辑过期，后台异步刷新）；② 加**互斥锁 / 分布式锁**：只有一个请求去查库并回填缓存，其余请求等待或降级；③ 缓存预热 + 尽量不让热点 key 同一时刻过期。",
    },
    {
      type: "heading",
      text: "高频题 5：缓存雪崩是什么？怎么应对？",
    },
    {
      type: "paragraph",
      text: "答题框架：缓存雪崩 = 大量 key **同时过期**，或缓存服务整体宕机，导致大量请求同时落到数据库，数据库被压垮。它和击穿的区别：击穿是单个热点 key，雪崩是一批 key 或整个缓存不可用。应对：① 给缓存 TTL 加随机抖动（不让大量 key 同一秒过期）；② 多级缓存（本地缓存 + 集中缓存），某层挂了还有兜底；③ 缓存服务高可用（Redis 主从/哨兵/Cluster）；④ 限流降级：数据库层面做保护，或在到库前限流。",
    },
    {
      type: "table",
      caption: "三类缓存「问题」速记",
      headers: ["问题", "本质", "典型应对"],
      rows: [
        ["缓存穿透", "查询不存在的 key，每次都 miss 打库", "缓存空值、布隆过滤器、参数校验"],
        ["缓存击穿", "一个热点 key 恰好过期，并发打库", "热点不过期/逻辑过期、互斥锁、预热"],
        ["缓存雪崩", "大量 key 同时过期或缓存不可用，打库", "TTL 随机抖动、多级缓存、高可用、限流降级"],
      ],
    },
    {
      type: "heading",
      text: "小结：一致性 vs 可用性的权衡",
    },
    {
      type: "paragraph",
      text: "面试答缓存题的核心是体现「权衡」：强一致需要牺牲可用性（或几乎每次回源），而缓存本质是用「短时不一致」换「高可用 + 高性能」。所以标准答案是一条**最终一致**的路线：更新数据库 → 删除缓存 → 失败重试 → TTL 兜底 → 必要时版本号。同时能区分穿透/击穿/雪崩三个词，给出各自的针对性解法，基本就能过关。",
    },
    {
      type: "heading",
      text: "自测",
    },
    {
      type: "quiz",
      question: "「缓存穿透」指的是？",
      options: [
        "某个热点 key 在缓存过期瞬间，大量请求一起打数据库",
        "查询缓存和数据库里都不存在的 key，每次都 miss 打到数据库",
        "大量 key 同时过期导致数据库被压垮",
        "缓存服务整体宕机",
      ],
      answer: 1,
      explanation: "穿透 = 查询不存在的 key 反复打库；A 是击穿（热点 key 过期瞬间），C/D 是雪崩方向。",
    },
    {
      type: "quiz",
      question: "Cache-Aside 模式下，更新一条数据最推荐的做法是？",
      options: [
        "先删缓存，再更新数据库",
        "只更新缓存，不碰数据库",
        "先更新数据库，再删除缓存，并对删除失败做重试、配 TTL 兜底",
        "更新数据库后，把缓存 key 设成永久不过期",
      ],
      answer: 2,
      explanation: "「先库后删 + 删除失败重试 + TTL 兜底」是 Cache-Aside 的标准最终一致做法。A 可能让旧值回填缓存；B/D 会长期不一致。",
    },
    {
      type: "quiz",
      question: "下面哪个做法最适合缓解「缓存击穿」？",
      options: [
        "给所有 key 加一个完全相同的 TTL",
        "对热点 key 采用不过期/逻辑过期 + 互斥锁，只允许一个请求回源",
        "把热点 key 缓存空值",
        "关掉缓存，全部走数据库",
      ],
      answer: 1,
      explanation: "击穿是热点 key 过期瞬间并发打库，互斥锁 / 热点不过期是最直接应对。A 反而可能加剧同时过期；C 是防穿透；D 丢掉了缓存价值。",
    },
    {
      type: "keypoints",
      items: [
        "缓存价值：热点读 + 降库负载；模式：Cache-Aside（读 miss 回源、写后删缓存）",
        "双写一致性：先更新数据库再删缓存 + 删除失败重试 + 短 TTL 兜底 = 最终一致",
        "缓存穿透：不存在的 key 反复打库 → 缓存空值 / 布隆过滤器 / 参数校验",
        "缓存击穿：热点 key 过期瞬间并发打库 → 热点不过期 / 互斥锁 / 预热",
        "缓存雪崩：大量 key 同时过期或缓存不可用 → TTL 随机抖动 / 多级缓存 / 高可用 / 限流降级",
        "本质是「最终一致换高性能」的权衡，别追求强一致",
      ],
    },
  ],
};
