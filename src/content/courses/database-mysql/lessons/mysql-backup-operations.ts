/* ==================================================================
 * 课时：备份、恢复与运维基础（mysql-backup-operations）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  "slug": "mysql-backup-operations",
  "courseSlug": "database-mysql",
  "title": "备份、恢复与运维基础",
  "summary": "备份不是「把数据拷一份」，而是「能在灾难发生那天，把数据恢复到你需要的那个时刻」。",
  "minutes": 18,
  "kind": "reading",
  "blocks": [
    {
      "type": "paragraph",
      "text": "前面学的索引、事务、锁都在处理一件事：让数据库在平时「跑得快、跑得对」。这一节讨论的却是那个平时几乎没人想起、出事时唯一能救你的能力——备份与恢复。核心认知要先立起来：**备份只有在「你能成功把它恢复出来」那一刻才算数。** 一个从未被验证过能恢复的备份，本质上只是一堆占着磁盘的文件。"
    },
    {
      "type": "quote",
      "text": "A backup you haven't tested restoring isn't a backup.（一个你没有验证过能恢复的备份，根本不算备份。）",
      "source": "数据库运维社区广为流传的一句警示"
    },
    {
      "type": "heading",
      "text": "为什么需要备份：最坏的场景总会发生"
    },
    {
      "type": "paragraph",
      "text": "备份保护的案例多种多样：有人手滑执行了 `DELETE FROM orders WHERE ...` 少了 WHERE 子句；有应用 bug 把整张表更新成了坏数据；有机器磁盘损坏、机房断电、甚至被勒索加密。这些场景里，索引、事务、慢查询优化全都帮不上忙——唯一能把数据找回来的手段，是从备份恢复。备份里存的是「历史某一时刻的数据 + 从那之后发生的所有变更」，让你能回到那个时刻或之后的任一瞬间。"
    },
    {
      "type": "list",
      "items": [
        "误操作：删了或改了不该删不该改的数据",
        "应用缺陷：一个 bug 在全表上写入了错误数据",
        "基础设施故障：磁盘损坏、断电、主机失效",
        "外部事件：勒索软件、机房灾难"
      ]
    },
    {
      "type": "heading",
      "text": "逻辑备份 vs 物理备份"
    },
    {
      "type": "paragraph",
      "text": "MySQL 的备份按「备份的是什么」分两大类：逻辑备份备份的是「导出成 SQL 语句的数据与结构」；物理备份备份的是「数据库在磁盘上的原始数据文件」。两者各有取舍，理解它们才能按场景选择。"
    },
    {
      "type": "table",
      "caption": "逻辑备份 vs 物理备份",
      "headers": ["维度", "逻辑备份（mysqldump 等）", "物理备份（文件快照 / xtrabackup 风格）"],
      "rows": [
        ["备份内容", "导出为可读的 SQL 语句（CREATE TABLE + INSERT）", "直接复制数据目录下的原始文件"],
        ["特点", "可读、可跨版本/跨平台迁移、可只导某张表", "速度快、能带二进制日志信息，适合大库"],
        ["缺点", "大库导出/导入慢，恢复通常也更慢", "文件不透明、依赖存储引擎与版本、恢复较不易读"],
        ["典型工具", "mysqldump", "文件系统快照、Percona XtraBackup 等"],
        ["最适合", "中小库、迁移、只导部分表/结构", "大库、追求快速备份与快速恢复的在线环境"]
      ]
    },
    {
      "type": "code",
      "title": "mysqldump：最常用的逻辑备份",
      "language": "text",
      "code": "# 备份整个 shop 库为 SQL 文件\nmysqldump --single-transaction -u shop_backup -p shop > shop_backup.sql\n\n# 只备份某张表的表结构（不带数据）\nmysqldump --no-data -u shop_backup -p shop product > product_schema.sql\n\n# 恢复：把 SQL 文件再导回去\nmysql -u shop_restore -p shop < shop_backup.sql"
    },
    {
      "type": "callout",
      "variant": "note",
      "title": "mysqldump 与 --single-transaction",
      "body": "对 InnoDB，`--single-transaction` 让 mysqldump 在一个事务里做一致性快照读，导出过程中不阻塞线上写入（注意：`--single-transaction` 默认对已提交数据做快照；涉及 ALTER TABLE / 某些特殊情况仍需留意，版本行为会略有差异）。不带它时 mysqldump 可能会对表加锁。这里点到为止，重点是你理解「逻辑备份 = 导出成 SQL」这一本质。"
    },
    {
      "type": "heading",
      "text": "全量备份 vs 增量备份"
    },
    {
      "type": "paragraph",
      "text": "备份覆盖的范围也可以分「全量」和「增量」。全量备份（Full Backup）把整份数据完整拷一份，是恢复的基座；增量备份（Incremental Backup）只记录自上次备份以来发生的变化。全量恢复起来简单可靠，但每天全量会越来越重；增量备份快，但恢复时要把「一个全量 + 一串增量」按顺序叠加，链条越长越复杂、越容易出错。"
    },
    {
      "type": "list",
      "items": [
        "全量备份：完整快照，恢复时一步到位；适合作为恢复基座",
        "增量备份：只记变化，快但恢复要按顺序叠加，链条长则风险高",
        "常见策略：周期性全量（如每周）+ 期间的增量/日志，把恢复点维持在你可接受的丢失范围内"
      ]
    },
    {
      "type": "heading",
      "text": "binlog：把恢复推进到「任意时间点」"
    },
    {
      "type": "paragraph",
      "text": "全量备份只能把你带到「备份那一瞬间」。但真实场景往往是「昨晚备份之后，今天下午两点误删了一批数据」——如果你只有全量备份，就只能回到昨晚，丢掉今天一整天的数据。这正是 binlog（二进制日志）的价值：它记录了数据库上发生过的每一次数据变更（这正是你在存储引擎与日志那一章学过的内容）。把「一份全量备份」+「从备份时刻到事故时刻的 binlog」重放，就能把数据恢复到任何一个精确的时间点，即**时间点恢复（Point-in-Time Recovery，PITR）**。"
    },
    {
      "type": "code",
      "title": "恢复到一个时间点（思路）",
      "language": "text",
      "code": "# 1) 先用全量备份恢复到一个基座时间点\nmysql -u shop_restore -p shop < shop_full_backup.sql\n\n# 2) 再把从备份时刻起、到事故发生时刻为止的 binlog 重放到目标库\n#    （按时间点或按日志坐标停止，把误操作之前的所有变更都重放回来）\nmysqlbinlog --stop-datetime=\"2026-02-20 14:00:00\" \\\n    mysql-bin.000123 mysql-bin.000124 ... | mysql -u shop_restore -p shop"
    },
    {
      "type": "callout",
      "variant": "tip",
      "title": "binlog 开着才有时间点恢复",
      "body": "时间点恢复依赖 binlog 被持续记录。如果你的 MySQL 没开 binlog，或 binlog 被覆盖 / 只保留很短，那么任何全量备份之后的数据都无法精确找回。所以工程上「全量备份 + 持续的 binlog」是一对搭档：全量决定「从哪开始」，binlog 决定「能精确追到哪」。"
    },
    {
      "type": "heading",
      "text": "恢复测试：备份的最后一公里"
    },
    {
      "type": "paragraph",
      "text": "为什么「没测过的备份不算备份」？因为备份成功 ≠ 恢复成功。可能备份文件损坏、可能导入时报错、可能备份时漏了某些表、可能 binlog 没有落在备份之后正确衔接。这些问题只有在「你真的把一个备份放到一台空库上完整恢复并验证数据一致」时才会暴露。所以："
    },
    {
      "type": "list",
      "items": [
        "定期（至少每周或每月）在隔离环境做一次完整恢复演练，不只是备份",
        "恢复后做数据校验：对比行数、抽样字段、跑关键查询，确认数据回来且正确",
        "演练要包括「全量 + binlog 时间点恢复」的完整链路，而不只是把全量导回去",
        "把恢复步骤写成可执行的标准化流程文档，别等事故当天才现场摸索"
      ]
    },
    {
      "type": "heading",
      "text": "RPO 与 RTO：用数字把备份目标说清楚"
    },
    {
      "type": "paragraph",
      "text": "谈备份不能只说「我们有备份」，要用两个指标量化我们能接受的程度。理解这两个概念后，备份策略的设计就有了明确方向。"
    },
    {
      "type": "definition",
      "term": "RPO（Recovery Point Objective，恢复点目标）",
      "definition": "能接受丢失多少数据，即恢复到「多长时间以内、哪个时间点之前」的状态。RPO 越小，意味着允许丢的数据越少。binlog 保留得越长、备份频率越高，RPO 可以越小。"
    },
    {
      "type": "definition",
      "term": "RTO（Recovery Time Objective，恢复时间目标）",
      "definition": "能接受系统停机多久，即从故障到业务恢复的时间上限。RTO 越小，要求恢复越快。物理备份/快照通常比从逻辑备份慢慢导入恢复更快，从而能支撑更小的 RTO。"
    },
    {
      "type": "table",
      "caption": "把备份策略对应到指标",
      "headers": ["策略选择", "对 RPO 的影响", "对 RTO 的影响"],
      "rows": [
        ["每天全量、无 binlog", "RPO≈1 天：最多丢昨天之后一天的数据", "一次全量恢复即可，RTO 取决于库大小"],
        ["每天全量 + 开启并保留 binlog", "RPO 极小：可通过时间点恢复找回备份后任何变更", "恢复要做全量 + 重放 binlog，RTO 略长但更精确"],
        ["频繁物理快照（在线大库）", "快照间隔即 RPO 上界，可做得较小", "物理恢复通常比逻辑导入快，RTO 更友好"]
      ]
    },
    {
      "type": "paragraph",
      "text": "RPO 与 RTO 是「业务能否接受」的权衡：想把 RPO 压到几乎为零（分秒不丢），就要付出持续 binlog 与更频繁备份的运维成本；想把 RTO 压到最短（秒级恢复），可能要付出备份与恢复链路更贵的代价。设计备份方案，本质是在可承受的成本下，把「最多丢多少」和「最长停多久」压到业务能接受的范围。"
    },
    {
      "type": "callout",
      "variant": "warning",
      "title": "备份文件本身也要保护好",
      "body": "备份几乎是数据库的最后一条命，因此备份文件要：与生产环境异地/异介质存放（防止同一机房一起灾）、加密与访问受控（防止备份泄露数据）、保留足够历史的版本（防止旧备份也被坏数据污染）、定期校验文件的完整性与可恢复性。否则备份本身可能就是下一个脆弱点。"
    },
    {
      "type": "quiz",
      "question": "某电商昨晚有一份全量备份，但今天下午 14:00 发生了误删。要从「昨晚的备份」恢复到「今天 13:59 的精确状态」，还需要什么？",
      "options": [
        "不需要别的，全量备份就能恢复到任意时刻",
        "需要从头到尾重新生成一份今早的新全量备份",
        "需要把昨晚全量恢复后，再重放昨晚备份时刻到 13:59 的 binlog",
        "需要物理备份（xtrabackup）才能做到时间点恢复"
      ],
      "answer": 2,
      "explanation": "全量备份只能回到备份那一瞬；要从备份后恢复到精确时刻，必须依赖 binlog（日志）。重放「备份时刻→13:59」的 binlog 才能得到该时间点的数据。这与备份形式是逻辑还是物理无关，只要 binlog 记录完整即可。"
    },
    {
      "type": "quiz",
      "question": "「RPO」描述的是哪个指标？",
      "options": [
        "从故障到业务恢复需要多长时间",
        "业务能接受丢失多少数据（恢复到哪个时间点之前）",
        "备份文件占用多少磁盘",
        "全量备份每周做几次"
      ],
      "answer": 1,
      "explanation": "RPO（恢复点目标）回答「最多能丢多少数据、恢复点最早到哪」；RTO 才回答「多久能恢复上线」。两者常被混淆，面试和实际方案里都要分清。"
    },
    {
      "type": "exercise",
      "title": "为 shop 设计一套备份策略并写恢复演练清单",
      "description": "为电商订单库自定义备份策略：确定全量备份频率、如何引入 binlog 支持时间点恢复、并给出你能接受的 RPO 与 RTO。然后写出一份「完整恢复演练」检查清单，覆盖从备份还原到数据校验的每一步。",
      "hint": "结合订单数据的价值：能不能接受丢一天？能不能接受停机两小时？再据此决定全量频率、binlog 保留时长与是否需要物理快照。"
    },
    {
      "type": "keypoints",
      "items": [
        "备份只有在能成功恢复时才有效：没测过的备份不算备份",
        "逻辑备份（mysqldump）导出 SQL，物理备份（文件/xtrabackup）拷原文件，各有取舍",
        "全量是恢复基座，增量/日志负责追平之后的变化",
        "binlog 是时间点恢复的关键：全量决定起点，binlog 决定能精确追到哪",
        "恢复要演练：隔离环境完整还原 + 数据校验，把事故当天的过程提前走通",
        "用 RPO（丢多少）与 RTO（停多久）把备份目标量化，用成本去平衡"
      ]
    }
  ]
};
