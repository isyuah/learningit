/* ==================================================================
 * 课时：聚簇索引与二级索引：回表是怎么发生的（mysql-clustered-secondary）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-clustered-secondary",
  courseSlug: "database-mysql",
  title: "聚簇索引与二级索引：回表是怎么发生的",
  summary: "InnoDB 整表就是一棵聚簇索引，理解二级索引、回表，以及主键选择的后果。",
  minutes: 20,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "上一课我们建立了 B+ 树的心智模型。这一课要把「一张 InnoDB 表」和「索引」画到同一张图上——因为 InnoDB 里，表和数据并不是两回事：整张表本身就是一棵以主键为键、以完整数据行作为叶子的 B+ 树，这棵树就叫聚簇索引（clustered index）。理解了这一点，你才能真正明白「回表」是什么意思，以及为什么「选什么做主键」那么重要。",
    },
    {
      type: "heading",
      text: "InnoDB 表 = 以主键为键的聚簇索引",
    },
    {
      type: "paragraph",
      text: "对 InnoDB 来说，`orders` 表在物理上就是一棵 B+ 树：主键 `id` 作为树的键，叶子节点里存的是这一整行数据（所有列：user_id、status、total_amount、created_at……）。主键一旦确定，这一行在表里的「物理存放次序」也就基本跟着主键走——主键有序，物理上大体有序（逻辑顺序由 B+ 树维护）。",
    },
    {
      type: "code",
      title: "聚簇索引的叶子存整行",
      language: "text",
      code: "聚簇索引（主键 id 为键）\n  根/内部节点：只存主键 id，用来指路\n  叶子节点   ：存 整行完整数据（id + user_id + status + total_amount + created_at）\n\n  [ 10 | 25 | ... ]           ← 内部节点：只存 id\n     |    \\\n  [id=10, user_id=1, status='paid', total=199.00, ...]   ← 叶子：整行\n  [id=25, user_id=3, status='pending', total=59.90, ...]  ← 叶子：整行",
    },
    {
      type: "paragraph",
      text: "所以「通过主键查一行」是最快、最直接的路径：直接用主键值在聚簇树里从根下探到叶子，叶子里就是整行，什么都不缺。这解释了为什么 `WHERE id = 42` 通常比 `WHERE user_id = 42` 还快——前者是聚簇索引直接定位，后者在没有索引时甚至要先做全表扫描。",
    },
    {
      type: "heading",
      text: "没有显式主键时，聚簇键由谁担任",
    },
    {
      type: "list",
      items: [
        "如果你显式定义了主键，就用它作聚簇键。",
        "如果没有主键，但表里有非空且唯一的索引，InnoDB 会选第一个这样的唯一索引作为聚簇键。",
        "如果两者都没有，InnoDB 会生成一个隐藏的、6 字节的自增行 id（row id）作为聚簇键——你查不到它，但它决定了物理排布。",
      ],
    },
    {
      type: "callout",
      variant: "note",
      title: "结论：几乎总要显式定义一个主键",
      body: "让 InnoDB 偷偷造隐藏 row id 或借用唯一索引，会失去对物理排布的控制。所以工程上每张表都该有一个明确的主键——这既是数据建模的规范，也直接决定了聚簇索引的形态。",
    },
    {
      type: "heading",
      text: "二级索引：叶子存「索引列 + 主键」",
    },
    {
      type: "paragraph",
      text: "给 `user_id` 建的那棵 `idx_user` 树，和聚簇索引不一样：它的叶子节点不存整行，只存「user_id + 主键 id」这两样。这种除了聚簇索引之外的辅助索引，就叫二级索引（secondary index）。为什么只要存主键？因为拿到主键之后，就可以回去聚簇索引里把整行捞出来——这个「拿了索引里的主键、再回主表取整行」的动作就叫回表（back to table，有时也按 MySQL 的说法叫 back to clustered index）。",
    },
    {
      type: "code",
      title: "二级索引 vs 聚簇索引的叶子",
      language: "text",
      code: "二级索引 idx_user（键 = user_id）\n  内部节点：只存 user_id 用来指路\n  叶子节点 ：存 (user_id, 主键 id)\n\n  [id=1  , user_id=42]\n  [id=88 , user_id=42]   ← 同一个 user 的多行，按 user_id 排序后聚在一起\n  [id=7  , user_id=7 ]\n\n查询 WHERE user_id=42：\n  ① 在 idx_user 树里定位到 user_id=42 的叶子 → 拿到主键 id=1、id=88\n  ② 再用 id=1、id=88 到聚簇索引里取整行  ← 这一步就是「回表」",
    },
    {
      type: "heading",
      text: "回表的成本：一个完整的「记账」",
    },
    {
      type: "paragraph",
      text: "理解回表的成本，是面试和优化的起点。假设你查「用户 42 的两笔订单完整信息」，走了二级索引然后又回表：",
    },
    {
      type: "table",
      caption: "两种访问方式的成本对比（概念性）",
      headers: ["查询路径", "在索引上做的事", "额外成本", "典型代价量级"],
      rows: [
        ["仅聚簇索引（主键点查）", "直接在聚簇树里下探到叶子", "叶子已是整行，无需额外动作", "只读 1~2 个页"],
        ["二级索引 + 回表", "在 idx_user 树定位，拿到主键", "每命中一行，都要再回聚簇索引按主键取整行", "每行多一次（可能随机的）主键页读取；命中越多越多"],
      ],
    },
    {
      type: "paragraph",
      text: "关键是「回表次数 = 命中的行数」。如果一个 `user_id` 有 500 笔订单，`WHERE user_id = 42` 命中 500 行，就要回表 500 次——每次回表是（大概率随机的）一次聚簇索引页读取。这比只读 500 个「连续的二级索引叶子」要贵得多，因为回表的目标行在聚簇树里的物理位置并不连续。这也是后面「覆盖索引」「索引下推」要解决的问题：想办法少回表、甚至不回表。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "命中很多行时，优化器可能放弃索引",
      body: "如果二级索引命中了一大部分行（例如全表一半以上），MySQL 优化器会发现「回表取整行」的代价接近甚至超过直接全表扫描，于是干脆放弃索引改做全表扫描——这时 EXPLAIN 里的 type 会从 ref/range 变成 ALL。这个判断我们在 EXPLAIN 课详细展开，这里先记住概念：回表多了，索引不一定划算。",
    },
    {
      type: "heading",
      text: "选择主键的后果：整数自增 vs 随机 UUID",
    },
    {
      type: "paragraph",
      text: "因为聚簇索引决定整张表的物理组织，主键的选择会放大到整表性能，而不只是「查得顺不顺」。受控对比一下两种选择：",
    },
    {
      type: "table",
      caption: "不同主键选择的代价",
      headers: ["主键类型", "写入时的物理组织", "对二级索引的影响", "结论"],
      rows: [
        ["自增整数 BIGINT AUTO_INCREMENT", "新行主键递增，追加到树最右侧，页分裂少、写入顺序友好", "二级索引叶子里存的这个主键短小紧凑，占用少", "首选：简单、写入快、索引小，回表也快"],
        ["随机 UUID / 长随机字符串", "主键随机，每次插入都可能落在树中间，频繁页分裂、碎片、写入变慢", "二级索引叶子要存这么长的主键，每个索引都膨胀", "尽量避免作为聚簇键：随机、无序、又大又慢"],
      ],
    },
    {
      type: "paragraph",
      text: "为什么自增整数好？因为新行的主键越来越大，插入时总是追加在聚簇树的最右侧，几乎不会把中间已经排好的页挤裂，写入是「顺序」的；而 UUID 主键随机分布，新行可能插在任何位置，频繁触发页分裂、产生磁盘碎片，写放大明显，而且每个二级索引都要多占一份主键的空间。当然，UUID 也有它的应用场景（如分布式无中心生成、避免暴露自增 id），但若以聚簇索引性能为第一权重，「短、有序、自增」是工程默认解。",
    },
    {
      type: "callout",
      variant: "tip",
      title: "顺带一提：二级索引的设计先别急",
      body: "这一课我们反复强调「不要让查询回表太多次」。解决它的最强工具——覆盖索引（cover index，让所需列都在二级索引里、从而不回表）——会在后面的 mysql-index-advanced 课重点讲。现在只需记住「回表 = 二级索引叶子拿主键 → 回聚簇索引取整行」这一机制，以及它每行一次随机 I/O 的代价模型。",
    },
    {
      type: "quiz",
      question: "关于 InnoDB 的聚簇索引与二级索引，下列说法正确的是？",
      options: [
        "聚簇索引的叶子节点只存主键，不存整行数据",
        "二级索引的叶子节点存「索引列 + 主键」，通过主键回表到聚簇索引才能拿到完整行",
        "二级索引的叶子节点和聚簇索引一样都存完整数据行",
        "InnoDB 的表数据与聚簇索引是两份独立存储，互不相关",
      ],
      answer: 1,
      explanation: "InnoDB 整表就是一棵以主键为键、叶子存整行的聚簇索引；二级索引叶子只存「索引列 + 主键」，要整行必须沿主键回表到聚簇索引。",
    },
    {
      type: "quiz",
      question: "为什么「自增整数主键」通常优于「随机 UUID 主键」作为聚簇键？",
      options: [
        "UUID 无法作为主键",
        "自增主键新行追加在聚簇树右侧，写入顺序友好、页分裂少，且二级索引里存储的主键更短小",
        "自增整数主键可以让表完全不需要二级索引",
        "UUID 会让 InnoDB 强制使用全表扫描",
      ],
      answer: 1,
      explanation: "自增主键插入顺序、写放大少；而随机 UUID 会频繁引发页分裂与碎片，且每个二级索引都要多存一份长的 UUID 键值，故常逊于短小的自增整数主键。",
    },
    {
      type: "exercise",
      title: "描述一次「二级索引 + 回表」的完整过程",
      description: "面对 `orders` 表（主键 id、二级索引 idx_user(user_id)），请用文字走一遍 `SELECT * FROM orders WHERE user_id = 7;` 的执行路径：先在哪个结构找什么、拿到什么、再做什么，以及为什么命中行数增加会让这条查询变贵。",
      hint: "先想官方二级索引叶子存了什么（user_id + 主键 id），拿到主键后再回聚簇索引取完整行；回表是每行一次的主键页读取。",
    },
    {
      type: "keypoints",
      items: [
        "InnoDB 整张表就是一棵以主键为键、叶子存整行的聚簇索引",
        "没有显式主键时，用第一个非空唯一索引，或隐藏的自动生成 row id 作聚簇键",
        "二级索引叶子只存「索引列 + 主键」，取整行需沿主键回表到聚簇索引",
        "回表次数 = 命中的行数，命中多时优化器可能放弃索引改全表扫描",
        "主键首选短小有序（自增整数），避免长随机 UUID 作为聚簇键",
      ],
    },
  ],
};
