/* ==================================================================
 * 课时：面试——索引高频题与答题框架（mysql-interview-index）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 本节把前置章节（B+ 树、聚簇/二级索引、索引设计）的索引知识按面试
 * 重新组织：高频题 + 答题框架 + 追问点 + 自测。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-interview-index",
  courseSlug: "database-mysql",
  title: "面试：索引高频题与答题框架",
  summary: "用「为什么 / 是什么 / 怎么办 / 坑在哪」的框架，把最左前缀、回表、覆盖索引、失效场景等高频索引题答成体系。",
  minutes: 22,
  kind: "quiz",
  blocks: [
    {
      type: "paragraph",
      text: "索引是 MySQL 面试的绝对高频区。面试官问索引，通常不是要你背结论，而是想看你能否自洽地讲清楚「为什么这么设计、什么场景用、什么场景坑」。这一节我们把前置章节里的索引知识整理成可直接开口的答题框架：先讲 B+ 树为什么被选中，再讲聚簇/二级索引与回表、覆盖索引、最左前缀，最后讲失效场景与「什么时候不该建索引」。每一题都给出「答题框架」和可能的「追问点」。",
    },
    {
      type: "heading",
      text: "高频题 1：为什么用 B+ 树，不用哈希或红黑树？",
    },
    {
      type: "paragraph",
      text: "答题框架：先承认「哈希索引在 MySQL 里也存在（Memory 引擎默认 / InnoDB 自适应哈希索引），但只是辅助」，然后从 B+ 树三个核心能力讲：① 支持范围查询与排序（哈希只能等值匹配，B+ 树叶节点有序链表天然支持 range）；② 树高矮、磁盘 IO 少（B+ 树非叶节点只存索引键，扇出高、树高约 3–4 层，一次查询只要读几个页）；③ 叶子节点存全部数据且有序相连，适合磁盘这种「按页随机读慢、顺序读快」的介质。再和红黑树对比：红黑树内存里有优势，但树高约 log2n，节点分散、对磁盘不友好；B+ 树把多个键装进一个页，一次 IO 能推进更多层，且天然有序便于范围/排序。",
    },
    {
      type: "list",
      items: [
        "等值匹配 → 哈希快，但无法支持范围与排序",
        "B+ 树：非叶存键不存数据 → 页内键多 → 树矮 → 磁盘 IO 少",
        "叶子节点有序链表 → 范围查询、ORDER BY 友好",
        "红黑树：内存友好但树高、节点随机，对磁盘页不友好",
      ],
    },
    {
      type: "callout",
      variant: "note",
      title: "追问点：那前缀索引呢？",
      body: "B+ 树索引支持「最左前缀」正是因为它内部按索引列的顺序逐层比较；哈希索引无法利用前缀，只能整键等值。前缀索引（prefix index）指的是对字符串列的前 N 个字符建索引，可以减小索引体积，代价是可能产生更多回表，且无法覆盖完整列值做覆盖查询。",
    },
    {
      type: "heading",
      text: "高频题 2：聚簇索引 vs 二级索引，什么是回表？",
    },
    {
      type: "paragraph",
      text: "答题框架：① InnoDB 的主键索引就是聚簇索引（clustered index）：叶子节点直接存放整行数据，表数据与索引存储在一起，一张表只能有一个聚簇索引；没有主键时 InnoDB 会用唯一非空索引或隐藏的 rowid 来充当。② 其它的普通索引（二级索引 / secondary index，又称辅助索引）叶子节点存的是索引列 + 主键值，而不是整行。③ 回表（table lookup）：用二级索引查到主键后，再拿主键去聚簇索引里取整行，这就是「回表」。④ 用二级索引查询时如果还需要其它未被二级索引覆盖的列，就必然要回表；回表多一次随机 IO，是常见的慢查询来源。",
    },
    {
      type: "list",
      items: [
        "聚簇索引 = 主键索引，叶子 = 整行数据，一张表一个",
        "二级索引叶子 = 索引列 + 主键值",
        "回表 = 二级索引 → 主键 → 聚簇索引取整行",
        "能只走二级索引就不回表，这是覆盖索引要解决的",
      ],
    },
    {
      type: "heading",
      text: "高频题 3：什么是覆盖索引？为什么能提速？",
    },
    {
      type: "paragraph",
      text: "答题框架：如果查询所需的全部列（WHERE + SELECT 的列）都已经包含在某一个二级索引里，那么引擎只需扫这个二级索引本身就能拿到所有数据，无需回表，这种索引就叫覆盖索引（covering index）。它省掉了一次次回表的随机 IO。典型用法：在订单表上，高频查询「按 user_id 取某个用户的 id + status」时，可以建辅助索引 (user_id, status)，让 SELECT id, status ... WHERE user_id=? 直接从二级索引返回，完全避免回表。注意：覆盖索引的代价是索引体积更大、写入成本更高，不能无脑建。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "追问点：using index 和 using index condition 区别？",
      body: "EXPLAIN 里 Extra 为 Using index 表示「只扫描索引就能得到结果（覆盖索引），不需要回表」。Using index condition 是「索引下推（ICP）」，指 MySQL 把 WHERE 中能在索引上判断的条件下推到存储引擎，在扫描二级索引时就过滤掉部分行，减少回表次数，但它本身可能仍需回表。一个是「完全不回表」，一个是「少回表」，二者不同，别混。",
    },
    {
      type: "heading",
      text: "高频题 4：最左前缀原则是什么？",
    },
    {
      type: "paragraph",
      text: "答题框架：复合索引 (a, b, c) 实际能高效利用的查询是 a、a,b、a,b,c 这三类（从最左列开始连续取），这叫最左前缀（leftmost prefix）。跳过了最左列，如只查 b 或 c，或查 a,c，索引通常无法高效使用（a 可用但会失去 b 的排序/过滤价值）。原因是 B+ 树内部先按第一列排序，第一列相等再按第二列，……所以必须从最左列开始才匹配索引的排序结构。答题时最好顺带讲「因此复合索引的列顺序主要由『等值查询的区分度』『范围列放最后』『能否命中前缀』来决定」，体现你会设计而非死记。",
    },
    {
      type: "heading",
      text: "高频题 5：哪些情况会导致索引失效？",
    },
    {
      type: "paragraph",
      text: "答题框架：按「对索引列做了破坏有序性 / 匹配结构的事」来归类，比背清单更显得懂原理：① 对索引列使用函数（如 YEAR(created_at)=2024），MySQL 无法用索引，应改写为范围查询或冗余导出列；② 隐式类型转换，字符串列被当成数字比较（如 WHERE phone = 138xxxx 而 phone 是字符串），会破坏匹配；③ 使用了前导通配符 LIKE '%abc'，因为不是最左前缀；④ OR 连接的条件里有非索引列，导致整个条件无法走索引；⑤ 复合索引查询没有遵循最左前缀；⑥ 对索引列做运算或负向条件（!=、NOT IN 等）在很多情况下无法高效使用索引。最后补一句：最稳妥的验证方式是看 EXPLAIN 的 type / Extra，而不是死记。",
    },
    {
      type: "heading",
      text: "高频题 6：什么时候不该建索引？",
    },
    {
      type: "paragraph",
      text: "答题框架：索引不是免费的。每多一个索引，写入（INSERT/UPDATE/DELETE）都要同步维护，占磁盘空间，还占用额外内存。因此：① 区分度极低的列（如性别，只有两三个取值）单独建索引收益极低，甚至可能被优化器弃用；② 频繁更新的列不适合建太多索引；③ 很少出现在 WHERE / ORDER BY / JOIN 列里的列不用建；④ 表很小（如几百行）时全表扫比走索引还快；⑤ 数据量大但只写不查的场景。一句话总结：为「高频、条件明确、区分度足够」的查询建索引，不为「几乎不查」的列建。",
    },
    {
      type: "heading",
      text: "高频题 7：为什么主键建议用自增整型？",
    },
    {
      type: "paragraph",
      text: "答题框架：分两层。① 类型上的理由：整型比较和排序比字符串（如 UUID）快、占用空间小，聚簇索引的键更小，树更高扇出、更矮，二级索引里存的「主键值」也更小，从而让每个页装更多行。② 顺序写入上的理由：自增主键是递增的，新行追加到 B+ 树最右侧，几乎不触发页分裂和数据移动；如果用 UUID 这类随机的字符串做主键，新值会落到树中间，频繁触发页分裂、碎片化、随机 IO，性能明显变差。最后点一句：聚簇索引背后的整行数据是「物理连续」的，保持写入顺序与主键顺序一致（顺序插入）对 InnoDB 最友好。",
    },
    {
      type: "heading",
      text: "自测",
    },
    {
      type: "quiz",
      question: "关于 B+ 树能被 MySQL 选作索引结构的原因，下列哪项说法正确？",
      options: [
        "B+ 树天然支持等值匹配，比哈希更适合精确查找",
        "B+ 树非叶节点只存索引键，扇出高、树矮，磁盘 IO 少，且叶子有序支持范围查询",
        "B+ 树树高比红黑树更深，因此查找更快",
        "B+ 树只能做等值查询，不能做范围查询"
      ],
      answer: 1,
      explanation: "哈希比 B+ 树更适合等值匹配（A 反了）；B+ 树树高约 3–4 层，比红黑树矮（C 反了）；B+ 树叶子有序链表正是为范围查询设计（D 反了）。正确是 B。",
    },
    {
      type: "quiz",
      question: "InnoDB 中，二级索引的叶子节点里存放的是什么？",
      options: [
        "整行数据",
        "索引列的值加上主键值",
        "指向整行数据的物理磁盘地址指针",
        "全部其它索引的拷贝"
      ],
      answer: 1,
      explanation: "二级索引叶子存「索引列 + 主键值」，拿到主键后需要回表到聚簇索引取整行；存整行的是聚簇索引（A 是聚簇索引的行为），存物理地址指针是早期引擎的做法（C 错），D 显然不对。",
    },
    {
      type: "quiz",
      question: "在 shop 库的 orders 表上，下列哪种查询最可能完全避免回表（使用覆盖索引）？",
      options: [
        "SELECT total_amount FROM orders WHERE user_id = 7",
        "SELECT id, status FROM orders WHERE user_id = 7",
        "SELECT * FROM orders WHERE id = 7",
        "SELECT created_at FROM orders WHERE status = 'paid'"
      ],
      answer: 1,
      explanation: "orders 已存在二级索引 idx_user(user_id)。若再建 (user_id, status) 这样的覆盖索引，SELECT id, status ... WHERE user_id=? 所需列全在索引里、且按最左前缀命中，无需回表。B 是覆盖索引经典场景。A 的 total_amount、C 的 *、D 只按 status（无对应二级索引且未命中最左前缀，created_at 也不在索引里）都无法覆盖。",
    },
    {
      type: "quiz",
      question: "复合索引 (a, b, c) 已建立，下列哪个查询无法高效利用该索引的最左前缀？",
      options: [
        "WHERE a = 1 AND b = 2",
        "WHERE a = 1 AND b = 2 AND c = 3",
        "WHERE b = 2 AND c = 3",
        "WHERE a = 1 AND c = 3"
      ],
      answer: 2,
      explanation: "最左前缀要求从最左列 a 开始连续匹配。跳过 a 只查 b,c 时索引无法高效使用，所以 C 无法利用。B 全列命中、A 命中 a,b、D 命中 a（c 部分无法利用但 a 可用），三者都比 C 强。",
    },
    {
      type: "keypoints",
      items: [
        "B+ 树：非叶存键 → 树矮 → 磁盘 IO 少；叶子有序 → 范围/排序；哈希只适合等值",
        "聚簇索引=主键索引（叶子存整行），二级索引叶子存「索引列+主键」，查完整数据要回表",
        "覆盖索引 = 查询列都在二级索引里，免回表；Using index 与 Using index condition 不同",
        "最左前缀：复合索引必须从最左列开始连续使用",
        "索引失效：函数/隐式转换/前导通配符/OR 非索引列/不满足最左前缀等",
        "不建索引：低区分度、频繁更新、几乎不查、表很小",
        "主键用自增整型：整型小且快，顺序插入少页分裂，聚簇索引与二级索引都更紧凑",
      ],
    },
  ],
};
