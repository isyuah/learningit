/* ==================================================================
 * 课时：B+ 树索引：为什么是 B+ 树（mysql-index-btree）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-index-btree",
  courseSlug: "database-mysql",
  title: "B+ 树索引：为什么是 B+ 树",
  summary: "从全表扫描的代价出发，理解 B+ 树结构、查找流程与它胜出其他数据结构的原因。",
  minutes: 20,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "你在电商库里查一条订单：`SELECT * FROM orders WHERE user_id = 42;`。如果没有索引，MySQL 只能把 `orders` 表从头到尾一遍一遍地看，逐行检查 `user_id` 是否等于 42，直到翻完整张表。这就是「全表扫描」（full table scan）。数据只有几百行时无所谓；当订单表涨到几百万行，一次订单查询就变成数百万次的逐行比对——这条 SQL 就成了拖垮整个库的「慢查询」。索引就是用来消灭这种 O(n) 扫描的。本章我们先把索引的底层数据结构——B+ 树——彻底讲清楚。",
    },
    {
      type: "heading",
      text: "没有索引时：SELECT 为什么是全表扫描",
    },
    {
      type: "paragraph",
      text: "表是一堆数据行的集合，MySQL 数据最终存在磁盘文件里。要判断某一行是否满足条件，就必须「看见」那一行、读出它的字段值。在没有索引的时候，引擎没有别的办法：只能从表的第一行开始，一行行读、一行行对比字段，直到末尾。行数是 N，最坏要读 N 行，平均读 N/2 行——这就是 O(n)。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "真正的瓶颈：不是 CPU 算得快不快，而是磁盘 I/O",
      body: "每一行的读取都意味着一次磁盘 I/O，而磁盘随机读一次要几毫秒。你或许觉得几毫秒很短，可一百万行、每行一次随机读，就是几千秒。所以索引优化的本质是「用更少的磁盘 I/O 定位到目标行」，而不是让 CPU 算得更快。这也是后面所有结构设计的第一性出发点：磁盘 I/O 是昂贵资源。",
    },
    {
      type: "heading",
      text: "让查找变快的数据结构：从二分查找说起",
    },
    {
      type: "paragraph",
      text: "如果能先知道「目标会在哪一片区域」，就不用全表翻。一个朴素想法是：把所有主键排好序，先看看目标大概是中间那个，比较一下大小，小于就往左半区继续找，大于就往右半区找——每次比较都能排除一半候选。这就是二分查找，复杂度从 O(n) 降到 O(log n)。问题在于数据存在磁盘上，不能像内存数组那样随意跳到任意地址；树结构把「二分」组织成了从根到叶的路径。",
    },
    {
      type: "heading",
      text: "为什么不是二叉搜索树（BST）或哈希",
    },
    {
      type: "table",
      caption: "BST / 哈希 / B 树 / B+ 树 对比",
      headers: ["结构", "查找", "范围查询", "磁盘友好度", "MySQL 用什么"],
      rows: [
        ["二叉搜索树 BST", "O(log n) 但树深大", "中序遍历较麻烦", "树高随数据量增长，节点太小、I/O 多", "不用（数据量大时退化为链表/过深）"],
        ["哈希索引", "O(1) 精确匹配", "不支持范围/排序", "一次随机 I/O 可定位", "仅作辅助（如 MEMORY），不作为默认主索引"],
        ["普通 B 树", "O(log n)，矮", "支持但不高效", "节点大、每次一个磁盘页", "不是 MySQL 默认表结构"],
        ["B+ 树", "O(log n)，很矮", "叶子链式有序，天然高效", "每节点=一个磁盘页，扇出高", "InnoDB 默认索引结构"],
      ],
    },
    {
      type: "paragraph",
      text: "二叉搜索树在内存里很好用，但放到磁盘上会「长太高」：树的高度近似 log₂N，数据量一大（比如 1000 万行），树深逼近二十几层，每次下探一层就是一次随机磁盘 I/O——代价和全表扫描一样不可接受。哈希索引查单个值极快（O(1)），但它天然无法支持范围查询（`> / < / BETWEEN`）、无法排序、也做不了前缀匹配，而后两者正是 SQL 和业务最常用的。MySQL 需要一种「既支持等值、又支持范围、还矮」的结构。",
    },
    {
      type: "heading",
      text: "B+ 树到底是什么",
    },
    {
      type: "paragraph",
      text: "B+ 树是一棵多路平衡搜索树，它有两个关键特性：非叶子节点只存「用来指路的键（key）」不存数据；叶子节点存下所有数据并自左向右用指针串成一条有序链表。你不需要死记它的形式化定义，记住下面这张「脑中图画」就够理解绝大多数原理了。",
    },
    {
      type: "code",
      title: "B+ 树的形状（示意，假设每个节点最多 3 个键）",
      language: "text",
      code: "                        [ 50 | 100 ]            ← 根节点（只存键，用来指路）\n                        /       |      \\\n          [ 10 | 25 ]     [ 60 | 75 ]   [ 120 ]       ← 内部节点：也是键\n          /   |    \\        /  |  \\      /   \\\n        [1,5,8] [12,20] [30,40] [55] [65,70] [90] [110][130]   ← 叶子：真正存数据\n           \\____|___|____/      |       \\____|____/\n              一条由左到右的有序链：1,5,8,12,20,30,40,55,65,70,90,110,130",
    },
    {
      type: "paragraph",
      text: "查找一个键，从根开始：根节点里比较键的大小决定往哪个子节点走，一路下探到叶子，再从叶子链表里横向找到目标。因为叶子层已经把所有键排好序并用指针串起来，做范围查询时只要先定位到起点叶，然后沿着链表往右一路读出即可——不需要回到上层反复回溯。",
    },
    {
      type: "heading",
      text: "为什么 B+ 树「矮」：高扇出 + 节点等于一个磁盘页",
    },
    {
      type: "paragraph",
      text: "B+ 树能保持极矮，靠的是「一个节点能容纳很多键」。InnoDB 把默认页大小设为约 16KB，而一个节点正好就是一个页。16KB 里能塞进成百上千个键值对（每个键+指针只要几十字节），于是每个节点有很高的「扇出值」（fan-out）——一个节点后面挂几百个孩子。",
    },
    {
      type: "paragraph",
      text: "这带来了两个决定性好处。第一：因为扇出高，树的高度非常小。哪怕一张表有几千万行，B+ 树的高度通常只有 3 层左右（根 + 一两层内部节点 + 叶子）。从根到叶每一次下探是一层，所以一次精确查找只要读大约 3 个磁盘页。第二：每个节点正好是一个磁盘页，读一个节点就是一次顺序（近似随机但数量极少）的页读取，磁盘每次都能整页一次性读入，命中率高、I/O 次数少。",
    },
    {
      type: "callout",
      variant: "note",
      title: "「3 层树」意味着大约几次磁盘 I/O",
      body: "以三层 B+ 树为例：根节点通常常驻内存（缓冲池/缓存命中），实际要读的页主要是「内部节点 + 叶子页」一两次。所以一次主键点查往往只需要个位数的磁盘页读取——这正是它能支撑高并发下毫秒级响应的根本原因。你可以把「树高 ≈ 3」当成一个熟练的直观估计：千万行量级的主键查询，也就是读几页的事。",
    },
    {
      type: "heading",
      text: "在 shop 库上亲手建一个索引",
    },
    {
      type: "paragraph",
      text: "我们拿 `orders` 表举例。它已经有主键 id（InnoDB 会为主键自动建索引），但我们想按 `user_id` 快速查某用户的订单。订单量大的时候 `WHERE user_id = 42` 没有索引就要全表扫描，所以我们给 `user_id` 建一个普通索引。",
    },
    {
      type: "code",
      title: "创建索引与点查",
      language: "sql",
      code: "-- 给 user_id 建索引（普通二级索引，叫 idx_user）\nCREATE INDEX idx_user ON orders (user_id);\n\n-- 点查：按 user_id 等值过滤，会走 idx_user 这条 B+ 树\nSELECT id, status, total_amount, created_at\nFROM orders\nWHERE user_id = 42;",
    },
    {
      type: "paragraph",
      text: "建好之后，这条 `WHERE user_id = 42` 的等值查询会：从 `idx_user` 这棵 B+ 树的根出发，比较 `user_id` 大小一路下探，在叶子层定位到所有 `user_id = 42` 的键（一个用户可能有多个订单，所以可能命中多个连续键），再从叶子链表横向读出。至于「读出键之后怎么拿到完整行」——那是下一节课「聚簇索引与回表」要讲的核心，现在你只需建立「查找 = 从根下探到叶、在叶上定位」的心智模型。",
    },
    {
      type: "heading",
      text: "心智模型：log N 次定位 + 一次范围扫描",
    },
    {
      type: "list",
      items: [
        "等值查询：在 B+ 树的 log N 层里逐层下探（大约 3 层），最后在叶子链表上精确命中一行或几行。",
        "范围查询：先下探到范围的「起点叶子」，然后顺着叶子间的链表向右连续读出，直到超过上界。",
        "排序：因为叶子本身有序，若查询能按索引键排序，MySQL 可以省掉额外的文件排序（filesort，后面 EXPLAIN 课再说）。",
        "数据量增长时树高几乎不变：多出来的行只是加进叶子页 / 偶尔让树长一层，所以「千万行依旧 3 层」是常态。",
      ],
    },
    {
      type: "callout",
      variant: "tip",
      title: "面试口头禅：为什么是 B+ 树",
      body: "一句话版本：B+ 树矮（高扇出、每层一个磁盘页、树高约 3 层）、叶子有序串联成链表所以既支持等值又支持高效范围查询和排序，相比 BST 树高更矮、相比哈希能支持范围和排序。把它表达成「少 I/O + 支持范围」两个点，基本就能覆盖 90% 的追问。",
    },
    {
      type: "quiz",
      question: "InnoDB 选择 B+ 树而不是哈希作为主要索引结构，最重要的原因是？",
      options: [
        "哈希支持范围查询和排序，B+ 树不支持",
        "B+ 树既能高效支持等值查询，又能高效支持范围查询与排序，而哈希无法支持范围查询",
        "B+ 树可以完全避免磁盘 I/O，哈希不行",
        "哈希在数据量大时会被 MySQL 直接禁止使用",
      ],
      answer: 1,
      explanation: "哈希只擅长精确等值匹配（O(1)），不支持范围查询、排序与最左前缀依赖的顺序遍历；B+ 树叶子有序串联，既能等值定位又能范围扫描，且树高约 3 层、I/O 次数少，因此成为 InnoDB 默认索引结构。",
    },
    {
      type: "exercise",
      title: "估算一次点查需要几次磁盘 I/O",
      description: "假设 orders 表有约 1000 万行，主键 id 的 B+ 树高度约 3 层（根 + 1 层内部节点 + 叶子页）。请你说明一次 `WHERE id = ?` 的主键点查大概需要读取几个磁盘页，并解释为什么量级是「个位数」而不是「百万次」。",
      hint: "从根到叶每一层对应一次页读取；根页通常常驻内存（被缓存命中），所以真正读取的页主要是内部节点页和叶子页，总共约 2~3 个页。",
    },
    {
      type: "keypoints",
      items: [
        "没有索引时 SELECT 是全表扫描，O(n)，代价主要是磁盘 I/O",
        "B+ 树非叶子存键、叶子存所有数据并串成有序链表",
        "节点=一个磁盘页、扇出高 → 树高约 3 层 → 点查只需读个位数页",
        "B+ 树优于 BST（树更矮）与哈希（支持范围与排序）",
        "查找心智模型：log N 次下探定位 + 叶子链表上的一次范围扫描",
      ],
    },
  ],
};
