/* ==================================================================
 * 课时：面试——数据库设计与场景题（mysql-interview-design）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 本节把设计/场景题（订单、点赞、好友、大表、排行榜、分页、扣库存）
 * 组织成「场景 + 决策 + 取舍 + 答题框架」。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-interview-design",
  courseSlug: "database-mysql",
  title: "面试：数据库设计与场景题",
  summary: "订单/点赞/好友关系设计、大表优化、排行榜计数器、深分页、幂等与并发扣库存——场景题的决策与答题框架。",
  minutes: 22,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "设计/场景题是数据库面试的后半场：面试官给一个业务场景，看你「先想清楚需求、再给可落地的 MySQL 方案、再谈取舍」的能力。这类题没有唯一答案，重点是体现结构化思维和工程判断。这一节我们用「场景 + 决策 + 取舍 + 答题框架」组织几道经典题，帮你建立一套可复用的提问—约束—方案—权衡的套路。",
    },
    {
      type: "heading",
      text: "场景题 1：如何设计订单表？",
    },
    {
      type: "paragraph",
      text: "答题框架：先拆需求——订单有「主表 + 明细」结构，且金额、数量必须可复算，要防篡改。然后给方案：订单主表（orders）存用户、总额、状态、时间；订单明细表（order_item）存商品、单价、数量；金额用 DECIMAL 而非 FLOAT（避免精度问题）；在明细表外键列（order_id）建二级索引，按 user_id 查询订单的主表也建索引（如 idx_user）。再谈取舍与进阶：状态字段用字符串枚举更可读；下单、扣库存、记支付放到一个事务里保证一致；订单量非常大的系统会按 user_id 或订单号做分库分表、冷热分离（历史订单归档），但这属于规模达到后才引入的复杂度，不要上来就分表。加分点：能提到「金额与数量冗余进明细、用唯一订单号做幂等」等真实落地的点。",
    },
    {
      type: "callout",
      variant: "tip",
      title: "追问点：为什么金额用 DECIMAL？",
      body: "FLOAT/DOUBLE 是二进制浮点，无法精确表示十进制小数（如 0.1），累计或比较会产生误差，涉及钱时必须用定点数 DECIMAL(10,2) 这种精确类型。这也是本题能体现「字段类型意识」的细节。",
    },
    {
      type: "heading",
      text: "场景题 2：如何设计点赞表？",
    },
    {
      type: "paragraph",
      text: "答题框架：先明确两个需求——「谁给哪个目标点过赞」（用于去重、展示个人点赞态）和「某个目标的点赞总数」。方案：核心是一张关系表 like(user_id, target_id, target_type, created_at)，主键或唯一约束（user_id, target_id）保证一人对一目标只能点一次（做幂等去重），并建反查索引（target_id）支撑「查这个目标的所有赞/计数」。点赞总数可以：① 实时 COUNT 关系表（简单但量大时慢）；② 在目标表冗余一个 like_count 字段，点赞/取消赞时原子更新；③ 更大的规模用 Redis 计数 + 落库，但那是引入新组件后的取舍。答题框架要点：先讲关系表保证正确性与去重的数据模型，再讲计数是「实时 count 还是冗余字段」的取舍，数量大到再谈缓存。",
    },
    {
      type: "heading",
      text: "场景题 3：如何设计好友/关注关系（多对多）？",
    },
    {
      type: "paragraph",
      text: "答题框架：好友/关注本质上是一张「关系表」表达的多对多。两种建模：① 关注/单向关系：一张表 follow(follower_id, followee_id, created_at)，加复合唯一键避免重复关注，建 (follower_id) 和 (followee_id) 两个方向的索引分别支撑「我关注了谁」和「谁关注了我」。② 好友（双向确认）关系：通常做法是把「A→B」和「B→A」各存一行，或用「用户对 + 状态」：friends(user_id, friend_id, status) 存 two rows for bidirectional（单向请求用 status 区分 pending/accepted）。更彻底但较重的方案是「中间表+两张方向索引」的双向 edges 存法。答题要点：强调「关系表 + 两个方向索引 + 唯一约束」是核心，同时说明单向关注和双向好友在「存几行/如何表达确认状态」上的差异。",
    },
    {
      type: "heading",
      text: "场景题 4：大表优化思路（分库分表、只读副本、归档、索引）",
    },
    {
      type: "paragraph",
      text: "答题框架：优化不是一上来就分库分表，而是一个阶梯。先讲「能不分的先优化」：① 索引与查询优化（对高频查询建合适索引、避免无谓回表和扫全表）；② 归档/冷热分离，把很少访问的历史数据（如超过 N 天的订单）定期移到归档表，主表变小、查询与索引都更快；③ 只读副本（read replicas）做读写分离，把读压力分摊走，缓解主库压力。只有当单表/单库的写入与容量确实到瓶颈时，才考虑：④ 垂直拆分（按业务模块拆库/拆表）与水平分库分表（按 shard key 如 user_id 哈希或取模分布到多库/多表）。水平分表要重点讲清楚取舍：带来全局唯一 ID、跨 shard 的 join/分页/事务、扩容（取模需要重分布，一致性哈希缓解）等复杂度——这些是用分布式成本换单机瓶颈，要说明只有规模逼到才值得。答题套路：先讲「索引/归档/副本」这些低成本的，再讲分库分表是最后手段与它引入的新问题。",
    },
    {
      type: "heading",
      text: "场景题 5：如何实现排行榜 / 计数器？",
    },
    {
      type: "paragraph",
      text: "答题框架：先区分「实时性要求」和「数据量」。计数器（count）在小到中等规模下直接在一张表维护一个 count 字段，用原子的 UPDATE ... SET cnt = cnt + 1（或 INSERT ... ON DUPLICATE KEY UPDATE cnt = cnt + 1）做并发安全的自增，避免「先查后改」的竞态；量很大、热点极高时再考虑 Redis 的 INCR + 定期落库。排行榜：如果数据量可控，直接用带排序索引的 SQL（ORDER BY score DESC + LIMIT）即可，MySQL 能利用索引排序；数据量大或要实时 TOP-K 时，Redis 的 ZSET 是更常见方案，DB 只做最终落库。答题要点：体现「先在 MySQL 内用正确 SQL/原子更新解决，量变大了再谈缓存组件」的取舍思路，而不是一上来就引 Redis。",
    },
    {
      type: "callout",
      variant: "note",
      title: "追问点：为什么计数要原子更新而不是先查后改？",
      body: "「先 SELECT 再 UPDATE」在并发下会丢更新：两个请求读到同一个旧值各自 +1，结果只 +1。用原子的 UPDATE ... SET cnt = cnt + 1 让「读当前值并 +1」在数据库内一步完成，配合行锁保证并发正确。这与扣库存的悲观/乐观锁思路一致。",
    },
    {
      type: "heading",
      text: "场景题 6：分页查询太深导致慢怎么办？",
    },
    {
      type: "paragraph",
      text: "答题框架：问题根源是 `LIMIT offset, size` 的深分页会「扫过并丢弃 offset 之前的所有行」，offset 越大越慢。解法分几档：① 「游标/键集分页」（keyset pagination）：用上一页最后一条的排序键作为条件继续取，如按自增 id，`WHERE id < last_seen_id ORDER BY id DESC LIMIT 20`，让数据库直接定位起点，几乎不随页数变慢——这是首选；② 「延迟关联」（deferred join）：先用覆盖索引查出本页主键集合，再回表 join 取完整行，减少深分页时大面积回表；③ 限制可翻页深度，超过一定页数就不让翻（如只允许前 N 页，后续用搜索/游标）。答题要点：能说出深分页为什么慢（offset 扫描丢弃），并给出 keyset/延迟关联这两个 MySQL 里的实用手段。",
    },
    {
      type: "heading",
      text: "场景题 7：如何做幂等，以及并发扣库存（乐观 / 悲观锁）？",
    },
    {
      type: "paragraph",
      text: "答题框架：分两问。第一问幂等：让同一请求重复执行结果一致，常用手法是客户端生成唯一请求号/订单号，服务端对重复请求做去重（唯一索引 + INSERT ... ON DUPLICATE，或「先查幂等键表」）；数据库层用唯一主键/唯一索引保证同一条记录只落一份。第二问扣库存——核心是「先查后写」会超卖，要避免并发读到同一个旧库存。悲观锁：`SELECT stock FROM product WHERE id=? FOR UPDATE` 先锁住该行再扣，并发正确但锁竞争明显、吞吐有限；乐观锁：`UPDATE product SET stock = stock - ? WHERE id=? AND stock - ? >= 0`（用条件保证不扣成负），或带版本号 `WHERE version = ?`，失败则重试，适合读多写少、冲突少；纯原子表达式 `stock = stock - 1` 在单行上也是并发安全的，但要靠 WHERE 条件防超卖。取舍：悲观锁简单可靠但串行化；乐观锁并发高但冲突多时重试频繁。加分点：能讲清「在事务里」与「用条件性更新」，以及先扣库存后回滚的补偿思想。",
    },
    {
      type: "heading",
      text: "自测",
    },
    {
      type: "quiz",
      question: "在订单表中表示金额字段，最合适的类型是？",
      options: [
        "FLOAT",
        "DOUBLE",
        "DECIMAL(10,2)",
        "INT"
      ],
      answer: 2,
      explanation: "金额必须精确，FLOAT/DOUBLE 是二进制浮点，无法精确表示十进制小数，会累积误差；INT 无法表达角分精度。DECIMAL 是定点精确类型，用于钱。",
    },
    {
      type: "quiz",
      question: "关于深分页（如 LIMIT 100000, 20）变慢的原因与对策，正确的说法是？",
      options: [
        "深分页本身不慢，是网络问题",
        "LIMIT offset, size 要扫过并丢弃 offset 之前的行，offset 越大越慢；常用键集分页（游标）或延迟关联优化",
        "直接把 LIMIT 改成 ORDER BY 就能解决",
        "深分页慢是因为没有唯一主键"
      ],
      answer: 1,
      explanation: "深分页慢源于 offset 扫描丢弃前面大量行；keyset（游标）分页和延迟关联能缓解。A、C、D 均不成立。",
    },
    {
      type: "quiz",
      question: "并发扣库存时，下面哪种做法最能直接防止「超卖」（库存扣成负数）？",
      options: [
        "先 SELECT 库存，再在应用里判断后 UPDATE",
        "直接用 UPDATE product SET stock = stock - 1 且不关心 WHERE 条件",
        "UPDATE product SET stock = stock - ? WHERE id = ? AND stock >= ?，配合事务与行锁/乐观重试",
        "在应用端用全局锁把所有人串行化即可，无需数据库层面处理"
      ],
      answer: 2,
      explanation: "在事务里用带 `AND stock >= ?` 的条件性更新，让「判断库存够不够」与「扣减」在数据库一条语句内原子完成，可防超卖。A 是先查后写会竞态；B 没有防负数的条件；D 把并发问题挪到应用端且不解决多机场景。",
    },
    {
      type: "quiz",
      question: "关于「主从复制可替代备份」的说法，正确的是？",
      options: [
        "主从复制就是备份，从库能恢复到任意时间点",
        "主从复制用于高可用与读写分离，不是备份；主库误删会同步到从库，备份才能时间点恢复",
        "只要挂了从库，任何误操作都不会丢数据",
        "binlog 只能用于主从同步，不能用于数据恢复"
      ],
      answer: 1,
      explanation: "复制把主库日志同步执行到从库，误删/误更新也会同步过去，不能当备份。备份（或备份+binlog）才能做时间点恢复。D 前半句对但后半句错——binlog 也可用于 PITR。",
    },
    {
      type: "keypoints",
      items: [
        "订单表：主表+明细，DECIMAL 存金额，明细外键建索引，下单/扣库存/支付放一个事务",
        "点赞：关系表(user_id,target_id) + 唯一约束去重 + 反查索引；计数用冗余字段原子更新，量大再谈缓存",
        "好友/关注：多对多关系表 + 两个方向索引 + 唯一约束；单向关注与双向好友在建行/状态上有差异",
        "大表优化阶梯：先索引/归档/只读副本，确需再分库分表（并讲清引入的 ID、跨 shard、扩容复杂度）",
        "计数器/排行榜：原子 UPDATE 自增、ORDER BY + LIMIT 用索引；量大再引 Redis 等缓存组件",
        "深分页：offset 扫描丢弃慢；用键集(游标)分页、延迟关联、限制翻页深度",
        "扣库存：事务 + 条件性更新防超卖；悲观锁简单但串行、乐观锁并发高但重启多",
        "幂等：客户端唯一请求号 + 数据库唯一索引/去重",
      ],
    },
  ],
};
