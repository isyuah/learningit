/* ==================================================================
 * 课时：面试——读写分离与主从一致性（mysql-interview-replica-consistency）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * 本节把它做主从复制的业务应用层话题：读写分离怎么搭、
 * 从库读到旧数据（主从延迟）怎么处理、以及「写后读一致性」。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "mysql-interview-replica-consistency",
  courseSlug: "database-mysql",
  title: "面试：读写分离与主从一致性",
  summary: "读写分离怎么搭、主从延迟导致从库读旧数据怎么处理、以及「写后读必须看到自己写」的强一致读方案。",
  minutes: 20,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "课程前面讲了主从复制的机制（主库写 binlog → 从库 relay log → SQL 线程重放；默认异步）。读写分离就是基于它做读扩展，但异步复制带来一个业务问题：主库刚写的、从库可能还没收到，读到旧数据。这一节从「架构怎么搭」讲到「主从延迟在业务上怎么兜」，回答后端面试最常追的一个落地问题。",
    },
    {
      type: "heading",
      text: "高频题 1：读写分离是什么、怎么搭？",
    },
    {
      type: "paragraph",
      text: "答题框架：读写分离 = 写操作（INSERT/UPDATE/DELETE，以及需要最新数据的事务）走主库，读操作走一个或多个只读从库（read replicas），从而分摊主库的读压力、提升整体读吞吐。搭建要点：① 底层靠前面讲的主从复制（binlog 同步到从库）；② 应用侧通过数据源路由/中间件把读写请求分发到主库或从库（例如读写分离代理、或应用层按「是否写操作」选数据源）；③ 从库设置 read_only，防止误写导致主从不一致。答题要点：能讲清「写主读从、复制链路、路由口径」这三件事。",
    },
    {
      type: "heading",
      text: "高频题 2：主从延迟会导致什么问题？",
    },
    {
      type: "paragraph",
      text: "答题框架：因为复制默认是异步的，主库提交后从库什么时候追平不确定。典型问题：**写后读不一致**——用户刚下单/改了个资料，立刻刷新读取，请求被路由到还没追平的从库，读到旧数据。这在「账户余额、订单状态、资料修改」这类对时效敏感的场景必须解决。答题时先点出「异步复制的本质：从库有延迟」，再给下面的方案。",
    },
    {
      type: "heading",
      text: "高频题 3：怎么处理从库读旧数据？",
    },
    {
      type: "paragraph",
      text: "答题框架：按业务对时效的敏感度分档：① 强一致读请求走主库：对必须立刻看到自己写的数据（如刚付款的订单、刚改的密码），这类读直接路由到主库，牺牲一点读分流换取正确；② 「写后读」会话内保证：如果用户刚执行过写，短时间内他的读也走主库（如按用户维度标记、短时间强制主库读）；③ 半同步复制（semi-sync）降低延迟窗口：主库至少等一个从库确认收到 binlog 才返回，减少「主库已提交但从库还没到」的出现概率；④ 从库追平等待：读前等从库 relay log 追到某个位点（较重，较少用）。答题要点：核心是「区分读写时效，该强一致的就走主库，其余再分流到从库」——不是所有读都强一致，也不是所有读都能容忍旧数据。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "追问点：读写分离后还能读到主库的「最新」吗？",
      body: "可以，但要你主动设计。默认从库是异步复制的旧视图，所以对强一致读要显式走主库或等追平。这正是「读写分离 ≠ 强一致读」的边界：读写分离是换「读扩展 + 短时最终一致」，无法天然保证写后立刻读到自己写的数据。面试答出这层权衡，比只说「挂了从库」更完整。",
    },
    {
      type: "heading",
      text: "高频题 4：数据库层 vs 应用层的路由怎么做？",
    },
    {
      type: "paragraph",
      text: "答题框架：读写分离的路由有两种主流实现：① 数据库中间件/代理层（如通过代理组件把 SQL 按读写分流，透明给应用）：对应用无侵入，但要引入一套代理组件、多一跳网络；② 应用层多数据源路由（如 ORM/数据源框架里配主从数据源，读写方法用不同数据源）：更灵活、能让代码显式控制「这条读走主库」，但侵入业务、靠约定或注解。答题要点：说清「代理层透明但增一跳、应用层灵活但要代码配合」，并根据系统规模选型——中小系统常选应用层多数据源，大流量常引入代理。",
    },
    {
      type: "heading",
      text: "小结：读写分离的正确姿势",
    },
    {
      type: "paragraph",
      text: "面试答读写分离，关键是体现「分层决策」：底层靠主从复制，架构上写主读从，业务上「按时效分路」——必须最新的写后读走主库或半同步保证，能容忍最终一致的普通读再分流到从库。同时能主动说出异步复制有延迟、从库不是强一致，以及路由在代理层还是应用层的取舍，就基本满分了。",
    },
    {
      type: "heading",
      text: "自测",
    },
    {
      type: "quiz",
      question: "读写分离中，对「用户刚付款后立刻刷新订单状态」这类请求，最稳妥的做法是？",
      options: [
        "一律走从库，允许读到旧的",
        "这类强一致读直接走主库，或结合半同步/写后读标记保证能看到最新",
        "从库同步之后才能读",
        "禁用读写分离",
      ],
      answer: 1,
      explanation: "写后读强一致：必须看到自己刚写入的数据，应走主库或用写后读标记/半同步降低延迟窗口。A 会读到旧状态；C 成本过高；D 过度。",
    },
    {
      type: "quiz",
      question: "主从延迟导致从库读到旧数据的根本原因是？",
      options: [
        "从库不允许读",
        "复制默认是异步的，主库提交后从库何时追平不确定",
        "主库和从库存储结构不同",
        "binlog 只用于备份",
      ],
      answer: 1,
      explanation: "默认异步复制，主库提交不等待从库，从库追平存在不可控延迟，因此可能读到旧数据。",
    },
    {
      type: "quiz",
      question: "哪种方式能「降低」主从复制中丢数据的窗口（而非消除延迟）？",
      options: [
        "只读从库多加几台",
        "半同步复制（主库等一个从库确认收到 binlog 再返回）",
        "把 read_only 打开",
        "让从库只做备份",
      ],
      answer: 1,
      explanation: "半同步让主库至少等一个从库确认收到 binlog，减少了主库崩溃时数据丢失的窗口。多挂从库是扩展读、不减少丢数据窗口；其余无关。",
    },
    {
      type: "keypoints",
      items: [
        "读写分离 = 写主读从，靠主从复制 + 路由（代理层或应用层多数据源）",
        "异步复制本质有延迟 → 从库可能读到旧数据",
        "写后读强一致：走主库 / 半同步 / 写后读标记；普通读再分流到从库",
        "按业务时效分路：该强一致的绝不读从库，能容忍最终一致的读从库",
        "路由：代理层透明但增一跳，应用层灵活但侵入业务",
        "心里清楚：读写分离换的是读扩展 + 最终一致，不是强一致读",
      ],
    },
  ],
};
