/* ==================================================================
 * 课时：三支柱：指标、日志、追踪（obs-three-pillars）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "obs-three-pillars",
  courseSlug: "observability",
  title: "三支柱：指标、日志、追踪",
  summary:
    "把指标、日志、追踪逐个讲透：各自的形态、回答的问题、成本与盲区，以及它们如何互补成完整的证据链。",
  minutes: 30,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "上一课 obs-mental-model 建立了两个基础：可观测性回答「什么 / 在哪 / 为什么」，答案藏在白盒暴露出的三种遥测信号里。这一课把指标、日志、追踪三个支柱逐个讲透——长什么样、回答什么问题、付什么成本、有什么天生盲区——再把它们放回同一个故障现场，看三信号如何各说各话又彼此补位。学完你应能回答：面对一个具体的排障问题，该去翻哪种信号。",
    },
    {
      type: "heading",
      text: "指标：系统的数值脉搏",
    },
    {
      type: "definition",
      term: "指标（metrics）",
      definition:
        "以「名称 + 标签 + 数值 + 时间」为基本形态、按固定时间间隔采集的数值序列，例如 `shop_http_requests_total{route=\"/orders\",status=\"500\"}` 在 15:00:00 的取值是 3。标签（label）是附加在指标上的维度（route、status……），用于对序列做切分。它描述一段时间内可加总的「画像」，而不记录任何一次具体事件。",
    },
    {
      type: "paragraph",
      text: "指标回答整体性问题：错误率在涨还是跌（趋势）、是否越过阈值（告警）、容量够不够。它把海量个体事件压缩成少量数值，聚合友好、存储紧凑、查询高效，可以秒级采集并保存数月。但代价是两层盲区：一是它不知道「为什么」——数值背后的具体请求、参数、异常都被聚合掉了；二是平均掩盖长尾——p50 看着 200ms 一切正常，但 p99 已到 2 秒，慢请求用户正在流失，聚合曲线却波澜不惊。围绕指标有一套命名直觉值得先记住：RED（请求型端点的速率/错误/时长）与 USE（资源型组件的利用率/饱和度/错误），落到 shop 的埋点在 obs-prometheus-instrumentation 展开。",
    },
    {
      type: "heading",
      text: "日志：最丰富，也最贵",
    },
    {
      type: "definition",
      term: "日志（logs）",
      definition:
        "按时间顺序追加的事件记录，每条含时间戳、级别与消息正文；理想形态是结构化键值对（如 `level=error msg=\"sqlite busy\" order_id=...`），便于按字段过滤。它是信息密度最高的信号，擅长回答「具体发生了什么」，但每条都要真实写入磁盘，量级上升后成本最先失控；要规模化检索，索引字段的设计就成了命门——这正是高基数问题登场的地方。",
    },
    {
      type: "definition",
      term: "高基数（high cardinality）",
      definition:
        "标签或字段的组合取值规模爆炸，导致按这些维度组织的索引与存储失控。基数（cardinality）指一个维度可取值的数量或组合的规模；高基数的典型来源是把「每次请求都不同」的值塞进维度——把 `order_id`、`user_id` 或完整 URL 当作指标标签或日志的索引字段，每来一个新请求就产生新取值，组合数以百万计，索引随之膨胀、检索退化到不可用。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "把日志当成唯一排障手段的代价",
      body: "日志能解释细节，却天生缺少整体视角与因果拼接：海量条目里看不出哪些属于同一次请求、问题卡在哪一环。只靠日志排障，常见结局是告警来了却无从判断严重度与范围，只能盲目 grep 关键词，低频偶发问题如大海捞针。正确用法是让指标负责发现、日志保留详情、追踪负责拼接，再用 trace_id 一类关联字段把三者粘起来——关联机制见 obs-exemplars-bridges，结构化日志见 obs-structured-logging。",
    },
    {
      type: "heading",
      text: "追踪：跨服务拼接因果",
    },
    {
      type: "definition",
      term: "追踪（traces）",
      definition:
        "以「一次请求（trace）由若干跨度（span）组成」为模型的数据：每个 span 记录一段有起止的工作（如调用 payment、执行 SQL），携带父子关系、属性与耗时；跨服务时用贯穿全链路的标识把各组件自己的 span 拼成一条完整路径。它提供单次请求的全路径视角，是唯一天生面向跨服务因果的信号；数据结构的细节在 obs-trace-model 展开。",
    },
    {
      type: "paragraph",
      text: "追踪直接回答「卡在哪一环」：把「order 收到请求 → 调 payment → 调 stock → SQLite 落库 → 返回」还原成一段段可见、可计时的 span。它的接入成本在三者中最高——要让链路跨组件拼接，每个服务都要埋点并在调用间传递上下文；存储侧真正的成本大头是「每个请求都全链路保存」，高流量下线性失控，因此生产普遍引入采样（sampling），只保留一部分请求的完整链路，以「低频偶发错误可能恰好没被采到」为代价换取可控成本（机制见 obs-sampling-propagation）。它的聚合视野也弱——回答不了整体趋势，那是指标的活。单看任何一个支柱都有洞，组合起来才是完整证据链——把三者放回上一课那个「下单慢」的升级版故障：支付回调变慢 + 偶发超时，`POST /payments` 延迟升高、timeout 错误爬升，订单创建失败率随之上升，根因是 stock 组件的 SQLite 写锁竞争（busy timeout 不够）加上 payment 对上游的超时设置过短。",
    },
    {
      type: "table",
      caption: "同一故障「支付回调慢 + 偶发超时」下，三信号各自能说什么、不能说什么",
      headers: ["信号", "能说什么", "不能说什么"],
      rows: [
        [
          "指标",
          "`shop_http_request_duration_seconds` 的 p99 从 200ms 涨到 1.8s、timeout 类 5xx 错误率爬升——把故障定性与定量圈定，触发告警",
          "说不出哪些具体请求失败、失败时的业务上下文，更说不出为什么",
        ],
        [
          "日志",
          "失败明细：某次回调的报错原文、SQLite busy timeout 的现场字段——why 的第一手素材",
          "看不出一次请求从入口到出口经历了哪些组件、各耗时多少；整体严重度与范围要问指标",
        ],
        [
          "追踪",
          "抽到的失败 trace 显示慢几乎全在 stock 扣库存那段 span，payment 在等待——把 where 钉死",
          "单条 trace 代表不了整体；被采样掉的低频失败 trace 可能根本不存在",
        ],
      ],
    },
    {
      type: "paragraph",
      text: "故障排下来是一个固定剧本：指标发现并圈定（what、严重度）→ 追踪定位组件与调用（where）→ 日志给出根因线索（why）→ 修复后回到指标验证。任何一环缺失，下一环都会失去方向——这就是三支柱必须作为一个整体来建设的原因，也是本课程后续所有课时展开的顺序。",
    },
    {
      type: "keypoints",
      items: [
        "指标是数值序列，聚合友好、存储便宜，负责趋势、告警与 what；代价是丢细节，且平均会掩盖长尾。",
        "日志是逐条事件，信息最丰富，负责具体细节；但写入与存储成本最高，盲目全量保留与全文检索不可持续。",
        "高基数 = 标签/字段组合取值爆炸：把 order_id、user_id 这类每请求不同的值塞进维度，索引与存储会失控。",
        "追踪以单次请求为线索拼接跨服务因果，是回答 where 的主力；成本靠采样控制，接入成本高于另两者。",
        "三信号互补：指标发现并圈定故障，追踪定位到组件与调用，日志提供根因细节——只依赖任何一种都会留盲区。",
        "RED/USE 是指标命名直觉；信号间的关联手段（exemplar、trace_id 关联日志）在后续课时展开。",
      ],
    },
  ],
};
