/* ==================================================================
 * 课时：消息队列：为什么需要它（rabbitmq-mental-model）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  slug: "rabbitmq-mental-model",
  courseSlug: "rabbitmq",
  title: "消息队列：为什么需要它",
  summary: "从同步调用的痛点出发，理解消息队列的削峰填谷、解耦、异步与可靠，以及异步引入的代价与边界。",
  minutes: 24,
  kind: "reading",
  blocks: [
    {
      type: "paragraph",
      text: "想象一个电商订单系统：用户下单后，订单服务需要让支付、库存、通知等多个下游都行动起来。最简单的方式是同步调用——订单服务在同一个 HTTP 请求里依次调用每一个下游。这套写法在小流量下完全够用，但当流量、下游数量和故障一起出现时，问题就来了。这一课先不碰任何 RabbitMQ 的 API，只回答一个问题：**消息队列到底解决了什么问题**。",
    },
    {
      type: "heading",
      text: "同步调用的三个痛点",
    },
    {
      type: "list",
      items: [
        "阻塞与延迟累加：下单请求要等支付、扣库存、发通知全部返回才结束，任何一个下游慢，整个下单就跟着慢。",
        "级联失败：库存服务一挂，正在等待它的支付、通知也被拖住，一个故障扩散成一片故障。",
        "流量尖峰无处消化：大促瞬间的下单洪峰直接打在数据库和下游服务上，来不及扩容就超时。",
      ],
    },
    {
      type: "heading",
      text: "消息队列：在中间加一层缓冲",
    },
    {
      type: "definition",
      term: "消息队列（Message Queue）",
      definition: "一种进程间通信方式：生产者把消息写入队列这个临时存储，消费者按自己的节奏从队列里取走并处理。生产与消费在时间和速率上解耦——生产者不必等消费者处理完，消费者也不必与生产者同时在线。",
    },
    {
      type: "list",
      items: [
        "Producer（生产者）：产生消息的一方，这里是订单服务，它把「订单已创建」这类事件交给队列。",
        "Consumer（消费者）：消费消息的一方，这里是支付、库存、通知等服务，各自取走自己关心的消息去处理。",
        "Broker（代理）：运行着队列的中间件服务器——也就是本课程的主角 RabbitMQ。",
      ],
    },
    {
      type: "paragraph",
      text: "引入队列之后，订单服务不再直接调用下游，唯一的约定变成了消息本身：订单服务只负责「把事件说清楚」，谁关心这件事、什么时候处理，由下游自己决定。",
    },
    {
      type: "heading",
      text: "四个核心价值",
    },
    {
      type: "list",
      items: [
        "异步：下单请求把消息交给 Broker 就立即返回，用户体感从「等所有下游」变成「毫秒级完成」，真正的工作由消费者在后台并行完成。",
        "削峰填谷：大促洪峰先涌入队列，消费者按自己的处理能力慢慢消费，把瞬间的尖峰拉平，保护数据库和下游服务不被击穿。",
        "解耦：新增一个下游（比如审计服务）不需要改订单服务代码，只要它也订阅同一类消息即可；下游故障也不影响下单主流程。",
        "可靠：Broker 可以把消息持久化，消费失败可以重试——消息不会因为一次处理失败就消失，这是 HTTP 同步调用很难天然提供的。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "异步不是免费的",
      body: "引入消息队列意味着下单成功后，支付、库存的完成是「最终一致」的：用户看到下单成功，但库存扣减可能还在路上。你必须接受这种延迟的一致性窗口，并处理消费失败后的补偿与重试。消息队列没有消灭分布式系统的复杂性，它只是把复杂性从「调用链」转移到了「消息的生命周期」上。",
    },
    {
      type: "heading",
      text: "边界：什么时候不需要消息队列",
    },
    {
      type: "list",
      items: [
        "低并发、下游可靠的同步场景：只有一两个下游且都能扛住流量，引入 MQ 只会白白增加一跳延迟和一个需要运维的组件。",
        "强一致的业务：比如转账必须原子地「扣款 + 加款」同时生效，这类强事务语义 MQ 无法保证，应该留在数据库事务里。",
        "实时性要求极高的交互：如果处理结果必须在下一次交互前返回（如登录校验），异步回执只会让流程更绕。",
      ],
    },
    {
      type: "callout",
      variant: "note",
      title: "下一步：把「队列」具体化为 AMQP 模型",
      body: "「消息队列」这四个字只是心智层面的图景。真正要使用 RabbitMQ，你需要理解它实现的 AMQP 0-9-1 模型：消息不是直接进队列，而是先进交换机（Exchange），再按绑定（Binding）规则被路由到队列。下一课我们就把这幅图景细化成可以操作的模型。",
    },
    {
      type: "quiz",
      question: "订单服务引入消息队列后，用户提交订单的 HTTP 请求什么时候返回？",
      options: [
        "等支付、库存、通知全部处理完成后返回",
        "消息被写入 Broker 后立即返回",
        "等所有消费者确认收到消息后返回",
        "等数据库事务提交后返回",
      ],
      answer: 1,
      explanation: "引入消息队列的核心价值之一就是异步：请求只需把消息交给 Broker 即返回，下游消费在后台进行，用户体感明显变快。选项 A 正是引入前同步调用的行为。",
    },
    {
      type: "keypoints",
      items: [
        "同步调用存在延迟累加、级联失败、无法应对流量尖峰三个痛点。",
        "消息队列在生产者与消费者之间加入缓冲，实现异步、削峰填谷、解耦与可靠。",
        "异步带来最终一致性与失败补偿的代价，强一致业务仍应留在数据库事务里。",
        "消息队列是「通信模式」而非银弹，低并发、强一致、高实时场景要谨慎引入。",
        "下一步将学习 RabbitMQ 的 AMQP 0-9-1 模型：Exchange、Queue 与 Binding。",
      ],
    },
  ],
};
