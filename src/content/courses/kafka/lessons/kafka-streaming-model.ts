/* ==================================================================
 * 课时：流处理心智模型与引擎选型（kafka-streaming-model）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 *
 * 事实核对（2026-09-08）：Kafka 4.3.1；Kafka Streams 概念/配置按
 * kafka.apache.org/43/streams/core-concepts 与 config-streams 核对；
 * ksqlDB 定位按 docs.ksqldb.io 概念页核对；Go 生态部分为客观现状陈述
 * （franz-go 无官方 Streams），替代路径为工程判断、不虚构 API。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "把“流处理”从营销词变成工程判断：流 vs 批 vs 事件驱动微服务的边界、流引擎里 at-least-once/exactly-once 的真实含义，以及 Go 团队没有官方 Kafka Streams 时的三条现实路径与选型决策表。",
  blocks: [
    {
      type: "paragraph",
      text: "第 5 章结束时你已能回答「一条消息从生产到消费，丢在哪、重在哪」；第 6、7 章把「事件契约」与「系统边界接驳」补齐——数据已经被持续地、可靠地送进 Kafka 了。本章换一个视角：数据**在流动**这件事本身能否成为一种计算模型？答案是可以，而且 Kafka 生态为此准备了整套工具：进程内库 [Kafka Streams](glossary:kafka-streams)、独立 SQL 服务 [ksqlDB](glossary:ksqldb)，以及外部集群 Flink/Spark。本课先不碰任何 API，只解决三个问题：流处理与批处理、事件驱动微服务的边界在哪；流引擎说的「处理保证」到底保证什么；以及**你的 Go 团队**没有官方 Kafka Streams 时，三条替代路径各自适合什么。",
    },
    {
      type: "heading",
      text: "三种“处理数据”的姿势",
    },
    {
      type: "definition",
      term: "批处理（batch processing）",
      definition:
        "对着一个**有界的、已经完整的数据集**运行一次计算任务（定时或触发式），任务结束即输出完整结果。例子：每晚把当天 `orders.events` 全量灌进数仓算日营收报表。它的心智是「数据静止后我来算」。",
    },
    {
      type: "definition",
      term: "流处理（stream processing）",
      definition:
        "对着**无界、持续到达的数据**做常驻计算：事件到达即处理，结果随事件持续更新。它不把数据分成「一批批作业」，而是用**时间窗口**（[拓扑、KStream/KTable、状态与窗口](/courses/kafka/lessons/kafka-streams-dsl-topology)课展开）把无限流切成有界片段来聚合。例子：每 5 分钟输出一次实时营收。",
    },
    {
      type: "definition",
      term: "事件驱动微服务（event-driven microservices）",
      definition:
        "一种**服务间集成方式**：服务通过发布/订阅事件协作，而不是同步调用对方接口。它关心的是「业务动作被可靠触发并最终一致」，通常每个事件触发一次业务副作用（发通知、扣库存），典型载体就是 Kafka 消费组（[消费者组](glossary:consumer-group) + 提交时机 + 幂等，即第 4、5 章的内容）。",
    },
    {
      type: "table",
      caption: "流处理 vs 批处理 vs 事件驱动微服务（一句话对比）",
      headers: ["维度", "批处理", "流处理", "事件驱动微服务"],
      rows: [
        ["时间模型", "数据完整后跑一次作业，按作业调度时间（小时/天）出结果", "无界数据到达即处理；用事件时间/处理时间窗口切片，毫秒~分钟级可见", "事件触发业务动作，一般按「顺序敏感的单 key 事件流」即时消费"],
        ["状态", "无需跨批次状态（或靠数仓表存历史）", "引擎维护跨事件的键状态与窗口状态，随事件增量更新", "状态基本在业务库/外部系统，Kafka 侧只存位点"],
        ["容错与恢复", "任务失败整体重跑（或断点续跑），结果确定性", "引擎从 checkpoint/changelog/位点恢复，事件会重放 → 重复是常态，靠引擎语义+下游幂等处理", "消费组再平衡 + 位点提交：处理完未提交会重读（重复），与第 5 章完全一致"],
        ["典型问题", "延迟高；适合对账、报表等终态计算", "「算到一半崩溃」的正确性要引擎保证", "本质是异步系统：顺序、一致性靠事件契约与幂等设计，不是框架给的"],
      ],
    },
    {
      type: "paragraph",
      text: "三者不是互斥选项，而是**同一堆事件在不同正确性要求下的三种用法**：批处理适合「要最终准确的历史报表」；流处理适合「要尽快看到累计值、并按事件时间重算」；事件驱动微服务适合「每个事件要可靠地驱动一次业务动作」。判断信号很简单——**如果结果只在数据到齐后才有意义，用批；如果每个新事件都要立刻影响一个可更新的结果，用流；如果事件只是服务间传递业务事实的载体、处理本身无界但单条独立，用事件驱动**。书舟里三样都有：日报表是批，营收看板是流，订单事件驱动通知服务是事件驱动。",
    },
    {
      type: "heading",
      text: "为什么 Kafka 天生适合当流引擎的底座",
    },
    {
      type: "paragraph",
      text: "流引擎最难的四件事——**输入可回放**（崩溃后从头/从位点重算）、**并行与顺序的边界**（任务怎么分）、**流与表两类数据**（事件流 vs 最新状态）、**实例协调**（挂了谁接管）——恰好是 Kafka 从第 1 章起就在解决的问题：消息落盘后按 offset 可[回放](glossary:commit-log)、分区是并行与顺序的边界（[分区](glossary:partition)内有序，任务按分区切）、compacted 主题天然就是「表」（[日志压缩](glossary:compaction)，存每个 key 最新值）、[消费组](glossary:consumer-group)协议负责分配与故障转移。所以 Kafka 系的流引擎（Streams/ksqlDB）选择**当“库/服务”而不是自建存储**：它们不复制数据、不发明新的协调协议，全部复用 broker 已有的机制。这也是为什么「流处理」对你不是全新知识领域——它的构件你已全部学过，本课只是把它们**编排**起来。",
    },
    {
      type: "callout",
      variant: "example",
      title: "用一句“零件清单”理解上段",
      body: "一个 5 分钟营收管道 = 输入主题 `orders.events`（第 1 章的提交日志与分区）→ 消费组协议把 12 个分区分给多个实例（第 4 章）→ 引擎按 `amount` 聚合，中间值进本地状态、变更写 compacted changelog 主题（第 2 章存储/压缩）→ 崩溃时新实例回放 changelog 恢复（提交日志回放 + 第 5 章 at-least-once 语义）。引擎没有魔法，只有编排。",
    },
    {
      type: "heading",
      text: "流应用长什么样：书舟的四种典型形态",
    },
    {
      type: "list",
      items: [
        "**实时指标看板**：`orders.events` 里金额字段（分为单位）按 5 分钟窗口求 `SUM`，得到「实时营收」曲线；行为事件 `user.behavior` 按分钟 `COUNT` 得到全站活跃。特征是**聚合值会持续更新**，用户看的是「到现在为止」的趋势。",
        "**告警与规则检测**：库存服务维护水位，`inventory.stock` 每一条变更都重新评估「低于阈值？」——注意这**不是**定时扫描数据库，而是每个事件到达就增量判断，阈值越界到告警的延迟从「分钟级轮询」降到「事件级」。",
        "**实时特征计算**：把 `user.behavior` 切成会话（30 分钟无操作视为会话结束），算每次会话页数/时长/加购率，产出特征事件给 `recommend`/`analytics` 服务。特征天然是「跨多个事件、随时间滑动」的聚合，是流处理最典型的用武之地。",
        "**流式 ETL / 富化（enrichment）**：把原始事件清洗、补字段、按新键路由后写到下游主题。它与第 7 章 [Kafka Connect](glossary:connect) 的分工是：**Connect 负责系统边界**（数据库/文件 ↔ Kafka），**流引擎负责 Kafka 内部持续加工**；两者常串成一条管道。",
      ],
    },
    {
      type: "callout",
      variant: "tip",
      title: "一个判断：为什么这些事用「消费组 + 定时任务」做不香？",
      body: "纯 Go 工程师的第一反应往往是：起个消费组每 5 分钟 flush 一次内存计数不就完了？对小规模确实可以（第 3 节的三条路之一）。但一旦要**按事件时间切片**（不是处理时间）、容忍**乱序到达**、在**任务迁移后恢复中间状态**，手写代码量会指数上升——这些恰恰是流引擎内置的能力。判断要不要上引擎，看你的状态有多复杂：计数类薄状态可以自管；多流关联、会话、事件时间窗口这类厚状态，交给引擎更划算。",
    },
    {
      type: "heading",
      text: "流引擎的“处理保证”：先说清范围",
    },
    {
      type: "paragraph",
      text: "引擎介绍页都爱写「exactly-once」，这个词你在[第 5 章投递语义课](/courses/kafka/lessons/kafka-delivery-semantics)已经学会了第一反应：**追问在什么范围内**。对流引擎，答案分两层。引擎内部常驻任务可能随时崩溃重启，恢复 = 从**已持久化的进度**（消费位点、checkpoint、状态 changelog）把没处理完的事件**重放**，于是：**at-least-once** = 崩溃恢复后事件至少被处理一遍（可能重复，重复结果可能已写到下游）；**exactly-once（引擎内）** = 引擎把「消费位点的推进、状态存储的更新、输出主题的写入」三者做成原子动作，重放时不会产生重复输出——Kafka Streams 对「读 Kafka 事件 → 处理 → 写回 Kafka」的闭环正是这么保证的（配置 `processing.guarantee`，默认 `at_least_once`，可设 `exactly_once_v2`，底层复用第 5 章的幂等生产者 + 事务 + read_committed）。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "引擎的 exactly-once 依然止步于 Kafka 边界",
      body: "「处理过程中写了自己的数据库、调了下游 API」——这些副作用发生在引擎视野之外，崩溃重放时仍可能执行两次。所以引擎 EOS 能省掉的是「Kafka 内闭环的重复」，省不掉的是「对外部世界的有效一次」：外部副作用依旧要靠幂等键/Outbox（第 5、6 章），这条铁律在流处理里同样成立，面试时主动说出来是加分项。",
    },
    {
      type: "heading",
      text: "引擎版图：三类运行形态",
    },
    {
      type: "paragraph",
      text: "市面上「流引擎」其实是三种完全不同的部署形态，别被名词糊住：",
    },
    {
      type: "table",
      caption: "三类流引擎形态（每类一行职责 + 代价）",
      headers: ["引擎", "运行形态", "语言/接口", "状态管理", "运维代价", "一行职责"],
      rows: [
        ["Kafka Streams", "**进程内 JVM 库**：拓扑代码打进你的应用，多个实例靠消费组自动分摊", "Java/Scala（Java DSL 为主；无官方 Go 版）", "RocksDB 本地状态 + Kafka changelog 主题容错", "最低：不引入新服务，但要管 JVM 与每实例的本地状态磁盘", "把「流计算」当普通微服务写，Kafka 既是输入输出也是协调器"],
        ["ksqlDB", "**独立服务**（server 进程），SQL 定义持续查询", "SQL（底层编译为 Kafka Streams 拓扑）", "与 Streams 同源：物化视图 + changelog", "中等：多一个常驻服务（可集群），但无代码部署", "让不写 Java 的团队（Go/数据/分析）用 SQL 定义流与表"],
        ["Flink / Spark Structured Streaming", "**外部集群框架**：提交作业到独立计算集群，由集群调度执行", "Java/Scala/Python/SQL 等多 SDK", "框架托管 checkpoint 状态（可配外部存储）", "最高：自建/托管一套计算集群", "跨多数据源、超大状态、需要批流一体的重型计算"],
      ],
    },
    {
      type: "paragraph",
      text: "形态差异带来的选型直觉：**Kafka Streams 把「流处理」压成了你熟悉的『多实例无状态服务』心智**——加实例就扩容（并行度上限 = 输入分区数），实例崩了由消费组协议重新分配（机制同[第 4 章消费组](/courses/kafka/lessons/kafka-consumer-groups-classic)）。ksqlDB 是同一套机制的 SQL 化外壳；Flink/Spark 则独立于 Kafka 存在，读 Kafka 只是它的一个 source——引入它们通常意味着团队要养一套新集群，书舟这类「数据已在 Kafka、逻辑不跨系统」的场景一般不划算。",
    },
    {
      type: "heading",
      text: "Go 生态现实：没有官方 Kafka Streams，然后呢？",
    },
    {
      type: "paragraph",
      text: "必须直说的事实：**Apache Kafka 官方从未提供 Go 版 Kafka Streams**（[Kafka Streams](glossary:kafka-streams) 只有 Java API，`kafka-streams-scala` 封装自 4.3 起被标记弃用、5.0 移除）；franz-go 是功能完整的**客户端**，不包含流处理引擎。所以「Go 团队要不要上流处理」的真实问题是**选哪条替代路径**。三条路都成立，区别在你要交什么：",
    },
    {
      type: "list",
      items: [
        "**路 A：自研消费者 + 外部状态**——用 franz-go 消费，状态放 Redis/内存/数据库，窗口与恢复逻辑自己写。书舟例子：库存水位告警——消费 `inventory.stock`，在 Redis 里维护 sku → 余量，跌破阈值发通知。取舍：零新基建、语义全部自管（提交时机、重复、崩溃恢复，恰好把第 4、5 章学的东西用上）；代价是事件时间窗口、乱序处理、任务迁移恢复这类厚能力要手搓，适合**薄状态 + 单条独立**的逻辑。",
        "**路 B：ksqlDB 结果主题 / REST**——SQL 定义持续聚合，服务端常驻执行，Go 侧消费它写出的结果主题或走 REST。书舟例子：实时营收看板（下一课完整跑通）。取舍：不写 Java、状态容错交给引擎；代价是多一个 JVM 服务要部署运维，且灵活逻辑（复杂 UDF、细粒度控制）不如代码。",
        "**路 C：非 Go 流服务**——让一个 Java/Kotlin 服务（Kafka Streams）或 Flink 作业承担重计算，作为独立团队/独立服务交付结果主题。书舟例子：行为特征计算——`user.behavior` 会话切分 + 多流关联 + 窗口特征，状态厚、规则多，值得专门养一个服务。取舍：Go 团队不直接写它，但通过「结果主题」消费成果，Java 服务与 Go 服务之间就是普通的主题契约——对 Go 侧反而是最干净的；代价是要有 JVM 人力与配套监控。",
      ],
    },
    {
      type: "table",
      caption: "决策表：Go 团队遇到这些需求时怎么选",
      headers: ["书舟场景", "推荐路径", "理由"],
      rows: [
        ["简单过滤/路由/单条转换，薄状态或无状态", "路 A：自研消费者", "一条 transform 不值得引入引擎；按第 4、5 章写好提交与幂等即可"],
        ["按时间窗口出营收/活跃等指标，逻辑可用 SQL 表达，团队纯 Go", "路 B：ksqlDB", "窗口、宽限期、乱序、状态容错全部内置；Go 只消费结果主题"],
        ["需要物化“当前值”供点查（如实时排行榜、库存余量查询）", "路 B：ksqlDB 物化表 + pull query", "引擎维护增量物化视图，Go/REST 点查比自研 Redis 维护省心"],
        ["厚状态：会话切分、多维关联、滑窗特征，且规则会持续演化", "路 C：独立 JVM Streams/Flink 服务", "逻辑复杂度已超过 SQL 舒适区；独立服务用主题契约与 Go 侧解耦"],
        ["状态极厚且要跨 Kafka 多数据源 + 批流一体（如数仓近实时层）", "路 C：Flink 集群", "自建计算集群的运维预算值得时再上"],
        ["以上都不是，只是要可靠驱动业务动作", "回第 4/5 章：普通消费组", "事件驱动 ≠ 流处理，别为它引引擎"],
      ],
    },
    {
      type: "callout",
      variant: "note",
      title: "选型是动态的：从 A 出发，把 B/C 当升级台阶",
      body: "书舟的典型演进是：先上路 A 把看板跑起来（数据量小时完全够用），当「5 分钟窗口」「滞后事件修正」「实例迁移不丢状态」开始变成你的 bug 来源时，把对应管道迁到路 B（ksqlDB）或路 C。引擎省的不是「写代码」，而是「分布式状态计算的正确性」——那条路 A 要自己趟。",
    },
    {
      type: "quiz",
      question:
        "批处理与流处理最本质的区别是下列哪一项？",
      options: [
        "流处理只能处理 JSON 数据，批处理可以处理任何格式",
        "流处理用 Go 写、批处理用 SQL 写；本质是语言之争",
        "批处理对“数据已完整”的有界数据集跑一次作业并产出终态结果；流处理对无界持续到达的数据常驻计算，用时间窗口切片并持续更新结果",
        "流处理一定比批处理快，所以实时看板只能选流处理",
      ],
      answer: 2,
      explanation:
        "有界+一次性作业 vs 无界+常驻+窗口切片，才是两者的分水岭（正确选项）。选项 4 是常见误区：延迟差异是**结果**不是**定义**，某些批场景（如深夜全量重算）用流处理反而更笨重；选项 1、2 与定义无关。判断场景该用哪种，看的是数据是否有界、结果是否随事件持续更新，而不是语言或格式。",
    },
    {
      type: "keypoints",
      items: [
        "批处理=有界数据集的一次性作业（终态结果）；流处理=无界数据的常驻计算（窗口切片、结果持续更新）；事件驱动微服务=事件触发的服务协作（单条独立、位点+幂等保可靠）——三者可按场景混用",
        "流应用四形态：实时指标看板、告警规则、实时特征、流式 ETL/富化；Connect 管系统边界，流引擎管 Kafka 内部加工",
        "流引擎的 at-least-once=崩溃后从 checkpoint/changelog/位点重放（可能重复）；exactly-once（引擎内）=消费位点+状态更新+输出写入原子化，仅限 Kafka 读-处理-写闭环；外部副作用仍靠幂等/Outbox",
        "引擎版图：Kafka Streams=进程内 JVM 库（最轻）；ksqlDB=独立 SQL 服务（底层即 Streams）；Flink/Spark=外部计算集群（最重，跨源/超厚状态再上）",
        "Go 生态现实：无官方 Kafka Streams——三条路：自研消费者+外部状态（薄状态）、ksqlDB 结果主题/REST（SQL 可表达的聚合）、独立非 Go 流服务（厚状态，按主题契约集成）",
        "演进建议：从自研起步，当分布式状态计算的正确性开始成为 bug 来源时，再升级到 ksqlDB 或独立流服务",
      ],
    },
    {
      type: "paragraph",
      text: "下一课进入机制层：无论你最终选 ksqlDB 还是自建 Streams 服务，都要懂同一套心智——[拓扑、KStream/KTable、状态与窗口（Java DSL 示意）](/courses/kafka/lessons/kafka-streams-dsl-topology)。它是整个第 8 章的“发动机”，读懂它之后，[ksqlDB：用 SQL 做流处理](/courses/kafka/lessons/kafka-ksqldb-streaming-sql)那课只是给这台发动机换了个 SQL 方向盘。",
    },
  ],
};
