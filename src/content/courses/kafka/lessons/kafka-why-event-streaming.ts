/* ==================================================================
 * 课时：为什么需要事件流平台（kafka-why-event-streaming）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "从同步调用的四个瓶颈出发，推导队列模型与提交日志模型的差异，建立 Kafka「分布式事件流平台」的定位与选型判断。",
  blocks: [
    {
      type: "paragraph",
      text: "大促当晚，「书舟书店」的订单峰值爬到了 5000 单/秒。web 服务把下单请求转给 orders（订单服务），而 orders 要立刻让一连串事情发生：inventory（库存）扣减库存、payment（支付）生成支付单、notify（通知）发短信与推送、analytics（分析）记录行为、recommend（推荐）更新用户特征。系统上线初期，这些反应都是 orders 用同步 HTTP 调用一个个完成的——代码简单直观，下单成功就是「调用全部成功」。但随着流量与团队变多，这条链路开始以各种姿势出问题。这一课从这些具体的痛点出发，推导出 Kafka 存在的原因；你不会看到任何安装与 API，只有一条完整的动机链。",
    },
    {
      type: "heading",
      text: "同步调用的四个瓶颈",
    },
    {
      type: "paragraph",
      text: "把下单做成「orders 逐个调用下游、全部成功才算完成」的同步过程，会踩中四个结构性问题——它们不是写代码不小心造成的，而是同步耦合这个模式本身自带的。",
    },
    {
      type: "list",
      items: [
        "**失败被级联放大**：端到端成功率是各环节成功率的乘积。每个下游偶尔抖动 1%，五个环节叠加后，用户可感知的下单失败率会被放大好几倍；而其中大部分失败其实与下单本身无关——是通知服务或分析服务出了问题。",
        "**慢下游拖垮上游**：一次同步调用占住 orders 的一个线程与连接。5000 单/秒的请求、每个下游平均几十毫秒，orders 的线程池与连接池先被耗尽——最先倒下的不是那个慢的下游，而是订单服务自己。这是分布式系统里最常见的雪崩形态。",
        "**宕机即丢业务**：payment 或 notify 短暂不可用时怎么办？等重试会让下单变慢，放弃调用则真的丢了这笔通知或对账记录。把「没送出去的调用」写进本地表、稍后补偿，本质是在每个业务系统里重复造一个消息系统的轮子。",
        "**事实没有留痕，无法回放**：analytics 想重算昨天的「加购→购买」转化率，新接入的风控团队想从历史订单学特征——但过去的调用结果早已烟消云散。同步调用只把数据交给「当时在场」的人，事后想再读一遍，无处可读。",
      ],
    },
    {
      type: "paragraph",
      text: "注意四个问题的共同根源：把「一件已经发生的事」（订单创建成功）与「对这件事的一系列即时反应」（扣库存、发通知、记分析）**耦合在同一个同步过程里**。反应方会变多、会变慢、会宕机、会想回头看历史，而「事实本身」却只短暂地活在调用链路过的地方，调用一结束就消失了。",
    },
    {
      type: "heading",
      text: "朴素解法一：进程内异步线程池",
    },
    {
      type: "paragraph",
      text: "直觉的第一步是：orders 收到请求后立刻返回，把下游调用丢进进程内的线程池队列，让 worker 慢慢处理。这缓解了「慢下游拖垮上游」，却只碰了四个瓶颈中的一个，还引入了新问题：内存队列活在进程地址空间里，orders 一重启、一发版，未处理的任务全部蒸发；削峰空间受单机内存限制，5000 单/秒的洪峰根本装不下；每个新消费方仍然要 orders 改代码。把「暂存」放在业务进程内部，只是把问题挪了个地方。",
    },
    {
      type: "heading",
      text: "朴素解法二：消息队列——解耦与削峰的里程碑",
    },
    {
      type: "paragraph",
      text: "下一步是把「待办暂存」从进程内搬到独立的系统：一个 broker（消息服务）持有队列，orders 只负责把消息放进队列，下游各自注册成消费者来取。从此 orders 不再认识 inventory、notify、analytics 是谁；下游忙时消息在队列里排队（削峰），下游崩溃后消息也不会丢（只要还没被确认）。这套以 RabbitMQ 为代表的**队列模型**，价值可以用三个词概括：**解耦、削峰、可靠暂存**。绝大多数后端第一次引入消息队列，得到的就是这三样。",
    },
    {
      type: "paragraph",
      text: "但对书舟的场景，队列模型的默认语义有三个错位。需要强调：这些不是缺陷，而是它的设计目标——队列模型优先服务的是「任务分发」。",
    },
    {
      type: "list",
      items: [
        "**消费即删除**：一条消息的生命周期是「投递 → 确认 → 删除」。消费者 ack 之后 broker 立刻丢弃它——它假设「任务做完就没了」。而「订单创建成功」这个事实，在分析做完之后仍然值得存在。",
        "**一条消息只归一个消费者**：队列把每条消息发给一个竞争消费它的 worker。想让库存、对账、分析、推荐各自消费同一份订单事件？可以，但必须预先为每个团队建好队列并绑定——消费方清单在消息产生之前就得定死。事后新加入的团队（比如风控）拿不到任何历史。",
        "**没有回放**：数据删了就删了。「重算昨天的营收」「修复 bug 后重放一遍」这类需求无从谈起——想重放，就得在消费侧自己把每条消息另存一份，等于把日志存储又实现了一遍。",
      ],
    },
    {
      type: "callout",
      variant: "note",
      title: "区分两个模型，问一个问题就够了",
      body: "问它：**「消息被处理完之后，它还应该存在吗？」** 队列回答「不该——处理完即删除，省存储」；日志回答「应该——它是发生过的事实，留着让后来的人也能读」。RabbitMQ 的经典队列主打前者（它后来提供的 Streams 类型是对后者的补丁式补充）；Kafka 从第一天就为后者设计。抱着队列思维用 Kafka，是新手最常见的错位——本课后面的部分会反复回到这句话。",
    },
    {
      type: "paragraph",
      text: "用一个具体的错位把上面的抽象钉死。大促结束第二天，运营要求 analytics 重算「过去 7 天每个 ISBN 的加购 → 购买转化率」，用于复盘——而这套统计逻辑是三个月后才上线的。队列模型下会发生什么？三个月前的消息早已 ack 即删；队列是按当时的消费方清单预先建好的，后来者根本没有位置可读。唯一的出路是求 orders 团队「再发一遍」——而事实是，orders 自己也早已不记得。假如当初每一次下单都被写进一份**保留 7 天的日志**，新逻辑上线要做的事情只有一件：从日志的开头开始读。不需要上游配合、不需要提前打招呼、不需要任何重放接口——「按需回放」是日志模型的默认能力，而不是某个团队额外提供的服务。",
    },
    {
      type: "heading",
      text: "范式转变：把数据当成「追加式提交日志」",
    },
    {
      type: "paragraph",
      text: "好消息是：日志模型你早就见过。任何严肃的数据库都不会把「变更」直接覆盖掉历史——redo log、binlog 先把变更**顺序追加**成日志，副本与订阅者靠重放日志保持一致。日志是计算机系统里最古老也最可靠的数据结构之一：只追加、有序、不改写已经写下的内容。",
    },
    {
      type: "paragraph",
      text: "现在把这个视角从「数据库内部」搬到「服务之间」：与其把消息当成「发给某个人的待办」，不如把每一次业务事实（订单创建、支付成功、库存扣减、用户加购）当作一条记录，追加进一个只增长的日志，让所有关心它的系统自己去读。这就是 [提交日志（commit log）](glossary:commit-log) 模型。它天然具备三种能力，恰好对应上面三个错位：",
    },
    {
      type: "list",
      items: [
        "**保留**：记录不会因为「被读过」而消失，而是按策略保存一段时间（例如订单事件保留数天到数周，供回放与分析）。",
        "**多订阅**：每个读者记录自己「读到了哪」，互不干扰——同一份日志，库存团队追实时，分析团队慢半拍慢慢算，谁也不拖累谁。",
        "**回放**：从头或从任意位置重读——新团队补齐历史、修完 bug 重算数据、审计复核，都只是「换个位置开始读」。",
      ],
    },
    {
      type: "definition",
      term: "事件（event）",
      definition: "已经发生、不可改变的事实记录，如 order.created（订单已创建）；与之相对的是「指令」，如 sendEmail（请去发邮件）。事件流平台承载的是前者：把事实记下来，由订阅方自行决定如何反应。如何设计事件的结构与命名，是第 6 章「事件建模」的主题。",
    },
    {
      type: "paragraph",
      text: "具体到 Kafka 的词汇表：同一类事件放进一个[主题（topic）](glossary:topic)；为了并行，主题被切成若干[分区（partition）](glossary:partition)，每个分区就是一条独立的子日志；分区内每条消息有一个递增的编号，叫[偏移量（offset）](glossary:offset)，订阅者靠它记录自己的进度。这三个词会在下一课逐个展开，这里只需要记住它们的角色：主题是分类，分区是并行与顺序的边界，偏移量是「我读到哪了」的书签。",
    },
    {
      type: "heading",
      text: "Kafka 的定位：分布式事件流平台",
    },
    {
      type: "paragraph",
      text: "Kafka 把「提交日志」做成了通用的分布式基础设施：一批 broker（服务节点）组成集群，主题分区的数据在节点间分散并保留多份副本以防单机故障；生产者往分区尾部追加记录；每个订阅方以独立身份读取；数据按配置的保留策略存活。一句话定位：**Kafka 是一个分布式事件流平台——分布式提交日志 + 多订阅 + 可配置的保留**。消息不会因为被读过而消失，这是它与队列模型最根本的分水岭。",
    },
    {
      type: "paragraph",
      text: "也正因为底层是同一份可回放的日志，这套设施能同时支撑多种用法：服务间异步解耦（本课的场景）、把数据库的变更变成事件流（CDC，第 7 章）、对事件流做持续计算（Kafka Streams 与 ksqlDB，第 8 章）、作为大数据管道的数据源。学习时先把它当「存储系统」来理解，就不会被这些用法绕晕——后面每一章拆开的，都是本课这张地图上的一个格子。",
    },
    {
      type: "heading",
      text: "选型决策表：什么时候该上 Kafka",
    },
    {
      type: "paragraph",
      text: "与 Kafka 竞争的不是「别的消息软件」这一个维度，而是一整条光谱。下表把四种常见方案放在同一组维度上定性比较：性能数字随负载与硬件差异极大，这里只比较**结构性差异**（数据模型、进度归属、生命周期），它们才是选型的决定因素。",
    },
    {
      type: "table",
      caption: "四种「异步数据通道」的结构性对比（定性）",
      headers: ["维度", "Kafka", "RabbitMQ（经典队列）", "Redis Streams", "数据库表 + 轮询"],
      rows: [
        ["底层模型", "分布式提交日志：保留 + 多订阅 + 回放", "队列：投递 → 确认 → 删除（其 Streams 类型除外）", "借鉴日志与消费组思想的流结构，条目长期保留（可设上限）", "普通表：一行是一条消息，用状态列标记处理进度"],
        ["多团队独立消费同一份数据", "原生支持：每个消费组各记各的偏移量，互不影响", "需要提前为每个订阅方建队列并绑定；后加入的团队拿不到历史", "原生支持：每个消费组独立进度，读后不删", "要自己实现多个游标或状态列，容易出错"],
        ["回放 / 重读", "从任意偏移量回放，数据存活期内随时重读", "无：ack 即删除；要回放只能在消费侧自留副本", "可在保留窗口内按 ID 范围重读", "SQL 随时可查，但「按序重放」语义要自己写"],
        ["削峰缓冲", "强：以磁盘为主，可积压大量消息", "强：消息可长期滞留队列", "中：受实例内存与磁盘限制，积压深度有限", "弱：数据库往往是最先被打满的环节"],
        ["延迟（定性）", "毫秒级为主：面向吞吐的批处理与拉取模型", "低：推送模型，无需轮询", "低：以内存为主", "高：取决于轮询间隔，通常秒级起步"],
        ["运维复杂度", "高：分布式集群、副本、分区、KRaft 元数据", "中", "低（跟随已有 Redis）", "无新组件，但查询、加锁、清理逻辑都在业务代码里"],
        ["最合适的场景", "高吞吐事件流：一份事实被多个团队订阅、要历史与回放", "点对点任务分发、命令 / 请求型工作队列", "与 Redis 同栈的中小规模流式或队列需求", "量小、已有数据库、想用 SQL 直接查的「伪事件」场景"],
      ],
    },
    {
      type: "paragraph",
      text: "决策口诀：**需要「一件事只有一个处理者、做完即消失」→ 选队列；需要「同一份事实被多个团队按自己的节奏消费、还要能回头看历史」→ 选日志模型，也就是 Kafka。** 量级很小、不想引入新系统时，Redis Streams 或数据库轮询足够；Kafka 的目标不是「轻量好用」，而是「量大、人多、要历史」时的确定性。",
    },
    {
      type: "heading",
      text: "适用边界：Kafka 不是万能消息系统",
    },
    {
      type: "callout",
      variant: "warning",
      title: "两个 Kafka 不主场的场景",
      body: "① **任务队列**：每个任务恰好被一个 worker 处理一次、处理完即从系统消失——这是纯队列语义。Kafka 的默认心智模型是「日志保留 + 各组独立消费」，不天然提供「每条消息恰好消费一次且消费即删」；要拿它做任务分发，需要用消费组加手动提交加幂等设计自己拼出队列语义（细节见[消费组与经典再平衡协议](/courses/kafka/lessons/kafka-consumer-groups-classic)），或选用 4.x 正在引入的 [Share 组](glossary:share-group)（KIP-932「Queues for Kafka」，本课程写作时为 preview），它提供更接近队列的工作分发。② **低延迟同步请求-响应**：Kafka 面向异步事件流与吞吐，把它当作几百微秒级 RPC 的通道是用错工具——同步调用请用 gRPC/HTTP 与负载均衡。判断方法仍然只有一个：你的数据「需要被记住并可以被多方重读」，还是「只需要被尽快处理掉」？",
    },
    {
      type: "paragraph",
      text: "反过来，当下面几条同时成立时，Kafka 是教科书级的答案：数据是**已发生的事实**而非待办指令；**多个独立团队**都要消费同一份数据；需要**按需回放、补历史**；吞吐高或可能突然飙升；希望消费方的增减不影响生产方。第 6 章还会进一步讨论「事件本身应该设计成什么样」，第 7、8 章则展示如何让外部数据库与流计算也挂上同一份日志。",
    },
    {
      type: "heading",
      text: "自测：你会怎么选",
    },
    {
      type: "quiz",
      question: "以下哪个场景最适合直接用 Kafka 解决？",
      options: [
        "用户注册后需要立刻发出一封验证邮件——邮件由一个 worker 发送一次即可，发完任务即结束。",
        "订单创建事件要被库存（实时扣减）、支付对账、行为分析、推荐四个团队独立消费；分析团队偶尔要重算过去 7 天的数据；新增消费团队时不应要求订单服务改代码。",
        "一个 web 服务需要毫秒级同步获取用户资料，希望用消息系统承载请求与响应。",
        "图片上传后要生成三档缩略图，每个任务只需一个 worker 处理，worker 崩溃后任务自动交给其他 worker。",
      ],
      answer: 1,
      explanation: "选 1：它命中了 Kafka 的全部设计目标——一份事件事实、多个团队独立消费、可回放历史、订阅方可随时增减。选项 0 与 3 是经典任务队列：每条消息只应被处理一次、处理完即消失，这是 RabbitMQ 一类队列模型的主场，用 Kafka 需要自己拼出队列语义；选项 2 是同步请求-响应，应使用 gRPC/HTTP，消息系统（尤其 Kafka 这种批处理加拉取的模型）不是低延迟 RPC 通道。判断口诀：数据需要被多方记住并可重读，选事件流；只需要被尽快处理掉，选队列；需要立刻拿到结果，选 RPC。",
    },
    {
      type: "keypoints",
      items: [
        "同步链路的四个瓶颈——失败放大、慢下游拖垮上游、宕机丢业务、无法回放——同源于「事实」与「即时反应」的耦合",
        "队列模型的生命周期是「投递 → 确认 → 删除」，是任务分发的好工具，但不是「一份数据多方订阅 + 回放」的模型",
        "Kafka 把业务事实当作分布式提交日志：保留一段时间、多个消费组独立推进、可任意回放",
        "决策口诀：需要被多方记住并可重读 → 事件流；只需被尽快处理掉 → 队列；需要立即拿结果 → RPC",
      ],
    },
    {
      type: "paragraph",
      text: "方向已经确立：用分布式提交日志承载业务事实。但「日志」在 Kafka 里不是抽象概念——主题、分区、偏移量这些结构具体到每条消息的落点，消息从生产到被读取会穿过一整套组件。下一课 [核心心智模型：一次消息的旅程](/courses/kafka/lessons/kafka-core-model) 会立起整门课的地图：每个组件一句话职责，外加一条消息从 orders 出发到 inventory 落地的完整旅程。地图立起来之后，[本地运行 Kafka 4.3（KRaft）与 CLI 初体验](/courses/kafka/lessons/kafka-kraft-quickstart) 再让你亲手看到这些概念变成真实进程与命令。",
    },
  ],
};
