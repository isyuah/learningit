/* ==================================================================
 * 课时：面试速查：核心机制问答（kafka-interview-core）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 *
 * 第 11 章第一课：把第 1–2 章的心智模型与机制压缩成「可直接口述的
 * 面试回答框架」。答案全部来自课程正文，不引入新机制。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary: "核心机制类高频面试题的回答框架：Kafka 是什么、与 MQ 的区别、一次旅程、拉模式、分区与顺序、分区数、不丢三段式、为什么快、副本组合推演，每题配易错点与复习链接。",
  blocks: [
    {
      type: "heading",
      text: "这一课怎么用",
    },
    {
      type: "paragraph",
      text: "第 11 章是「面试速查与总复习」，三课分别覆盖**核心机制**（本课）、**可靠性语义**、**集群运维与系统设计**。它们是复习地图，不是新知识：每题的「回答框架」都是课程正文压缩成的口述稿，只提炼了面试官真正想听到的结构。用法只有一条——**先自答，再对照**：看到问题先在心里（或出声）讲一遍，讲完再展开框架逐条核对，差的不是「记住答案」，而是「你的表述有没有把术语说准、把默认值说对、把边界划清」。发现自己某一条答不上来或答得含糊，点开题后的**关联课时**重读对应段落，再回来重答一遍；全部过完后，用 [总复习测验](/courses/kafka/lessons/kafka-final-checkpoint) 做一次整体检验。",
    },
    {
      type: "callout",
      variant: "note",
      title: "面试表达的三条通用习惯（本课所有题的底层）",
      body: "**一、拆段回答**：凡问「Kafka 是否保证 X」，先拆链路再下结论（生产者段 / Broker 段 / 消费端段），这是整门课反复训练的三段式。**二、给默认值**：说到 acks、提交、起点时带上默认值（acks 默认 all、franz-go 自动提交默认 5 秒、消费起点默认 earliest 等），一个准确的默认值能立刻把回答从「概念正确」提到「用过且核对过」。**三、主动划边界**：答完保证立刻补一句「它保证不了什么」——面试官问可靠性，等的就是你主动说出边界。本课所有框架都按这三条组织。",
    },
    {
      type: "heading",
      text: "Q1. 一句话说清 Kafka 是什么（提交日志心智模型）",
    },
    {
      type: "paragraph",
      text: "这通常是第一题，也是送分或送命题。送命版本是把 Kafka 说成「一个高性能消息队列」——不是错得离谱，而是暴露你拿队列心智模型在学它，后面所有关于顺序、回放、消费组的追问都会跟着歪。正确姿势是先立心智模型，再给组件地图。",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "**定位句**：Kafka 是一个**分布式事件流平台**，底层是一个分布式[提交日志（commit log）](glossary:commit-log)——把业务事实按顺序追加记录、保留一段时间、供任意多个订阅方独立读取和回放，而不是把消息「投递给某个人」。",
        "**提交日志的三个性质**（每一条都要能展开一句）：只追加、按序编号；消费**不删除**消息，删除只由[保留策略](glossary:retention)决定；每个读者（[消费组](glossary:consumer-group)）自己记「读到哪了」，互不干扰，随时可以回放。",
        "**与队列的分水岭一句话**：队列是「投递 → 确认 → 删除」，消息处理完就消失；日志是「追加 → 保留 → 多读」，一条事实可以被库存、分析、风控各自读一遍，事后新来的团队还能从头补读。",
        "**落到组件地图**（备用一句）：消息按[主题（topic）](glossary:topic)分类、主题切成[分区（partition）](glossary:partition)获得并行与顺序边界、分区内消息有递增的[偏移量（offset）](glossary:offset)；数据由多个 [Broker](glossary:broker) 各存副本，元数据由 [KRaft](glossary:kraft) quorum 管理——brokers 存业务数据，controller quorum 管元数据。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 全程用「队列/消息中间件」称呼它，把「削峰填谷」当第一卖点——那是队列的卖点，Kafka 的第一性卖点是「事实留痕、可多方重读」。② 以为「消息被所有消费者读完就会被清理」——不会，清理只由保留策略触发，与谁读过无关。③ 答组件时把消费组协调器（coordinator）和 [控制器（controller）](glossary:controller)混为一谈：前者管消费组与提交，后者管集群元数据，是两个不同角色。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[为什么需要事件流平台](/courses/kafka/lessons/kafka-why-event-streaming)（队列 vs 日志的完整推导）、[核心心智模型：一次消息的旅程](/courses/kafka/lessons/kafka-core-model)（组件全景与三个钉子）。",
    },
    {
      type: "heading",
      text: "Q2. Kafka 和 RabbitMQ 的本质区别",
    },
    {
      type: "paragraph",
      text: "面试官常让「对比 Kafka 与 RabbitMQ」，考察的不是两家公司或性能数字，而是你是否理解**队列模型与日志模型是两种不同的数据生命周期**。记住：RabbitMQ 的经典队列是任务分发工具，Kafka 是事件流存储平台，二者可以共存于同一家公司，选型取决于数据该被「处理完就忘」还是「记住并供多方重读」。",
    },
    {
      type: "list",
      items: [
        "**数据生命周期**：队列是投递 → 确认 → 删除（ack 即消失，默认假设任务做完就没价值）；Kafka 是追加 → 保留 → 回放，被读过不删，只按保留策略清理。",
        "**消费模型**：队列一条消息只给一个竞争 worker（work queue）；Kafka 里每个[消费组](glossary:consumer-group)都读全量、各组进度独立——库存组追实时、分析组慢慢算，互不拖累；组内成员按分区瓜分，分区是并行与顺序的单位。",
        "**回放与补历史**：RabbitMQ 的 ack 即删让「重算昨天的数据」无从谈起；Kafka 只要数据还在保留期内，新组从 earliest 或把组位置重置，就能免费重读——不需要上游配合、不需要复制数据。",
        "**消息路由能力**：RabbitMQ 有 exchange/routing key/优先级/延迟消息等丰富的路由与队列特性；Kafka 的路由只有一个维度——key 决定分区（顺序由此而来），没有原生延迟消息、优先级、按需选择性消费。",
        "**适用边界一句话**：需要「一件事恰好被处理一次、做完即消失、分给多个 worker」→ 选队列；需要「一份事实被多个团队按自己节奏消费、还要能回头看历史」→ 选 Kafka。Kafka 拿来做纯任务队列也可以（消费组 + 手动提交 + 幂等自己拼语义），或用 4.x 正在引入的 [Share 组](glossary:share-group)（KIP-932，preview 状态）。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 说「RabbitMQ 比 Kafka 快」——量级结论要看你比什么：Kafka 面向吞吐的批处理与拉取模型在「高吞吐事件流」上是强项，RabbitMQ 推送模型的低延迟在任务分发上有优势，直接比谁快是伪命题，先问场景。② 说「Kafka 支持延迟消息」——原生不支持，课程里重试延迟是用多级重试主题或外部调度实现的。③ 拿 RabbitMQ 后来补的 Streams 类型来论证「RabbitMQ 也能做日志」——它确实是补丁式补充，但面试要讲的是**经典模型的默认心智**，别把对照讲糊。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[为什么需要事件流平台](/courses/kafka/lessons/kafka-why-event-streaming)（四种方案的完整决策表与适用边界）、[核心心智模型](/courses/kafka/lessons/kafka-core-model)（提交日志的保留/多订阅/回放三能力）。",
    },
    {
      type: "heading",
      text: "Q3. 请口述「一次消息的旅程」",
    },
    {
      type: "paragraph",
      text: "这道题考的是你能不能把「生产者 → Broker → 消费者」整条链路在脑内跑一遍，而不是背组件名词。口述时用一个具体例子（书舟的「订单已创建」）串起来最有说服力。下面是按课程十一站压缩成的八句口述稿，能顺着讲完并说清每句的「为什么」，这一题就稳了。",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "**序列化与路由**：orders 服务把订单事件序列化成字节（Kafka 只搬运字节，格式约定是生产消费双方的契约），带上 key = `order_id`，客户端分区器对 key 做 murmur2 哈希取模，**同 key 永远进同一分区**——这是「同一订单事件有序」的来源。",
        "**发送给 leader**：消息不是发给「主题」这个抽象，而是发给目标分区的 leader 副本所在 broker；消息先在客户端缓冲里攒批再发出，不是逐条发。",
        "**顺序落盘获得 offset**：leader 把记录追加到分区日志尾部——只追加的顺序写——分配递增编号 [offset](glossary:offset)，此刻数据躺在页缓存里。",
        "**副本同步**：follower 副本像特殊消费者一样从 leader 拉取并写自己的日志；跟得上 leader 的副本构成 [ISR](glossary:isr)。",
        "**按 acks 确认**：生产者按 [acks](glossary:acks) 配置决定何时算成功——默认 all，即等当前 ISR 全部落盘；注意确认的是「已安全落盘」，**不是「已送达消费者」**。",
        "**协调器分配，消费者拉取**：[组协调器](glossary:coordinator)早已把分区分配给了消费组某成员；消费者主动发 Fetch 拉取，处理完一批业务（幂等扣库存）。",
        "**处理完才提交**：成功后提交偏移量——「这个分区我读到 1271 了」；提交时机决定崩溃时是丢（提交太早）还是重（提交太晚）。",
        "**消息仍在日志里**：整趟旅程结束，消息还在分区里；analytics 组停在 900 慢慢算，新团队还能从头读——数据何时消失只由保留策略决定。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 把「确认成功」说成「消息已送达消费者」——acks 管的是落盘复制，与消费毫无关系。② 口述到消费就结束、忘了最后一句「消息仍在、可回放」——这句才是提交日志模型的灵魂，也最容易丢。③ 说「消息被消费后删除/移出主题」——没有这回事，消费者只是复制并处理了一个视图。④ 把「分区 3」说成「主题的队列」时注意用词：分区是并行的子日志，不是队列。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[核心心智模型：一次消息的旅程](/courses/kafka/lessons/kafka-core-model)（十一站全图与三个钉子）、[franz-go 初体验](/courses/kafka/lessons/kafka-go-client-hello)（旅程的代码形态）。",
    },
    {
      type: "heading",
      text: "Q4. 为什么 Kafka 用拉（pull）而不是推（push）",
    },
    {
      type: "paragraph",
      text: "这是高频原理题，标准答法不是背「官方文档这么说」，而是从「推送模型必须解决什么问题」反推：broker 推消息，就必须替消费者做调度——处理不过来怎么办？于是要引入在途数量限制（prefetch/credit）、慢消费者检测等一整套路流控。Kafka 把消费节奏完全交还客户端，代价由消费者承担。",
    },
    {
      type: "list",
      items: [
        "**慢消费者不会压垮 broker**：broker 只负责存好数据、按需供应；消费者不来取就没有任何压力，慢只会表现为自己的[消费滞后（lag）](glossary:lag)上涨，不拖累集群与别人——推送模型里慢消费者是要被流控甚至断开的「问题对象」。",
        "**天然批量**：消费者按批拉取，fetch 请求可以让 broker「攒够字节或等一小段时间再返回」，把网络往返与处理开销摊薄到一批消息上，契合 Kafka 的吞吐导向。",
        "**断线续读免费**：消息不因被读而删除，消费者崩溃后带着自己的位置回来接着读即可——「读游标在客户端、数据在服务端」，这正是日志模型相对队列的红利；推送模型必须靠未确认重投等机制才能做到不丢。",
        "**代价**：消费者要自己管理位置与提交、处理空转（拉空了阻塞等待而非忙等）、承担重读风险——位置管理复杂度从 broker 转移到了客户端。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 以为「拉 = 轮询 = 延迟高」——不是：fetch 请求会挂在 broker 上等新数据（franz-go 默认最多等 5 秒或攒够字节才返回），是 long-poll 式挂起等待，不是忙轮询，毫秒级延迟场景依然可用。② 说「推模型更实时」要小心：延迟取决于实现，Kafka 拉的延迟在低吞吐主题下由 fetch 等待时间控制，可调。③ 把「背压」说成 Kafka 独有的——拉模型是**结构性自带背压**（消费者天然不会收到超过自己索取能力的消息），而推送模型要靠 prefetch 机制模拟背压，这才是本质差别。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[拉取模型、位置管理与提交时机](/courses/kafka/lessons/kafka-consumer-poll-commit)（拉 vs 推的展开与 PollFetches 循环）。",
    },
    {
      type: "heading",
      text: "Q5. 分区、key 与顺序：Kafka 的顺序保证到底是什么",
    },
    {
      type: "paragraph",
      text: "顺序问题是面试必考，考察你是否精确掌握「分区是顺序与并行的唯一边界」。标准答案分三层：保证什么、靠什么保证、代价是什么。",
    },
    {
      type: "list",
      items: [
        "**保证的边界**：顺序是**分区内**的，不是主题内的。同一分区内消息按追加顺序获得递增 offset、消费者按序读到；跨分区没有全局顺序，谁先谁后 Kafka 不回答也不保证。",
        "**业务顺序靠 key 获得**：同一业务实体（一个订单）的多个事件要按序处理，就让它们带同一个 key——默认分区器对非空 key 做 murmur2 哈希取模（跨语言客户端一致，因为哈希算法被刻意统一），**同 key 必进同分区**，分区内自然有序。书舟的 `orders.events` 用 `order_id`、`user.behavior` 用 `user_id`。",
        "**key 路由的准确表述**：注意「同 key 同分区」是**默认分区器实现的产物**，成立前提是分区数不变、没人换分区器——它不是 Kafka 层面独立于实现的承诺。无 key 的消息走 sticky 策略（攒到约 64 KiB 换分区、倾向积压最少者），长期近似均匀。",
        "**代价**：跨分区无全局序；想要全主题严格有序只能单分区（放弃并行）；组内消费并行度上限 = 分区数（一个分区同一时刻只归一个成员）；key 选得太粗会出热点（一个 key 的流量全压一个分区，加实例也救不了）。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 说「Kafka 保证同 key 消息按发送顺序到达」——要补半句「同一客户端实例内」：分区内顺序由追加顺序决定，但跨实例并发发送同一 key 时，到达顺序仍取决于各实例何时写入；另外幂等关闭 + 多 in-flight 时，重试会造成同分区内乱序（第 3 章的事故剧本）。② 说「主题消息全局有序」——只有单分区主题才成立。③ 把 key 当消息内容的一部分来消费依赖——key 是路由依据，消费端别依赖它的存储细节。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[主题、分区与键：并行与顺序的边界](/courses/kafka/lessons/kafka-topics-partitions-keys)（分区器语义与推导全流程）、[核心心智模型](/courses/kafka/lessons/kafka-core-model)（钉子一：分区的双重身份）。",
    },
    {
      type: "heading",
      text: "Q6. 分区数怎么定？扩容有什么陷阱",
    },
    {
      type: "paragraph",
      text: "这是把「机制理解」升级为「设计能力」的题。回答要同时给出**决策流程**（三个约束）和**扩容代价**（为什么必须一次定对），最好带上一个量级推导过程而不是直接报数字。",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "**约束一 · 吞吐**：单分区是一条串行追加流，吞吐受单台 broker 磁盘顺序写、网卡与请求处理限制。全主题峰值吞吐 ÷ 单分区可持续吞吐 = 分区数下限。经验量级（KB 级小消息）单分区每秒数千到上万条，但**不许照抄数字**——必须用目标消息大小压测。",
        "**约束二 · 消费并行度**：一个消费组内一个分区同一时刻只归一个成员，所以组内最大并行度 = 分区数。想让 8 个实例都干活，分区数就得 ≥ 8。",
        "**约束三 · 增长余量**：按未来 1–2 年峰值算，别按今天均值算（理由见下一步——扩容有真实代价）。书舟推导示范：`orders.events` 下单峰值 5000 单/秒 × 每单 3~5 个事件 ≈ 2 万条/秒，取 12 分区；`user.behavior` 10 万条/秒，取 24 分区——量级示范，压测校准。",
        "**扩容陷阱**：分区数**只能增不能减**（官方不支持减少分区，涉及合并有序日志）。增加分区时取模底数变了：hash(key) % 12 变 % 24，约一半 key 会换到新分区，而 **Kafka 不迁移任何旧数据**——同一订单的历史事件留在老分区、新事件可能进新分区，跨分区顺序断裂不是临时抖动而是**结构性事实**；订单生命周期横跨扩容窗口时可能先看到 paid 再看到 created。",
        "**缓解手段**：低峰扩容、下游按事件时间而非到达序处理、消费端幂等、或重建跨窗口 key 的会话——都是补救，真正解法是**按 2 年峰值一次定对**。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 拿「平均吞吐」而不是「峰值 × 增长」算——分区按峰值定。② 以为扩分区 Kafka 会重排/迁移旧数据让同 key 连续——不会，这正是坑。③ 以为「单分区 lag 高就加分区加实例」——热点分区由单个 key 压满时，加分区/加实例都无效（并行上限是分区数，热点分区同刻只有一个消费者），要先查是不是 key 倾斜。④ 报「单分区 X 万条/秒」这种拍脑袋常数——量级 + 假设 + 必须压测才是完整答案。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[主题、分区与键](/courses/kafka/lessons/kafka-topics-partitions-keys)（三重约束推导与扩容代价）、[容量规划与性能调优](/courses/kafka/lessons/kafka-capacity-tuning)（吞吐模型与压测方法）。",
    },
    {
      type: "heading",
      text: "Q7. 消息不丢怎么保证？（三段式回答）",
    },
    {
      type: "paragraph",
      text: "凡是被问到「不丢消息」，立刻进入本课 callout 里说的三段式。三段 = 生产者到 Broker 段、Broker 副本段、Broker 到消费者段，每段说清「由什么机制决定、怎么配才不丢、默认是什么」。",
    },
    {
      type: "list",
      items: [
        "**第一段 · 生产者 → Broker**：丢不丢看 [acks](glossary:acks)（0=发完不管、1=leader 落盘即成功、all=等当前 ISR 全部落盘）与是否重试。保证手段：acks=all（franz-go 默认）+ 有限重试覆盖瞬态故障 + 幂等生产者去重 + 显式交付上限暴露真故障。默认组合下，**已确认**消息在这一段不丢。",
        "**第二段 · Broker 内部（副本）**：丢不丢看 replication.factor、[ISR](glossary:isr) 与 [min.insync.replicas](glossary:min-insync)、以及 [unclean leader election](glossary:unclean-election) 开关。生产姿势：RF=3 + minISR=2 + 关闭 unclean（官方默认关）——多数派先持久化才算提交；ISR 跌破 2 时 acks=all 写入被拒（NotEnoughReplicas），把「静默丢」变成「显式失败」。",
        "**第三段 · Broker → 消费者**：丢不丢看**提交时机**。先提交后处理 = 崩溃跳消息（丢）；处理完再提交 = 崩溃后重读（重复但不丢）。可靠姿势：处理成功再提交、`OnPartitionsRevoked` 补交、幂等消费吸收残余重复。",
        "**收口句**：把三段拼起来，官方默认配置（acks=all + 幂等 + 处理完提交）给出的是 **at-least-once**——已确认消息在「至少一个 ISR 副本存活」的前提下不丢，但消费端崩溃会带来重复；重复不是 bug，是 at-least-once 的固有代价，靠幂等兜底。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 只答一段（最常见的：只答 acks=all，忘了消费端提交时机与副本段）。② 说「Kafka 保证不丢」——没有任何单一配置能横跨三段打包承诺，即使全配好也是 at-least-once + 各种边界（ISR 全灭、unclean、进程重启窗口）。③ 把「幂等生产者」当防丢手段——它防的是**重试造成的重复**，防丢靠 acks 与副本；把「事务」当万能——它只管 Kafka 内闭环（见可靠性课）。④ 默认值说错会大幅减分：acks 默认 all（franz-go）、自动提交默认开启 5 秒（提交上一轮 poll 的位置）、unclean 默认关闭、minISR broker 默认 1。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[投递语义：从 at-most-once 到 exactly-once](/courses/kafka/lessons/kafka-delivery-semantics)（丢失与重复的三段归属表）、[可靠发布：acks、重试与幂等生产者](/courses/kafka/lessons/kafka-producer-reliability-acks)、[拉取模型与提交时机](/courses/kafka/lessons/kafka-consumer-poll-commit)。",
    },
    {
      type: "heading",
      text: "Q8. Kafka 为什么快？（以第 2 章存储课为准）",
    },
    {
      type: "paragraph",
      text: "「为什么快」要答的是**设计哲学**而非魔法：把随机 IO 转成顺序 IO、把缓存交给操作系统、把网络开销摊薄到批上。四个机制按权重讲，最后补一句「数字要压测」收尾。",
    },
    {
      type: "list",
      items: [
        "**顺序写**：分区日志只追加，写磁盘是连续 IO——机械盘顺序写比随机写高几个数量级（官方设计文档举例 7200 转盘阵列顺序写约 600 MB/s 对随机写约 100 kB/s，只为量级直觉）；Kafka 把「磁盘很慢」变成了「顺序磁盘其实很快」。",
        "**OS 页缓存**：Kafka 写日志后并不着急刷盘，数据先落页缓存即可响应，热数据消费直接命中页缓存——**追上进度的消费者在磁盘上几乎没有读活动**；Kafka 刻意不在进程内自建缓存，把内存管理交给操作系统（换页、回收都由 OS 决策）。",
        "**批量**：客户端攒批发送（key 消息粘批、无 key sticky 到约 64 KiB）、broker 端顺序追加、消费者按批 fetch——网络往返与系统调用被摊薄到一批消息上，这是吞吐的关键放大器，与零拷贝配合使用。",
        "**零拷贝**：网络发送走 sendfile 路径，数据从页缓存直接进网卡，不经用户态拷贝——消费是读路径零拷贝的典型受益者。",
        "**分区并行**：主题切成多分区分布到多 broker/多磁盘，吞吐随分区横向扩展；以上单点机制 × 分区并行 = 集群吞吐。",
        "**收口句**：性能哲学是「随机 IO 转顺序 IO、缓存交给 OS、能批就批、能零拷贝就零拷贝」；所有具体数字（单分区几条/秒、多少 MB/s）都依赖消息大小、硬件、副本数与批量设置——**上线前压测**，不报拍脑袋常数。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 说「Kafka 快因为数据在内存里」——错：数据主体在磁盘（页缓存是 OS 管理的加速层），Kafka 进程内没有自己的全量缓存，这正是它与 Redis 类系统的本质区别，也是它能积压海量消息的原因。② 零拷贝不是万能——它优化的是「页缓存 → 网卡」的消费路径；生产者写入侧的顺序写与批处理同样关键。③ 别把「高吞吐」讲成「低延迟」——Kafka 面向吞吐的批处理与拉取模型在延迟上不是极致（毫秒级为主），面试被追问时主动承认这是取舍。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[存储：Segment、保留策略与日志压缩](/courses/kafka/lessons/kafka-storage-segments-retention)（顺序写/页缓存/零拷贝的原文出处）、[批量、压缩与吞吐调优](/courses/kafka/lessons/kafka-producer-batching-throughput)（客户端侧批量的另一半）。",
    },
    {
      type: "heading",
      text: "Q9. 场景组合题：RF=3、minISR=2、acks=all，逐台宕机会发生什么",
    },
    {
      type: "paragraph",
      text: "这类题考的是三个数字的**联动推演**，不是背定义。先立两条底层规则：**acks=all 等的是「当前 ISR 全体」而不是全部副本**；**min.insync.replicas 是 broker 侧的下限保险丝——ISR 数量低于它，acks=all 写入被拒**。然后让机器一台台倒下去推演：",
    },
    {
      type: "list",
      ordered: true,
      items: [
        "**正常态（ISR = {A, B, C}）**：acks=all 要求三份都落盘才算成功——注意不是「两份」：minISR=2 只是下限，all 等的是 ISR 全体，ISR=3 就等 3 份。",
        "**宕一台（ISR 缩到 2，恰好等于 minISR）**：写入继续成功，acks=all 等幸存的 2 个 ISR 成员都确认——不触发拒绝，因为 2 ≥ 2。此时若宕的是 leader，controller 从 ISR 选新 leader，已确认消息不丢（新 leader 拥有全部已确认数据）。",
        "**再宕一台（ISR 只剩 1，低于 minISR=2）**：acks=all 的新写入被拒，返回 NotEnoughReplicas(AfterAppend) 一类错误——可重试，客户端退避等待 ISR 回升。**这是设计意图**：系统选择「拒绝写入、让业务感知降级」，而不是把已确认消息冒险压在单份副本上继续「成功」。存活节点还能读。",
        "**ISR 全灭（unclean 开关登场）**：没有任何同步副本可接任 leader。默认 `unclean.leader.election.enable=false`：等 ISR 副本恢复（若永久损坏，分区持续不可用——用可用性换「已确认数据一条不丢」）；打开开关：允许不在 ISR 的落后副本当选，分区快速恢复，但它缺 leader 尾部的已确认消息，当选即截断——**已确认消息永久丢失**。",
        "**收口句**：min.insync.replicas 把「悄悄丢已确认数据」变成「可见的写入失败」；unclean=false 把「可见的失败」变成「等 ISR 恢复」。两个开关都是在用可用性换确定性——订单支付类数据丢了没法交代，短暂写入降级远比静默丢单可接受。",
      ],
    },
    {
      type: "callout",
      variant: "warning",
      title: "易错点",
      body: "① 以为 acks=all 是「等 replication.factor 全部副本确认」——错，等的是**当前 ISR 全体**，落后副本掉出 ISR 后就不再等它。② 以为 minISR=2 时「all 只需 2 份确认」——错，minISR 是下限不是目标值，ISR=3 时 all 等 3 份；把它说成「多数派确认」是通俗近似，严谨说法是「ISR 全体 + ISR ≥ minISR」。③ 说 unclean 默认开启——默认关闭（官方从 0.11 起默认保数据）。④ 忘了描述单副本（RF=1）时 minISR=2 的荒谬：ISR 永远只有 1 个成员，所有 acks=all 写入直接全拒——开发环境可用，生产请当成「没有备份」。",
    },
    {
      type: "paragraph",
      text: "**关联课时**：[副本、ISR 与数据安全](/courses/kafka/lessons/kafka-replication-isr)（故障推演表与 ISR 收缩机制）、[可靠发布：acks、重试与幂等生产者](/courses/kafka/lessons/kafka-producer-reliability-acks)（acks=all 边界与 NotEnoughReplicas 处置）。",
    },
    {
      type: "heading",
      text: "本课收束：核心机制自检",
    },
    {
      type: "keypoints",
      items: [
        "Kafka = 分布式提交日志 / 事件流平台：追加、保留、多组独立读取与回放；消费不删除消息，删除只由保留策略决定",
        "与队列的分水岭：投递-确认-删除 vs 追加-保留-多读；需要「被记住并多方重读」选 Kafka，需要「做完即消失的任务分发」选队列",
        "一次旅程口述八句：序列化+key 路由 → 发 leader → 顺序落盘获 offset → 副本同步 → acks 确认 → 协调器分配+拉取 → 处理完提交 → 消息仍在日志",
        "拉模型把消费节奏交给客户端：慢消费者只涨自己的 lag，结构自带背压；代价是位置管理归消费者",
        "顺序是分区内的：同 key 同分区（默认分区器 murmur2 取模的产物）；跨分区无全局序；组内并行上限 = 分区数",
        "分区数按「峰值吞吐、消费并行度、2 年增长」三重约束定；扩容只增不减且让同 key 新旧数据可能分居两区（Kafka 不迁移旧数据）",
        "不丢消息三段式：acks+重试（生产者段）、RF/ISR/minISR/unclean（Broker 段）、提交时机（消费段）；默认组合 = at-least-once",
        "快的原因：顺序写 + OS 页缓存 + 批量 + 零拷贝 + 分区并行；数字必须压测，别报常数",
        "RF=3/minISR=2/acks=all 推演：all 等当前 ISR 全体；ISR=1 时拒绝写入；unclean 默认关——两个开关都在用可用性换确定性",
      ],
    },
    {
      type: "quiz",
      question: "面试官问：「一条消息被某个消费组处理完之后，它在 Kafka 里怎么样了？」以下哪个回答最准确？",
      options: [
        "消息从分区日志中删除，因为 Kafka 只保留未被消费的消息以节省存储",
        "消息转移到 __consumer_offsets 主题里长期保存，供以后审计回放",
        "消息仍留在原分区日志里，直到保留策略（时间/大小/压缩）决定清理它；其他消费组仍可独立读取，该组也可重置位置回放",
        "消息被压缩成一个聚合记录，只有当所有消费组都处理完才会被清除",
      ],
      answer: 2,
      explanation: "这是提交日志模型与队列模型的根本差异：消费只移动该组自己的偏移量书签，不删除任何数据。消息何时消失只由保留策略决定，与「谁读过、读到哪」无关；正因为如此，其他组能独立读、该组也能重置位置回放历史。选项 1 是队列语义；选项 2 混淆了 __consumer_offsets（只存各组提交的位移，不存消息本体）；选项 4 的「所有组处理完才清除」同样是把队列思维套在日志上。",
    },
  ],
};
