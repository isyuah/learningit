/* ==================================================================
 * 课时：事件建模与兼容性思维（kafka-event-modeling）
 * ----------------------------------------------------------------
 * 一节课 = 一个 Lesson 对象。slug 必须与 course.ts 大纲中的一致。
 * 内容块类型见 ../../../types.ts；写作规范见 docs/CONTENT-AUTHORING.md。
 *
 * 第 6 章第一课：从“消息怎么可靠地送”转向“消息本身是什么”。
 * 兼容性三型的定义与 Avro 字段演化例子按 Confluent Schema Registry
 * 官方文档 schema-evolution / API reference 口径核对（2026-09）。
 * ================================================================== */
import type { Lesson } from "../../../types";

export const lesson: Lesson = {
  summary:
    "把事件当作“已发生的事实”来设计：信封字段约定、时间语义、事件命名，以及向后/向前/完全兼容与演进纪律。",
  blocks: [
    {
      type: "paragraph",
      text: "第 5 章结束时，你已经能把一条消息从生产到消费可靠地送完：acks、提交时机、死信、幂等消费。但第 1 章到第 5 章里，消息的 Value 一直是一坨「先 `json.Marshal` 一下再说」的字节——生产方和消费方靠心照不宣的 JSON 结构通信，第 3 章在代码注释里给你留了一句话：*「事件结构规范见第 6 章」*。这一章就兑现这句话。本课先解决「消息应该长什么样」：什么是事件、时间字段怎么放、字段怎么命名；下一课解决「契约怎么被机器管理」（[Schema Registry](glossary:schema-registry)）；再下一课解决「业务库和 Kafka 怎么一致地产生事件」（事务性 [Outbox](glossary:outbox)、CDC）。",
    },
    {
      type: "heading",
      text: "先分清两样东西：事件（event）与指令（command）",
    },
    {
      type: "definition",
      term: "事件（event）",
      definition:
        "已经发生的事实，用过去时描述，不可否认、不可改变。例如「订单 20260908-000123 已创建」「该订单已支付」。事件是领域发出的客观陈述，发布者不期待、也不等待某个确定的处理结果。",
    },
    {
      type: "definition",
      term: "指令（command）",
      definition:
        "还没有发生的意图或请求，用祈使/将来语义描述，可能被拒绝。例如「请把订单 20260908-000123 取消」「给用户 u_10086 发优惠券」。指令带有明确的期望结果与失败语义，通常需要回复（成功/失败/回执）。",
    },
    {
      type: "paragraph",
      text: "判据很简单：**它是不是已经发生了？**「用户下单」这个动作发生之后，才产生 `order.created` 事件；而「用户点了下单按钮」本身只是意图，那是 HTTP 请求/指令的领域。把指令发进事件主题，是所有事件建模事故里最隐蔽的一种——表面看「消息发出去了、也有人消费了」，但语义上你在用 Kafka 传 RPC：消费者必须回复结果、失败必须重试到成功、消息顺序成了指令的执行顺序……这些全是[消息队列/提交日志](glossary:commit-log)不擅长、也承诺不了的东西。Kafka 主题的[保留策略](glossary:retention)、[分区](glossary:partition)顺序、可回放这些特性，全部是为「事实的日志」设计的，不是为「请求的队列」设计的。",
    },
    {
      type: "heading",
      text: "时间语义：occurred_at 与“处理时间”分离",
    },
    {
      type: "paragraph",
      text: "事件是事实，事实最重要的属性是**它发生的时刻**。但「发生时刻」在分布式系统里有歧义——一条订单事件会经过至少三个时钟：业务事实发生的时刻（用户下单那刻）、生产者把它写进 Kafka 的时刻、消费者处理它的时刻。三者可能差几毫秒，也可能差几天。第 3 章在讲 `Record.Timestamp` 时已经预告过这个坑：**Kafka 记录自带的时间戳是存储元数据（默认 CreateTime 语义，客户端写入时打点），不是业务事实时间**。本课把三者的分工彻底定下来：",
    },
    {
      type: "table",
      caption: "一条事件的三种时间，各归各用",
      headers: ["时间", "谁打点", "含义", "用途", "能重放吗"],
      rows: [
        ["`occurred_at`（事件体里）", "业务发生处，事实发生时", "事实在业务世界里发生的时刻", "审计、按事实排序、窗口聚合的业务时间、迟到事件判定", "能：随消息体重放，永远不变"],
        ["Kafka 记录时间戳（`Record.Timestamp`）", "生产者/存储层", "记录被生产/追加的时刻（存储元数据）", "[retention](glossary:retention) 过期、时间戳索引、运维排障", "重放后是新值（因为重放本身发生在新的时刻）"],
        ["消费处理时间（消费方自记 `processed_at`）", "消费者", "这条消息被本服务处理完的时刻", "消费延迟/链路耗时统计、处理记录表的时间戳", "重放后也是新值"],
      ],
    },
    {
      type: "paragraph",
      text: "为什么不能偷懒只留 Kafka 时间戳？因为**存储时间不携带业务语义**。书舟的场景里立刻就有反例：订单服务凌晨批量补录昨日漏单（离线渠道回传），`order.created` 的事实发生在昨天 23:58，落进 Kafka 却是今天 02:00——按 Kafka 时间戳算「下单高峰分布」会把凌晨补录算成夜间订单；`notify` 想给用户发「下单成功」短信，也必须按 `occurred_at` 判断这是不是一条应该现在提醒的旧消息。更普遍的是**回放**：重放历史事件时，Kafka 时间戳是新的、`occurred_at` 才是历史。所以纪律只有一条：**业务时间一律进消息体，字段名 `occurred_at`，UTC 的 RFC3339 字符串**（书舟第 3 章起统一如此；时间精度与是否带小数秒，由团队在契约文档里定死，不要在消费端猜）。",
    },
    {
      type: "heading",
      text: "书舟事件信封：本课程的事件契约",
    },
    {
      type: "paragraph",
      text: "从本课开始，书舟所有发往主题的事件统一使用下面的「信封」结构。此前第 1、3、5 章为了演示发送 API 用过扁平的临时结构（字段直接摊在顶层、没有事件 id、没有版本），那些代码是「无契约时代的字节」；现在契约定了：本书后续内容（Schema Registry、Connect、实战）里的事件示例都以本课信封为正式口径；个别沿用第 1–5 章扁平演示体的地方会显式标注，并给出与信封的字段映射（第 10 章实战就是例子）。信封的职责是**把「描述事件本身的元数据」和「事件承载的业务数据」分开**：元数据由框架/基础设施统一处理，业务数据随事件类型变化。",
    },
    {
      type: "code",
      title: "orders.events 中的一条 order.created（信封 v1 正例）",
      language: "json",
      code: `// 教学注解：行内注释仅为讲解，真实事件 JSON 不含注释；字段语义见下表
{
  "event_id": "9f3c8a1e-7b2d-4f6e-8a7c-3d1b9c2e4f5a",   // 事件实例唯一 id（UUID）
  "type": "order.created",                            // <聚合>.<过去分词>，主题内细分事件类型
  "occurred_at": "2026-09-08T10:00:03.217Z",          // 事实发生时刻（业务时钟，UTC）
  "version": 1,                                       // 事件结构（payload 的 schema）版本
  "order_id": "20260908-000123",                      // 聚合根 id；与消息 key 相同（冗余方便消费方）
  "payload": {                                        // 业务负载：随 type 变化
    "amount_cents": 5900,
    "currency": "CNY",
    "items": [
      { "sku": "978-7-5100-1234-5", "qty": 1, "unit_price_cents": 5900 }
    ]
  }
}

// 消息 key = "20260908-000123"（同 key 同分区 → 同一订单的全部事件有序）；
// Value = 上面对象的序列化字节（本课仍是 JSON；加 schema 管理是下一课的事）。`,
    },
    {
      type: "table",
      caption: "信封字段约定（书舟统一口径）",
      headers: ["字段", "类型", "语义与约定"],
      rows: [
        ["`event_id`", "string，全局唯一", "事件实例的唯一标识（UUID/ULID/雪花均可）。同一次事实无论重试、重发多少次，event_id 不变——它是消费端幂等与全链路去重的键（第 5 章幂等消费的“业务键”首选它）"],
        ["`type`", "string", "事件类型：`<聚合名>.<过去分词>`，如 order.created / order.paid / order.cancelled；一个主题内通过它区分不同事件"],
        ["`occurred_at`", "string（RFC3339，UTC）", "事实发生时刻，见上一节；迟到/回放事件的判定依据"],
        ["`version`", "int", "payload 结构的语义版本，从 1 起；消费方可以按它写兼容分支。注意它不同于 Schema Registry 里注册表自增的“版本号”，二者不必相等（下一课区分）"],
        ["`order_id`", "string", "所属聚合 id，与消息 key 冗余存放：消费端与调试工具不解析 key 也能拿到归属"],
        ["`payload`", "object", "与 type 对应的业务数据；只放领域语义字段，禁止夹带实现细节（见反模式）"],
      ],
    },
    {
      type: "callout",
      variant: "tip",
      title: "字段名只是示例约定，真正的关键是“语义固定 + schema 化”",
      body: "不同的团队对信封字段的叫法可能不同（有人叫 id/eventType/eventTime/…）。本课程把上述六个字段定为书舟口径并贯穿后续章节，是因为**一旦事件被多个团队消费，字段名的任何改动都是跨团队变更**。比字段名更重要的是两件事：每个字段的语义要写到文档里定死（时间用 UTC、金额用分、枚举值有哪些）；以及这个结构要被一个可校验的 schema 管理起来——否则“字段叫什么都行”会退化成“字段叫什么都对不上”，这正是下一课 Schema Registry 解决的。",
    },
    {
      type: "heading",
      text: "事件命名：过去式，读起来像一句公告",
    },
    {
      type: "list",
      items: [
        "**`order.created` / `order.paid` / `order.cancelled`**：书舟订单生命周期的主干事件。命名规则 `<聚合名>.<过去分词>`，整门课一致（第 1–5 章演示代码里也是这三个名字）。",
        "**命名读起来必须像“已经发生”**：`OrderCreated`、`order-created`、`order.create`、`OrderCreateRequest` 都违背这个原则——最后一个是典型的指令味。过去分词（created/paid/cancelled/fulfilled）是英语里表达完成态最自然的形式，多数团队直接沿用。",
        "**主题按聚合/领域划分，type 在主题内细分**：订单事件都进 `orders.events`，靠 `type` 区分；而不是每个事件类型一个主题（主题爆炸会让消费方维护几十个订阅，也失去“一个订单一个分区全序”的收益）。一个订单的状态机：`order.created` →（`order.paid` 或 `order.cancelled`），支付与取消互斥——消费方按 `type` 分派处理，同一订单的消息天然同分区有序，状态机不会被乱序推进。",
      ],
    },
    {
      type: "heading",
      text: "为什么要谈“兼容性”：演进是常态，破坏是事故",
    },
    {
      type: "paragraph",
      text: "事件一旦进了主题，就进了[提交日志](glossary:commit-log)：默认保留期内，消息会一直留在日志里，等**现在或将来的任何读者**来读——只有[保留策略](glossary:retention)（时间/大小/压缩）会删除它，**消费本身不会让消息消失**，这正是它与「队列 ack 即删」的分水岭。而它的读者不是一个你能同时停机的进程——是支付服务、通知服务、数据分析、未来的新服务，它们各自按自己的节奏升级。生产端想给 `order.created` 加一个 `coupon_code` 字段，问题立刻变成：**加了字段的新事件、还留在分区里的旧事件、以及还没升级的旧消费者，怎么办？**",
    },
    {
      type: "list",
      items: [
        "消费者读到一条缺 `coupon_code` 的旧消息——如果它的代码把 `coupon_code` 当必填解析，直接崩；",
        "还没升级的旧消费者读到一条带 `coupon_code` 的新消息——如果它的解析器对“未知字段”报错（严格模式），也崩；",
        "数据仓库/湖里的历史数据与今日数据结构不一致，SQL 查询开始报错或静默给错值；",
        "最隐蔽的：字段没变名、但**语义变了**（`amount` 从“元”改成“分”、`occurred_at` 从本地时间改成 UTC），所有下游在不知情的情况下继续用旧语义解释新数据。",
      ],
    },
    {
      type: "paragraph",
      text: "这就是「无 schema 的裸 JSON 演进困境」：裸 JSON 谁都能写、谁都能读，但**没有任何一方能回答“现在线上到底有几种结构、互相能不能读”**。答案是：把每个版本的结构登记到一处，由机器按规则判断「新版本能不能安全发布」——这就是[Schema Registry](glossary:schema-registry)存在的理由（下一课落地）。本课先把判断规则本身讲清楚，它就是下面三类兼容性。",
    },
    {
      type: "heading",
      text: "三类兼容性：向后、向前、完全",
    },
    {
      type: "paragraph",
      text: "下面的定义沿用 Confluent Schema Registry 官方文档的句式（`BACKWARD` 也是它的默认配置），把「旧」定义为**当前已注册的最新版本**，「新」定义为**准备注册的下一版本**：",
    },
    {
      type: "table",
      caption: "兼容性三型（定义与部署顺序含义）",
      headers: ["类型", "官方语义（谁读谁的数据）", "允许的典型变更（以 Avro 规则为例）", "对升级顺序的要求"],
      rows: [
        ["向后兼容（BACKWARD）", "用新 schema 的消费者，能读用旧 schema 写的数据（新读旧）", "删除字段；新增**带默认值**的字段", "先升级全部消费者，再让生产者发新格式；顺序若反（生产者先发新格式），未升级的旧消费者去读新数据——这不在 BACKWARD（只保证新读旧）承诺内，可能崩"],
        ["向前兼容（FORWARD）", "用旧 schema 的消费者，能读用新 schema 写的数据（旧读新）", "新增字段（旧消费者会忽略不认识的字段）；删除**带默认值**的字段", "先升级生产者发新格式，再升级消费者——旧消费者对新数据照读不误"],
        ["完全兼容（FULL）", "双向都成立（新读旧、旧读新都行）", "新增/删除**可选字段**（带默认值）这类保守变更", "生产与消费可以独立、任意顺序升级"],
      ],
    },
    {
      type: "paragraph",
      text: "为什么“新增可选字段”是向后兼容、而“新增必填字段”不是？回到 Avro 的解码规则想：读者用自己的 schema 去解释写者的字节。新 schema 比旧数据多一个字段时，解码器需要给缺失字段一个值——**有默认值就能填（兼容），没有默认值就只能报错（不兼容）**。反过来，新数据比旧 schema 多字段时，旧解码器直接跳过不认识的字段（Avro 的 schema resolution 规则），所以“新增字段”天然向前兼容；而**删除**字段是否向前兼容，取决于旧 schema 里那个字段有没有默认值——旧读者读新数据时缺了字段，没默认值就崩。这套规则的细节随格式不同而不同（Avro、JSON Schema、Protobuf 各有细则，见 Confluent 官方兼容性文档），但“**加字段要给默认、删字段要谨慎、改语义要开新字段**”的思想是通用的。",
    },
    {
      type: "paragraph",
      text: "下面用书舟 `order.created` 的 payload 演示五种典型变更（假设 v1 的 schema 已经写进注册表并声明了各字段的默认值情况——「默认值」是 schema 的属性，存在于 schema 文本里，而不是消息数据里，这正是它必须被机器管理的原因）：",
    },
    {
      type: "table",
      caption: "具体字段演化 → 兼容性判断（书舟 order.created 示例，Avro 规则口径）",
      headers: ["变更（v1 → v2）", "向后（新读旧）", "向前（旧读新）", "结论"],
      rows: [
        ["新增可选字段 `coupon_code`（带默认值，如 null 表示未用券）", "✔（旧数据缺字段时用默认）", "✔（旧消费者忽略新字段）", "完全兼容；最推荐的演进姿势"],
        ["删除可选字段 `currency`（v1 中带默认值 CNY）", "✔（新消费者忽略旧数据多余字段）", "✔（旧消费者读不到该字段时用默认）", "完全兼容；可安全下线"],
        ["删除必填字段 `items`（v1 中无默认值）", "✔（新消费者忽略旧数据里的 items）", "✘（旧消费者读新数据时缺 items 且无默认 → 崩）", "只向后兼容：必须先升消费者"],
        ["新增必填字段 `channel`（无默认值）", "✘（新消费者读旧数据时缺 channel → 崩）", "✔（旧消费者忽略新字段）", "只向前兼容：必须先升生产者"],
        ["字段语义变更：`amount_cents` 从“分”改为“元”", "✘", "✘", "不兼容：数值含义变了，解码不报错但数据全错——只能开新字段或新主题"],
      ],
    },
    {
      type: "paragraph",
      text: "还有两个配套概念，先点一下名、细节在下一课执行时展开：**传递性（transitive）**——上面的检查默认只和“最新版本”比（`BACKWARD` 非传递），`*_TRANSITIVE` 变体会要求和**全部历史版本**比，历史越长越严格；以及**为什么官方默认 BACKWARD**——Kafka 里消费者随时可以 seek 回主题开头重读全部历史（[提交日志](glossary:commit-log)的可回放性），默认向后兼容保证“用最新 schema 能读通整条历史”，这对数据平台类消费方（数仓、流处理）是刚需。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "兼容性是“schema 间的关系”，不是“我觉得没问题”",
      body: "兼容判断必须交给**同一个规则实现**（Schema Registry 的兼容性检查，或等价工具）在发布时执行，而不是靠代码评审时人眼把关：人眼只能看到“当前这一版”，看不到注册表里躺着的历史版本；而且规则细节（默认值规则、类型放宽/收窄、union 分支增减）相当繁琐，人肉执行必漏。工程上正确姿势是：schema 变更走注册（或 CI 里调注册表的 compatibility 接口），被拒就说明破坏兼容——下一课你会亲眼看到一次 409。",
    },
    {
      type: "heading",
      text: "演进纪律：可操作的五条",
    },
    {
      type: "list",
      items: [
        "**默认只加不改不删**：新增字段带默认值（语义上等价于“旧数据里它取默认”）；任何对既有字段的修改（改名、改类型、改单位、改枚举值含义）一律视为破坏性变更。",
        "**加默认值优先于“允许为空”**：给默认值让老数据可解码；如果字段确实可能没值，用“类型带 null 且默认 null”（Avro 的 `[\"null\", T]` union 写法）表达“可选”，不要用魔法值（如空字符串、-1、\"unknown\"）代替 null——魔法值会污染统计。",
        "**弃用不删除**：想删字段时，先在文档与 schema 注释里标 deprecated，观察一段时间确认没有消费者依赖（用消费方日志/指标佐证），之后仍建议保留字段、停止写入新值，而非直接删——删除会让历史数据与未来 schema 的关系变脆。",
        "**破坏性变更开新主题，不做“版本号主题内硬切”**：确需不兼容演进（换格式、换语义模型）时，按书舟命名约定另建主题（如 `orders.events` 的迁移目标），新旧双写，消费方逐个迁移后再停旧主题——不要让一个主题里同时躺着两套互相读不了的 schema（“每字段一个版本号塞进同一主题”是伪方案，消费方无法静态知道该用哪个 schema 解）。",
        "**每个版本都要能追溯到人**：schema 变更记录谁、何时、为什么改（注册表版本 + 代码评审 + 变更说明），这是事件系统长期可维护的底线；否则半年后没人敢动 schema。",
      ],
    },
    {
      type: "heading",
      text: "反模式：把事件模型做坏的常见姿势",
    },
    {
      type: "callout",
      variant: "warning",
      title: "反模式一：把“指令/请求”当事件发",
      body: "典型症状：事件名叫 `order.cancel`（祈使）、`notify.user`（动作）、`send.email`（任务），消费者处理失败后把消息退回、要求生产者负责重试。本质是把 Kafka 当成了同步 RPC 总线：事件流是广播式的事实日志，没有“请求-响应”语义、没有针对单条消息的内置回执；拿它传指令，最终你会用死信+重放手工拼一个 RPC 框架出来。正确的拆分：指令走 API/命令通道（HTTP/gRPC/命令消息），**指令执行成功后产生的事件**才进 Kafka。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "反模式二：把实现细节泄漏进契约",
      body: "payload 里出现 `db_row_id`、`service_internal_flag`、`tenant_code_legacy`、报错文案、缓存 key、机房代号……这些是“某个服务内部长什么样”，不是“领域里发生了什么”。它们把消费者的正确性绑定到生产者的实现上：生产者重构表结构，事件消费者跟着崩。判断标准：**这条字段换一家公司、换一种实现，语义还成立吗？**成立（订单金额、支付渠道、收货地址）才放进事件；不成立（内部主键、临时状态、实现备注）留在服务内部。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "反模式三：事件过细或过粗",
      body: "过细：把订单的每个字段变化各发一个事件（`order.amount_changed`、`order.address_changed`、`order.status_changed`……），消费方要拼图才能还原一次业务操作，且“一次下单”的真实语义被拆碎——应该发领域里**有意义的一次事实**（下单、支付、取消），字段变化作为该事件的 payload 内容。过粗：把整行状态快照当“领域事件”发（payload = 订单表全字段），字段一多，事件语义退化成“表变更通知”，消费方被迫 diff 才知道发生了什么——那是 CDC（变更数据捕获）的活，属于[Outbox 与 CDC 决策](/courses/kafka/lessons/kafka-outbox-cdc-es)的对比范围。判据：**这条事件能不能用一句过去时的业务公告说清楚？**说得清就是合适粒度。",
    },
    {
      type: "callout",
      variant: "warning",
      title: "反模式四：没有版本意识，裸 JSON 裸奔演进",
      body: "不登记 schema、不写字段默认值、改字段不通知、拿“兼容”当口头禅。第 5 章的教训是“别在代码里假设不重”，这一章的教训是“别在契约里假设不变”。演进纪律 + 机器校验（下一课）是事件系统的安全网：裸奔不是省事，是把“某天某团队上线后全体下游神秘报错”这件事从概率变成必然。",
    },
    {
      type: "quiz",
      question:
        "orders.events 的 value schema 当前是 v1（subject 默认配置 BACKWARD，只对最新版本检查）。团队想注册 v2：在 payload 里新增一个**必填字段** `channel`（无默认值）。注册会发生什么？为什么？",
      options: [
        "注册成功：BACKWARD 只限制删除字段，新增字段永远允许，v2 直接生效",
        "注册被拒（409）：BACKWARD 要求“用新 schema 的消费者能读旧数据”，而旧数据没有 channel、新 schema 又无默认值可填，新消费者读旧消息会失败",
        "注册成功但只向前兼容：需要先把全部消费者升级到 v2，否则旧消费者读新数据会崩",
        "注册被拒，但把 subject 兼容性改成 NONE 后就能通过，且从此任何演进都安全",
      ],
      answer: 1,
      explanation:
        "BACKWARD 的检查对象是“新 schema 读旧数据”：v2 比旧数据多一个无默认值的必填字段，解码旧消息时无法取值 → 不兼容，注册表拒绝（HTTP 409）。选项 3 混淆了方向：新增必填字段是“向前兼容”（旧消费者能读新数据），但 BACKWARD 配置下它仍被拒。选项 4 是危险误读：NONE 只是关闭检查，并不会消除“新消费者读不了旧数据”的真实问题，跨版本读数据仍然会崩——所以破坏性变更的正解是开新主题做迁移（演进纪律第 4 条）。",
    },
    {
      type: "keypoints",
      items: [
        "事件 = 已发生的事实（过去时），指令 = 待执行的意图（祈使）——别拿事件主题传 RPC；Kafka 是事实的[提交日志](glossary:commit-log)，不是请求的队列",
        "时间三分离：业务时间 `occurred_at` 进消息体（重放不变），Kafka 记录时间戳是存储元数据，`processed_at` 由消费方自记",
        "书舟事件信封：`event_id`（唯一，幂等键）+ `type`（聚合.过去分词）+ `occurred_at` + `version` + `order_id`（与 key 冗余）+ `payload`（业务数据）",
        "裸 JSON 的演进困境：没人能回答“线上到底有几种结构、互相能不能读”——需要集中式注册与机器校验（下一课）",
        "向后兼容=新读旧（先升消费者）；向前兼容=旧读新（先升生产者）；完全兼容=双向。新增必填字段只向前、删除无默认字段只向后、语义变更双不兼容",
        "演进纪律：默认只加不改不删、新字段带默认、弃用先于删除、破坏性变更开新主题双写迁移、变更可追溯",
        "反模式：指令当事件、实现细节泄进契约、事件过细/过粗（快照是 CDC 的活）、裸 JSON 无版本演进",
      ],
    },
    {
      type: "paragraph",
      text: "本课定的是「契约长什么样」；但契约的*演进*要真正安全，还需要一处**所有生产方、消费方共享的权威登记处**，在每次发布时用统一规则做兼容性裁决——这就是下一课的主角：[Schema Registry 实战（Go / pkg/sr）](/courses/kafka/lessons/kafka-schema-registry-go)。你会看到注册/版本/兼容性检查的工作流、schema id 如何随消息字节走（magic + 4 字节 id 的前缀），以及 Go 团队到底怎么用 franz-go 的 `pkg/sr` 落地它。",
    },
  ],
};
